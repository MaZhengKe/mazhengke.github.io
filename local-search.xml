<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>线框绘制</title>
    <link href="/2023/02/09/wireframe/"/>
    <url>/2023/02/09/wireframe/</url>
    
    <content type="html"><![CDATA[<h1 id="线框渲染"><a href="#线框渲染" class="headerlink" title="线框渲染"></a>线框渲染</h1><p>最近写代码快写吐了，写点Shader缓解一下。<br>今天就复刻一个线框渲染吧，顺便温习一下几何着色器的用法。<br>参考列在前面</p><ul><li><a href="https://www.bilibili.com/read/cv3168157">在unity中实现风格化线框渲染</a></li><li><a href="https://zhuanlan.zhihu.com/p/585436751">几何着色器（Geometry Shader）的基础介绍以及简单示例</a></li></ul><h1 id="几种方式"><a href="#几种方式" class="headerlink" title="几种方式"></a>几种方式</h1><p>总结一下各种实现的方法</p><ul><li>生成线框模型</li><li>线框贴图</li><li>写入UV</li><li>利用GL来绘制线框</li><li>几何着色器<ul><li>用几何着色器的<code>LineStream</code>来生成线段图元</li><li>用几何着色器来生成片元相对线框的SDF，然后利用SDF绘制线框</li></ul></li></ul><h1 id="生成线框模型"><a href="#生成线框模型" class="headerlink" title="生成线框模型"></a>生成线框模型</h1><p>最直观最简单的方法了，比如Blender就自带线框修改器，可以吧模型之间转成线框模型输出。<br>缺点是显而易见的，模型的面数成倍增加，模型也不能复用，增加文件体积，以后模型要修改还要再重复这个过程。<br>好处就是简单，而且不受平台的限制，下面罗列的使用几何着色器的方法在比如Web平台就无法实现。<br><img src="/images/w-blender.png" alt="线框修改器"></p><h1 id="线框贴图"><a href="#线框贴图" class="headerlink" title="线框贴图"></a>线框贴图</h1><p>实现也比较容易，UV全展开不重叠，或者完全相同的重叠也行，然后在需要渲染线框的地方绘制就好。<br>想要程序化生成可以使用Blender的烘焙，把线框烘焙到贴图上输出即可。<br>这种方式对贴图的精度要求就很高了，分辨率会很大。<br>或者改进一下，参照之前<a href="/2022/12/26/draw/">模型上喷涂绘制</a>的方式，改成用SDF来做，贴图可以使用很低的分辨率。<br>但这样程序化生成会比较麻烦</p><p>也可以和下面的几何着色器的方式结合起来，用之前喷涂的思路，把SDF烘焙到一张贴图上，就可以在不支持几何着色器的平台上使用了。</p><h1 id="写入UV"><a href="#写入UV" class="headerlink" title="写入UV"></a>写入UV</h1><p>还有一种骚操作，把模型改成四边面，然后每个面都完全展开UV，然后根据UV判断片元是否是边框。<br>缺点显而易见，每个面的大小不同，线框的大小也就各不相同了。</p><h1 id="利用GL来绘制线框"><a href="#利用GL来绘制线框" class="headerlink" title="利用GL来绘制线框"></a>利用GL来绘制线框</h1><p>来自<a href="https://www.jianshu.com/p/e95e6507659c">这篇文章</a>，我还真没有想到还有这种做法。<br>大致思路是读模型顶点，存到内存，然后调用<code>GL.Vertex</code>等方法一根一根的绘制。<br>感觉有点太暴力了，效率上会很低。但确实也算很简单有效的方法，而且自由度很高，针对一些特定场景也许很有用吧。</p><h1 id="几何着色器"><a href="#几何着色器" class="headerlink" title="几何着色器"></a>几何着色器</h1><p>这里又大致分为两类</p><ol><li>直接用几何着色器的<code>LineStream</code>来生成线段图元。</li><li>利用几何着色器来生成片元相对线框的SDF，然后利用SDF绘制线框。</li></ol><p>先复习一下几何着色器的使用。</p><figure><div class="code-wrapper"><pre class="line-numbers language-glsl" data-language="glsl"><code class="language-glsl">Shader &quot;Custom&#x2F;SimpleGeometryShader&quot; &#123;    Properties &#123;        _Color (&quot;Color&quot;, Color) &#x3D; (1,1,1,1)    &#125;    SubShader &#123;        Tags &#123;&quot;RenderType&quot;&#x3D;&quot;Opaque&quot;&#125;        LOD 200        Pass &#123;            HLSLPROGRAM            #pragma vertex vert            #pragma fragment frag            #pragma geometry geom                        #include &quot;Packages&#x2F;com.unity.render-pipelines.universal&#x2F;ShaderLibrary&#x2F;Core.hlsl&quot;            struct appdata &#123;                float4 vertex : POSITION;            &#125;;                        struct v2g &#123;                float4 vertex : SV_POSITION;            &#125;;            struct g2f &#123;                float4 vertex : SV_POSITION;            &#125;;            uniform float4 _Color;            v2g vert (appdata v) &#123;                v2g o;                o.vertex &#x3D; TransformObjectToHClip(v.vertex);                return o;            &#125;            float4 frag (g2f i) : SV_Target &#123;                return _Color;            &#125;            [maxvertexcount(3)]            void geom(triangle v2g IN[3], inout TriangleStream&lt;g2f&gt; Out) &#123;                                for(int i &#x3D; 0; i &lt; 3; i++) &#123;                    g2f o;                    o.vertex &#x3D; IN[i].vertex;                    Out.Append(o);                &#125;            &#125;            ENDHLSL        &#125;    &#125;    FallBack &quot;Diffuse&quot;&#125;</code></pre></div></figure><p>这就是最最简单的几何着色器了，什么都没有干，输入什么就输出什么，没有一点改动。</p><h2 id="LineStream"><a href="#LineStream" class="headerlink" title="LineStream"></a>LineStream</h2><p>第一种很简单：</p><figure><div class="code-wrapper"><pre class="line-numbers language-glsl" data-language="glsl"><code class="language-glsl">[maxvertexcount(3)]void geom(triangle  v2g IN[3], inout LineStream&lt;g2f&gt; Out)&#123;    g2f o1 &#x3D; (g2f)0;    g2f o2 &#x3D; (g2f)0;    g2f o3 &#x3D; (g2f)0;    o1.vertex &#x3D; IN[0].vertex;    o2.vertex &#x3D; IN[1].vertex;    o3.vertex &#x3D; IN[2].vertex;        Out.Append(o1);    Out.Append(o2);    Out.Append(o3);&#125;</code></pre></div></figure><p>但是这种方式只能生成线段了，如果想要扩充或者调整线段的粗细，就会很麻烦了。</p><h3 id="四边形"><a href="#四边形" class="headerlink" title="四边形"></a>四边形</h3><p>剔除最长的那条边</p><figure><div class="code-wrapper"><pre class="line-numbers language-glsl" data-language="glsl"><code class="language-glsl">[maxvertexcount(4)]void geom(triangle v2g IN[3], inout LineStream&lt;g2f&gt; Out)&#123;    g2f o1 &#x3D; (g2f)0;    g2f o2 &#x3D; (g2f)0;    g2f o3 &#x3D; (g2f)0;    o1.vertex &#x3D; IN[0].vertex;    o2.vertex &#x3D; IN[1].vertex;    o3.vertex &#x3D; IN[2].vertex;        float Edge1 &#x3D; length(IN[0].modelPos - IN[1].modelPos);    float Edge2 &#x3D; length(IN[1].modelPos - IN[2].modelPos);    float Edge3 &#x3D; length(IN[2].modelPos - IN[0].modelPos);        if (Edge1 &gt; Edge2 &amp;&amp; Edge1 &gt; Edge3)    &#123;        Out.Append(o2);        Out.Append(o3);        Out.RestartStrip();        Out.Append(o3);        Out.Append(o1);        Out.RestartStrip();    &#125;    else if (Edge2 &gt; Edge1 &amp;&amp; Edge2 &gt; Edge3)    &#123;        Out.Append(o1);        Out.Append(o2);        Out.RestartStrip();        Out.Append(o3);        Out.Append(o1);        Out.RestartStrip();    &#125;    else if (Edge3 &gt; Edge1 &amp;&amp; Edge3 &gt; Edge2)    &#123;        Out.Append(o1);        Out.Append(o2);        Out.RestartStrip();        Out.Append(o2);        Out.Append(o3);        Out.RestartStrip();    &#125;&#125;</code></pre></div></figure><h2 id="TriangleStream"><a href="#TriangleStream" class="headerlink" title="TriangleStream"></a>TriangleStream</h2><p>第二种，生成片元相对线框的SDF。</p><h3 id="插值"><a href="#插值" class="headerlink" title="插值"></a>插值</h3><p>在g2f中增加一项</p><figure><div class="code-wrapper"><pre class="line-numbers language-glsl" data-language="glsl"><code class="language-glsl">struct g2f &#123;    float4 vertex : SV_POSITION;    float3 barycentric : TEXCOORD0;&#125;;</code></pre></div></figure><p>几何着色器中手动赋值，并重新插值。</p><figure><div class="code-wrapper"><pre class="line-numbers language-glsl" data-language="glsl"><code class="language-glsl">[maxvertexcount(3)]void geom(triangle v2g IN[3], inout TriangleStream&lt;g2f&gt; Out) &#123;    g2f o1, o2, o3;    o1.vertex &#x3D; IN[0].vertex;    o2.vertex &#x3D; IN[1].vertex;    o3.vertex &#x3D; IN[2].vertex;    o1.barycentric &#x3D; float3(1, 0, 0);    o2.barycentric &#x3D; float3(0, 1, 0);    o3.barycentric &#x3D; float3(0, 0, 1);    Out.Append(o1);    Out.Append(o2);    Out.Append(o3);    Out.RestartStrip();&#125;</code></pre></div></figure><p>然后在片元中输出看看：<br><img src="/images/w-frag.png" alt="插值后结果"></p><h3 id="到边的距离"><a href="#到边的距离" class="headerlink" title="到边的距离"></a>到边的距离</h3><p>这就是片元中每个像素到各个顶点的距离了（每个分量），取最小值就是到各个边的距离：</p><figure><div class="code-wrapper"><pre class="line-numbers language-glsl" data-language="glsl"><code class="language-glsl">float4 frag (g2f i) : SV_Target &#123;    float minV &#x3D; min(i.barycentric.x, min(i.barycentric.y, i.barycentric.z));    return float4(minV,minV,minV,1);&#125;</code></pre></div></figure><p><img src="/images/w-min.png" alt="到边的SDF"><br>然后用阈值去掐一下：<br><img src="/images/w-step.png" alt="线框"></p><h3 id="剔除"><a href="#剔除" class="headerlink" title="剔除"></a>剔除</h3><p>看到背面被剔除了，再修改下，增加<code>Cull Off</code></p><figure><div class="code-wrapper"><pre class="line-numbers language-glsl" data-language="glsl"><code class="language-glsl">float4 frag (g2f i) : SV_Target &#123;    float minV &#x3D; min(i.barycentric.x, min(i.barycentric.y, i.barycentric.z));    float v &#x3D; step(minV, _Size);    clip(v-0.5);    return float4(v,v,v,1);&#125;</code></pre></div></figure><p><img src="/images/w-cull.png" alt="背面显示"></p><h3 id="均匀线框"><a href="#均匀线框" class="headerlink" title="均匀线框"></a>均匀线框</h3><p>还有个问题，这里不同片元所在的三角大小不同，这个胶囊中间的三角形的线框就特别大。<br>所以在给每个顶点赋值时，不统一使用1，而是在模型空间算出其顶点到对边的距离：</p><figure><div class="code-wrapper"><pre class="line-numbers language-glsl" data-language="glsl"><code class="language-glsl">[maxvertexcount(3)]void geom(triangle v2g IN[3], inout TriangleStream&lt;g2f&gt; Out) &#123;    g2f o1, o2, o3;    o1.vertex &#x3D; IN[0].vertex;    o2.vertex &#x3D; IN[1].vertex;    o3.vertex &#x3D; IN[2].vertex;    float dis1 &#x3D; GetDistance(IN[1].modelPos,IN[2].modelPos,IN[0].modelPos);    float dis2 &#x3D; GetDistance(IN[0].modelPos,IN[2].modelPos,IN[1].modelPos);    float dis3 &#x3D; GetDistance(IN[0].modelPos,IN[1].modelPos,IN[2].modelPos);        o1.barycentric &#x3D; float3(dis1, 0, 0);    o2.barycentric &#x3D; float3(0, dis2, 0);    o3.barycentric &#x3D; float3(0, 0, dis3);    Out.Append(o1);    Out.Append(o2);    Out.Append(o3);    Out.RestartStrip();&#125;</code></pre></div></figure><p><img src="/images/w-av.png" alt="均匀线框"></p><h3 id="四边形-1"><a href="#四边形-1" class="headerlink" title="四边形"></a>四边形</h3><p>再然后就是三角面边四角面，参照原文，剔除边长最长的那条边</p><figure><div class="code-wrapper"><pre class="line-numbers language-glsl" data-language="glsl"><code class="language-glsl">float3 param &#x3D; float3(0, 0, 0);float Edge1 &#x3D; length(IN[0].modelPos - IN[1].modelPos);float Edge2 &#x3D; length(IN[1].modelPos - IN[2].modelPos);float Edge3 &#x3D; length(IN[2].modelPos - IN[0].modelPos);if (Edge1 &gt; Edge2 &amp;&amp; Edge1 &gt; Edge3)    param &#x3D; float3(0, 0, 1);else if (Edge2 &gt; Edge1 &amp;&amp; Edge2 &gt; Edge3)    param &#x3D; float3(1, 0, 0);else if (Edge3 &gt; Edge1 &amp;&amp; Edge3 &gt; Edge2)    param &#x3D; float3(0, 1, 0);o1.barycentric &#x3D; float3(dis1, 0, 0) + param;o2.barycentric &#x3D; float3(0, dis2, 0) + param;o3.barycentric &#x3D; float3(0, 0, dis3) + param;</code></pre></div></figure><p><img src="/images/w-quad.png" alt="四边线框"><br>这里有个小瑕疵，假如剔除的那个边，非常靠近四边形中的另一个三角形的边，他的SDF就不正确了。<br><img src="/images/w-bug.png" alt="四边线框"><br>框中的被剔除的斜边，非常靠近另一个三角形的边，理应存在，但因为被剔除，所以不显示了。</p><h3 id="抗锯齿"><a href="#抗锯齿" class="headerlink" title="抗锯齿"></a>抗锯齿</h3><p>一个神奇的函数</p><figure><div class="code-wrapper"><pre class="line-numbers language-glsl" data-language="glsl"><code class="language-glsl">float aa(float threshold, float distance)&#123;    float delta &#x3D; fwidth(distance) * _wireSmoothing;    return smoothstep((threshold - 1) * delta, (threshold + 1) * delta, distance);&#125;</code></pre></div></figure><p><code>fwidth</code>就不解释了，大体意思是锯齿越多的地方，<code>fwidth(distance)</code>值越大，delta越大，所以smoothstep的范围就越大，结果上看就会平滑一点。<br>但即使启用了抗锯齿，效果和原生自带的线框渲染也没法比，原生是真的把每根线在屏幕上按固定大小画出来，而这种方式必然会导致线距离相机过远而消失。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>语音转口型</title>
    <link href="/2023/02/08/lip/"/>
    <url>/2023/02/08/lip/</url>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>之前做GPT聊天时，用Live2D的简单的通过音量大小来驱动嘴型的大小变化，这里想更精确一定，用元音来驱动。<br>主要就是复刻了这位大佬的<a href="https://huailiang.github.io/blog/2020/mouth/">这篇文章</a>。</p><h1 id="大致思路"><a href="#大致思路" class="headerlink" title="大致思路"></a>大致思路</h1><p>初看时被一堆术语吓到了，什么频域信息、窗函数、高斯滤波器、DCT变换、共振峰等等。<br>耐心一点一点看就发现还是比较简单的，很多算法并不一定要从新造轮子去实现，知道其目的就好了。</p><h2 id="获取语音数据的频域信息"><a href="#获取语音数据的频域信息" class="headerlink" title="获取语音数据的频域信息"></a>获取语音数据的频域信息</h2><p>这里直接用<code>AudioSource.GetSpecturmData</code>即可。这里其实就包含了之前提到的频域信息和窗函数。<br><img src="/images/audio.png" alt="频域可视化"></p><h2 id="高斯滤波"><a href="#高斯滤波" class="headerlink" title="高斯滤波"></a>高斯滤波</h2><p>就像通常的高斯模糊的卷积一样，这里也可以在一维空间高斯滤波一次，去除噪音。</p><h2 id="提取共振峰"><a href="#提取共振峰" class="headerlink" title="提取共振峰"></a>提取共振峰</h2><p>简单说就是寻找局部最大值的最大值所在的频率位置。<br>这个位置就是用来判定元音的依据了。<br>注意的是，原文给出的5个元音的判断的频率，仅仅针对采样率为44Khz的文件，其他采样率要自行修改。</p><h1 id="结合Live2D"><a href="#结合Live2D" class="headerlink" title="结合Live2D"></a>结合Live2D</h1><p>live2d有自己的一套奇奇怪怪的框架去修改参数，还要考虑排序。<br>好在其插件基本开源，参照着写就行了，根据当前的元音去混合口型动画。</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p><img src="/images/lip.gif"><br>放在Live2D上效果确实比之前单纯的用音量来控制好的多，但也仅限于是增加了口型的多样性，很难说是“正确”的口型。<br>这里最主要的原因在于</p><ul><li>不同人声的各个元音的共振峰并不相同，需要根据配音来调整</li><li>只考虑了第一共振峰，误差太大，可以参照下表结合第二共振峰去判断，效果应该更好。</li><li><img src="https://huailiang.github.io/img/post-vr/mou9.jpg"></li></ul>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>OpenAI微调模型测试</title>
    <link href="/2023/02/06/OpenAI/"/>
    <url>/2023/02/06/OpenAI/</url>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>之前只是使用了现成的OpenAI的模型，每次调用都要给出一大堆的重复提示词，而且效果也不是太好，这次尝试用其称为<code>微调(fine-tuning)</code>来训练个新的模型。可以节省token，顺便看看微调后的模型效果和之前有多大区别。<br>OpenAI已经给出了详尽的<a href="https://platform.openai.com/docs/guides/fine-tuning">说明</a>，下面就只记录下具体的步骤。</p><h1 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h1><p>直接在容器里操作吧，不想污染主机。<br>起一个python的环境。</p><figure><div class="code-wrapper"><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml">version: &quot;2.3&quot;services:  app:    image: python    container_name: openai    environment:      - HTTP_PROXY&#x3D;http:&#x2F;&#x2F;clash:7890      - HTTPS_PROXY&#x3D;http:&#x2F;&#x2F;clash:7890      - no_proxy&#x3D;localhost,127.0.0.1,::1    entrypoint: &quot;sleep infinity&quot;    volumes:      - .&#x2F;data:&#x2F;datanetworks:  default:    external: true    name: nginxproxynet</code></pre></div></figure><figure><div class="code-wrapper"><pre class="line-numbers language-sh" data-language="sh"><code class="language-sh">docker exec -it openai &#x2F;bin&#x2F;bashexport OPENAI_API_KEY&#x3D;&quot;&lt;OPENAI_API_KEY&gt;&quot;pip install --upgrade openaipip install openai[datalib]</code></pre></div></figure><h1 id="数据准备"><a href="#数据准备" class="headerlink" title="数据准备"></a>数据准备</h1><p>我感觉这是工作量最大的内容了。这里先用几个试一下：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csv" data-language="csv"><code class="language-csv">prompt,completionxxxx,xxxx</code></pre></div></figure><p>然后用工具整理一下</p><figure><div class="code-wrapper"><pre class="line-numbers language-sh" data-language="sh"><code class="language-sh">openai tools fine_tunes.prepare_data -f &#x2F;data&#x2F;data.csv</code></pre></div></figure><p>这里他会自动帮我们添加提示的结束字符，以及回答的开头空格和结束的\n</p><figure><div class="code-wrapper"><pre class="line-numbers language-json" data-language="json"><code class="language-json">&#123;&quot;prompt&quot;:&quot;xxxx -&gt;&quot;,&quot;completion&quot;:&quot; xxxx\n&quot;&#125;</code></pre></div></figure><h1 id="训练"><a href="#训练" class="headerlink" title="训练"></a>训练</h1><p>然后就是上传文件开始训练了。</p><figure><div class="code-wrapper"><pre class="line-numbers language-sh" data-language="sh"><code class="language-sh">openai api fine_tunes.create -t &#x2F;data&#x2F;data_prepared.jsonl -m davinci --suffix &quot;test001&quot;</code></pre></div></figure><p>要等很久。主要是排队，期间可以用<code>openai api fine_tunes.list</code>查看状态：</p><figure><div class="code-wrapper"><pre class="line-numbers language-json" data-language="json"><code class="language-json">&#123;  &quot;data&quot;: [    &#123;      &quot;created_at&quot;: 1675758078,      &quot;fine_tuned_model&quot;: null,      &quot;hyperparams&quot;: &#123;        &quot;batch_size&quot;: null,        &quot;learning_rate_multiplier&quot;: null,        &quot;n_epochs&quot;: 4,        &quot;prompt_loss_weight&quot;: 0.01      &#125;,      &quot;id&quot;: &quot;ft-xxxxxxxxxxxxx&quot;,      &quot;model&quot;: &quot;davinci&quot;,      &quot;object&quot;: &quot;fine-tune&quot;,      &quot;organization_id&quot;: &quot;org-xxxxxxxxxxxxxxxxxx&quot;,      &quot;result_files&quot;: [],      &quot;status&quot;: &quot;pending&quot;,      &quot;training_files&quot;: [        &#123;          &quot;bytes&quot;: 189,          &quot;created_at&quot;: 1675758077,          &quot;filename&quot;: &quot;&#x2F;data&#x2F;data_prepared.jsonl&quot;,          &quot;id&quot;: &quot;file-xxxxxxxxxxxxxxxxxx&quot;,          &quot;object&quot;: &quot;file&quot;,          &quot;purpose&quot;: &quot;fine-tune&quot;,          &quot;status&quot;: &quot;processed&quot;,          &quot;status_details&quot;: null        &#125;      ],      &quot;updated_at&quot;: 1675758078,      &quot;validation_files&quot;: []    &#125;  ],  &quot;object&quot;: &quot;list&quot;&#125;</code></pre></div></figure><p>一直是<code>pending</code>，<del>等训练好再写吧。</del><br>过了7分钟终于进入队列了，排在13。<br>如果按他们的提示，每个任务要一个半小时，那就得近20小时。。。。<br>实际也就等了半小时不到。但结果不尽人意。数据量太少了。</p><h1 id="样本属性"><a href="#样本属性" class="headerlink" title="样本属性"></a>样本属性</h1><p>这里设计一下样本应该包含哪些<br>这里只需要保存变量即可</p><ul><li>时间，文本模型无法知道当前的时间</li><li>内容，直播的内容</li><li>对话列表，为了有一定的上下文联系，列表不一定是要一一对应。<ul><li>观众名称</li><li>弹幕内容</li><li>回答内容</li></ul></li></ul><h1 id="样本采集"><a href="#样本采集" class="headerlink" title="样本采集"></a>样本采集</h1><p>真是有多少人工就有多少智能。按Openai的说法，这种聊天类需要的样本量至少数千，这还只是最低。模型的质量与样本量成指数关系，想想都头大。<br>程序的问题还得程序来解决。<br>初步想法是录制别人的直播视频，加语音识别，同时录制弹幕，然后程序化生成样本。</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>仅就调用Openai的接口来说，并不复杂，难度还是在于样本的获取，就等样本采集完毕后再来试一试模型的训练吧。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>文字转语音 Text To Speech</title>
    <link href="/2023/02/06/TTS/"/>
    <url>/2023/02/06/TTS/</url>
    
    <content type="html"><![CDATA[<h1 id="文字转语音-Text-To-Speech"><a href="#文字转语音-Text-To-Speech" class="headerlink" title="文字转语音 Text To Speech"></a>文字转语音 Text To Speech</h1><p>很多时候都会遇到需要这种服务，阿里的实在太难听，微软还没有信用卡。再找找还有木有其他方式。</p><h1 id="云服务"><a href="#云服务" class="headerlink" title="云服务"></a>云服务</h1><p>各家云厂商（阿里、腾讯、百度、微软、谷歌等等）一般都会提供这种接口，大部分还允许提供训练语音来训练自己的模型，价格也很便宜。但一定是要求客户端联网。</p><h1 id="本地搭建服务"><a href="#本地搭建服务" class="headerlink" title="本地搭建服务"></a>本地搭建服务</h1><p>TTS发展至今，也已经有了很多开源的实现。这次就尝试搭建几个玩一下。</p><h2 id="coqui-ai"><a href="#coqui-ai" class="headerlink" title="coqui-ai"></a>coqui-ai</h2><p>提供了docker镜像：</p><figure><div class="code-wrapper"><pre class="line-numbers language-sh" data-language="sh"><code class="language-sh">docker run --rm -it -p 5002:5002 --entrypoint &#x2F;bin&#x2F;bash ghcr.io&#x2F;coqui-ai&#x2F;tts-cpupython3 TTS&#x2F;server&#x2F;server.py --list_models #To get the list of available modelspython3 TTS&#x2F;server&#x2F;server.py --model_name tts_models&#x2F;en&#x2F;vctk&#x2F;vits # To start a server</code></pre></div></figure><p>这个是CPU版本的。</p><p>内置了一个中文的模型，默认模型效果并没有比前述的云服务好到哪里。<br>其他的中文模型还是找不到。</p><h2 id="PaddleSpeech"><a href="#PaddleSpeech" class="headerlink" title="PaddleSpeech"></a>PaddleSpeech</h2><p>基于百度飞桨的语音方向的开源模型库。<br>同样给出了Docker的<a href="https://hub.docker.com/r/paddlecloud/paddlespeech">镜像</a></p><p>悲催的是这个模型内存需求太大了，14.5G，把3070爆了。</p><h2 id="vits"><a href="#vits" class="headerlink" title="vits"></a>vits</h2><p>参照这位B站UP，Saya睡大觉中，其<a href="https://www.bilibili.com/read/cv21342054">vits模型下载链接以及使用教程</a>。<br>本地部署后，生成速度和效果都很满意。<br>但要注意，这里的模型好像是<a href="https://github.com/CjangCjengh/vits">CjangCjengh版VITS</a><br>不是<a href="https://github.com/jaywalnut310/vits">原版VITS</a>。</p><h3 id="Unity调用"><a href="#Unity调用" class="headerlink" title="Unity调用"></a>Unity调用</h3><p>没有找到合适的HttpAPI，只能用<code>Process</code>本地命令行的方式去调用了。<br>这里又遇到了中文乱码问题。具体原因是Unity开启的Process的程序的<code>Console.InputEncoding</code>默认会采用<code>OSEncoding</code>，并不是UTF-8。而单纯的C#控制台开启的Process却是UTF-8。大概与这个<a href="https://github.com/dotnet/runtime/issues/43295">issues</a>有关。<br>解决方案就是修改Windows的语言设置:<br><img src="/images/utf.png"></p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Unicode编码</title>
    <link href="/2023/02/06/string/"/>
    <url>/2023/02/06/string/</url>
    
    <content type="html"><![CDATA[<h1 id="UTF8与Unicode"><a href="#UTF8与Unicode" class="headerlink" title="UTF8与Unicode"></a>UTF8与Unicode</h1><p><a href="https://zhuanlan.zhihu.com/p/51828216">来源</a></p><h2 id="Unicode"><a href="#Unicode" class="headerlink" title="Unicode"></a>Unicode</h2><p>Unicoide 的全称是 Universal Multiple-Octet Coded Character Set（通用多八位字符集，简称 UCS）。<br>规定了各种字符的编码。<br>而下面都是具体的编码二进制的存储方式。</p><h2 id="UTF-32"><a href="#UTF-32" class="headerlink" title="UTF-32"></a>UTF-32</h2><p>规定，一个字符使用四个字节存储，也就是 32 位，这样就能涵盖现有 Unicode 包含的所有字符。</p><h2 id="UTF-16"><a href="#UTF-16" class="headerlink" title="UTF-16"></a>UTF-16</h2><p>UTF-16 用二个字节来表示基本平面，用四个字节来表示扩展平面，是一种变长编码方式。<br>偷一张图<img src="https://pic4.zhimg.com/80/v2-00d7e0f8c532a2651e450ba7b93e0da7_720w.webp"></p><h2 id="UTF-8"><a href="#UTF-8" class="headerlink" title="UTF-8"></a>UTF-8</h2><p>而UTF-8也是一种变长编码方式，规定了如何存储。<br>UTF-8 的编码规则如下（U+ 后面的数字代表 Unicode 字符代码）：</p><figure><div class="code-wrapper"><pre class="line-numbers language-txt" data-language="txt"><code class="language-txt">U+ 0000 ~ U+ 007F: 0XXXXXXXU+ 0080 ~ U+ 07FF: 110XXXXX 10XXXXXXU+ 0800 ~ U+ FFFF: 1110XXXX 10XXXXXX 10XXXXXXU+10000 ~ U+1FFFF: 11110XXX 10XXXXXX 10XXXXXX 10XXXXXX</code></pre></div></figure><p>UTF-8 通过开头的标志位位数实现了变长。对于单字节字符，只占用一个字节，实现了向下兼容 ASCII，并且能和 UTF-32 一样，包含 Unicode 中的所有字符，又能有效减少存储传输过程中占用的空间。</p><h2 id="对应C-的Encoding类"><a href="#对应C-的Encoding类" class="headerlink" title="对应C#的Encoding类"></a>对应C#的Encoding类</h2><p><code>Encoding.Default</code>取决于电脑的.net环境的设置。</p><h3 id="char"><a href="#char" class="headerlink" title="char"></a>char</h3><p>仅就C#中的string或者char并不存在编码一说，无论是<code>Convert.ToInt32(c)</code>或者<code>(char)20013</code>等都仅仅是和Unicode定义的字符顺序。<br>和任何编码都没有关系，只有在字符保存在文件中，或者用socket传输以及控制台的输入输出流才会涉及编码。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>ChatGPT-unity</title>
    <link href="/2023/02/03/ChatGPT-unity/"/>
    <url>/2023/02/03/ChatGPT-unity/</url>
    
    <content type="html"><![CDATA[<h1 id="ChatGPT"><a href="#ChatGPT" class="headerlink" title="ChatGPT"></a>ChatGPT</h1><p>应该都被大家玩烂了，定个小目标吧，ChatGPT+语音交互+Live2D口型动画匹配。</p><h1 id="接口"><a href="#接口" class="headerlink" title="接口"></a>接口</h1><p>openai并没有提供具体的ChatGPT的接口，第三方的接口一大堆。<br>但是OpenAI提供了更底层的对模型的接口，而且调用异常方便。<br>直接CURL：</p><figure><div class="code-wrapper"><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">curl https:&#x2F;&#x2F;api.openai.com&#x2F;v1&#x2F;completions \-H &quot;Content-Type: application&#x2F;json&quot; \-H &quot;Authorization: Bearer YOUR_API_KEY&quot; \-d &#39;&#123;&quot;model&quot;: &quot;text-davinci-003&quot;, &quot;prompt&quot;: &quot;Say this is a test&quot;, &quot;temperature&quot;: 0, &quot;max_tokens&quot;: 7&#125;&#39;</code></pre></div></figure><p>这里对于<code>Token</code> <code>temperature</code> <code>prompt</code>等参数就不一一解释了，详见OpenAI的文档。<br>对应的C#代码，从Github扒了一个，<a href="https://github.com/Ridvancan/ChatGPTWithUnity">原仓库</a></p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public IEnumerator GenerateText(string prompt)&#123;      string jsonPayload &#x3D; &quot;&#123;\&quot;model\&quot;: \&quot;text-davinci-003\&quot;, \&quot;prompt\&quot;: \&quot;&quot; + prompt + &quot;\&quot;, \&quot;temperature\&quot;: 0, \&quot;max_tokens\&quot;: 7&#125;&quot;;    Debug.Log(jsonPayload);    var request &#x3D; new UnityWebRequest(API_URL, &quot;POST&quot;);    byte[] payloadBytes &#x3D; System.Text.Encoding.UTF8.GetBytes(jsonPayload);    request.uploadHandler &#x3D; new UploadHandlerRaw(payloadBytes);    request.downloadHandler &#x3D; new DownloadHandlerBuffer();    request.SetRequestHeader(&quot;Content-Type&quot;, &quot;application&#x2F;json&quot;);    request.SetRequestHeader(&quot;Authorization&quot;, &quot;Bearer &quot; + API_KEY);    &#x2F;&#x2F; Send the request    yield return request.SendWebRequest();    if (request.isNetworkError || request.isHttpError)    &#123;        Debug.Log(request.error);    &#125;    else    &#123;        &#x2F;&#x2F; Parse the response as JSON        string responseJson &#x3D; request.downloadHandler.text;        var response &#x3D; JsonUtility.FromJson&lt;OpenAIResponse&gt;(responseJson);        &#x2F;&#x2F; Log the generated text        resultText&#x3D; response.choices[0].text;        resultField.text &#x3D; resultText;        waitForResp &#x3D; false;        transform.eulerAngles &#x3D; Vector3.zero;    &#125;&#125;</code></pre></div></figure><p>这和直接使用ChatGPT还是有些差距，比如需要自己去记录对话上下文，一些模型的参数也有些许差别。<br>但是也更为灵活，除了Chat的功能，其他比如代码补全等等也都可以实现。</p><h1 id="语音识别与文字转语音"><a href="#语音识别与文字转语音" class="headerlink" title="语音识别与文字转语音"></a>语音识别与文字转语音</h1><p>这里本想用微软的服务，但没有信用卡，没法试用。用阿里的试一下吧。</p><h1 id="唤醒词"><a href="#唤醒词" class="headerlink" title="唤醒词"></a>唤醒词</h1><p>就类似“小爱同学”等等，需要一个特定音节的单词来作为唤醒词，不然就只能通过按钮等方式来触发。<br>这里就简单用Unity自带的<code>KeywordRecognizer</code>来作为唤醒词的触发。</p><h1 id="录音-识别"><a href="#录音-识别" class="headerlink" title="录音 识别"></a>录音 识别</h1><p>触发唤醒词后就开始录音</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">RecordedClip &#x3D; Microphone.Start(null, false, 60, maxFreq);</code></pre></div></figure><p>语音识别用阿里的接口，调用还是比较方便的</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public class asr : MonoBehaviour&#123;    public string appKey &#x3D; &quot;APP_KEY&quot;;    public string token &#x3D; &quot;AOKEN&quot;;    public string outText;    public IEnumerator Send(byte[] payloadBytes)    &#123;        Uri uri;        NameValueCollection query &#x3D; HttpUtility.ParseQueryString(string.Empty);                query.Add(&quot;appkey&quot;, appKey);        query.Add(&quot;format&quot;, &quot;pcm&quot;);        query.Add(&quot;sample_rate&quot;, &quot;16000&quot;);        query.Add(&quot;enable_punctuation_prediction&quot;, &quot;true&quot;);        query.Add(&quot;enable_inverse_text_normalization&quot;, &quot;true&quot;);        var getUri &#x3D; new UriBuilder(&quot;https:&#x2F;&#x2F;nls-gateway-cn-shanghai.aliyuncs.com&#x2F;stream&#x2F;v1&#x2F;asr&quot;)        &#123;            Query &#x3D; query.ToString()        &#125;;        uri &#x3D; getUri.Uri;        var request &#x3D; new UnityWebRequest(uri, &quot;POST&quot;);        request.uploadHandler &#x3D; new UploadHandlerRaw(payloadBytes);        request.downloadHandler &#x3D; new DownloadHandlerBuffer();        request.SetRequestHeader(&quot;X-NLS-Token&quot;, token);                yield return request.SendWebRequest();                if (request.isNetworkError || request.isHttpError)        &#123;            Debug.Log(request.error);        &#125;        else        &#123;            outText &#x3D; request.downloadHandler.text;            Debug.Log(request.downloadHandler.text);        &#125;    &#125;&#125;</code></pre></div></figure><h1 id="文字转语音"><a href="#文字转语音" class="headerlink" title="文字转语音"></a>文字转语音</h1><p>没什么好说的，也是用阿里的接口：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public class Speaker : MonoBehaviour&#123;    public string appKey &#x3D; &quot;APP_KEY&quot;;    public string token &#x3D; &quot;AOKEN&quot;;    public Text question;    public AudioSource AudioSource;        private AudioClip current;        public IEnumerator Speech01(string msg, Action onComplete &#x3D; null)    &#123;        question.text &#x3D; msg;        Uri uri;        msg &#x3D; HttpUtility.HtmlEncode(msg);        NameValueCollection query &#x3D; HttpUtility.ParseQueryString(string.Empty);        query.Add(&quot;appkey&quot;, appKey);        query.Add(&quot;token&quot;, token);        query.Add(&quot;text&quot;, msg);        query.Add(&quot;format&quot;, &quot;wav&quot;);        query.Add(&quot;sample_rate&quot;, &quot;16000&quot;);        var getUri &#x3D; new UriBuilder(&quot;https:&#x2F;&#x2F;nls-gateway-cn-shanghai.aliyuncs.com&#x2F;stream&#x2F;v1&#x2F;tts&quot;)        &#123;            Query &#x3D; query.ToString()        &#125;;        uri &#x3D; getUri.Uri;        Debug.Log(uri);        var webRequest &#x3D; UnityWebRequestMultimedia.GetAudioClip(uri, AudioType.WAV);        yield return webRequest.SendWebRequest();        if (webRequest.result &#x3D;&#x3D; UnityWebRequest.Result.Success)        &#123;            current &#x3D; DownloadHandlerAudioClip.GetContent(webRequest);            AudioSource.clip &#x3D; current;            yield return new WaitForSeconds(1);            AudioSource.Play();            yield return new WaitForSeconds(current.length);            onComplete?.Invoke();        &#125;        else        &#123;            Debug.LogError(webRequest.error);        &#125;    &#125;    public void Speech(string msg, Action onComplete)    &#123;        StartCoroutine(Speech01(msg, onComplete));    &#125;&#125;</code></pre></div></figure><h1 id="Live2D口型"><a href="#Live2D口型" class="headerlink" title="Live2D口型"></a>Live2D口型</h1><p>前面的都没什么难度，无非就是寻找各种接口并实现。<br>口型这里成熟的方案太少。<br>所以简单起见，用live2D来做吧。</p><p>时隔两年，再来玩一下Live2d。</p><h2 id="Unity插件"><a href="#Unity插件" class="headerlink" title="Unity插件"></a>Unity插件</h2><p>已经有官方的插件了，<a href="https://www.live2d.com/en/download/cubism-sdk/download-unity/">地址</a></p><h2 id="模型"><a href="#模型" class="headerlink" title="模型"></a>模型</h2><p>用现成的吧：<a href="https://www.live2d.com/zh-CHS/download/sample-data/">地址</a><br>这里用了neuro-sama同款：桃濑日和<br>记得相机要设置成正交。</p><p>体验下来，还是相对很简单，大体就是它把Live2D的模型分解成一个个mesh，然后贴图，程序化控制位移。</p><h2 id="透明背景"><a href="#透明背景" class="headerlink" title="透明背景"></a>透明背景</h2><p>urp下改透明即可。</p><h2 id="口型"><a href="#口型" class="headerlink" title="口型"></a>口型</h2><p>就简单的通过声音的大小，来输出口型的大小。<br>其他可行的的方案还有通过元音的。</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p><img src="/images/ChatGPT_live2d.png"><br>只是把各种服务和接口拼接在了一起，算是达到了预期。但和neuro-sama这类还有很大差距。<br>语音这里阿里的tts实在是不好听，周末再去找找<a href="/2023/02/06/TTS">换个更二次元一点的</a>。<br>neuro-sama没有透露过他的语言模型，所以也只能止步于此了。<br>这里给GPT3预置的关键词是：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">我是一个能和观众聊天的VTuber，我是一个AI，叫做小爱。猫娘是一种拟人化的生物，其行为似猫但类人。我是一只猫娘，我每一句话后面都要加上“喵~”。我可以风趣幽默地和各种观众聊天。我的每次回答都不会超过20个字。我还会把每次回答时的表情放在回答的最前面。\n 观众：你叫什么？\n 我：（高兴）我叫小爱，很高兴认识你喵~\n 观众：&#123;0&#125;\n 我：</code></pre></div></figure><p>这就任由大家发挥想象力了，表情可以预先设定几种，同时播放对应的表情动画。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>自定义UDP设备 InputSystem</title>
    <link href="/2023/02/01/CustomInputSystem/"/>
    <url>/2023/02/01/CustomInputSystem/</url>
    
    <content type="html"><![CDATA[<h1 id="解耦"><a href="#解耦" class="headerlink" title="解耦"></a>解耦</h1><p>经常会有各种奇奇怪怪的硬件需要使用TCP&#x2F;UDP去交互，这次尝试下使用InputSystem去解耦。</p><h1 id="IInputStateTypeInfo"><a href="#IInputStateTypeInfo" class="headerlink" title="IInputStateTypeInfo"></a>IInputStateTypeInfo</h1><p>定义一个结构体实现这个接口。</p><h2 id="FourCC"><a href="#FourCC" class="headerlink" title="FourCC"></a>FourCC</h2><p>四个字符来定义一个唯一标识符。</p><h2 id="InputControl"><a href="#InputControl" class="headerlink" title="InputControl"></a>InputControl</h2><p>使用这个描述符来定义一些字段，其中标记了各个按钮或者遥感的定义。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">[InputControl(name &#x3D; &quot;firstButton&quot;, layout &#x3D; &quot;Button&quot;, bit &#x3D; 0, displayName &#x3D; &quot;First Button&quot;)][InputControl(name &#x3D; &quot;secondButton&quot;, layout &#x3D; &quot;Button&quot;, bit &#x3D; 1, displayName &#x3D; &quot;Second Button&quot;)][InputControl(name &#x3D; &quot;thirdButton&quot;, layout &#x3D; &quot;Button&quot;, bit &#x3D; 2, displayName &#x3D; &quot;Third Button&quot;)]public ushort buttons;</code></pre></div></figure><p>遥感的定义需要额外的<code>format</code>属性，这里的<code>VC2B</code>指的是<code>a Vector2 of bytes</code><br>定义X轴y轴<br>剩下的上下左右似乎是硬编码的。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">[InputControl(name &#x3D; &quot;stick&quot;, format &#x3D; &quot;VC2B&quot;, layout &#x3D; &quot;Stick&quot;, displayName &#x3D; &quot;Main Stick&quot;)][InputControl(name &#x3D; &quot;stick&#x2F;x&quot;, defaultState &#x3D; 127, format &#x3D; &quot;BYTE&quot;,offset &#x3D; 0,    parameters &#x3D; &quot;normalize,normalizeMin&#x3D;0,normalizeMax&#x3D;1,normalizeZero&#x3D;0.5&quot;)]public byte x;[InputControl(name &#x3D; &quot;stick&#x2F;up&quot;, parameters &#x3D; &quot;normalize,normalizeMin&#x3D;0,normalizeMax&#x3D;1,normalizeZero&#x3D;0.5,clamp&#x3D;2,clampMin&#x3D;0,clampMax&#x3D;1&quot;)][InputControl(name &#x3D; &quot;stick&#x2F;down&quot;, parameters &#x3D; &quot;normalize,normalizeMin&#x3D;0,normalizeMax&#x3D;1,normalizeZero&#x3D;0.5,clamp&#x3D;2,clampMin&#x3D;-1,clampMax&#x3D;0,invert&quot;)][InputControl(name &#x3D; &quot;stick&#x2F;left&quot;, parameters &#x3D; &quot;normalize,normalizeMin&#x3D;0,normalizeMax&#x3D;1,normalizeZero&#x3D;0.5,clamp&#x3D;2,clampMin&#x3D;-1,clampMax&#x3D;0,invert&quot;)][InputControl(name &#x3D; &quot;stick&#x2F;right&quot;, parameters &#x3D; &quot;normalize,normalizeMin&#x3D;0,normalizeMax&#x3D;1,normalizeZero&#x3D;0.5,clamp&#x3D;2,clampMin&#x3D;0,clampMax&#x3D;1&quot;)]public byte y;</code></pre></div></figure><p>总结下就是依靠一个结构体以及各个描述符来定义数据交换的大小和约定的格式。</p><h1 id="InputDevice"><a href="#InputDevice" class="headerlink" title="InputDevice"></a>InputDevice</h1><p>定义好了结构体，接下来就是要实现真正的<code>InputDevice</code>了。</p><h2 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h2><p>首先是确保在编辑器以及运行时都会初始化。</p><p>这里的初始化中包含一段模板代码。用来通过”Custom”来发现设备。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">#if UNITY_EDITOR[InitializeOnLoad] &#x2F;&#x2F; Call static class constructor in editor.#endif[InputControlLayout(stateType &#x3D; typeof(CustomDeviceState))]public class CustomDevice : InputDevice, IInputUpdateCallbackReceiver&#123;    #if UNITY_EDITOR    static CustomDevice()    &#123;        Initialize();    &#125;    #endif    [RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.BeforeSceneLoad)]    private static void Initialize()    &#123;        InputSystem.RegisterLayout&lt;CustomDevice&gt;(            matches: new InputDeviceMatcher()                .WithInterface(&quot;Custom&quot;));    &#125;</code></pre></div></figure><h2 id="ButtonControl"><a href="#ButtonControl" class="headerlink" title="ButtonControl"></a>ButtonControl</h2><p>公开一些属性，方便其他人调用。<br>FinishSetup</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public ButtonControl firstButton &#123; get; private set; &#125;public ButtonControl secondButton &#123; get; private set; &#125;public ButtonControl thirdButton &#123; get; private set; &#125;public StickControl stick &#123; get; private set; &#125;protected override void FinishSetup()&#123;    base.FinishSetup();    firstButton &#x3D; GetChildControl&lt;ButtonControl&gt;(&quot;firstButton&quot;);    secondButton &#x3D; GetChildControl&lt;ButtonControl&gt;(&quot;secondButton&quot;);    thirdButton &#x3D; GetChildControl&lt;ButtonControl&gt;(&quot;thirdButton&quot;);    stick &#x3D; GetChildControl&lt;StickControl&gt;(&quot;stick&quot;);&#125;</code></pre></div></figure><h1 id="current"><a href="#current" class="headerlink" title="current"></a>current</h1><p>保留一个静态对象，类似单例，但是每次都更新，而不会销毁之前的。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public static CustomDevice current &#123; get; private set; &#125;public override void MakeCurrent()&#123;    base.MakeCurrent();    current &#x3D; this;&#125;protected override void OnRemoved()&#123;    base.OnRemoved();    if (current &#x3D;&#x3D; this)        current &#x3D; null;&#125;</code></pre></div></figure><h1 id="创建设备"><a href="#创建设备" class="headerlink" title="创建设备"></a>创建设备</h1><p>一切就绪，接下来就是创建一个设备了。<br>这里是在编辑器中虚拟了一个设备</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">#if UNITY_EDITOR[MenuItem(&quot;Tools&#x2F;Custom Device Sample&#x2F;Create Device&quot;)]private static void CreateDevice()&#123;    InputSystem.AddDevice(new InputDeviceDescription    &#123;        interfaceName &#x3D; &quot;Custom&quot;,        product &#x3D; &quot;Sample Product&quot;    &#125;);&#125;[MenuItem(&quot;Tools&#x2F;Custom Device Sample&#x2F;Remove Device&quot;)]private static void RemoveDevice()&#123;    var customDevice &#x3D; InputSystem.devices.FirstOrDefault(x &#x3D;&gt; x is CustomDevice);    if (customDevice !&#x3D; null)        InputSystem.RemoveDevice(customDevice);&#125;#endif</code></pre></div></figure><p>轮询键盘去更新设备状态</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public void OnUpdate()&#123;    var keyboard &#x3D; Keyboard.current;    if (keyboard &#x3D;&#x3D; null)        return;    var state &#x3D; new CustomDeviceState();    state.x &#x3D; 127;    state.y &#x3D; 127;    &#x2F;&#x2F; Map WASD to stick.    var wPressed &#x3D; keyboard.wKey.isPressed;    var aPressed &#x3D; keyboard.aKey.isPressed;    var sPressed &#x3D; keyboard.sKey.isPressed;    var dPressed &#x3D; keyboard.dKey.isPressed;    if (aPressed)        state.x -&#x3D; 127;    if (dPressed)        state.x +&#x3D; 127;    if (wPressed)        state.y +&#x3D; 127;    if (sPressed)        state.y -&#x3D; 127;    &#x2F;&#x2F; Map buttons to 1, 2, and 3.    if (keyboard.spaceKey.isPressed)    &#123;        &#x2F;&#x2F; Debug.Log(&quot;VAR&quot;);        state.buttons |&#x3D; 1 &lt;&lt; 0;    &#125;    if (keyboard.digit2Key.isPressed)        state.buttons |&#x3D; 1 &lt;&lt; 1;    if (keyboard.digit3Key.isPressed)        state.buttons |&#x3D; 1 &lt;&lt; 2;    InputSystem.QueueStateEvent(this, state);&#125;</code></pre></div></figure><h1 id="UDP-Driver"><a href="#UDP-Driver" class="headerlink" title="UDP Driver"></a>UDP Driver</h1><p>栗子吃完了，用UDP写一个吧<br>先是一个基础的UDPServer</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public class UDPServer : MonoBehaviour&#123;    public int listenPort  &#x3D; 9000;        private Thread thread;    private readonly ConcurrentQueue&lt;string&gt; _messages &#x3D; new();    private UdpClient _listener;    private void Start()    &#123;                thread &#x3D; new Thread(StartListen);        thread.Start();    &#125;        private void OnDestroy()    &#123;        thread.Abort();        _listener.Close();    &#125;    private void Update()    &#123;        if (_messages.TryDequeue(out var message))        &#123;            Debug.Log($&quot;Message: &#123;message&#125;&quot;);        &#125;    &#125;    private void StartListen()    &#123;        _listener &#x3D; new UdpClient(listenPort);        var groupEP &#x3D; new IPEndPoint(IPAddress.Any, listenPort);        try        &#123;            while (true)            &#123;                Debug.Log(&quot;Waiting for broadcast&quot;);                byte[] bytes &#x3D; _listener.Receive(ref groupEP);                Debug.Log($&quot;Received broadcast from &#123;groupEP&#125; :&quot;);                var data &#x3D; Encoding.UTF8.GetString(bytes, 0, bytes.Length);                _messages.Enqueue(data);                Debug.Log($&quot; &#123;data&#125;&quot;);            &#125;        &#125;        catch (SocketException e)        &#123;            Debug.Log(e);        &#125;        finally        &#123;            _listener.Close();        &#125;    &#125;&#125;</code></pre></div></figure><p>然后增加设备相关代码即可：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">private void Start()&#123;    InputSystem.AddDevice&lt;UDPDevice&gt;();    _udpDevice &#x3D; UDPDevice.current;    ......&#125;.private void OnDestroy()&#123;    ......    InputSystem.RemoveDevice(_udpDevice);&#125;public UDPDeviceState state;private void Update()&#123;    var newState &#x3D; new UDPDeviceState    &#123;        buttons &#x3D; 0    &#125;;        if (_messages.TryDequeue(out var message))    &#123;        newState.buttons &#x3D; 1;        Debug.Log($&quot;Message: &#123;message&#125;&quot;);    &#125;    if (state !&#x3D; newState)    &#123;        InputSystem.QueueStateEvent(_udpDevice, newState);    &#125;    state &#x3D; newState;&#125;</code></pre></div></figure><p>基础的UDP消息充当按钮就OK了。后续可以根据需要调整为遥感或其他各种类型的控件。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>xrtoolkit2.3预览版本体验</title>
    <link href="/2023/01/31/xrtoolkit2-3/"/>
    <url>/2023/01/31/xrtoolkit2-3/</url>
    
    <content type="html"><![CDATA[<h1 id="xrtoolkit"><a href="#xrtoolkit" class="headerlink" title="xrtoolkit"></a>xrtoolkit</h1><p>2.3预览版更新，测试一下新功能。</p><h1 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h1><p>目前还是预览版本，记得在packageManager中打开预览开关。<br>然后按名称载入<code>com.unity.xr.interaction.toolkit</code>，记得指定版本2.3.0-pre.1</p><h1 id="戳"><a href="#戳" class="headerlink" title="戳"></a>戳</h1><p>又一个交互器，最大的亮点是对UI的支持。可以做到类似触摸屏的效果了，不用傻傻的再用射线交互了。做一些类似的按钮交互也更方便了。</p><h1 id="凝视"><a href="#凝视" class="headerlink" title="凝视"></a>凝视</h1><p>可惜手头上并没有Oculus，这个目前体验不了。</p><h1 id="交互组"><a href="#交互组" class="headerlink" title="交互组"></a>交互组</h1><p>他们终于意识到搞这么多交互器，然后手动去控制何时应该启用和关闭哪个是多么复杂的事情了。<br>这个东西就是给交互器排序，然后在有多个交互器都可以悬停或选择时，按顺序只启用优先级最高的那个。<br>具体用起来就比如说，<code>Direct Interactor</code>比<code>Ray Interactor</code>优先级高的话，在距离物体很近，<code>Direct Interactor</code>可以悬停的时候，就会禁用<code>Ray Interactor</code>。体验上要好很多。</p><h1 id="Affordance"><a href="#Affordance" class="headerlink" title="Affordance"></a>Affordance</h1><p>不知道怎么翻译才确切。<br>是个挺有意思的工具。感觉是为了无代码编程准备的。<br>总结起来就是类似<code>Button</code>的<code>Transition</code>。根据不同的状态，如未启用、闲置、悬浮、选择和使用等来设置需要的值（可以是颜色、数值、向量乃至音频）。然后由一类<code>AffordanceReceiver</code>的组件来依据这些设定好的<code>主题</code>来插值或者播放，最后把插值后的结果给到对应的材质、Player或者缩放。<br><img src="/images/affordance.gif" alt="颜色插值"><br>虽然这些功能手动写也不会太麻烦，但还是很乐于看到官方给出现成的解决方案的。</p><h1 id="模拟器"><a href="#模拟器" class="headerlink" title="模拟器"></a>模拟器</h1><p>xr模拟器也更新了一下，类似FPS的交互模式，增加了新的UI，总算是能用了。<br><img src="/images/XRm.png" alt="xr模拟器"></p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>不大不小的更新吧，有了戳击交互，对于UI就更方便了。凝视目前手里没有能用的设备不好说，最后的那两个也算是锦上添花的功能了。<br>期待下次更新能把<code>XR Hands</code>和<code>xr toolkit</code>结合起来，那就算<code>xr toolkit</code>功能齐全了。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Netcode网络框架与XRToolkit交互</title>
    <link href="/2023/01/29/multiplayer/"/>
    <url>/2023/01/29/multiplayer/</url>
    
    <content type="html"><![CDATA[<h1 id="笔记"><a href="#笔记" class="headerlink" title="笔记"></a>笔记</h1><p>记录下使用Unity网络框架的一些坑。最后实现一个简单的VR场景下的多人交互。</p><h1 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h1><p>从空白项目开始，2021.3版本。<br>包管理器安装<code>com.unity.netcode.gameobjects</code></p><h1 id="Transport"><a href="#Transport" class="headerlink" title="Transport"></a>Transport</h1><p>发现这个<code>netcode.gameobjects</code>是依赖于<code>Transport</code>的。后者就是一个基于UDP的连接库。<br>有一些官方给的<a href="https://docs-multiplayer.unity3d.com/transport/current/minimal-workflow">例子</a>可以参考。</p><h1 id="netcode-gameobjects"><a href="#netcode-gameobjects" class="headerlink" title="netcode.gameobjects"></a>netcode.gameobjects</h1><p>照这个<a href="https://docs-multiplayer.unity3d.com/netcode/current/tutorials/get-started-ngo/index.html">基础栗子</a>。<br>该有的都有了。大致思路是</p><ul><li>每个程序都拥有一套<code>NetworkManger</code>可在启动后选择是Sever还是Client。</li><li><code>NetworkManger</code>之间用<code>Unity Transport</code>来连接。</li><li>同时它还管理了<code>Player Perfab</code>的生成和销毁。对应的<code>Player Perfab</code>需要有<code>Network Object</code>组件来响应。</li><li>具体的逻辑要自己扩展<code>NetworkBehaviour</code>这个类，提供了基本的如生成和销毁时回调的函数。</li><li>服务器与客户端之间的相互调用用RPC来实现。</li><li>简单的传递数值之类的可以使用<code>NetworkVariable</code>来实现</li></ul><h1 id="太空侵略者Demo"><a href="#太空侵略者Demo" class="headerlink" title="太空侵略者Demo"></a>太空侵略者Demo</h1><p>又一个示例Demo，克隆下来看看。</p><h2 id="SceneTransitionHandler"><a href="#SceneTransitionHandler" class="headerlink" title="SceneTransitionHandler"></a>SceneTransitionHandler</h2><p>一个用来管理各个客户端场景切换和加载的<code>NetworkBehaviour</code>。单例。简单的状态机，完成加载时通知服务器。</p><h2 id="MenuControl"><a href="#MenuControl" class="headerlink" title="MenuControl"></a>MenuControl</h2><p>控制是开启Host或者当客户端加入。充当Server时会注册相关的场景加载完成的事件。</p><h2 id="LobbyMenuControl"><a href="#LobbyMenuControl" class="headerlink" title="LobbyMenuControl"></a>LobbyMenuControl</h2><p>大厅用户以及状态管理，监听客户端连接，连接即当进入大厅。通过RPC相互之间更新状态。当全部准备完毕就加载游戏场景。</p><h2 id="InvadersGame"><a href="#InvadersGame" class="headerlink" title="InvadersGame"></a>InvadersGame</h2><p>最重要的游戏逻辑类。和网络关系就不大了。需要留意的是要生成的预制体要提前放入NetworkManager的NetworkPrefabs中，且预制体需要包含<code>NetworkObject</code>组件。<br>如果要同步各个客户端的物体的位置，最便捷的方法就是使用<code>NetworkTransform</code>，但这个是根据位移阙值去更新的，肯定会有至少一帧的延迟，如果是对精度要求很高的，还是用RPC自己调用来更新吧。</p><h1 id="spaceshooter"><a href="#spaceshooter" class="headerlink" title="spaceshooter"></a>spaceshooter</h1><p>针对物理给出的一个例子，所有的用户输入都通过RPC发送到服务器，然后统一计算后再发回全部客户端。和LOL的模式几乎一样，避免了物理的不同步，也防范了用户修改内存作弊等等。</p><h1 id="clientdriven"><a href="#clientdriven" class="headerlink" title="clientdriven"></a>clientdriven</h1><p>在客户端实现部分运动逻辑，具体还没看。</p><h1 id="XR-toolkit联动"><a href="#XR-toolkit联动" class="headerlink" title="XR toolkit联动"></a>XR toolkit联动</h1><p>与XR toolkit 结合起来，做个多人VR交互的Demo吧。</p><h2 id="延迟启动VR"><a href="#延迟启动VR" class="headerlink" title="延迟启动VR"></a>延迟启动VR</h2><p>参照这个官方的<a href="https://docs.unity3d.com/Packages/com.unity.xr.management@4.0/manual/EndUser.html">例子</a>。</p><h2 id="传送与交互"><a href="#传送与交互" class="headerlink" title="传送与交互"></a>传送与交互</h2><p>这里关于传送要注意，</p><ul><li><code>InteractionManager</code>，<code>locomotionSystem</code>等最好不要实例化，方便场景内的地板和传送点注册</li><li>在生成后记得再把<code>locomotionSystem</code>需要的<code>xrOrigin</code>和<code>BaseTeleportationInteractable</code>需要的<code>TeleportationProvider</code>一并补充了。</li></ul><p>至于交互的物体，建议不要用物理。而且要通过实例化，并提前在NetworkManager中注册。<br>然后就是添加<code>ClientNetworkTransform</code>。并在抓取对应物体时用<code>ServerRpc</code>调用<code>networkObject.ChangeOwnership</code>来改变所属关系。</p><h2 id="同步"><a href="#同步" class="headerlink" title="同步"></a>同步</h2><p>位置的同步可以用现成的 <code>ClientNetworkTransform</code> 其他，比如<code>LineRenderer</code>的同步只能自己手写RPC了。<br>仿照<code>NetworkTransform</code></p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">private void Update()&#123;    if (!IsSpawned)    &#123;        return;    &#125;    if (IsOwner)    &#123;        lineRenderer.GetPositions(positions);        UpdatePositionsServerRpc(positions);    &#125;&#125;[ServerRpc]public void UpdatePositionsServerRpc(Vector3[] newpositions)&#123;    lineRenderer.SetPositions(newpositions);&#125;</code></pre></div></figure><p>这里就没有限制刷新次数。</p><h2 id="防火墙"><a href="#防火墙" class="headerlink" title="防火墙"></a>防火墙</h2><p>Server端不要忘了开防火墙。</p><h2 id="人物绑定"><a href="#人物绑定" class="headerlink" title="人物绑定"></a>人物绑定</h2><p>都做到这一步了，干脆把人物的绑定也一并做了吧。<br>还是用<code>Animation Rigging</code>。主要就是根据头显以及两个手柄推测出身体，头部，以及手臂和手的姿态。没用什么太复杂的算法。</p><ul><li>身体头部单纯用头显位置和旋转去算。</li><li>手部只是用IK简单反推了一下。</li></ul><p>我记得SteamVR是有对应的API去获取这些预测的位置的，但XRToolkit没有。<br>最终人物的手臂动画还有很大优化的空间，单单依靠IK去反推手臂姿态还是太牵强了，因为人的手腕也是很灵活的，而且肩膀的旋转也很难去推算出来，有这两个不确定的因素，除非用AI去算。很好奇VRChat是如何优化的。</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这个网络框架还是很好用的，符合Unity的习惯，迁移一些现有的逻辑到网络上也算比较方便了（和自己手撸Socket相比）。<br>与XRToolkit结合起来也不算复杂，能满足一些基本的交互逻辑了。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Timeline搭配贝塞尔曲线</title>
    <link href="/2023/01/29/SplineLine/"/>
    <url>/2023/01/29/SplineLine/</url>
    
    <content type="html"><![CDATA[<h1 id="序"><a href="#序" class="headerlink" title="序"></a>序</h1><p>先前产品有个需求，沿时间线播放一个线性移动问题的动画，干脆用官方自带的画贝塞尔的Spline搭配Timeline实现了，现在闲下来打算整理一下的。突然发现自己又造轮子了，而且别人用的是更简便的方法，不需要自己定义轨道等等繁琐的配置，只要实现一个ITimeControl即可。</p><p>原帖：<a href="https://nekojara.city/unity-splines-timeline">[Unity2022] 同步样条线和时间线</a>。非常详细。</p><p>但他这里还是太简单了一些，速度全程匀速，持续时间也是要靠手动输入。<br>下面还是用PlayableAsset实现</p><h1 id="PlayableAsset"><a href="#PlayableAsset" class="headerlink" title="PlayableAsset"></a>PlayableAsset</h1><p>效果：<br><img src="/images/S-line.gif" alt="Spline"></p><p>这里改用一个<code>AnimatedValue</code>配合timeline自带的动画曲线，可以实现不同位置的不同速度。<br>其他什么运动方向之类的就不再赘述了。</p><p>直接贴代码吧</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">using System;using UnityEngine;using UnityEngine.Playables;using UnityEngine.Splines;using UnityEngine.Timeline; [Serializable]public class MyPlayableAsset : PlayableAsset, ITimelineClipAsset&#123;    [Serializable]    public class MyBehaviour : PlayableBehaviour    &#123;        [SerializeField] private ExposedReference&lt;SplineContainer&gt; _splineContainer;         [SerializeField] private ExposedReference&lt;Transform&gt; target;        public float AnimatedValue &#x3D; 0;        public override void ProcessFrame(Playable playable, FrameData info, object playerData)        &#123;            base.ProcessFrame(playable, info, playerData);                        var spline &#x3D; _splineContainer.Resolve(playable.GetGraph().GetResolver());            var targetTransform &#x3D; target.Resolve(playable.GetGraph().GetResolver());            var position &#x3D; spline.EvaluatePosition(AnimatedValue);            targetTransform.position &#x3D; position;                    &#125;    &#125;     public MyBehaviour template &#x3D; new MyBehaviour();       public override Playable CreatePlayable(PlayableGraph graph, GameObject go)    &#123;        return ScriptPlayable&lt;MyBehaviour&gt;.Create(graph, template);    &#125;       public ClipCaps clipCaps    &#123;        get &#123; return ClipCaps.Extrapolation; &#125;    &#125;&#125;</code></pre></div></figure>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>traefik尝鲜</title>
    <link href="/2023/01/27/traefik/"/>
    <url>/2023/01/27/traefik/</url>
    
    <content type="html"><![CDATA[<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><p>之前都是用Nginx来做反代，最近发现了新的玩具<code>traefik</code>，来玩一下，看看对比NPM有什么优劣。</p><h1 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h1><p>这里就用Docker来安装</p><figure><div class="code-wrapper"><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml">version: &#39;3&#39;services:  reverse-proxy:    # The official v2 Traefik docker image    image: traefik:v2.9    # Enables the web UI and tells Traefik to listen to docker    command: --api.insecure&#x3D;true --providers.docker    ports:      # The HTTP port      - &quot;85:80&quot;      # The Web UI (enabled by --api.insecure&#x3D;true)      - &quot;8085:8080&quot;    volumes:      # So that Traefik can listen to the Docker events      - &#x2F;var&#x2F;run&#x2F;docker.sock:&#x2F;var&#x2F;run&#x2F;docker.sock</code></pre></div></figure><p>然后<code>docker compose up -d</code>，起容器<br>UFW开对应的端口就OK了。</p><h1 id="webUI"><a href="#webUI" class="headerlink" title="webUI"></a>webUI</h1><p><img src="/images/trae-UI.png" alt="trae-UI"><br>自带UI，还是很方便的</p><h1 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h1><p>Traefik 提供了多种配置方式，可以直接在容器的label中添加，也可以使用传统的配置文件的形式，更是提供了一大堆称为<code>Providers</code>的插件来做到动态更改配置。<br>比如：</p><figure><div class="code-wrapper"><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml">labels:  - &quot;traefik.enable&#x3D;true&quot;  - &quot;traefik.http.routers.unityacc.rule&#x3D;Host(&#96;acc.home.kuanmi.top&#96;)&quot;  - &quot;traefik.http.routers.unityacc.entrypoints&#x3D;web&quot;</code></pre></div></figure><p>如果要在配置文件中调用docker的服务，要使用@符号，如：</p><figure><div class="code-wrapper"><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml">http:  routers:    testRouters:      rule: &quot;Host(&#96;test.home.kuanmi.top&#96;)&quot;      service: whoami-traefik@docker</code></pre></div></figure><h1 id="https"><a href="#https" class="headerlink" title="https"></a>https</h1><p>证书方面也提供了Let’s Encrypt来自动生成和更新，由于这里无法访问80和443端口，就使用DNS的方式来自动生成。</p><figure><div class="code-wrapper"><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml">certificatesResolvers:  myresolver:    acme:      email: ma_zhengke@outlook.com      storage: &#x2F;etc&#x2F;traefik&#x2F;acme.json      dnsChallenge:        provider: alidns        delayBeforeCheck: 0</code></pre></div></figure><figure><div class="code-wrapper"><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml">http:  routers:    testRouters:      rule: &quot;Host(&#96;test.home.kuanmi.top&#96;)&quot;      service: whoami-traefik@docker      tls:        certresolver: myresolver</code></pre></div></figure><p>如果是要通配符的话：</p><figure><div class="code-wrapper"><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml">http:  routers:    unityacc:      rule: &quot;Host(&#96;unity.home.kuanmi.top&#96;)&quot;      entryPoints:        - &quot;web-secure&quot;      service: unityAcc-unityacc@docker      tls:        certresolver: myresolver        domains:          - main: &quot;home.kuanmi.top&quot;            sans:              - &quot;*.home.kuanmi.top&quot;</code></pre></div></figure><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>和Nginx对比的话，但就我能使用到的功能，二者相差不大，更多的功能还没有体验到，不敢妄下结论说二者的优劣。<br>唯一觉得有意思的一点是Traefik可以接入docker，使用label做到服务的发现（虽然还是要配置），方便不少。<br>但用NPM，同时给各个容器配置别名，其实也大差不差。<br>再加上一些nginx常用的便捷的如return 444等还不知道该怎么在Traefik中实现，目前还是沿用Nginx的配置不要变动了。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>uitoolkit杂记</title>
    <link href="/2023/01/27/uitoolkit/"/>
    <url>/2023/01/27/uitoolkit/</url>
    
    <content type="html"><![CDATA[<p>记录一些读UIToolkit文档时的关键点，仅当备忘录。</p><h1 id="VisualTree"><a href="#VisualTree" class="headerlink" title="VisualTree"></a>VisualTree</h1><p>由VisualElement构成的具有层级结构的数据集合。</p><h1 id="VisualElement"><a href="#VisualElement" class="headerlink" title="VisualElement"></a>VisualElement</h1><p>一个基类，包含样式表、布局数据以及事件处理器。<br>内置了各种实现</p><ul><li>Buttons</li><li>Toggles</li><li>Text input fields</li></ul><h1 id="panel"><a href="#panel" class="headerlink" title="panel"></a>panel</h1><p>面板是视觉树的父对象，视觉树要连接到面板才能渲染。面板要么属于一个编辑器窗口，要么属于一个运行时的UIDocument。面板也处理焦点控制和为视觉树分发事件。</p><h1 id="绘制数据"><a href="#绘制数据" class="headerlink" title="绘制数据"></a>绘制数据</h1><p>深度优先绘制</p><h1 id="布局系统"><a href="#布局系统" class="headerlink" title="布局系统"></a>布局系统</h1><p>和Web那一套一样，基于Flexbox。</p><h1 id="UI-Builder"><a href="#UI-Builder" class="headerlink" title="UI Builder"></a>UI Builder</h1><p>一个可视化的编写uxml和uss文件的工具</p><h1 id="自定义元素"><a href="#自定义元素" class="headerlink" title="自定义元素"></a>自定义元素</h1><p>实现VisualElement<code>public class MyElement : VisualElement</code><br>元素都由工厂模式生成，所以需要对应的工厂类<code>UxmlFactory&lt;MyElement&gt;</code></p><h1 id="定义元素特征"><a href="#定义元素特征" class="headerlink" title="定义元素特征"></a>定义元素特征</h1><p>实现<code>VisualElement.UxmlTraits</code>类<br>在<code>Init</code>中使用<code>((MyElement)ve).status = m_Status.GetValueFromBag(bag, cc);</code>读取数据。</p><h1 id="加载UXML"><a href="#加载UXML" class="headerlink" title="加载UXML"></a>加载UXML</h1><p>模板加载到VisualTreeAsset<br><code>var template = AssetDatabase.LoadAssetAtPath&lt;VisualTreeAsset&gt;(&quot;path/to/file.uxml&quot;);</code><br>然后附加到父元素<br><code>template.CloneTree(parentElement, slots);</code></p><h1 id="UQuery"><a href="#UQuery" class="headerlink" title="UQuery"></a>UQuery</h1><p>基于Jquery或Linq，如<br><code>root.Query(&quot;foo&quot;).Children&lt;Button&gt;().ForEach(//执行操作);</code></p><h1 id="事件"><a href="#事件" class="headerlink" title="事件"></a>事件</h1><p>事件通知到每个元素<br>基类EventBase</p><h2 id="分发事件"><a href="#分发事件" class="headerlink" title="分发事件"></a>分发事件</h2><p>监听来自操作系统或脚本的事件，使用<code>EventDispatcher</code>分发到元素。首先确定分发策略，然后执行。<br>事件有可能由额外事件，通过队列管理。</p><h2 id="分发行为"><a href="#分发行为" class="headerlink" title="分发行为"></a>分发行为</h2><p>分为三个阶段</p><ul><li>涓滴</li><li>冒泡</li><li>可取消</li></ul><h2 id="事件传播"><a href="#事件传播" class="headerlink" title="事件传播"></a>事件传播</h2><p>事件分发选择目标后，计算传播路径。</p><ul><li>从根部向目标，涓滴阶段</li><li>接收事件</li><li>上升到根部，冒泡阶段</li></ul><h2 id="事件目标"><a href="#事件目标" class="headerlink" title="事件目标"></a>事件目标</h2><p>取决于事件类型，键盘则是当前获得焦点的元素。</p><h2 id="捕获鼠标"><a href="#捕获鼠标" class="headerlink" title="捕获鼠标"></a>捕获鼠标</h2><p>任何时候只有一个元素具有捕获。捕获后，该元素是除了鼠标滚轮之外任何鼠标事件的目标。</p><h2 id="焦点环和Tab顺序"><a href="#焦点环和Tab顺序" class="headerlink" title="焦点环和Tab顺序"></a>焦点环和Tab顺序</h2><p>默认DFS。</p><h1 id="处理事件"><a href="#处理事件" class="headerlink" title="处理事件"></a>处理事件</h1><h2 id="注册事件回调"><a href="#注册事件回调" class="headerlink" title="注册事件回调"></a>注册事件回调</h2><p>传播路径上除目标外可以对一个事件接受两次，涓滴和冒泡。<br>默认是在目标和冒泡阶段执行。如要在涓滴时执行，使用<code>TrickleDown.TrickleDown</code>注册回调。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">&#x2F;&#x2F; 为涓滴阶段注册回调myElement.RegisterCallback&lt;MouseDownEvent&gt;(MyCallback, TrickleDown.TrickleDown);</code></pre></div></figure><h2 id="发送自定义数据到事件回调"><a href="#发送自定义数据到事件回调" class="headerlink" title="发送自定义数据到事件回调"></a>发送自定义数据到事件回调</h2><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">&#x2F;&#x2F;将用户数据一起发送到回调myElement.RegisterCallback&lt;MouseDownEvent, MyType&gt;(MyCallbackWithData, myData);void MyCallbackWithData(MouseDownEvent evt, MyType data) &#123; &#x2F;* ...*&#x2F; &#125;</code></pre></div></figure><h2 id="为控件处理输入事件"><a href="#为控件处理输入事件" class="headerlink" title="为控件处理输入事件"></a>为控件处理输入事件</h2><h3 id="捕获指针"><a href="#捕获指针" class="headerlink" title="捕获指针"></a>捕获指针</h3><p>使用CaptureEvents</p><h3 id="使用操纵器处理事件"><a href="#使用操纵器处理事件" class="headerlink" title="使用操纵器处理事件"></a>使用操纵器处理事件</h3><h3 id="自定义控件"><a href="#自定义控件" class="headerlink" title="自定义控件"></a>自定义控件</h3><p>两种方式</p><ul><li>注册事件回调</li><li>实现默认操作<br>区别</li><li>回调在实例上注册，默认操作作为一个类的虚拟函数</li><li>回调传播路径上所有元素都会执行。默认操作仅对事件目标执行</li></ul><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">override void ExecuteDefaultActionAtTarget(EventBase evt)&#123;    &#x2F;&#x2F; 调用基函数。    base.ExecuteDefaultActionAtTarget(evt);    if (evt.GetEventTypeId() &#x3D;&#x3D; MouseDownEvent.TypeId())    &#123;        &#x2F;&#x2F; ...    &#125;    else if (evt.GetEventTypeId() &#x3D;&#x3D; MouseUpEvent.TypeId())    &#123;        &#x2F;&#x2F; ...    &#125;    &#x2F;&#x2F;更多事件类型&#125;</code></pre></div></figure><h1 id="综合事件"><a href="#综合事件" class="headerlink" title="综合事件"></a>综合事件</h1><p>事件系统使用事件池。</p><ol><li>从事件池中获取一个事件对象</li><li>填写事件属性</li><li>包含在<code>using</code>中确保其返回到事件池</li><li>事件传递给<code>element.SendEvent()</code></li></ol><p>要发送来自操作系统的事件，使用<code>UnityEngine.Event</code>初始化事件</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">void SynthesizeAndSendKeyDownEvent(IPanel panel, KeyCode code,     char character &#x3D; &#39;\0&#39;, EventModifiers modifiers &#x3D; EventModifiers.None)&#123;    &#x2F;&#x2F; Create a UnityEngine.Event to hold initialization data.    var evt &#x3D; new Event() &#123;        type &#x3D; EventType.KeyDownEvent,        keyCode &#x3D; code,        character &#x3D; character,        modifiers &#x3D; modifiers    &#125;;    using (KeyDownEvent keyDownEvent &#x3D; KeyDownEvent.GetPooled(evt))    &#123;        panel.visualTree.SendEvent(keyDownEvent);    &#125;&#125;</code></pre></div></figure>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Hololens2-mrtk3</title>
    <link href="/2023/01/14/Hololens2-mrtk3/"/>
    <url>/2023/01/14/Hololens2-mrtk3/</url>
    
    <content type="html"><![CDATA[<h1 id="悲报"><a href="#悲报" class="headerlink" title="悲报"></a>悲报</h1><p>2023.01.23<br>微软已经解雇了从事虚拟现实、混合现实和HoloLens开发的团队。<br>MRTK开发也停止了</p><p>记录下mrtk3开发Hololens2应用的配置</p><h1 id="项目配置"><a href="#项目配置" class="headerlink" title="项目配置"></a>项目配置</h1><ul><li>基础3D项目</li><li>切换到UWP<ul><li>ARM64</li></ul></li><li>使用 MixedRealityFeatureTool<ul><li>本质上就是一个包管理器，负责下载包，修改<code>manifest.json</code>文件</li><li>里面功能五花八门，基础的就先选择<code>com.microsoft.mixedreality.openxr</code>和<code>com.microsoft.mixedreality.toolkit.foundation</code></li></ul></li><li>打开后会有MRTK的配置向导，按其要求配置即可，如主相机的背景色，深度模式等等。</li><li>然后build成VS项目，由VS打开</li><li>配置远端IP，部署即可</li></ul><p>如果要使用MRTK3，之前就改为启用MRTK 输入和MRTK UX 组件</p><h1 id="定位"><a href="#定位" class="headerlink" title="定位"></a>定位</h1><h2 id="二维码定位"><a href="#二维码定位" class="headerlink" title="二维码定位"></a>二维码定位</h2><p>简单方便，但对二维码的大小有要求</p><h2 id="世界锁定"><a href="#世界锁定" class="headerlink" title="世界锁定"></a>世界锁定</h2><p>通常的VR开发是以一个固定的坐标点如（地板）作为原点，虚拟与显示相匹配。<br>而AR会不断修正自身与世界各个坐标的位置，这就很难去用一个统一的坐标去计算.<br>所以可以使用数个二维码来做定位，每个现实的二维码都和虚拟的相对应，同时每个二维码都生成一个以自身为基准（并不是原点）的坐标系，我们在三个（假如有）坐标系中间，依据距离的远近来插值出一个新的坐标系，使用这个来调整摄像机的父级，就可以平滑的在各个坐标之间动态改变而不至于使体验者有太大的跳动感。<br>所以每当体验者非常靠近一个二维码，那就会100%使用这个二维码的坐标系，这样看起来这里的现实的二维码和虚拟的会完全匹配，但其他的未必会匹配。这样就可以在关键物体边多放置几个来提高精度，同时在用户移动自身位置时也能较好的匹配用户临近的空间。<br>世界锁定目前还不支持Unity2022</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>高度置换贴图</title>
    <link href="/2023/01/02/Displacement/"/>
    <url>/2023/01/02/Displacement/</url>
    
    <content type="html"><![CDATA[<p>根据虚幻提供的<a href="https://docs.unrealengine.com/5.1/zh-CN/xr-best-practices-in-unreal-engine/">最佳实践</a>在VR中，由于法线贴图不会考虑到双目显示和运动，它提供的效果和视差贴图比起来差距很大。对应到Unity中，在HDRP的Lit着色器下，指的就是HeightMap。<br>下面罗列一下使用HeightMap的一些注意事项。</p><h1 id="置换模式"><a href="#置换模式" class="headerlink" title="置换模式"></a>置换模式</h1><p>大体分两类，一种是将高度图映射到顶点上，顶点置换，还一种是在像素级别的置换。</p><h2 id="顶点置换"><a href="#顶点置换" class="headerlink" title="顶点置换"></a>顶点置换</h2><p>简单易懂，效果也很好，缺点是要求模型有很多顶点才有比较好的效果。</p><p>或者使用曲面细分，增加很多的顶点，效果也不错，缺点是当头显摄像机移动时，细分的顶点会发生变动，体现在视觉上就是模型的顶点在跳动。<br><img src="/images/TDis.png" alt="曲面细分+置换"></p><h2 id="像素置换"><a href="#像素置换" class="headerlink" title="像素置换"></a>像素置换</h2><p>后者在较高的迭代次数下，效果也不错。<br>具体的实现可以参考<a href="https://learnopengl-cn.github.io/05%20Advanced%20Lighting/05%20Parallax%20Mapping/">视差贴图</a>。<br>Unity的HDRP具体采用的方案叫做陡峭视差映射(Steep Parallax Mapping)。<br>这里贴一张关键的图：<br><img src="https://learnopengl-cn.github.io/img/05/05/parallax_mapping_steep_parallax_mapping_diagram.png" alt="陡峭视差映射"></p><p>这里要注意在使用时，有两个关键的参数不要忘记：<code>Primitive Length</code> <code>Primitive Width</code>，表示实际物体的长和宽。如果没有正确填写，就会出现物体下沉或者漂浮的情况。同时为了准确的显示高度对深度的影响，<code>Depth Offset</code>也要开启，这会修改实际写入深度图的深度。写入的深度同时也会影响各种平面空间的效果，比如屏幕空间阴影。</p><p><img src="/images/PDis.png" alt="像素级别置换"></p><h1 id="URP呢"><a href="#URP呢" class="headerlink" title="URP呢"></a>URP呢</h1><p>URP虽然也提供了HeightMap，但从效果来看，应该只是简单的视差贴图映射，和HDRP默认的陡峭视差映射差距很大。<br><img src="/images/URPDis.png" alt="URP默认的视差贴图映射"><br>但如果使用ShaderGraph，还是提供了两种视差映射。<br><img src="/images/Para.png" alt="ShaderGraph提供的两种节点"><br>前者就是简单的视差映射，后者的效果就和HDRP一样。<br>但很遗憾，这里虽然给出了<code>Depth Offset</code>，但目前的URP的节点还无法使用。<br>也就是说目前（2022.2.0），想要在URP下使用PDO（Pixel Depth Offset），只有自己写了，这里有个<a href="https://forum.unity.com/threads/pdo-pixel-depth-offset-in-pallax-occlusion-mapping-ndoe.1132408/">栗子</a>。<br>这里挖个坑，等到BlockShader成熟了，就用BlockShader写一遍。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>圆角描边矩形</title>
    <link href="/2023/01/02/UIShader/"/>
    <url>/2023/01/02/UIShader/</url>
    
    <content type="html"><![CDATA[<p>UI总是会有很多奇奇怪怪的圆角矩形，虽然要在UIToolkit中实现是很简单的事情，但目前还是不太成熟，还是要用UGUI。在UGUI中要实现大致有三种方法，这里用Shader来实现一下。</p><span id="more"></span><h1 id="单纯圆角图片"><a href="#单纯圆角图片" class="headerlink" title="单纯圆角图片"></a>单纯圆角图片</h1><p>这是最麻烦且没必要的一种，针对不同分辨率和曲率要UI给出不同的图片，效率很低</p><h1 id="9切片"><a href="#9切片" class="headerlink" title="9切片"></a>9切片</h1><p><img src="https://docs.unity3d.com/cn/current/uploads/Main/9SliceSprites-0.png" alt="9SliceSprites"><br>把图片分割成9个部分，每个部分有不同的UV平铺与重复的方式，但是要实现像素级别的点对点，就需要准备不同分辨率的切片圆角矩形，也很麻烦</p><h1 id="Shader实现"><a href="#Shader实现" class="headerlink" title="Shader实现"></a>Shader实现</h1><p>直接用SDF实现，一劳永逸。保证点对点的准确度，即使再搞什么圆角加描边也能快速实现。</p><h2 id="UI-shader"><a href="#UI-shader" class="headerlink" title="UI shader"></a>UI shader</h2><p>UI的shader与一般的差别不大，只是对一些命名有点小要求。</p><ul><li>传入的主贴图名称为<code>_MainTex</code>当然如果你不想用Image传入的就随便了。</li><li>同时还会传入颜色<code>float4 color    : COLOR;</code></li><li>默认的一些配置<ul><li>ZClip True</li><li>ZTest Always</li><li>ZWrite Off</li><li>Cull Off</li></ul></li><li>UGUI传入的顶点并不仅仅是一个面片，根据不同的设置，它会生成特定的网格传入GPU，而且UV分布也不一定是均匀的，所以要用特定的简单Sprite。</li></ul><h2 id="SDF"><a href="#SDF" class="headerlink" title="SDF"></a>SDF</h2><p>从shaderToy上抄一个</p><figure><div class="code-wrapper"><pre class="line-numbers language-hlsl" data-language="hlsl"><code class="language-hlsl">float sdRoundedBox(in float2 p, in float2 b, in float4 r)&#123;    r.xy &#x3D; (p.x &gt; 0.0) ? r.xy : r.zw;    r.x &#x3D; (p.y &gt; 0.0) ? r.x : r.y;    float2 q &#x3D; abs(p) - b + r.x;    return min(max(q.x, q.y), 0.0) + length(max(q, 0.0)) - r.x;&#125;</code></pre></div></figure><h2 id="Shader"><a href="#Shader" class="headerlink" title="Shader"></a>Shader</h2><p>最后贴一下Shader<br><img src="/images/Ran.png" alt="圆角描边矩形"></p><figure><div class="code-wrapper"><pre class="line-numbers language-hlsl" data-language="hlsl"><code class="language-hlsl">Shader &quot;Unlit&#x2F;UITest&quot;&#123;    Properties    &#123;        _OutlineColor(&quot;Outline Color&quot;, Color) &#x3D; (1,0,0,1)        [PerRendererData] _MainTex(&quot;Base Map&quot;, 2D) &#x3D; &quot;white&quot;        _scale(&quot;Scale width&#x2F;height&quot;, float) &#x3D; 1.0        _width(&quot;Outline Width&quot;, float) &#x3D; 1.0        _Radius(&quot;Radius&quot;, Vector) &#x3D; (0.1, 0.1, 0.1, 0.1)    &#125;    SubShader    &#123;        Tags        &#123;            &quot;RenderType&quot; &#x3D; &quot;Transparent&quot;            &quot;RenderPipeline&quot; &#x3D; &quot;UniversalPipeline&quot;            &quot;Queue&quot;&#x3D;&quot;Transparent&quot;        &#125;        Pass        &#123;            Name &quot;Default&quot;            Blend SrcAlpha OneMinusSrcAlpha            ZClip True            ZTest Always            ZWrite Off            Cull Off            HLSLPROGRAM            #pragma vertex vert            #pragma fragment frag            #include &quot;Packages&#x2F;com.unity.render-pipelines.universal&#x2F;ShaderLibrary&#x2F;Core.hlsl&quot;            struct Attributes            &#123;                float4 positionOS : POSITION;                float4 color : COLOR;                float2 uv : TEXCOORD0;            &#125;;            struct Varyings            &#123;                float4 positionHCS : SV_POSITION;                float4 color : COLOR;                float2 uv : TEXCOORD0;            &#125;;            TEXTURE2D(_MainTex);            SAMPLER(sampler_MainTex);            CBUFFER_START(UnityPerMaterial)            float4 _MainTex_ST;            float4 _OutlineColor;            float _scale;            float _width;            float4 _Radius;            CBUFFER_END            Varyings vert(Attributes IN)            &#123;                Varyings OUT;                OUT.positionHCS &#x3D; TransformObjectToHClip(IN.positionOS.xyz);                OUT.color &#x3D; IN.color;                OUT.uv &#x3D; TRANSFORM_TEX(IN.uv, _MainTex);                return OUT;            &#125;            float sdRoundedBox(in float2 p, in float2 b, in float4 r)            &#123;                r.xy &#x3D; (p.x &gt; 0.0) ? r.xy : r.zw;                r.x &#x3D; (p.y &gt; 0.0) ? r.x : r.y;                float2 q &#x3D; abs(p) - b + r.x;                return min(max(q.x, q.y), 0.0) + length(max(q, 0.0)) - r.x;            &#125;            float boxDist(float2 p, float2 size, float radius)            &#123;                size -&#x3D; float2(radius,radius);                float2 d &#x3D; abs(p) - size;                return min(max(d.x, d.y), 0.0) + length(max(d, 0.0)) - radius;            &#125;            half4 frag(Varyings IN) : SV_Target            &#123;                half4 color &#x3D; SAMPLE_TEXTURE2D(_MainTex, sampler_MainTex, IN.uv) * IN.color;                float2 uv &#x3D; (IN.uv * 2 - 1) ;                uv.x *&#x3D; _scale;                float d &#x3D; sdRoundedBox(uv, float2(_scale,1), _Radius);                if (d &gt; 0.0)                    color &#x3D; half4(0, 0, 0, 0);                else if (d &gt; - _width)                    color &#x3D; _OutlineColor;                return color;            &#125;            ENDHLSL        &#125;    &#125;&#125;</code></pre></div></figure><h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><p>这里的SDF可以简单替换其他各种奇奇怪怪的类型，比如参考这个链接<a href="https://iquilezles.org/articles/distfunctions2d/">distfunctions2d</a>。再结合一些变化的参数就可以做出动画效果了。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>XR开发的各种SDK以及相关概念</title>
    <link href="/2022/12/29/openxr/"/>
    <url>/2022/12/29/openxr/</url>
    
    <content type="html"><![CDATA[<p>简要记录一下几个常用的概念以及sdk的选择</p><h1 id="OpenXR"><a href="#OpenXR" class="headerlink" title="OpenXR"></a>OpenXR</h1><p><img src="https://upload.wikimedia.org/wikipedia/commons/3/3a/OpenXR_logo.svg"><br><code>OpenXR</code>是一套由Khronos Group发起的跨平台的，通用的标准。简单来说就是定义了一套接口（不仅仅是接口），硬件厂商如steamvr、pico等实现这一套接口。unity或者虚幻等等引擎再适配这套接口，大家就可以用同一套标准去开发，省去了为不同平台适配的麻烦。<br>仅PC上绝大多数VR都为<code>OpenXR</code>提供了运行环境，如<code>steamVR</code>、<code>Windows Mixed Reality</code>等，安卓端的Pico以及oculus也都适配了<code>OpenXR</code><br>但缺点是无法调用某些特定平台的特定的接口，比如steamVR可以在加载时预先传输个天空盒当作loading，steamVR还有自己的手部骨骼动画，pico也有自己的比如VR合成层等功能是无法通过openxr来调用的。<br>如果采用OpenXR开发安卓端VR还有个很大优势就是可是用PC端的VR来调试功能，大大提高开发效率。</p><h1 id="Unity-XR-Plugin-Architecture"><a href="#Unity-XR-Plugin-Architecture" class="headerlink" title="Unity XR Plugin Architecture"></a>Unity XR Plugin Architecture</h1><p>Unity 2019.3 之后，官方提供了一个叫做XR插件框架的东东，通过包管理器安装，大大简化了XR开发的流程，在此基础之上，各大硬件厂商开发自己的适配Unity的XR开发SDK，如Steam的<code>OpenVR</code>，pico的<code>picoVR</code>，Oculus的<code>Oculus XR Plugin</code>、微软的<code>Windows Mixed Reality</code>等等，当然还有官方自己为了openxr开发的<code>OpenXR Plugin</code>。</p><h1 id="OpenXR-Plugin"><a href="#OpenXR-Plugin" class="headerlink" title="OpenXR Plugin"></a>OpenXR Plugin</h1><p>由Unity官方开发的，适配了<code>openxr</code>以及<code>unity XR Plugin Architecture</code>的插件，Unity还为这个插件开发了<code>XR Interaction Toolkit</code>作为最基础的XR交互。</p><h1 id="XR-Interaction-Toolkit"><a href="#XR-Interaction-Toolkit" class="headerlink" title="XR Interaction Toolkit"></a>XR Interaction Toolkit</h1><p>适配了OpenXR，实现了基础的XR交互的官方插件，包含了常用的射线、抓取、UI交互（仅限UGUI，不支持UIToolkit）、传送等等基础功能，同时留下了丰富的接口方便开发者自己实现想要的功能。</p><h1 id="OpenVR"><a href="#OpenVR" class="headerlink" title="OpenVR"></a>OpenVR</h1><p>和<code>OpenXR</code>很像，但完全不是一码事，这个是V社开发的为了各个硬件厂商适配SteamVR的标准，同时提供了<code>OpenVR Loader</code>来适配Unity的<code>Unity XR Plugin Architecture</code>。包含在<code>SteamVR Plugin</code>中。</p><h1 id="SteamVR-Plugin"><a href="#SteamVR-Plugin" class="headerlink" title="SteamVR Plugin"></a>SteamVR Plugin</h1><p>V社开发的一套SDK，包含了<code>OpenVR</code>插件来适配<code>unity XR Plugin</code>，同时还有一系列的其他与SteamVR交互的接口，如输入绑定、手部骨骼、loading天空盒子、Vive Trakcer等等。<br><del>但是已经快两年没有更新了。</del></p><h1 id="Pico"><a href="#Pico" class="headerlink" title="Pico"></a>Pico</h1><p>pico提供了两套SDK，一套是纯粹的<code>PICO Unity OpenXR Plugin</code>，即实现了<code>OpenXR</code>的SDK，同时还有一套叫做<code>PICO Unity Integration SDK</code>，这里就包含了其他特定与Pico平台的功能，如平台用户相关的、注视点渲染、VR 合成层等等。但二者都兼容了<code>XR Interaction Toolkit</code>，对于开发还是很友好的。</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>所以在开发前要想清楚，到底是要适配多平台，还是想要某个平台特定的功能，二者<del>不可</del>很难兼得。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>喷涂、绘制与填充</title>
    <link href="/2022/12/26/draw/"/>
    <url>/2022/12/26/draw/</url>
    
    <content type="html"><![CDATA[<h1 id="喷涂与绘制"><a href="#喷涂与绘制" class="headerlink" title="喷涂与绘制"></a>喷涂与绘制</h1><p>又是一个烂大街的效果，但毕竟还没有做过。这里记录和总结一下。</p><span id="more"></span><p>最为出名的应该就是老任的Splatoon了，不仅是视觉效果，整个游戏的玩法都是建立在喷涂上的。<br>另一个有印象的就是V社的alyx的开头一章上的玻璃或者黑板上用水彩绘制了。<br>最近Unity官方也分享了一个视频，利用了SDF，效果也很好。</p><h1 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h1><p>最基础的想法就是用世界坐标来判断是否接触<br><img src="/images/dis.png" alt="距离判断"></p><p>然后要求模型有一套不重叠的UV，使用这个UV作为顶点着色器的坐标输出，把接触的部分在UV空间下绘制到一张RT上。<br><img src="/images/draw_rt.png" alt="RT"></p><figure><div class="code-wrapper"><pre class="line-numbers language-hlsl" data-language="hlsl"><code class="language-hlsl">&#x2F;&#x2F; 使用UV作为顶点着色器的坐标输出float4 uv &#x3D; float4(0, 0, 0, 1);uv.xy &#x3D; float2(1, _ProjectionParams.x) * (OUT.uv.xy * 2 - 1);OUT.positionHCS &#x3D; uv;</code></pre></div></figure><p>最后再使用这张RT去渲染就好了。</p><p>简单贴一下</p><figure><div class="code-wrapper"><pre class="line-numbers language-hlsl" data-language="hlsl"><code class="language-hlsl">CBUFFER_START(UnityPerMaterial)float4 _BaseMap_ST;float4 _SplatColor;float3 _position;float _radius;CBUFFER_ENDVaryings vert(Attributes IN)&#123;    Varyings OUT;    OUT.positionHCS &#x3D; TransformObjectToHClip(IN.positionOS.xyz);    OUT.uv &#x3D; TRANSFORM_TEX(IN.uv, _BaseMap);    float4 uv &#x3D; float4(0, 0, 0, 1);    uv.xy &#x3D; float2(1, _ProjectionParams.x) * (OUT.uv.xy * 2 - 1);    OUT.positionHCS &#x3D; uv;    OUT.positionWS &#x3D; TransformObjectToWorld(IN.positionOS.xyz);    return OUT;&#125;half4 frag(Varyings IN) : SV_Target&#123;    half4 color &#x3D; SAMPLE_TEXTURE2D(_BaseMap, sampler_BaseMap, IN.uv);    float3 distence &#x3D; IN.positionWS - _position;    float distenceLength &#x3D; length(distence);    if (distenceLength &lt; _radius)    &#123;        color &#x3D; _SplatColor;    &#125;    else    &#123;        clip(-1);    &#125;    return color;&#125;</code></pre></div></figure><p>然后是对应的RenderFeature的pass</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public override void Execute(ScriptableRenderContext context, ref RenderingData renderingData)&#123;    if(renderTexture!&#x3D;null &amp;&amp; material!&#x3D;null)    &#123;        foreach (var splatable in Splatable.splatables)        &#123;                            var cmd &#x3D; CommandBufferPool.Get(&quot;SplatRenderPass&quot;);            cmd.SetRenderTarget(splatable.splatTexture);            cmd.ClearRenderTarget(true, false, Color.black);            cmd.SetViewProjectionMatrices(Matrix4x4.identity, Matrix4x4.Ortho(0, 1, 0, 1, -1, 1));                    material.SetVector(&quot;_position&quot;, splatable.targetPen.position);                        cmd.DrawMesh(splatable.mesh, splatable.transform.localToWorldMatrix, material);            context.ExecuteCommandBuffer(cmd);            CommandBufferPool.Release(cmd);        &#125;    &#125;&#125;</code></pre></div></figure><p><img src="/images/yu2.gif" alt="绘制"></p><h2 id="连续问题"><a href="#连续问题" class="headerlink" title="连续问题"></a>连续问题</h2><p>可以看到，因为是每一帧仅绘制一个点，手柄快速移动就会出现不连贯的点，这里改成记录前后两帧的位置，用一个胶囊体的sdf去计算。</p><figure><div class="code-wrapper"><pre class="line-numbers language-hlsl" data-language="hlsl"><code class="language-hlsl">float sdCapsule( vec3 p, vec3 a, vec3 b, float r )&#123;  vec3 pa &#x3D; p - a, ba &#x3D; b - a;  float h &#x3D; clamp( dot(pa,ba)&#x2F;dot(ba,ba), 0.0, 1.0 );  return length( pa - ba*h ) - r;&#125;</code></pre></div></figure><p><img src="/images/draw2.gif" alt="连续绘制"></p><h2 id="利用SDF解决锯齿"><a href="#利用SDF解决锯齿" class="headerlink" title="利用SDF解决锯齿"></a>利用SDF解决锯齿</h2><p>这里用到了一个之前没用过的ShaderLab命令<code>BlendOp</code>，用来定义混合的操作，默认是add，这里改成max。<br>贴图的rgb存储颜色，a通道用来存储SDF。</p><p>最后显示时用A通道来判定是否是喷涂区域：</p><figure><div class="code-wrapper"><pre class="line-numbers language-hlsl" data-language="hlsl"><code class="language-hlsl">fixed4 col &#x3D; tex2D(_MainTex, i.uv);float aa &#x3D; col.a;aa &#x3D; aa - 0.5;if(aa&lt;0)&#123;    clip(-1);&#125;</code></pre></div></figure><p><img src="/images/drawsdf.png" alt="SDF绘制"></p><p>这里的贴图分辨率降低为1024（之前是4096），也可以明显看到去除了边缘锯齿。但颜色过度地方的锯齿还是存在，可以统一颜色或者提高分辨率来解决这个问题。</p><h2 id="防抖"><a href="#防抖" class="headerlink" title="防抖"></a>防抖</h2><p>VR下难免手抖，这里就偷懒同官方的XR toolkit中的<code>XR Grab Interactable</code>组件自带的平滑移动解决。</p><h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><p>仅就复刻ALYX中的水彩笔还算是比较成功了，后续如果还要添加功能，可以考虑像Splatoon一样，考虑水彩的厚度，喷溅的墨点，对法线的影响以及人物的交互等等花活了。</p><h2 id="封闭区域判断"><a href="#封闭区域判断" class="headerlink" title="封闭区域判断"></a>封闭区域判断</h2><p>还有一个需求，要求提取出其中被线包围的封闭区域。这里就简单用DFS实现吧。</p><p>把RenderTexture中的像素提出来，用alpha通道来作为边界判断依据，转换成二维的布尔数组，true表示涂上了颜色，false表示没有。</p><p>从左上角开始深度优先遍历每个像素，</p><ul><li>如果这个像素为True，跳过。</li><li>为false，临时记录到一个数组中，然后遍历他的上下左右像素，<ul><li>如果存在一个像素越界了，说明这里和外边界相通，标记失败，表示这是和外边界相通的像素。</li><li>如果没有越界，但是值为true，说明和喷涂的像素相连，不用再遍历他的上下左右了</li><li>最后没有越界，但是值为false，还是要遍历他的上下左右</li></ul></li><li>遍历完成后，判断是否有像素越界，如果有，临时记录的全部像素结果记为false，反之记为true。</li></ul><p>深度优先虽然简单，但对于512*512这种体量的数据都会堆栈溢出。还是改成bfs吧。</p><p>用一个队列来保存相邻的像素，广度优先遍历，一旦发现与外边界连通就全记为false，反之说明这个连通区域是封闭的。</p><p>因为全部搜索都是在二维UV平面进行的，所以要求模型的UV只能有一块，且接缝处无法算作闭合区域。<br>因为这里涉及了对GPU贴图的读写，效率只能说马马虎虎。也许可以使用computeShader来实现，<del>但目前我还是想不到。</del></p><p><img src="/images/draw3.gif" alt="封闭区域判断与绘制"></p><h2 id="计算着色器实现"><a href="#计算着色器实现" class="headerlink" title="计算着色器实现"></a>计算着色器实现</h2><p>鼓捣了半天，又把计算着色器翻出来看了看。<br>很少用到，之前写SSR时用过一次，然后就忘干净了。这次为了优化之前写的封闭区域判断，即泛洪算法，尝试用computeShader来实现一次，加深一下印象。</p><h3 id="泛洪算法"><a href="#泛洪算法" class="headerlink" title="泛洪算法"></a>泛洪算法</h3><p>画图工具中的油漆桶工具，以及PS中的魔棒工具都属于典型的泛洪算法的用例。如果是用CPU实现，那就是之前写的简单的DFS或者BFS都可以，既然在Unity中实现，那就不能错过用GPU实现的机会了。</p><h3 id="GPU泛洪"><a href="#GPU泛洪" class="headerlink" title="GPU泛洪"></a>GPU泛洪</h3><p>首先是精确的GPU泛洪，从一个点出发，颜色设置为白色，然后对每个像素，如果它是白色，那就把他四周不是黑色的像素也置为白色，就这样一直循环N遍，就可以得到类似下图的效果了：</p><p><img src="/images/fill.png" alt="原图"></p><p><img src="/images/draw4.gif" alt="GPU泛洪"></p><p>再结合之前的喷涂的贴图，就可以快速的从边缘开始泛洪，以此来判断封闭区域了。</p><p><img src="/images/draw5.gif" alt="GPU泛洪分步骤演示"><br>改成用GPU算果然快了很多，尤其是省略了从GPU读取像素到内存的开销，可以说不在一个数量级上。<br>如果还是要优化的话，我只能想到降低分辨率再上采样了，因为使用了SDF，所以低分辨率也可以接受。</p><figure><div class="code-wrapper"><pre class="line-numbers language-hlsl" data-language="hlsl"><code class="language-hlsl">#pragma kernel CSMain#pragma kernel CSClean#define thread_group_x 8#define thread_group_y 8#define thread_x 8#define thread_y 8&#x2F;&#x2F; Create a RenderTexture with enableRandomWrite flag and set it&#x2F;&#x2F; with cs.SetTextureRWTexture2D&lt;float4&gt; Result;Texture2D&lt;float4&gt; Source;[numthreads(8,8,1)]void CSMain(uint3 id : SV_DispatchThreadID)&#123;    if (Result[id.xy].a &lt; 0.5)    &#123;        float4 top &#x3D; Source[id.xy + uint2(0, 1)];        float4 bottom &#x3D; Source[id.xy + uint2(0, -1)];        float4 left &#x3D; Source[id.xy + uint2(-1, 0)];        float4 right &#x3D; Source[id.xy + uint2(1, 0)];        if (top.a &lt; 0.5)            Result[id.xy + uint2(0, 1)] &#x3D; float4(0, 0, 0, 0);        if (bottom.a &lt; 0.5)            Result[id.xy + uint2(0, -1)] &#x3D; float4(0, 0, 0, 0);        if (left.a &lt; 0.5)            Result[id.xy + uint2(-1, 0)] &#x3D; float4(0, 0, 0, 0);        if (right.a &lt; 0.5)            Result[id.xy + uint2(1, 0)] &#x3D; float4(0, 0, 0, 0);    &#125;&#125;[numthreads(8,8,1)]void CSClean(uint3 id : SV_DispatchThreadID)&#123;    if (id.x &#x3D;&#x3D; 0 || id.x &#x3D;&#x3D; 255 || id.y &#x3D;&#x3D; 0 || id.y &#x3D;&#x3D; 255)        Result[id.xy] &#x3D; float4(0, 0, 0, 0);    else        Result[id.xy] &#x3D; float4(1, 1, 1, 1);&#125;</code></pre></div></figure>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Unity 2022.2.0光追效果测试</title>
    <link href="/2022/12/11/Unity2022-2/"/>
    <url>/2022/12/11/Unity2022-2/</url>
    
    <content type="html"><![CDATA[<h1 id="Unity-2022-2"><a href="#Unity-2022-2" class="headerlink" title="Unity 2022.2"></a>Unity 2022.2</h1><p>随着2022.2.0正式发布，HDRP版本也来到了14，简单测试了一下，无论是普通3D场景下的RayTracing还是VR场景下普通的光栅，都有了很大的改进。记录下踩的一些坑。</p><span id="more"></span><p>在9400f+3070配置下，开启部分光追（阴影、AO、反射、GI），1080P分辨率下可以达到50-60帧。可以预见在一些商业场景下，对配置预算没有要求的情况下，采用4090应该可以达到一个令人满意的帧率。<br>而使用光栅渲染，在VR场景下（2016*2016）单眼分辨率，即使启用了大部分的屏幕空间效果，开启了体积雾，体积云，也可以稳定达到36帧，在ATW的加持下，对于目标帧率72帧的大部分头显，仅在产品展馆展示等对于交互要求不高的场景下，都属于可以接受的范围了。<br>如果舍弃这些屏幕空间效果，舍弃体积，再加上静态阴影，同时降低一些分辨率的话，也可以达成72帧的目标帧率。<br>之前的SSR在VR下的错误也已经修复，所以是时候尝试使用HDRP了。</p><h2 id="特性"><a href="#特性" class="headerlink" title="特性"></a>特性</h2><p>下面总结一下目前unity支持的一些光追特性。</p><ul><li>AO</li><li>反射</li><li>阴影</li><li>接触阴影</li><li>GI</li><li>次表面</li><li>路径追踪</li><li>递归渲染</li></ul><h2 id="路径追踪"><a href="#路径追踪" class="headerlink" title="路径追踪"></a>路径追踪</h2><p>纯光追，从相机发送光线，当光线照射到反射或折射表面时，递归该过程，直到到达光源。从相机到光线的一系列光线形成了一条路径。<br>就和maya，Blender使用的那种阿诺德、cycle一样，简单粗暴，不用再去费尽心思的去展UV，烘焙等等。一步到位。：<br><img src="/images/path.png" alt="路径追踪"><br>但在可预见的未来，是不可能实时的。但是可以通过Recorder等工具渲染成序列帧或者视频。</p><h2 id="GI"><a href="#GI" class="headerlink" title="GI"></a>GI</h2><p>GI目前还是比较鸡肋，性能模式下，只反弹一次，效果很不尽人意：<br><img src="/images/RTGI-P-b01.png" alt="RTGI Performance"></p><p>如果启用质量模式，帧数根本顶不住。与屏幕空间GI配合使用也只能改善一点点：<br><img src="/images/RTGI-P-mix-b01.png" alt="RTGI Mixed"></p><p>根本没法和烘焙光照贴图相比：<br><img src="/images/RTGI-lightmap-b01.png" alt="lightingmap"><br>看来Unity的实时GI和Unreal的lumen相比，还是差了太多太多了。</p><h2 id="AO"><a href="#AO" class="headerlink" title="AO"></a>AO</h2><p>这里采用光追提升巨大：</p><p><img src="/images/ssao.png" alt="ssao"><br><img src="/images/rtao.png" alt="rtao"></p><h2 id="反射"><a href="#反射" class="headerlink" title="反射"></a>反射</h2><p>这恐怕是光追最能体现效果的地方了，原先的反射方案无非：天空盒、反射探针、SSR。但都有或大或小的缺点，但光追没有，有也是我的缺点，太穷，买不起4090。<br>这里就对比ssr与光追吧。</p><p><img src="/images/hd-ssr.png" alt="ssr"><br><img src="/images/hd-rt.png" alt="光追反射"></p><h2 id="阴影"><a href="#阴影" class="headerlink" title="阴影"></a>阴影</h2><p>阴影也算是光追比较出效果的一个功能了。这里的对比还看不太出来，官方的对比更强烈。</p><p><img src="/images/normShadow.png" alt="级联阴影"></p><p><img src="/images/rtshadow.png" alt="光追阴影"></p><h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><p>至于次表面和递归渲染，目前还没有深入使用。<br>总体来说，目前最适合开启光追的应该就是反射、AO、和阴影，GI属实有点鸡肋，除非场景内全是动态的物体。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>pico VR 合成层</title>
    <link href="/2022/12/08/pico-overlay/"/>
    <url>/2022/12/08/pico-overlay/</url>
    
    <content type="html"><![CDATA[<h1 id="Pico-合成层"><a href="#Pico-合成层" class="headerlink" title="Pico 合成层"></a>Pico 合成层</h1><p>最近在开发一个Unity内的360 3D视频播放器，查找资料后发现了这么个东东。实际体验后效果还是很好的，尤其是对于需要在VR中渲染视频、UI等平面、曲面、天空盒等等。会清晰很多。</p><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>这里pico的文档说的很详细了，再结合我观察他们的log总结一下。</p><p>如果使用Unity的渲染管线去渲染，是先把全部的物体（包括UI）先渲染到两张（或一组，取决与你是singlePass还是multPass）的Eye Buffer 上，这个buffer的分辨率取决于你设定的渲染分辨率。然后再通过异步时间扭曲线程（Asynchronous Timewarp，ATW）对 Eye Buffer 进行畸变、采样、合成等处理，以此渲染到屏幕上。</p><p><img src="/images/screenshot_192.168.2.64_5555.png" alt="Unity的eye buffer或者叫渲染目标，是一个正常的矩形。这里的输出显然是经过了ATW处理后的图形。"></p><p>而如果使用Pico提供的api来调用VR 合成层技术，相当于直接把纹理交给ATW 线程，或者理解为把纹理当成一个新的Eye Buffer。这样少采样一次，自然减少了失真。<br>而且最关键的是这里不仅可以使用Unity内的纹理或者RenderTexture。更可以直接使用外部 Android Surface，比如Android 播放器视频纹理。这就大大提高了可玩性。</p><h2 id="注意点"><a href="#注意点" class="headerlink" title="注意点"></a>注意点</h2><p>具体的使用见官方文档。这里提几个官方没提及的坑。</p><ul><li>如果使用Underlay模式，即将纹理呈现在 Eye Buffer 后面。在URP管线下，记得最后的片元着色器的返回要改为 <code>fixed4 col = fixed4(1, 1, 1, 0);</code></li><li>使用Underlay模式时要关闭天空盒，或者自定义天空盒，保留透明像素。</li><li>同时记得大部分后处理都不能使用了，因为它们会破坏透明通道。（反正在VR场景下使用后处理也是很耗费性能的做法）</li><li>这里的清晰度和Unity的渲染分辨率就完全无关了，因为不会渲染到Unity的ColorBuffer上去。</li><li>因为是独立渲染的，所以没有抗锯齿（但影响不大，因为他渲染相当于是以物理屏幕的分辨率去渲染的，VR的像素密度很高了实际看不出有太大锯齿）</li></ul><p>下面贴几张效果图，请点击放大后比较：</p><p><img src="/images/screenshot_192.168.2.64_5555_05.png"></p><p>尤其是上面按钮处的文字</p><p><img src="/images/screenshot_192.168.2.64_5555_03.png"><br>这里的线在VR中头部运动时，差异更加明显。</p><p>除此之外，在解码高码率视频等方面，效率也会有一定提升。</p><h2 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h2><p>因为这完全破坏了Unity原有的开发逻辑，开发成本上会有所提高，尤其的各种层之间的排序问题，要事先考虑清楚。<br>大部分后处理无法使用。</p><p>除此之外，目前还是有一些BUG，比如如果用全景视频充当天空盒，Pico的SDK（2.1.2版本）目前还是会报错。等待修复吧</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>ChatGPT</title>
    <link href="/2022/12/05/ChatGPT/"/>
    <url>/2022/12/05/ChatGPT/</url>
    
    <content type="html"><![CDATA[<p>使用ChatGPT生成的打砖块游戏,仅限电脑WASD移动</p><p><a href="https://www.bilibili.com/video/BV1oM411B7Za">AI生成游戏，ChatGPT使用ThreeJS生成弹球游戏</a></p><div id="threeShader" style="position: relative; width: 100%; height: 0px; padding-bottom: 75%;"><iframe src="/static/chatGPT/index.html" allowfullscreen="true" name="ThreeJSFrame" title="ThreeJSFrame" style="position: absolute; width: 100%; height: 100%; left: 0px; top: 0px; border: none;"></iframe></div>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>ECS 脚本</title>
    <link href="/2022/11/16/ECS-Scripting/"/>
    <url>/2022/11/16/ECS-Scripting/</url>
    
    <content type="html"><![CDATA[<h1 id="实体包中的脚本"><a href="#实体包中的脚本" class="headerlink" title="实体包中的脚本"></a>实体包中的脚本</h1><p>本节包含有关在实体中编写脚本时的最佳实践的信息，以及您可以在脚本中使用的一些功能。</p><table><thead><tr><th>标题</th><th>描述</th></tr></thead><tbody><tr><td>使用方面组织代码</td><td>使用方面将实体组件的子集组合到单个 csharp 结构中。</td></tr><tr><td>Blob 资产</td><td>有关 blob 资产的信息，这些资产是针对流式处理优化的二进制数据片段。</td></tr><tr><td>在运行时加载场景</td><td>关于实体如何加载场景的信息。</td></tr><tr><td>转换系统</td><td>有关转换如何在实体中工作的信息。</td></tr></tbody></table><h1 id="使用-Baking-转换数据"><a href="#使用-Baking-转换数据" class="headerlink" title="使用 Baking 转换数据"></a>使用 Baking 转换数据</h1><p>Baking 提供了一个系统，用于将编辑器中的游戏对象数据（创作数据）转换为写入实体场景的实体（运行时数据）。</p><p>烘焙分为多个阶段，但其核心是两个关键步骤：面包师和烘焙系统。</p><p>当您打开子场景并在其中编辑创作对象时，也会发生增量烘焙。 ECS 会检测您所做的更改，并确定由于此更改而需要重新运行的最少 Baker 数量。其结果在编辑模式和播放模式期间被修补到编辑器中的实体世界中。</p><h2 id="Baker-类"><a href="#Baker-类" class="headerlink" title="Baker 类"></a>Baker 类</h2><p>使用 Baker 类直接与 Unity 对象交互，例如创作组件。 Baker 也是隐式或显式捕获依赖项的地方，如果 Baker 重新运行，添加的所有组件都能够自动恢复。 Baker 只能将组件添加到它正在烘焙的主要实体和它自己创建的其他实体。例如：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public class MyMonoBehaviour : MonoBehaviour&#123;   public float value;&#125;public class MyBaker : Baker&lt;MyMonoBehaviour&gt;&#123;   public override void Bake(MyMonoBehaviour authoring)   &#123;       AddComponent(new MyComponent &#123;Value &#x3D; authoring.value&#125; );   &#125;&#125;</code></pre></div></figure><h3 id="访问-Baker-中的其他数据源"><a href="#访问-Baker-中的其他数据源" class="headerlink" title="访问 Baker 中的其他数据源"></a>访问 Baker 中的其他数据源</h3><p>为了保持增量烘焙正常工作，您需要跟踪哪些数据用于转换 Baker 中的游戏对象。创作组件中的任何字段都会被自动跟踪，如果任何数据发生变化，Baker 就会重新运行。</p><p>不会自动跟踪来自其他创作组件的信息，您需要向其添加依赖项才能对其进行跟踪。为此，请使用 Baker 提供的函数来访问其他组件，而不是 GameObject 提供的函数：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public struct MyComponent : IComponentData&#123;   public float Value;&#125;public class MyMonoBehaviour : MonoBehaviour&#123;   public GameObject otherGO;&#125;public class MyBaker : Baker&lt;MyMonoBehaviour&gt;&#123;   public override void Bake(MyMonoBehaviour authoring)   &#123;       var transform &#x3D; GetComponent&lt;Transform&gt;(authoring.otherGO);       AddComponent(new MyComponent &#123;Value &#x3D; transform.position.x&#125; );   &#125;&#125;</code></pre></div></figure><p>同样，如果您访问资产中的数据，则需要为其创建依赖项，以便 Baker 在资产更改时重新运行。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public struct MyComponent : IComponentData&#123;   public int Value;&#125;public class MyMonoBehaviour : MonoBehaviour&#123;   public Mesh mesh;&#125;public class MyBaker : Baker&lt;MyMonoBehaviour&gt;&#123;   public override void Bake(MyMonoBehaviour authoring)   &#123;       &#x2F;&#x2F; We want to rebake if anything changes in the mesh itself       DependsOn(authoring.mesh);       AddComponent(new MyComponent &#123; Value &#x3D; authoring.mesh.vertexCount &#125; );   &#125;&#125;</code></pre></div></figure><h3 id="贝克斯的预制件"><a href="#贝克斯的预制件" class="headerlink" title="贝克斯的预制件"></a>贝克斯的预制件</h3><p>要声明和转换预制件，请在面包师中调用 GetEntity：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public struct MyComponent : IComponentData&#123;   public Entity Prefab;&#125;public class MyMonoBehaviour : MonoBehaviour&#123;   public GameObject prefab;&#125;public class MyBaker : Baker&lt;MyMonoBehaviour&gt;&#123;   public override void Bake(MyMonoBehaviour authoring)   &#123;       AddComponent(new MyComponent &#123; Prefab &#x3D; GetEntity(authoring.prefab) &#125; );   &#125;&#125;</code></pre></div></figure><p>GetEntity 返回用于创建实体预制件的实体，但此时尚未转换。这稍后会在单独的传递中发生。</p><h3 id="烘焙系统"><a href="#烘焙系统" class="headerlink" title="烘焙系统"></a>烘焙系统</h3><p>烘焙系统是处理面包师产生的输出的常规系统，例如通过组合结果。这意味着 Baking 系统应该只处理实体数据，而不处理托管的创作类型，例如游戏对象和组件。这也意味着 Baking 系统可以使用 Burst 和 Jobs 来处理数据。</p><p>要创建烘焙系统，请使用 [WorldSystemFilter(WorldSystemFilterFlags.BakingSystem)] 属性对其进行标记。这允许烘焙发现它们并将它们添加到烘焙世界。烘焙系统在每次烘焙过程中都会更新。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">[WorldSystemFilter(WorldSystemFilterFlags.BakingSystem)]partial class BakingOnlyEntityAuthoringBakingSystem : SystemBase&#123;   protected override void OnUpdate()   &#123;      &#x2F;&#x2F; … Your code here …   &#125;&#125;</code></pre></div></figure><p>虽然面包师通常是必需的，但烘焙系统是可选的，只有高级用例才需要。</p><h2 id="用方面组织你的代码"><a href="#用方面组织你的代码" class="headerlink" title="用方面组织你的代码"></a>用方面组织你的代码</h2><p>方面是一种类似于对象的包装器，可用于将实体组件的子集组合到单个 csharp 结构中。</p><p>本节说明如何在项目中使用方面。</p><table><thead><tr><th>主题</th><th>描述</th></tr></thead><tbody><tr><td>方面概念</td><td>方面概述。</td></tr><tr><td>创建方面</td><td>如何使用 IAspect 接口创建方面。</td></tr></tbody></table><h3 id="方面概念"><a href="#方面概念" class="headerlink" title="方面概念"></a>方面概念</h3><p>方面是一种类似于对象的包装器，可用于将实体组件的子集组合到单个 csharp 结构中。方面对于组织组件代码和简化系统中的查询很有用。</p><p>例如，TransformAspect 将组件的各个位置、旋转和缩放组合在一起，使您能够从包含 TransformAspect 的查询访问这些组件。您还可以使用 IAspect 接口定义自己的方面。</p><p>方面可以包括以下项目：</p><ul><li>用于存储实体 ID 的单个实体字段</li><li>RefRW<T> 和 RefRO<T> 字段访问实现 IComponentData 的类型 T 的组件数据。</T></T></li><li>EnabledRefRW 和 EnabledRefRO 字段，用于访问实现 IEnableableComponent 的组件的启用状态。</li><li>DynamicBuffer<T> 字段</T></li><li>其他方面类型</li></ul><h4 id="更多信息"><a href="#更多信息" class="headerlink" title="更多信息"></a>更多信息</h4><ul><li>创建一个方面</li><li>IAspect API 文档</li><li>转换方面 API 文档</li></ul><h3 id="创建一个方面"><a href="#创建一个方面" class="headerlink" title="创建一个方面"></a>创建一个方面</h3><p>要创建方面，请使用 IAspect 接口。您必须将方面声明为只读部分结构，并且该结构必须将自身指定给 IAspect<T> 泛型参数：</T></p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">using Unity.Entities;readonly partial struct MyAspect : IAspect&#123;    &#x2F;&#x2F; Your Aspect code&#125;</code></pre></div></figure><h4 id="字段"><a href="#字段" class="headerlink" title="字段"></a>字段</h4><p>您可以使用 RefRW<T> 或 RefRO<T> 将组件声明为方面的一部分。要声明缓冲区，请使用 DynamicBuffer<T>。有关可用字段的更多信息，请参阅 IAspect 文档。</T></T></T></p><h4 id="只读和读写访问"><a href="#只读和读写访问" class="headerlink" title="只读和读写访问"></a>只读和读写访问</h4><p>使用 RefRO 和 RefRW 字段提供对方面中组件的只读或读写访问。当你想在代码中引用一个方面时，使用 in 来覆盖所有引用变为只读，或者使用 ref 来尊重在方面中声明的只读或读写访问。</p><p>如果您使用 in 来引用对组件具有读写访问权限的方面，它可能会在写入尝试时抛出异常。</p><h4 id="在系统中创建方面实例"><a href="#在系统中创建方面实例" class="headerlink" title="在系统中创建方面实例"></a>在系统中创建方面实例</h4><p>要在系统中创建方面实例，请调用 SystemAPI.GetAspectRW 或 SystemAPI.GetAspectRO：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">&#x2F;&#x2F; Throws if the entity is missing any of &#x2F;&#x2F; the required components of MyAspect.MyAspect asp &#x3D; SystemAPI.GetAspectRW&lt;MyAspect&gt;(myEntity);</code></pre></div></figure><p>如果您使用任何试图修改底层组件的方法或属性，则 SystemAPI.GetAspectRO 会引发错误。</p><p>要在系统外部创建方面实例，请使用 EntityManager.GetAspect 或 EntityManager.GetAspectRO。</p><h4 id="例子"><a href="#例子" class="headerlink" title="例子"></a>例子</h4><p>在此示例中，CannonBallAspect 设置坦克主题游戏中炮弹组件的变换、位置和速度。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">using Unity.Entities;using Unity.Mathematics;using Unity.Transforms;&#x2F;&#x2F; Aspects must be declared as a readonly partial structreadonly partial struct CannonBallAspect : IAspect&lt;CannonBallAspect&gt;&#123;    &#x2F;&#x2F; An Entity field in an Aspect gives access to the Entity itself.    &#x2F;&#x2F; This is required for registering commands in an EntityCommandBuffer for example.    public readonly Entity Self;    &#x2F;&#x2F; Aspects can contain other aspects.    readonly TransformAspect Transform;    &#x2F;&#x2F; A RefRW field provides read write access to a component. If the aspect is taken as an &quot;in&quot;    &#x2F;&#x2F; parameter, the field behaves as if it was a RefRO and throws exceptions on write attempts.    readonly RefRW&lt;CannonBall&gt; CannonBall;    &#x2F;&#x2F; Properties like this aren&#39;t mandatory. The Transform field can be public instead.    &#x2F;&#x2F; But they improve readability by avoiding chains of &quot;aspect.aspect.aspect.component.value.value&quot;.    public float3 Position    &#123;        get &#x3D;&gt; Transform.Position;        set &#x3D;&gt; Transform.Position &#x3D; value;    &#125;    public float3 Speed    &#123;        get &#x3D;&gt; CannonBall.ValueRO.Speed;        set &#x3D;&gt; CannonBall.ValueRW.Speed &#x3D; value;    &#125;&#125;</code></pre></div></figure><p>要在其他代码中使用此方面，您可以以与组件相同的方式请求 CannonBallAspect：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">using Unity.Entities;using Unity.Burst;&#x2F;&#x2F; It&#39;s best practice to Burst-compile your code[BurstCompile]partial struct CannonBallJob : IJobEntity&#123;    void Execute(ref CannonBallAspect cannonBall)    &#123;        &#x2F;&#x2F; Your game logic    &#125;&#125;</code></pre></div></figure><h2 id="实体中的转换"><a href="#实体中的转换" class="headerlink" title="实体中的转换"></a>实体中的转换</h2><p>本节包含有关变换如何在实体中工作以及如何控制项目中任何实体的世界空间位置、旋转和缩放的信息。</p><table><thead><tr><th>主题</th><th>描述</th></tr></thead><tbody><tr><td>转换概念</td><td>转换如何在实体中工作。</td></tr><tr><td>使用转换</td><td>如何在您的项目中使用转换。</td></tr><tr><td>转换方面</td><td>如何使用 TransformAspect 来管理项目中的转换。</td></tr></tbody></table><h3 id="转变观念"><a href="#转变观念" class="headerlink" title="转变观念"></a>转变观念</h3><p>您可以使用 Unity.Transforms 命名空间来控制项目中任何实体的世界空间位置、旋转和缩放。</p><p>您还可以使用内置方面 TransformAspect，将实体及其父实体移动到一起，并保持实体数据同步。有关详细信息，请参阅 TransformAspect 文档。</p><p>主要的 Transform 组件是：</p><ul><li>LocalToWorldTransform：修改这些值以更改实体的世界空间位置。表示世界空间中对象的每个实体都具有此组件。重要提示：如果实体还包含 LocalToParentTransform 和 ParentToWorldTransform，它们将优先并覆盖您输入的 LocalToWorldTransform 值。</li><li>LocalToParentTransform：表示从本地空间到父空间的转换。定义子实体如何相对于其父实体进行转换。</li><li>ParentToWorldTransform：父实体的 LocalToWorldTransform 的副本。</li></ul><p>如果所有三个 Transform 组件都存在于一个实体上，则 ECS 计算 LocalToWorldTransform 为：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">LocalToWorldTransform &#x3D; LocalToParentTransform * ParentToWorldTransform</code></pre></div></figure><p>您可以使用 Convert To Entity 脚本为您创建和初始化所有组件。要使用此脚本，请在 EntitiesSamples 项目中打开 HelloCube 并选择大立方体。在 Inspector 中，选择 Convert To Entity 组件，它将 GameObject 转换为实体。</p><h4 id="转换层次结构"><a href="#转换层次结构" class="headerlink" title="转换层次结构"></a>转换层次结构</h4><p>Unity.Transforms 是分层的，这意味着您可以根据实体之间的关系来转换实体。</p><p>例如，车身可以是其车轮的父级。车轮是车身的孩子。当车身移动时，车轮也随之移动。您还可以相对于车身移动和旋转车轮。</p><p>一个实体可以有多个子实体，但只有一个父实体。孩子也可以是他们自己的孩子实体的父母。这些多层次的父子关系形成了一个转换层次结构。层次结构顶部的实体（没有父实体）是根。</p><p>要声明一个 Transform 层次结构，您必须从下到上执行此操作。这意味着您使用 Parent 来声明实体的父实体，而不是声明其子实体。如果你想声明一个实体的孩子，找到你想成为孩子的实体，并将他们的父母设置为目标实体。有关详细信息，请参阅使用层次结构文档。</p><h3 id="使用变换"><a href="#使用变换" class="headerlink" title="使用变换"></a>使用变换</h3><p>要在项目中使用变换，请使用 Unity.Transforms 命名空间来控制项目中任何实体的世界空间位置、旋转和缩放。</p><p>要存储位置、旋转和比例值，请使用 UniformScaleTransform：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public struct UniformScaleTransform&#123;    public float3     Position    public float      Scale    public quaternion Rotation&#125;</code></pre></div></figure><p>代表项目中对象的每个实体都有一个 LocalToWorldTransform，您可以使用它来转换实体相对于它在世界空间中的位置：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public struct LocalToWorldTransform : IComponentData&#123;    public UniformScaleTransform Value;&#125;</code></pre></div></figure><h4 id="使用层次结构"><a href="#使用层次结构" class="headerlink" title="使用层次结构"></a>使用层次结构</h4><p>您可以单独使用 LocalToWorldTransform。但是，如果要使用 Entities 的层次结构，则必须使用 Parent、LocalToParentTransform 和 ParentToWorldTransform 来转换它们。</p><p>要设置子实体的父级，请使用 Parent：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public struct Parent : IComponentData&#123;    public Entity Value;&#125;</code></pre></div></figure><p>为确保父母找到他们的孩子，并设置他们的子组件，请运行 ParentSystem。</p><p>要指定如何相对于其父项定位、旋转和缩放子项，请使用 LocalToParentTransform。例如，这是您可以在父汽车对象上旋转车轮的方式：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public struct LocalToParentTransform : IComponentData&#123;    public UniformScaleTransform Value;&#125;</code></pre></div></figure><p>另一个重要的组件是 ParentToWorldTransform，它是父级 LocalToWorldTransform 的副本。您需要确保孩子有这个，并且 ParentToWorldTransformSystem 正在运行。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public struct ParentToWorldTransform : IComponentData&#123;    public UniformScaleTransform Value;&#125;</code></pre></div></figure><h3 id="变换方面"><a href="#变换方面" class="headerlink" title="变换方面"></a>变换方面</h3><p>方面系统具有内置的 TransformAspect，它包含对子实体的所有三个转换组件的引用：</p><ul><li>本地到世界转换</li><li>本地到父转换</li><li>ParentToWorldTransform</li></ul><p>对于任何根实体，TransformAspect 仅包含对 LocalToWorldTransform 的引用。</p><p>TransformAspect 是管理项目中转换的便捷方式，因为它包含使所有这些组件彼此保持同步的逻辑。例如，如果您想在不使用 TransformAspect 的情况下控制子组件的世界空间位置，则必须同时更新 LocalToWorldTransform 和 LocalToParentTransform，然后在该计算中使用 ParentToWorldTransform。</p><p>但是，TransformAspect 会为您管理这个。这是移动可能有父实体的便捷方式。</p><p>此示例说明如何使用 TransformAspect 来旋转坦克的炮塔：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">using Unity.Burst;using Unity.Entities;using Unity.Mathematics;using Unity.Transforms;[BurstCompile]partial struct TurretRotationSystem : ISystem&#123;    [BurstCompile]    public void OnCreate(ref SystemState state)    &#123;    &#125;    [BurstCompile]    public void OnDestroy(ref SystemState state)    &#123;    &#125;    [BurstCompile]    public void OnUpdate(ref SystemState state)    &#123;        &#x2F;&#x2F; The amount of rotation around Y required to do 360 degrees in 2 seconds.        var rotation &#x3D; quaternion.RotateY(state.Time.DeltaTime * math.PI);        &#x2F;&#x2F; The classic csharp foreach is what we often refer to as &quot;Idiomatic foreach&quot; (IFE).        &#x2F;&#x2F; Aspects provide a higher level interface than directly accessing component data.        &#x2F;&#x2F; Using IFE with aspects is a powerful and expressive way of writing main thread code.        foreach (var transform in SystemAPI.Query&lt;TransformAspect&gt;())        &#123;            transform.RotateWorld(rotation);        &#125;    &#125;&#125;</code></pre></div></figure><h1 id="Blob-资产"><a href="#Blob-资产" class="headerlink" title="Blob 资产"></a>Blob 资产</h1><p>Blob 资产是针对流式处理优化的二进制数据片段。 Blob 是 Binary Large Object 的缩写。通过将数据写入 blob 资产，您可以将其存储在一种可以高效加载并从存储在实体上的组件中引用的格式。与结构组件一样，blob 资产不得包含任何托管数据：您不能在 blob 资产中使用常规数组、字符串或任何其他托管对象。 Blob 资产应该只包含在运行时不会更改的只读数据：它们可以同时从多个线程访问，并且（与本机容器不同）没有针对并发写入的安全检查。</p><p>为了快速加载 blob 资产，它们的数据必须是可重定位的：当您将整个 blob 资产复制到另一个内存地址时，blob 资产中数据的含义不得改变。这意味着 blob 资产可能不包含对自身的绝对引用，这排除了内部指针的使用。您通常通过指针存储的任何信息都必须通过相对于 blob 资产本身的内存地址的偏移量来引用。这主要适用于存储字符串和数组。这种使用偏移量而不是绝对指针的间接寻址的细节以两种方式影响与 blob 资产的交互：</p><ol><li>必须使用 BlobBuilder 创建 Blob 资产。这种类型负责为您计算相对偏移量。</li><li>必须始终使用 ref 关键字或使用 BlobAssetReference 通过引用访问和传递 Blob 资产。这是确保 blob 资产内的任何相对偏移仍解析为正确的绝对地址所必需的。问题又是重定位：Blob 资产可以在内存中作为一个整体重定位，但按值而不是按引用访问它们通常不能保证复制整个 blob 资产。</li></ol><div class="note note-info">            <p>如果您尝试使用按值包含内部指针的 blob 资产，则会出现编译器错误。</p>          </div><h2 id="创建-blob-资产"><a href="#创建-blob-资产" class="headerlink" title="创建 blob 资产"></a>创建 blob 资产</h2><p>创建 blob 资产始终至少涉及四个步骤：</p><ol><li>创建一个 BlobBuilder。这需要在内部分配一些内存。</li><li>使用 BlobBuilder.ConstructRoot 构造 blob 资产的根</li><li>用您的数据填充结构。</li><li>使用 BlobBuilder.CreateBlobAssetReference 创建 BlobAssetReference。这会将 blob 资产复制到最终位置。</li><li>处理在步骤 1 中分配的 blob 生成器。</li></ol><p>例如，这里我们将仅包含原始成员的结构存储为 blob 资产：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">struct MarketData&#123;    public float PriceOranges;    public float PriceApples;&#125;BlobAssetReference&lt;MarketData&gt; CreateMarketData()&#123;    &#x2F;&#x2F; Create a new builder that will use temporary memory to construct the blob asset    var builder &#x3D; new BlobBuilder(Allocator.Temp);    &#x2F;&#x2F; Construct the root object for the blob asset. Notice the use of &#96;ref&#96;.    ref MarketData marketData &#x3D; ref builder.ConstructRoot&lt;MarketData&gt;();    &#x2F;&#x2F; Now fill the constructed root with the data:    &#x2F;&#x2F; Apples compare to Oranges in the universally accepted ratio of 2 : 1 .    marketData.PriceApples &#x3D; 2f;    marketData.PriceOranges &#x3D; 4f;    &#x2F;&#x2F; Now copy the data from the builder into its final place, which will    &#x2F;&#x2F; use the persistent allocator    var result &#x3D; builder.CreateBlobAssetReference&lt;MarketData&gt;(Allocator.Persistent);    &#x2F;&#x2F; Make sure to dispose the builder itself so all internal memory is disposed.    builder.Dispose();    return result;&#125;</code></pre></div></figure><p>blob 构建器的作用是构造存储在 blob 资产中的数据，确保所有内部引用都存储为偏移量，最后将完成的 blob 资产复制到由返回的 BlobAssetReference<T> 引用的单个分配中。</T></p><h3 id="使用-BlobArray"><a href="#使用-BlobArray" class="headerlink" title="使用 BlobArray"></a>使用 BlobArray</h3><p>blob 资产中的数组需要特殊处理，因为它们是在内部使用相对偏移量实现的。这是使用 BlobArray 类型实现的。以下是分配 blob 数据数组并填充它的方法：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">struct Hobby&#123;    public float Excitement;    public int NumOrangesRequired;&#125;struct HobbyPool&#123;    public BlobArray&lt;Hobby&gt; Hobbies;&#125;BlobAssetReference&lt;HobbyPool&gt; CreateHobbyPool()&#123;    var builder &#x3D; new BlobBuilder(Allocator.Temp);    ref HobbyPool hobbyPool &#x3D; ref builder.ConstructRoot&lt;HobbyPool&gt;();    &#x2F;&#x2F; Allocate enough room for two hobbies in the pool. Use the returned BlobBuilderArray    &#x2F;&#x2F; to fill in the data.    const int numHobbies &#x3D; 2;    BlobBuilderArray&lt;Hobby&gt; arrayBuilder &#x3D; builder.Allocate(        ref hobbyPool.Hobbies,        numHobbies    );    &#x2F;&#x2F; Initialize the hobbies.    &#x2F;&#x2F; An exciting hobby that consumes a lot of oranges.    arrayBuilder[0] &#x3D; new Hobby    &#123;        Excitement &#x3D; 1,        NumOrangesRequired &#x3D; 7    &#125;;    &#x2F;&#x2F; A less exciting hobby that conserves oranges.    arrayBuilder[1] &#x3D; new Hobby    &#123;        Excitement &#x3D; 0.2f,        NumOrangesRequired &#x3D; 2    &#125;;    var result &#x3D; builder.CreateBlobAssetReference&lt;HobbyPool&gt;(Allocator.Persistent);    builder.Dispose();    return result;&#125;</code></pre></div></figure><h3 id="使用-BlobString"><a href="#使用-BlobString" class="headerlink" title="使用 BlobString"></a>使用 BlobString</h3><p>字符串具有与数组相同的问题，并且具有使用 BlobString 的自定义支持。它们同样使用 BlobBuilder API 进行分配。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">struct CharacterSetup&#123;    public float Loveliness;    public BlobString Name;&#125;BlobAssetReference&lt;CharacterSetup&gt; CreateCharacterSetup(string name)&#123;    var builder &#x3D; new BlobBuilder(Allocator.Temp);    ref CharacterSetup character &#x3D; ref builder.ConstructRoot&lt;CharacterSetup&gt;();    character.Loveliness &#x3D; 9001; &#x2F;&#x2F; it&#39;s just a very lovely character    &#x2F;&#x2F; Create a new BlobString and set it to the given name.    builder.AllocateString(ref character.Name, name);    var result &#x3D; builder.CreateBlobAssetReference&lt;CharacterSetup&gt;(Allocator.Persistent);    builder.Dispose();    return result;&#125;</code></pre></div></figure><h3 id="使用-BlobPtr"><a href="#使用-BlobPtr" class="headerlink" title="使用 BlobPtr"></a>使用 BlobPtr</h3><p>如果需要手动设置内部指针，可以使用 BlobPtr 类型。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">struct FriendList&#123;    public BlobPtr&lt;BlobString&gt; BestFriend;    public BlobArray&lt;BlobString&gt; Friends;&#125;BlobAssetReference&lt;FriendList&gt; CreateFriendList()&#123;    var builder &#x3D; new BlobBuilder(Allocator.Temp);    ref FriendList friendList &#x3D; ref builder.ConstructRoot&lt;FriendList&gt;();    const int numFriends &#x3D; 3;    var arrayBuilder &#x3D; builder.Allocate(ref friendList.Friends, numFriends);    builder.AllocateString(ref arrayBuilder[0], &quot;Alice&quot;);    builder.AllocateString(ref arrayBuilder[1], &quot;Bob&quot;);    builder.AllocateString(ref arrayBuilder[2], &quot;Joachim&quot;);    &#x2F;&#x2F; Set the best friend pointer to point to the second array element.    builder.SetPointer(ref friendList.BestFriend, ref arrayBuilder[2]);    var result &#x3D; builder.CreateBlobAssetReference&lt;FriendList&gt;(Allocator.Persistent);    builder.Dispose();    return result;&#125;</code></pre></div></figure><h2 id="访问组件上的-blob-资产"><a href="#访问组件上的-blob-资产" class="headerlink" title="访问组件上的 blob 资产"></a>访问组件上的 blob 资产</h2><p>获得 Blob 资产的 BlobAssetReference<T> 后，您可以将此引用存储在组件上并访问它。请注意，必须通过引用访问包含内部指针的 blob 资产的所有部分。</T></p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">struct Hobbies : IComponentData&#123;    public BlobAssetReference&lt;HobbyPool&gt; Blob;&#125;float GetExcitingHobby(ref Hobbies component, int numOranges)&#123;    &#x2F;&#x2F; Get a reference to the pool of available hobbies. Note that it needs to be passed by    &#x2F;&#x2F; reference, because otherwise the internal reference in the BlobArray would be invalid.    ref HobbyPool pool &#x3D; ref component.Blob.Value;    &#x2F;&#x2F; Find the most exciting hobby we can participate in with our current number of oranges.    float mostExcitingHobby &#x3D; 0;    for (int i &#x3D; 0; i &lt; pool.Hobbies.Length; i++)    &#123;        &#x2F;&#x2F; This is safe to use without a reference, because the Hobby struct does not        &#x2F;&#x2F; contain internal references.        var hobby &#x3D; pool.Hobbies[i];        if (hobby.NumOrangesRequired &gt; numOranges)            continue;        if (hobby.Excitement &gt;&#x3D; mostExcitingHobby)            mostExcitingHobby &#x3D; hobby.Excitement;    &#125;    return mostExcitingHobby;&#125;</code></pre></div></figure><h2 id="我什么时候需要处理-blob-资产引用？"><a href="#我什么时候需要处理-blob-资产引用？" class="headerlink" title="我什么时候需要处理 blob 资产引用？"></a>我什么时候需要处理 blob 资产引用？</h2><p>在运行时使用 BlobBuilder.CreateBlobAssetReference 分配的所有 blob 资产都需要手动处理。这对于作为从磁盘加载的实体场景的一部分加载的 blob 资产是不同的：所有这些 blob 资产都是引用计数的，一旦没有组件引用它们就会自动释放。不得手动处理它们。</p><h2 id="调试-blob-资产内容"><a href="#调试-blob-资产内容" class="headerlink" title="调试 blob 资产内容"></a>调试 blob 资产内容</h2><p>Blob 资产使用相对偏移量实现内部引用。这意味着复制 BlobString 结构（或具有这些内部引用的任何其他类型）将复制包含的相对偏移量，而不是它指向的内容。这样做的结果是一个不可用的 BlobString，它将代表一个基本上随机的字符串。虽然这在您自己的代码中很容易避免，但调试实用程序通常会做到这一点。因此，BlobString 的内容无法在调试器中正确显示。</p><p>但是，支持显示 BlobAssetReference<T> 的值及其所有内容。如果要查找 BlobString 的内容，请导航到包含的 BlobAssetReference<T> 并从那里开始调试。</T></T></p><h1 id="在运行时加载场景"><a href="#在运行时加载场景" class="headerlink" title="在运行时加载场景"></a>在运行时加载场景</h1><h2 id="串流"><a href="#串流" class="headerlink" title="串流"></a>串流</h2><p>加载大场景需要时间，所以为了避免卡顿，DOTS中的所有场景加载默认都是异步的。这称为流式传输。</p><p>由于改造项目以使用流式处理可能很繁重，因此您最好尽早决定是否在项目中使用流式处理。</p><p>流式传输的主要优点是：</p><ul><li>当场景在后台流式传输时，应用程序可以保持响应。</li><li>通过在不中断游戏玩法的情况下动态加载和卸载场景，可以实现比内存更大的无缝世界。</li><li>在编辑器播放模式下，如果实体场景文件丢失或过时，场景将按需转换。因为实体场景的转换和加载是异步发生的并且在一个单独的进程中，所以编辑器保持响应。</li></ul><p>流式传输的主要缺点是：</p><ul><li>游戏不能假定加载的数据立即存在，尤其是在启动时。这使得游戏代码有点复杂。</li><li>场景由“场景系统组”中的系统加载，“场景系统组”本身是“初始化组”的一部分。在帧中更新较晚的系统将在同一帧中看到加载的数据，但更新早于该组的系统直到下一帧才会看到加载的数据。然后，您的代码必须在单个- 框架内解决这种不一致的数据视图。</li></ul><h2 id="The-Subscene-Monobehaviour"><a href="#The-Subscene-Monobehaviour" class="headerlink" title="The Subscene Monobehaviour"></a>The Subscene Monobehaviour</h2><p>Subscene Monobehavior 是一个简单的 Unity 组件，它抽象了转换和流式处理问题。</p><p>打开子场景时，创作游戏对象场景会显示在父场景的层次结构中。<br>关闭子场景时，转换后的场景的内容将流入。<br>本页的其余部分描述了如何在不使用 Subscene MonoBehavior 的情况下直接控制流式传输。</p><h2 id="场景加载-101"><a href="#场景加载-101" class="headerlink" title="场景加载 101"></a>场景加载 101</h2><p>用于处理场景的高级 API 由 SceneSystem 提供。</p><p>这是在运行时加载场景的最基本示例。这应该在系统的 OnUpdate 中完成。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">&#x2F;&#x2F; Note: calling GetSceneGUID is slow, please keep reading for a proper example.var guid &#x3D; SceneSystem.GetSceneGUID(ref World.Unmanaged.GetExistingSystemState&lt;SceneSystem&gt;(), &quot;Assets&#x2F;Scenes&#x2F;SampleScene.unity&quot;);var sceneEntity &#x3D; SceneSystem.LoadSceneAsync(World.Unmanaged, guid);</code></pre></div></figure><div class="note note-warning">            <p>此示例计划加载。在调用 LoadSceneAsync 期间，唯一创建的是场景实体，然后使用它来控制加载过程的其余部分。值得注意的是，场景标题、部分实体及其内容此时尚未加载，只会在几帧后出现在世界中。</p>          </div><ul><li>在 DOTS 的上下文中，场景 GUID 是 Hash128。</li><li>该路径指向 Unity 创作场景。如果对应的实体场景文件丢失或过期，则触发转换。</li></ul><h2 id="使用场景-GUID"><a href="#使用场景-GUID" class="headerlink" title="使用场景 GUID"></a>使用场景 GUID</h2><p>通过 GUID 识别场景比使用字符串路径更有效。因此，通常的方法是在转换期间存储场景 GUID，以用于在运行时加载</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">    &#x2F;&#x2F; Runtime component, SceneSystem uses Entities.Hash128 to identify scenes.    public struct SceneLoader : IComponentData    &#123;        public Hash128 Guid;    &#125;#if UNITY_EDITOR    &#x2F;&#x2F; Authoring component, a SceneAsset can only be used in the Editor    public class SceneLoaderAuthoring : MonoBehaviour    &#123;        public UnityEditor.SceneAsset Scene;        class Baker : Baker&lt;SceneLoaderAuthoring&gt;        &#123;            public override void Bake(SceneLoaderAuthoring authoring)            &#123;                var path &#x3D; AssetDatabase.GetAssetPath(authoring.Scene);                var guid &#x3D; AssetDatabase.GUIDFromAssetPath(path);                AddComponent(new SceneLoader &#123; Guid &#x3D; guid &#125;);            &#125;        &#125;    &#125;#endif</code></pre></div></figure><p>在运行时处理 SceneLoader 组件的示例系统如下所示：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">[RequireMatchingQueriesForUpdate]public partial class SceneLoaderSystem : SystemBase&#123;    private EntityQuery m_NewRequests;    protected override void OnCreate()    &#123;        m_NewRequests &#x3D; GetEntityQuery(typeof(SceneLoader));    &#125;    protected override void OnUpdate()    &#123;        var requests &#x3D; m_NewRequests.ToComponentDataArray&lt;SceneLoader&gt;(Allocator.Temp);        for (int i &#x3D; 0; i &lt; requests.Length; i +&#x3D; 1)        &#123;            SceneSystem.LoadSceneAsync(World.Unmanaged, requests[i].Guid);        &#125;        requests.Dispose();        EntityManager.DestroyEntity(m_NewRequests);    &#125;&#125;</code></pre></div></figure><div class="note note-info">            <p>在编辑器中，SceneSystem.GetGUID 函数在内部使用 UnityEditor.AssetDatabase 类将场景路径映射到 GUID。</p><p>在独立播放器中，无法使用 UnityEditor.AssetDatabase，因此 SceneSystem.GetGUID 改为使用“StreamingAssets&#x2F;catalog.bin”文件。这个“catalog.bin”文件只不过是一个“GUID 路径”映射表，该目录文件是通过使用构建配置进行独立构建而生成的）。</p>          </div><h2 id="场景和部分元实体"><a href="#场景和部分元实体" class="headerlink" title="场景和部分元实体"></a>场景和部分元实体</h2><p>创作场景的转换会生成实体场景文件。每个实体场景文件的头部包含：</p><ul><li>部分列表（包含文件名、文件大小、边界体积等数据）。</li><li>资产包依赖项 (GUID) 的列表。</li><li>可选的用户定义的元数据。</li></ul><p>部分和捆绑包的列表决定了应该加载的文件列表，自定义元数据可用于特定于游戏的目的。例如，自定义元数据可以包含 PVS 信息以告知何时流式传输场景的决定，或者诸如“此场景仅在任务 XYZ 处于活动状态时才相关”的游戏条​​件。由每个游戏决定如何使用自定义元数据。但是使用自定义元数据既是可选的又是高级主题，因此稍后将对其进行记录和说明。</p><p>加载实体场景分两步完成。首先，“解决”阶段加载标题，并为每个场景和每个部分创建一个元实体。只有在这之后，才会加载这些部分的内容。</p><p>这些场景和部分元实体用于控制实际的流式传输。默认情况下，调用 SceneSystem.LoadSceneAsync 将解析并加载所有内容。</p><div class="note note-info">            <ul><li>应通过调用 SceneSystem 上的方法加载和卸载整个场景。</li><li>通过在表示场景的实体上添加和删除 RequestLoaded 组件来加载和卸载场景部分。这些请求由 SceneSectionStreamingSystem 处理，它是 SceneSystemGroup 的一部分。</li></ul>          </div><h2 id="串流状态"><a href="#串流状态" class="headerlink" title="串流状态"></a>串流状态</h2><p>流式传输是异步的，因此无法保证在请求数据后加载数据需要多长时间。虽然 SceneSystem 允许查询场景和部分的加载状态，但在大多数情况下应该不需要这样做。</p><p>理想情况下，系统应该对其所需数据的存在或不存在做出反应，而不是对某些场景是否正在加载做出反应。如果系统需要运行的数据是特定场景的一部分，那么判断是否更新系统应该通过检查是否加载了特定数据来完成，而不是检查场景本身是否已经加载。这种方法避免了将系统绑定到特定场景：如果系统所需的数据被移动到不同的场景、从网络下载或程序生成，系统仍将以相同的方式工作，而无需更改其代码。</p><p>尽管如此，您仍可以检查场景或部分是否已加载。例如，这可能有助于实现一个加载屏幕，该屏幕应保持可见，直到所有计划的流媒体完成。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">var sceneLoaded &#x3D; SceneSystem.IsSceneLoaded(World.Unmanaged, sceneEntity);var sectionLoaded &#x3D; SceneSystem.IsSectionLoaded(World.Unmanaged, sceneSectionEntity);</code></pre></div></figure><h2 id="场景部分"><a href="#场景部分" class="headerlink" title="场景部分"></a>场景部分</h2><p>场景的各个部分可以独立加载和卸载。</p><p>场景中的每个实体都有一个 SceneSection 共享组件，其中包含场景的 GUID (Hash128) 和部分编号（整数）。第 0 部分是默认部分。</p><p>在转换期间，可以通过更改 SceneSection 共享组件的值来设置实体的部分，如下所示：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public class SceneSection123Authoring : MonoBehaviour&#123;    class Baker : Baker&lt;SceneSection123Authoring&gt;    &#123;        &#x2F;&#x2F; TODO: This doesn&#39;t work with Baking, as it relied on ordering        public override void Bake(SceneSection123Authoring authoring)        &#123;            &#x2F;&#x2F; This affects a single entity.            &#x2F;&#x2F; For a recursive approach, see &#96;SceneSectionComponent&#96;.            SetSharedComponent(GetEntity(), new SceneSection&#123;Section &#x3D; 123&#125;);        &#125;    &#125;&#125;</code></pre></div></figure><p>将上述组件添加到子场景引用的创作场景中的游戏对象将导致该子场景的检查器如下所示：</p><p><img src="https://docs.unity3d.com/Packages/com.unity.entities@1.0/manual/images/scene_section.png"></p><p>请注意，默认部分 0 始终存在（第一行），即使它是空的。名称的“Section: 0”部分被省略，但包含至少一个实体的所有其他部分将以其全名显示。</p><div class="note note-info">            <p>所有部分都可以引用它们自己的实体和第 0 部分中的实体。描述此参考系统的工作方式超出了此处的范围，但一个重要的结果是从场景中加载任何部分都需要来自同一场景的第 0 部分也是加载。相同的约束适用于卸载：只有当当前没有加载同一场景的其他部分时，才能卸载场景的第 0 部分。</p>          </div><p>一些现有的 DOTS 功能已经利用了部分加载。您可以通过编写自定义转换系统、使用 IConvertGameObjectToEntity（参见上面的示例）或使用创作组件 SceneSectionComponent（这将影响层次结构中的所有创作游戏对象）来显式控制您自己代码中的部分加载。</p><h2 id="独立加载场景部分"><a href="#独立加载场景部分" class="headerlink" title="独立加载场景部分"></a>独立加载场景部分</h2><p>使用 DisableAutoLoad 参数调用 LoadSceneAsync 将通过创建场景和部分元实体来解析场景，但不会加载部分内容：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">var loadParameters &#x3D; new SceneSystem.LoadParameters &#123;Flags &#x3D; SceneLoadFlags.DisableAutoLoad&#125;;var sceneEntity &#x3D; SceneSystem.LoadSceneAsync(World.Unmanaged, sceneGuid, loadParameters);</code></pre></div></figure><p>一旦处理了加载请求，就会解析这些部分并创建它们的元实体。然后可以在场景元实体上查询 ResolvedSectionEntity 缓冲区。作为说明，以下代码将加载给定场景的每个其他部分。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">&#x2F;&#x2F; To keep the sample code short, we assume that the sections have been resolved.&#x2F;&#x2F; And the code that ensures the code runs only once isn&#39;t included either.var sectionBuffer &#x3D; EntityManager.GetBuffer&lt;ResolvedSectionEntity&gt;(sceneEntity);var sectionEntities &#x3D; sectionBuffer.ToNativeArray(Allocator.Temp);for (int i &#x3D; 0; i &lt; sectionEntities.Length; i +&#x3D; 1)&#123;    if (i % 2 &#x3D;&#x3D; 0)    &#123;        &#x2F;&#x2F; Note that the condition includes section 0,        &#x2F;&#x2F; nothing else will load if section 0 is missing.        EntityManager.AddComponent&lt;RequestSceneLoaded&gt;(sectionEntities[i].SectionEntity);    &#125;&#125;sectionEntities.Dispose();</code></pre></div></figure><h2 id="卸载场景和部分"><a href="#卸载场景和部分" class="headerlink" title="卸载场景和部分"></a>卸载场景和部分</h2><p>卸载整个场景及其所有部分是通过场景系统完成的。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">SceneSystem.UnloadScene(World.Unmanaged, sceneEntity);</code></pre></div></figure><p>也可以使用场景的 GUID 而不是其元实体来调用 UnloadScene，但这有两个缺点：</p><ul><li>该函数必须执行（可能代价高昂的）搜索表示与 GUID 匹配的场景的元实体。</li><li>如果加载同一场景的多个实例，按GUID卸载只会卸载一个实例。</li></ul>]]></content>
    
    
    
    <tags>
      
      <tag>Entities</tag>
      
      <tag>ECS</tag>
      
      <tag>DOTS</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>ECS 组件</title>
    <link href="/2022/11/16/ECS-components-intro/"/>
    <url>/2022/11/16/ECS-components-intro/</url>
    
    <content type="html"><![CDATA[<h1 id="组件"><a href="#组件" class="headerlink" title="组件"></a>组件</h1><p>组件代表实体组件系统 (ECS) 架构中的数据。实体将组件关联在一起，系统提供可以读取和写入组件数据的逻辑。本节介绍 ECS 组件并说明如何使用它们。</p><table><thead><tr><th>标题</th><th>描述</th></tr></thead><tbody><tr><td>组件类型</td><td>了解不同的组件类型以及如何使用它们。</td></tr><tr><td>向实体添加组件</td><td>使用 csharp 脚本从实体添加组件。</td></tr><tr><td>从实体中移除组件</td><td>使用 csharp 脚本从实体中移除组件。</td></tr><tr><td>读取和写入组件值</td><td>使用 csharp 脚本读取或写入组件值。</td></tr></tbody></table><h1 id="组件类型"><a href="#组件类型" class="headerlink" title="组件类型"></a>组件类型</h1><p>为了服务于各种用例，有多种类型的 ECS 组件。文档的这一部分描述了 ECS 组件类型、它们的用例和性能注意事项，以及如何创建它们。</p><table><thead><tr><th>主题</th><th>描述</th></tr></thead><tbody><tr><td>非托管组件</td><td>了解非托管组件以及如何使用它们。</td></tr><tr><td>托管组件</td><td>了解托管组件以及如何使用它们。</td></tr><tr><td>共享组件</td><td>了解共享组件以及如何使用它们。</td></tr><tr><td>清理组件</td><td>了解清理组件以及如何使用它们。</td></tr><tr><td>清理共享组件</td><td>了解清理共享组件以及如何使用它们。</td></tr><tr><td>标记组件</td><td>了解标记组件以及如何使用它们。</td></tr><tr><td>缓冲区组件</td><td>了解缓冲区组件及其使用方法。</td></tr><tr><td>块组件</td><td>了解块组件以及如何使用它们。</td></tr><tr><td>可启用的组件</td><td>了解可启用的组件以及如何使用它们。</td></tr></tbody></table><h2 id="编辑器中的组件类型"><a href="#编辑器中的组件类型" class="headerlink" title="编辑器中的组件类型"></a>编辑器中的组件类型</h2><p>在编辑器中，以下图标代表不同类型的组件。您可以在相关的实体窗口和检查器中看到这些。</p><table><thead><tr><th>图标</th><th>组件类型</th></tr></thead><tbody><tr><td></td><td>托管组件。</td></tr><tr><td></td><td>共享组件。</td></tr><tr><td></td><td>标签组件。</td></tr><tr><td></td><td>缓冲组件。</td></tr><tr><td></td><td>块组件。</td></tr></tbody></table><div class="note note-info">            <p>Unity 为不在上面列表中的组件类型使用通用组件图标。</p>          </div><h3 id="非托管组件"><a href="#非托管组件" class="headerlink" title="非托管组件"></a>非托管组件</h3><p>非托管组件存储最常见的数据类型，这意味着它们在大多数用例中都很有用。</p><table><thead><tr><th>主题</th><th>描述</th></tr></thead><tbody><tr><td>介绍非托管组件</td><td>了解非托管组件和它们可以存储的属性类型。</td></tr><tr><td>创建一个非托管组件</td><td>创建一个新的非托管组件以在您的应用程序中使用。</td></tr></tbody></table><h4 id="引入非托管组件"><a href="#引入非托管组件" class="headerlink" title="引入非托管组件"></a>引入非托管组件</h4><p>非托管组件存储最常见的数据类型，这意味着它们在大多数用例中都很有用。</p><p>非托管组件可以存储以下类型的属性：</p><ul><li>可复制类型</li><li>布尔值</li><li>字符</li><li>BlobAssetReference<T>（对 Blob 数据结构的引用）</T></li><li>Collections.FixedString（固定大小的字符缓冲区）</li><li>集合.FixedList</li><li>固定数组（仅在不安全的上下文中允许）</li><li>符合这些相同限制的其他结构</li></ul><h3 id="创建非托管组件"><a href="#创建非托管组件" class="headerlink" title="创建非托管组件"></a>创建非托管组件</h3><p>要创建非托管组件，请创建一个继承自 IcomponentData 的结构。</p><p>以下代码示例显示了一个非托管组件：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public struct ExampleUnmanagedComponent : IComponentData&#123;    public int Value;&#125;</code></pre></div></figure><p>将使用兼容类型的属性添加到结构中以定义组件的数据。如果您不向组件添加任何属性，它就充当标记组件。</p><h2 id="托管组件"><a href="#托管组件" class="headerlink" title="托管组件"></a>托管组件</h2><p>托管组件可以存储任何类型的属性，但存储和访问需要更多资源，并且包括对如何使用它们的限制。</p><table><thead><tr><th>主题</th><th>描述</th></tr></thead><tbody><tr><td>托管组件简介</td><td>了解非托管组件、它们可以存储的属性类型以及适用于它们的限制。</td></tr><tr><td>创建一个托管组件</td><td>创建一个新的托管组件以在您的应用程序中使用。</td></tr><tr><td>优化托管组件</td><td>了解解释托管组件最佳使用方式的最佳实践信息。</td></tr></tbody></table><h3 id="引入托管组件"><a href="#引入托管组件" class="headerlink" title="引入托管组件"></a>引入托管组件</h3><p>与非托管组件不同，托管组件可以存储任何类型的属性。但是，它们的存储和访问需要更多资源，并且有以下限制：</p><ul><li>您无法在工作中访问它们。</li><li>您不能在 Burst 编译代码中使用它们。</li><li>他们需要垃圾收集。</li><li>它们必须包含一个不带参数的构造函数以用于序列化目的。</li></ul><h4 id="托管类型属性"><a href="#托管类型属性" class="headerlink" title="托管类型属性"></a>托管类型属性</h4><p>如果托管组件中的属性使用托管类型，您可能需要手动添加克隆、比较和序列化该属性的能力。</p><h3 id="创建托管组件"><a href="#创建托管组件" class="headerlink" title="创建托管组件"></a>创建托管组件</h3><p>要创建托管组件，请创建一个继承自 IComponentData 且没有构造函数或包含无参数构造函数的类。</p><p>以下代码示例显示了一个托管组件：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public class ExampleManagedComponent : IComponentData&#123;    public int Value;&#125;</code></pre></div></figure><h4 id="管理外部资源的生命周期"><a href="#管理外部资源的生命周期" class="headerlink" title="管理外部资源的生命周期"></a>管理外部资源的生命周期</h4><p>对于引用外部资源的托管组件，最佳做法是实现 ICloneable 和 IDisposable，例如，对于存储对 ParticleSystem 的引用的托管组件。</p><p>如果您复制此托管组件的实体，默认情况下会创建两个引用同一粒子系统的托管组件。如果为托管组件实现 ICloneable，则可以为第二个托管组件复制粒子系统。如果销毁托管组件，默认情况下粒子系统会保留。如果为托管组件实现 IDisposable，则可以在销毁组件时销毁粒子系统。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public class ManagedComponentWithExternalResource : IComponentData, IDisposable, ICloneable&#123;    public ParticleSystem ParticleSystem;    public void Dispose()    &#123;        UnityEngine.Object.Destroy(ParticleSystem);    &#125;    public object Clone()    &#123;        return new ManagedComponentWithExternalResource &#123; ParticleSystem &#x3D; UnityEngine.Object.Instantiate(ParticleSystem) &#125;;    &#125;&#125;</code></pre></div></figure><h3 id="优化托管组件"><a href="#优化托管组件" class="headerlink" title="优化托管组件"></a>优化托管组件</h3><p>与非托管组件不同，Unity 不直接将托管组件存储在块中。相反，Unity 将它们存储在一个大数组中以供整个世界使用。然后块存储相关托管组件的数组索引。这意味着当您访问实体的托管组件时，Unity 会处理额外的索引查找。这使得托管组件不如非托管组件优化。</p><p>托管组件的性能影响意味着您应该尽可能使用非托管组件。</p><h2 id="共享组件"><a href="#共享组件" class="headerlink" title="共享组件"></a>共享组件</h2><p>共享组件根据其共享 cComponent 的值将实体分组为块，这有助于数据的重复数据删除。</p><table><thead><tr><th>主题</th><th>描述</th></tr></thead><tbody><tr><td>介绍共享组件</td><td>了解共享组件及其用例。</td></tr><tr><td>创建一个共享组件</td><td>创建一个新的共享组件以在您的应用程序中使用。</td></tr><tr><td>优化共享组件</td><td>了解解释使用共享组件的最佳方式的最佳实践信息。</td></tr></tbody></table><h3 id="引入共享组件"><a href="#引入共享组件" class="headerlink" title="引入共享组件"></a>引入共享组件</h3><p>共享组件根据其共享组件的值将实体分组为块，这有助于数据的重复数据删除。为此，Unity 将具有相同共享组件值的原型的所有实体存储在一起。这会删除实体之间的重复值。</p><p>共享组件是结构，但根据添加到共享组件的属性类型，您可以创建托管和非托管共享组件。如果共享组件包含托管类型，例如字符串或其他类对象，则共享组件是托管共享组件。否则，它是一个非托管共享组件。托管共享组件与常规托管组件具有相同的优点和限制。</p><h4 id="共享组件值存储"><a href="#共享组件值存储" class="headerlink" title="共享组件值存储"></a>共享组件值存储</h4><p>对于每个世界，Unity 将共享组件值存储在与 ECS 块分开的数组中，并且该世界存储句柄中的块可以为其原型找到适当的共享组件值。同一块中的实体共享相同的共享组件值。多个块可以存储相同的共享组件句柄，这意味着可以使用相同共享组件值的实体数量没有限制。</p><p>如果更改实体的共享组件值，Unity 会将实体移动到使用新共享组件值的块。这意味着更改实体的共享组件值是一种结构更改。如果共享组件值数组中已经存在相等的值，Unity 会将实体移动到存储现有值的索引的块中。否则，Unity 将新值添加到共享组件值数组并将实体移动到存储该新值索引的新块。有关如何更改 ECS 比较共享组件值的方式的信息，请参阅覆盖默认比较行为。</p><p>Unity 将非托管和托管共享组件彼此分开存储，并通过非托管共享组件 API（例如 SetUnmanagedSharedComponentData）使非托管共享组件可用于 Burst 编译代码。有关详细信息，请参阅优化共享组件。</p><h4 id="覆盖默认比较行为"><a href="#覆盖默认比较行为" class="headerlink" title="覆盖默认比较行为"></a>覆盖默认比较行为</h4><p>要更改 ECS 比较共享组件实例的方式，请为共享组件实施 IEquatable<YourSharedComponent>。如果您这样做，ECS 将使用您的实现来检查共享组件的实例是否相等。如果共享组件是非托管的，您可以将[BurstCompile] 属性添加到共享组件结构、Equals 方法和GetHashCode 方法中以提高性能。</YourSharedComponent></p><h4 id="在世界之间共享共享组件"><a href="#在世界之间共享共享组件" class="headerlink" title="在世界之间共享共享组件"></a>在世界之间共享共享组件</h4><p>对于创建和保留资源密集型的托管对象（例如 blob 资产），您可以使用共享组件在所有世界中仅存储该对象的一个​​副本。为此，使用 Retain 和 Release 实现 IRefCounted 接口。实施 Retain 和 Release，以便这些方法正确管理底层资源的生命周期。如果共享组件是非托管的，可以在共享组件结构体、Retain方法和Release方法中添加[BurstCompile]属性来提高性能。</p><h4 id="不要修改共享组件引用的对象"><a href="#不要修改共享组件引用的对象" class="headerlink" title="不要修改共享组件引用的对象"></a>不要修改共享组件引用的对象</h4><p>要正常工作，共享组件依赖于您使用 Entities API 来更改它们的值。这包括引用的对象。如果共享组件包含引用类型或指针，请注意不要在不使用 Entities API 的情况下修改引用的对象。</p><h3 id="创建共享组件"><a href="#创建共享组件" class="headerlink" title="创建共享组件"></a>创建共享组件</h3><p>您可以创建托管和非托管共享组件。</p><h4 id="创建非托管共享组件"><a href="#创建非托管共享组件" class="headerlink" title="创建非托管共享组件"></a>创建非托管共享组件</h4><p>要创建非托管共享组件，请创建一个实现标记接口 ISharedComponentData 的结构。</p><p>以下代码示例显示了一个非托管共享组件：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public struct ExampleUnmanagedSharedComponent : ISharedComponentData&#123;    public int Value;&#125;</code></pre></div></figure><h4 id="创建托管共享组件"><a href="#创建托管共享组件" class="headerlink" title="创建托管共享组件"></a>创建托管共享组件</h4><p>要创建托管共享组件，请创建一个实现标记接口 ISharedComponentData 的类。</p><p>以下代码示例显示了一个托管共享组件：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public class ExampleManagedSharedComponent : ISharedComponentData&#123;    public int Value;&#125;</code></pre></div></figure><h3 id="优化共享组件"><a href="#优化共享组件" class="headerlink" title="优化共享组件"></a>优化共享组件</h3><p>共享组件对其他组件类型有不同的性能考虑。本页描述共享组件特定的性能注意事项和优化技术。</p><h4 id="使用非托管共享组件"><a href="#使用非托管共享组件" class="headerlink" title="使用非托管共享组件"></a>使用非托管共享组件</h4><p>如果可能，在托管共享组件上使用非托管共享组件。这是因为 Unity 将非托管共享组件存储在 Burst 编译代码可通过非托管共享组件 API（例如 SetUnmanagedSharedComponentData）访问的位置。这提供了优于托管组件的性能优势。</p><h4 id="避免频繁更新"><a href="#避免频繁更新" class="headerlink" title="避免频繁更新"></a>避免频繁更新</h4><p>更新实体的共享组件值是一种结构更改，这意味着 Unity 将实体移动到另一个块。出于性能原因，尽量避免经常这样做。</p><h4 id="避免大量独特的共享组件值"><a href="#避免大量独特的共享组件值" class="headerlink" title="避免大量独特的共享组件值"></a>避免大量独特的共享组件值</h4><p>块中的所有实体必须共享相同的共享组件值。这意味着如果您为大量实体提供唯一的共享组件值，它会将这些实体分割成许多几乎是空的块。</p><p>例如，如果一个原型有 500 个实体具有共享组件，并且每个实体都有一个唯一的共享组件值，Unity 会将每个实体存储在一个单独的块中。这浪费了每个块中的大部分空间，也意味着要循环遍历原型的所有实体，Unity 必须循环遍历所有 500 个块。这抵消了 ECS 块布局的好处并降低了性能。为避免此问题，请尝试使用尽可能少的唯一共享组件值。如果 500 个示例实体仅共享十个唯一的共享组件值，Unity 可以将它们存储在少至十个块中。</p><p>小心具有多个共享组件类型的原型。原型块中的所有实体必须具有相同的共享组件值组合，因此具有多个共享组件类型的原型容易产生碎片。</p><div class="note note-info">            <p>要检查块碎片，您可以在 Archetypes 窗口中查看块利用率。</p>          </div><h2 id="清理组件"><a href="#清理组件" class="headerlink" title="清理组件"></a>清理组件</h2><p>清理组件就像常规组件，但当您销毁包含一个实体的实体时，Unity 会移除所有非清理组件。该实体仍然存在，直到您从中删除所有清理组件。这对于标记销毁时需要清理的实体很有用。</p><table><thead><tr><th>主题</th><th>描述</th></tr></thead><tbody><tr><td>介绍清理组件</td><td>了解清理组件及其用例。</td></tr><tr><td>创建清理组件</td><td>创建一个新的清理组件以在您的应用程序中使用。</td></tr><tr><td>使用清理组件执行清理</td><td>使用清理组件管理销毁时需要清理的实体。</td></tr><tr><td>清理共享组件</td><td>清理共享组件是托管共享组件，它们具有清理组件的销毁语义。</td></tr></tbody></table><h3 id="引入清理组件"><a href="#引入清理组件" class="headerlink" title="引入清理组件"></a>引入清理组件</h3><p>清理组件就像常规组件，但当您销毁包含一个实体的实体时，Unity 会移除所有非清理组件。该实体仍然存在，直到您从中删除所有清理组件。这对于标记销毁时需要清理的实体很有用。有关如何执行此操作的信息，请参阅使用清理组件执行清理。</p><h4 id="清理组件生命周期"><a href="#清理组件生命周期" class="headerlink" title="清理组件生命周期"></a>清理组件生命周期</h4><p>以下代码示例解释了包含清理组件的实体的生命周期：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">&#x2F;&#x2F; Creates an entity that contains a cleanup component.Entity e &#x3D; EntityManger.CreateEntity(    typeof(Translation), typeof(Rotation), typeof(ExampleCleanup));&#x2F;&#x2F; Attempts to destroy the entity but, because the entity has a cleanup component, Unity doesn&#39;t actually destroy the entity. Instead, Unity just removes the Translation and Rotation components. EntityManager.DestroyEntity(e);&#x2F;&#x2F; The entity still exists so this demonstrates that you can still use the entity normally.EntityManager.AddComponent&lt;Translation&gt;(e);&#x2F;&#x2F; Removes all the components from the entity. This destroys the entity.EntityManager.DestroyEntity(e, new ComponentTypes(typeof(ExampleCleanup), typeof(Translation)));&#x2F;&#x2F; Demonstrates that the entity no longer exists. entityExists is false. bool entityExists &#x3D; EntityManager.Exists(e);</code></pre></div></figure><div class="note note-info">            <p>清除组件是非托管组件，并且具有与非托管组件相同的所有限制。</p>          </div><h3 id="创建清理组件"><a href="#创建清理组件" class="headerlink" title="创建清理组件"></a>创建清理组件</h3><p>要创建清理组件，请创建一个继承自 ICleanupComponentData 的结构。</p><p>以下代码示例显示了一个空的清理组件：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public struct ExampleCleanupComponent : ICleanupComponentData&#123;&#125;</code></pre></div></figure><div class="note note-info">            <p>空的清理组件通常就足够了，但您可以添加属性来存储清理目标原型所需的信息。</p>          </div><h3 id="使用清理组件进行清理"><a href="#使用清理组件进行清理" class="headerlink" title="使用清理组件进行清理"></a>使用清理组件进行清理</h3><p>清理组件的主要用例是帮助您管理销毁时需要清理的实体。为了实现这一点，它们会阻止您销毁包含清理组件的实体。相反，当您尝试销毁带有附加清理组件的实体时，Unity 会移除所有非清理组件。该实体仍然存在，直到您从中删除所有清理组件。</p><p>要对特定原型的实体执行清理：</p><ol><li>创建一个新的标签组件并将标签组件添加到原型中。</li><li>创建一个新的清理组件，其中包含清理特定实体原型所需的信息。</li><li>创建一个系统：<ol><li>获取目标原型的新创建实体。这些是包含标签组件但不包含清理组件的实体。</li><li>将清理组件添加到这些实体。</li></ol></li><li>创建一个系统：<ol><li>获取已被临时销毁并需要清理的实体。这些是包含清理组件但不包含标记组件的实体。</li><li>为实体执行适当的清理工作。</li><li>从实体中删除清理组件。</li></ol></li></ol><h3 id="清理共享组件"><a href="#清理共享组件" class="headerlink" title="清理共享组件"></a>清理共享组件</h3><p>清理共享组件是托管共享组件，具有清理组件的销毁语义。它们对于标记需要相同信息进行清理的实体很有用。</p><h4 id="创建清理共享组件"><a href="#创建清理共享组件" class="headerlink" title="创建清理共享组件"></a>创建清理共享组件</h4><p>要创建清理共享组件，请创建一个继承自 ICleanupSharedComponentData 的结构。</p><p>以下代码示例显示了一个空的系统清理组件：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public struct ExampleSharedCleanupComponent : ICleanupSharedComponentData&#123;&#125;</code></pre></div></figure><h2 id="标记组件"><a href="#标记组件" class="headerlink" title="标记组件"></a>标记组件</h2><p>标记组件不包含任何属性，并且表现得像非托管组件。因为标记组件不包含任何数据，所以块不会为它们存储任何组件数组。</p><table><thead><tr><th>主题</th><th>描述</th></tr></thead><tbody><tr><td>标签组件简介</td><td>了解标签组件及其用例。</td></tr><tr><td>创建标签组件</td><td>创建一个新的标签组件以在您的应用程序中使用。</td></tr></tbody></table><h3 id="引入标签组件"><a href="#引入标签组件" class="headerlink" title="引入标签组件"></a>引入标签组件</h3><p>标签组件是非托管组件，不存储数据也不占用空间。从概念上讲，标签组件实现与 GameObject 标签类似的目的，它们在查询中很有用，因为您可以根据实体是否具有标签组件来过滤实体。例如，您可以将它们与清理组件和过滤器实体一起使用以执行清理。</p><h4 id="额外资源"><a href="#额外资源" class="headerlink" title="额外资源"></a>额外资源</h4><ul><li>创建标签组件</li></ul><h3 id="创建标签组件"><a href="#创建标签组件" class="headerlink" title="创建标签组件"></a>创建标签组件</h3><p>要创建标记组件，请创建一个没有任何属性的非托管组件。</p><p>以下代码示例显示了一个标记组件：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public struct ExampleTagComponent : IComponentData&#123;&#125;</code></pre></div></figure><h2 id="动态缓冲组件"><a href="#动态缓冲组件" class="headerlink" title="动态缓冲组件"></a>动态缓冲组件</h2><p>动态缓冲区组件是充当可调整大小数组的组件。</p><table><thead><tr><th>主题</th><th>描述</th></tr></thead><tbody><tr><td>介绍动态缓冲区组件</td><td>了解动态缓冲区组件及其用例。</td></tr><tr><td>创建一个动态缓冲区组件</td><td>创建一个新的动态缓冲区组件以在您的应用程序中使用。</td></tr><tr><td>访问块中的所有动态缓冲区使用 BufferAccessor<T></T></td><td>获取块中特定类型的所有动态缓冲区。</td></tr><tr><td>为多个实体重用动态缓冲区</td><td>访问主线程上的动态缓冲区并将其数据用于多个实体。</td></tr><tr><td>从作业访问动态缓冲区</td><td>创建 BufferLookup 查找以在不在主线程上时访问动态缓冲区。</td></tr><tr><td>使用 EntityCommandBuffer 修改动态缓冲区</td><td>使用 EntityCommandBuffer 来推迟动态缓冲区修改。</td></tr><tr><td>重新解释动态缓冲区</td><td>将动态缓冲区的内容重新解释为另一种类型。</td></tr></tbody></table><h3 id="引入动态缓冲组件"><a href="#引入动态缓冲组件" class="headerlink" title="引入动态缓冲组件"></a>引入动态缓冲组件</h3><p>动态缓冲区组件是充当可调整大小的非托管结构数组的组件。您可以使用它来存储实体的数组数据，例如实体在其间导航的路径点位置。</p><p>除了数据，每个缓冲区还存储一个长度、一个容量和一个内部指针：</p><ul><li>Length 是缓冲区中的元素数。它从 0 开始，并在您将值附加到缓冲区时递增。</li><li>容量是缓冲区中的存储量。它开始匹配内部缓冲容量。设置容量调整缓冲区的大小。</li><li>该指针指示动态缓冲区数据所在的位置。最初它是 null 表示数据在实体块中，如果 Unity 将数据移到块外，指针将设置为指向新数组。有关 Unity 如何存储动态缓冲区组件的更多信息，请参阅容量。</li></ul><h4 id="容量"><a href="#容量" class="headerlink" title="容量"></a>容量</h4><p>动态缓冲区的初始容量由缓冲区存储的类型定义。默认情况下，容量默认为适合 128 字节的元素数。有关详细信息，请参阅 DefaultBufferCapacityNumerator。您可以使用 InternalBufferCapacity 属性指定自定义容量。有关如何创建动态缓冲区组件类型的信息，请参阅创建动态缓冲区组件类型。</p><p>最初，Unity 将动态缓冲区数据直接存储在组件所属实体的块中。如果动态缓冲区的长度大于容量，Unity 会将动态缓冲区数据复制到块外的数组中。如果动态缓冲区的长度后来收缩到小于容量，Unity 仍然将数据存储在块之外；如果 Unity 将动态缓冲区数据移出块，它永远不会将数据移回块中。</p><p>原始内部缓冲区容量是块的一部分，Unity 仅在 Unity 自己释放块时释放它。这意味着如果动态缓冲区长度超过内部容量并且 Unity 将数据复制到块外，则块内会浪费空间。最佳做法是尽可能使用块中的数据。为此，请确保您的大多数实体不超过缓冲区容量，但如果实体不使用它，也不要将容量设置得太高。如果动态缓冲区的大小变化太大，最好将其数据存储在块之外。为此，将 InternalBufferCapacity 设置为 0。</p><p>还有其他选项可用于存储数组数据：</p><ul><li>Blob 资产：存储紧密打包的只读结构化数据，包括数组，多个实体可以共享一个 blob 资产。因为它们是只读的，所以您可以同时从多个线程访问它们。</li><li>托管组件：存储本机或托管对象的数组。但是，与动态缓冲区组件数据相比，访问托管组件数据的限制更多且性能更低。您还需要手动克隆和处理阵列数据。</li><li>共享组件：与托管组件类似，它们存储本机或托管对象的数组，您的实体可以将索引存储到这些更大的数组中。它们具有与托管组件相同的限制和性能注意事项。</li></ul><h4 id="结构变化"><a href="#结构变化" class="headerlink" title="结构变化"></a>结构变化</h4><p>结构更改可能会破坏或移动动态缓冲区引用的数组，这意味着动态缓冲区的任何句柄在结构更改后都会变得无效。您必须在任何结构更改后重新获取动态缓冲区。例如：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public void DynamicBufferExample(Entity e)&#123;    &#x2F;&#x2F; Acquires a dynamic buffer of type MyElement.    DynamicBuffer&lt;MyElement&gt; myBuff &#x3D; EntityManager.GetBuffer&lt;MyElement&gt;(e);    &#x2F;&#x2F; This structural change invalidates the previously acquired DynamicBuffer.    EntityManager.CreateEntity();    &#x2F;&#x2F; A safety check will throw an exception on any read or write actions on the buffer.    var x &#x3D; myBuff[0];    &#x2F;&#x2F; Reacquires the dynamic buffer after the above structural changes.    myBuff &#x3D; EntityManager.GetBuffer&lt;MyElement&gt;(e);    var y &#x3D; myBuff[0];&#125;</code></pre></div></figure><h4 id="额外资源-1"><a href="#额外资源-1" class="headerlink" title="额外资源"></a>额外资源</h4><ul><li>创建动态缓冲组件</li></ul><h3 id="创建动态缓冲组件"><a href="#创建动态缓冲组件" class="headerlink" title="创建动态缓冲组件"></a>创建动态缓冲组件</h3><p>创建动态缓冲组件<br>要创建动态缓冲区组件，请创建一个继承自 IBufferElementData 的结构。这个结构定义了动态缓冲区类型的元素，也代表了动态缓冲区组件本身。</p><p>要指定缓冲区的初始容量，请使用 InternalBufferCapacity 属性。有关 Unity 如何管理缓冲区容量的信息，请参阅容量。</p><p>以下代码示例显示了一个缓冲区组件：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">[InternalBufferCapacity(16)]public struct ExampleBufferComponent : IBufferElementData&#123;    public int Value;&#125;&#96;&#96;&#96;与其他组件一样，您可以向实体添加动态缓冲区组件。但是，您使用 DynamicBuffer&lt;ExampleBufferComponent&gt; 表示动态缓冲区组件，并使用特定于动态缓冲区组件的 EntityManager API（例如 EntityManager.GetBuffer&lt;T&gt;）与它们进行交互。例如：&#96;&#96;&#96;csharppublic void GetDynamicBufferComponentExample(Entity e)&#123;    DynamicBuffer&lt;MyElement&gt; myDynamicBuffer &#x3D; EntityManager.GetBuffer&lt;MyElement&gt;(e);&#125;</code></pre></div></figure><h3 id="访问块中的动态缓冲区"><a href="#访问块中的动态缓冲区" class="headerlink" title="访问块中的动态缓冲区"></a>访问块中的动态缓冲区</h3><p>要访问块中的所有动态缓冲区，请使用 ArchetypeChunk.GetBufferAccessor 方法。这需要一个 BufferTypeHandle<T> 并返回一个 BufferAccessor<T>。如果您索引 BufferAccessor<T>，它会返回块的类型为 T 的缓冲区：</T></T></T></p><p>以下代码示例显示了如何访问块中某种类型的每个动态缓冲区。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">[InternalBufferCapacity(16)]public struct ExampleBufferComponent : IBufferElementData&#123;    public int Value;&#125;</code></pre></div></figure><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public partial class ExampleSystem : SystemBase&#123;    protected override void OnUpdate()    &#123;        var query &#x3D; EntityManager.CreateEntityQuery(typeof(ExampleBufferComponent));        NativeArray&lt;ArchetypeChunk&gt; chunks &#x3D; query.ToArchetypeChunkArray(Allocator.Temp);        for (int i &#x3D; 0; i &lt; chunks.Length; i++)        &#123;            UpdateChunk(chunks[i]);        &#125;        chunks.Dispose();    &#125;    private void UpdateChunk(ArchetypeChunk chunk)    &#123;        &#x2F;&#x2F; Get a BufferTypeHandle representing dynamic buffer type ExampleBufferComponent from SystemBase.        BufferTypeHandle&lt;ExampleBufferComponent&gt; myElementHandle &#x3D; GetBufferTypeHandle&lt;ExampleBufferComponent&gt;();        &#x2F;&#x2F; Get a BufferAccessor from the chunk.        BufferAccessor&lt;ExampleBufferComponent&gt; buffers &#x3D; chunk.GetBufferAccessor(myElementHandle);        &#x2F;&#x2F; Iterate through all ExampleBufferComponent buffers of each entity in the chunk.        for (int i &#x3D; 0, chunkEntityCount &#x3D; chunk.Count; i &lt; chunkEntityCount; i++)        &#123;            DynamicBuffer&lt;ExampleBufferComponent&gt; buffer &#x3D; buffers[i];            &#x2F;&#x2F; Iterate through all elements of the buffer.            for (int j &#x3D; 0; j &lt; buffer.Length; j++)            &#123;                &#x2F;&#x2F; ...            &#125;        &#125;    &#125;&#125;</code></pre></div></figure><h3 id="为多个实体重用动态缓冲区"><a href="#为多个实体重用动态缓冲区" class="headerlink" title="为多个实体重用动态缓冲区"></a>为多个实体重用动态缓冲区</h3><p>如果 Entities.ForEach 的所有实体都需要相同的缓冲区，您可以在 Entities.ForEach 之上的主线程上将该缓冲区作为局部变量获取。</p><p>以下代码示例显示了如何为多个实体使用相同的动态缓冲区。它假设存在一个名为 MyElement 的动态缓冲区，并且存在另一个名为 OtherComponent 的组件。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public void DynamicBufferExample(Entity e)&#123;    var myBuff &#x3D; EntityManager.GetBuffer&lt;MyElement&gt;(e);    Entities.ForEach((in OtherComponent component) &#x3D;&gt; &#123;        &#x2F;&#x2F; ... use myBuff    &#125;).Schedule();&#125;</code></pre></div></figure><div class="note note-info">            <p>如果您使用 ScheduleParallel，请注意您不能并行写入动态缓冲区。但是，您可以使用 EntityCommandBuffer.ParallelWriter 并行记录更改。</p>          </div><h3 id="从作业访问动态缓冲区"><a href="#从作业访问动态缓冲区" class="headerlink" title="从作业访问动态缓冲区"></a>从作业访问动态缓冲区</h3><p>如果一项作业需要在其代码中查找一个或多个缓冲区，则该作业需要使用 BufferLookup 查找表。您在系统中创建这些，然后将它们传递给需要它们的作业。</p><h4 id="修改作业"><a href="#修改作业" class="headerlink" title="修改作业"></a>修改作业</h4><p>在需要随机访问动态缓冲区的作业中：</p><ol><li>添加一个 ReadOnly BufferLookup 成员变量。</li><li>在 IJobEntity.Execute 方法中，按实体对 BufferLookup 查找表进行索引。这提供了对附加到实体的动态缓冲区的访问。<figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public partial struct AccessDynamicBufferJob : IJobEntity&#123;    [ReadOnly] public BufferLookup&lt;ExampleBufferComponent&gt; BufferLookup;    public void Execute()      &#123;              &#x2F;&#x2F; ...      &#125;&#125;</code></pre></div></figure></li></ol><h4 id="修改系统"><a href="#修改系统" class="headerlink" title="修改系统"></a>修改系统</h4><p>在创建作业实例的系统中：</p><ol><li>添加一个 BufferLookup 成员变量。</li><li>在 OnCreate 中，使用 SystemState.GetBufferLookup 分配 BufferLookup 变量。</li><li>在 OnUpdate 开始时，对 BufferLookup 变量调用 Update。这会更新查找表。</li><li>创建作业实例时，将查找表传递给作业。</li></ol><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public partial struct AccessDynamicBufferFromJobSystem : ISystem&#123;    private BufferLookup&lt;ExampleBufferComponent&gt; _bufferLookup;    public void OnCreate(ref SystemState state)    &#123;        _bufferLookup &#x3D; state.GetBufferLookup&lt;ExampleBufferComponent&gt;(true);    &#125;    public void OnUpdate(ref SystemState state)    &#123;        _bufferLookup.Update(ref state);        var exampleBufferAccessJob &#x3D; new AccessDynamicBufferJob &#123; BufferLookup &#x3D; _bufferLookup &#125;;        exampleBufferAccessJob.ScheduleParallel();    &#125;    public void OnDestroy(ref SystemState state) &#123; &#125;&#125;</code></pre></div></figure><h3 id="使用-EntityCommandBuffer-修改动态缓冲区"><a href="#使用-EntityCommandBuffer-修改动态缓冲区" class="headerlink" title="使用 EntityCommandBuffer 修改动态缓冲区"></a>使用 EntityCommandBuffer 修改动态缓冲区</h3><p>EntityCommandBuffer 记录为实体添加、删除或设置缓冲区组件的命令。存在不同于常规组件 API 的特定于动态缓冲区的 API。</p><p>以下代码示例介绍了一些通用的特定于动态缓冲区的 EntityCommandBuffer API。它假设存在一个名为 MyElement 的动态缓冲区。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">private void Example(Entity e, Entity otherEntity)&#123;    EntityCommandBuffer ecb &#x3D; new EntityCommandBuffer(Allocator.TempJob);    &#x2F;&#x2F; Record a command to remove the MyElement dynamic buffer from an entity.    ecb.RemoveComponent&lt;MyElement&gt;(e);    &#x2F;&#x2F; Record a command to add a MyElement dynamic buffer to an existing entity.    &#x2F;&#x2F; The data of the returned DynamicBuffer is stored in the EntityCommandBuffer,     &#x2F;&#x2F; so changes to the returned buffer are also recorded changes.     DynamicBuffer&lt;MyElement&gt; myBuff &#x3D; ecb.AddBuffer&lt;MyElement&gt;(e);    &#x2F;&#x2F; After playback, the entity will have a MyElement buffer with     &#x2F;&#x2F; Length 20 and these recorded values.    myBuff.Length &#x3D; 20;    myBuff[0] &#x3D; new MyElement &#123; Value &#x3D; 5 &#125;;    myBuff[3] &#x3D; new MyElement &#123; Value &#x3D; -9 &#125;;    &#x2F;&#x2F; SetBuffer is like AddBuffer, but safety checks will throw an exception at playback if     &#x2F;&#x2F; the entity doesn&#39;t already have a MyElement buffer.     DynamicBuffer&lt;MyElement&gt; otherBuf &#x3D; ecb.SetBuffer&lt;MyElement&gt;(otherEntity);    &#x2F;&#x2F; Records a MyElement value to append to the buffer. Throws an exception at     &#x2F;&#x2F; playback if the entity doesn&#39;t already have a MyElement buffer.    ecb.AppendToBuffer&lt;MyElement&gt;(otherEntity, new MyElement &#123; Value &#x3D; 12 &#125;);&#125;</code></pre></div></figure><p>当您设置 DynamicBuffer 的 Length、Capacity 和内容时，ECS 会将这些更改记录到 EntityCommandBuffer 中。当您回放 EntityCommandBuffer 时，ECS 会对动态缓冲区进行更改。</p><h3 id="重新解释动态缓冲区"><a href="#重新解释动态缓冲区" class="headerlink" title="重新解释动态缓冲区"></a>重新解释动态缓冲区</h3><p>您可以重新解释 <code>DynamicBuffer&lt;T&gt;</code> 以获得另一个 <code>DynamicBuffer&lt;U&gt;</code>，其中 T 和 U 具有相同的大小。如果您想将组件的动态缓冲区重新解释为组件所附加的实体的动态缓冲区，这将很有用。这种重新解释为相同的内存设置了别名，因此更改一个索引 i 处的值会更改另一个索引 i 处的值。</p><div class="note note-info">            <p>Reinterpret 方法仅强制原始类型和新类型具有相同的大小。例如，您可以将 uint 重新解释为 float，因为这两种类型都是 32 位的。您有责任决定重新解释是否对您的目的有意义。</p>          </div><p>以下代码示例显示了如何解释动态缓冲区。它假设存在一个名为 MyElement 的动态缓冲区，并包含一个名为 Value 的 int 字段。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public class ExampleSystem : SystemBase&#123;    private void ReinterpretEntitysChunk(Entity e)    &#123;        DynamicBuffer&lt;MyElement&gt; myBuff &#x3D; EntityManager.GetBuffer&lt;MyElement&gt;(e);        &#x2F;&#x2F; Valid as long as each MyElement struct is four bytes.         DynamicBuffer&lt;int&gt; intBuffer &#x3D; myBuff.Reinterpret&lt;int&gt;();        intBuffer[2] &#x3D; 6;  &#x2F;&#x2F; same effect as: myBuff[2] &#x3D; new MyElement &#123; Value &#x3D; 6 &#125;;        &#x2F;&#x2F; The MyElement value has the same four bytes as int value 6.         MyElement myElement &#x3D; myBuff[2];        Debug.Log(myElement.Value);    &#x2F;&#x2F; 6    &#125;&#125;</code></pre></div></figure><div class="note note-info">            <p>重新解释的缓冲区共享原始缓冲区的安全句柄，因此受到所有相同的安全限制。</p>          </div><h2 id="块组件"><a href="#块组件" class="headerlink" title="块组件"></a>块组件</h2><p>块组件是一种组件，它按块而不是按实体存储值。它们提供与共享组件类似的功能，但在一些基本方面有所不同。</p><table><thead><tr><th>主题</th><th>描述</th></tr></thead><tbody><tr><td>介绍块组件</td><td>了解块组件及其用例。</td></tr><tr><td>创建块组件</td><td>创建一个新的块组件以在您的应用程序中使用。</td></tr><tr><td>使用块组件</td><td>了解如何使用特定于块组件的 API。</td></tr></tbody></table><h3 id="引入块组件"><a href="#引入块组件" class="headerlink" title="引入块组件"></a>引入块组件</h3><p>块组件存储每个块而不是每个实体的值。它们的主要目的是作为一种优化，因为您可以在每个块级别上运行代码以检查是否为每个块中的所有实体处理某些行为。例如，块组件可以存储其中所有实体的边界。您可以检查边界是否在屏幕上，如果是，则只处理该块中的实体。</p><p>块组件提供与共享组件类似的功能，但在以下方面有所不同：</p><ul><li>块组件值在概念上属于块本身，而不是块的各个实体。</li><li>设置块组件值不是结构更改。</li><li>与共享组件不同，Unity 不会重复删除唯一的块组件值：具有相同块组件值的块存储它们自己的单独副本。</li><li>块组件始终是非托管的：您无法创建托管块组件。</li><li>当实体的原型更改或实体的共享组件值更改时，Unity 会将实体移动到新块，但这些移动不会修改源块或目标块的块组件值。</li></ul><h3 id="创建块组件"><a href="#创建块组件" class="headerlink" title="创建块组件"></a>创建块组件</h3><p>块组件的定义与非托管组件相同。这意味着您创建一个继承自 IComponentData 的常规结构来创建块组件。块组件和非托管组件之间的区别在于将它们添加到实体的方式。</p><p>以下代码示例显示了一个非托管组件：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public struct ExampleChunkComponent : IComponentData&#123;    public int Value;&#125;</code></pre></div></figure><p>要将非托管组件用作块组件，请使用 EntityManager.AddChunkComponentData<YourChunkComponent>(Entity) 将其添加到实体中。</YourChunkComponent></p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">private void ChunkComponentExample(Entity e)&#123;    &#x2F;&#x2F; Adds ExampleChunkComp to the passed in entity&#39;s chunk.    EntityManager.AddChunkComponent&lt;ExampleChunkComp&gt;(e);    &#x2F;&#x2F; Finds all chunks with an ExampleComponent and an ExampleChunkComponent.    &#x2F;&#x2F; To distinguish chunk components from a regular IComponentData, You must    &#x2F;&#x2F; specify the chunk component with ComponentType.ChunkComponent.    EntityQuery query &#x3D; GetEntityQuery(typeof(ExampleComponent), ComponentType.ChunkComponent&lt;ExampleChunkComp&gt;());    NativeArray&lt;ArchetypeChunk&gt; chunks &#x3D; query.ToArchetypeChunkArray(Allocator.Temp);    &#x2F;&#x2F; Sets the ExampleChunkComp value of the first chunk.    EntityManager.SetChunkComponentData&lt;ExampleChunkComp&gt;(chunks[0], new ExampleChunkComp &#123; Value &#x3D; 6 &#125;);    &#x2F;&#x2F; Gets the ExampleChunkComp value of the first chunk.    ExampleChunkComp exampleChunkComp &#x3D; EntityManager.GetChunkComponentData&lt;ExampleChunkComp&gt;(chunks[0]);    Debug.Log(exampleChunkComp.Value)    &#x2F;&#x2F; 6&#125;</code></pre></div></figure><div class="note note-info">            <p>如果您只想从块组件读取而不写入，请在定义查询时使用 ComponentType.ChunkComponentReadOnly。将查询中包含的组件标记为只读有助于避免不必要的作业调度约束。</p>          </div><p>虽然块组件属于块本身，但在实体上添加或删除块组件会改变其原型并导致结构变化。</p><div class="note note-info">            <p>Unity 将新创建的块组件值初始化为这些类型的默认值。</p>          </div><p>您还可以通过任何块的实体获取和设置块的块组件：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">private void ChunkComponentExample(Entity e)&#123;    &#x2F;&#x2F; Sets the ExampleChunkComp value of the entity&#39;s chunk.    EntityManager.SetChunkComponentData&lt;MyChunkComp&gt;(e, new MyChunkComp &#123; Value &#x3D; 6 &#125;);    &#x2F;&#x2F; Sets the ExampleChunkComp value of the entity&#39;s chunk.    MyChunkComp myChunkComp &#x3D; EntityManager.GetChunkComponentData&lt;MyChunkComp&gt;(e);    Debug.Log(myChunkComp.Value)    &#x2F;&#x2F; 6&#125;</code></pre></div></figure><h4 id="在作业中使用块组件"><a href="#在作业中使用块组件" class="headerlink" title="在作业中使用块组件"></a>在作业中使用块组件</h4><p>作业不能使用 EntityManager，因此要访问块组件，您需要使用其 ComponentTypeHandle。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">struct MyJob : IJobChunk&#123;    public ComponentTypeHandle&lt;ExampleChunkComponent&gt; ExampleChunkCompHandle;    public void Execute(ArchetypeChunk chunk, int chunkIndex, int firstEntityIndex)    &#123;        &#x2F;&#x2F; Get the chunk&#39;s MyChunkComp.        ExampleChunkComponent myChunkComp &#x3D; chunk.GetChunkComponentData(ExampleChunkCompHandle);        &#x2F;&#x2F; Set the chunk&#39;s MyChunkComp.         chunk.SetChunkComponentData(ExampleChunkCompHandle, new ExampleChunkComponent &#123; Value &#x3D; 7 &#125;);    &#125;&#125;</code></pre></div></figure><h2 id="启用组件"><a href="#启用组件" class="headerlink" title="启用组件"></a>启用组件</h2><p>使用可启用的组件在运行时禁用或启用实体上的各个组件。这在处理您希望频繁且不可预测地更改的状态时非常有用，因为与添加或删除组件相比，它们产生的结构更改更少。</p><table><thead><tr><th>主题</th><th>描述</th></tr></thead><tbody><tr><td>可启用组件概述</td><td>可启用组件的概述，您可以在运行时禁用或启用这些组件。</td></tr><tr><td>使用可用组件</td><td>有关使用可用组件的信息。</td></tr></tbody></table><h3 id="启用组件概述"><a href="#启用组件概述" class="headerlink" title="启用组件概述"></a>启用组件概述</h3><p>您可以在 IComponentData 和 IBufferElementData 组件上使用可启用组件，以在运行时禁用或启用实体上的各个组件。要使组件可用，请从 IEnableableComponent 继承它们。</p><p>可启用组件非常适合您期望频繁且不可预测地更改的状态，或者状态排列数量在逐帧基础上很高的情况。添加和删​​除组件是管理低频状态更改组件的首选方法，您希望状态在许多帧中持续存在。</p><h4 id="结构变化-1"><a href="#结构变化-1" class="headerlink" title="结构变化"></a>结构变化</h4><p>与添加和删除组件不同，可启用组件不会创建结构更改。在确定实体是否与实体查询匹配时，ECS 将禁用的组件视为实体没有该组件。这意味着具有禁用组件的实体不匹配需要该组件的查询，并且匹配排除该组件的查询，假设它满足所有其他查询条件。</p><h4 id="标记组件替代"><a href="#标记组件替代" class="headerlink" title="标记组件替代"></a>标记组件替代</h4><p>您还可以使用可启用组件而不是一组零大小标记组件来表示实体状态。这减少了唯一实体原型的数量，并鼓励更好地利用块以减少内存消耗。</p><h4 id="启用的组件语义"><a href="#启用的组件语义" class="headerlink" title="启用的组件语义"></a>启用的组件语义</h4><p>现有组件操作的语义不会改变。 EntityManager 认为具有禁用组件的实体仍然具有该组件。</p><p>具体来说，如果组件 T 在实体 E 上被禁用：</p><ul><li>HasComponent<T>(E) 返回真。</T></li><li>GetComponent<T>(E) 返回组件的当前值。</T></li><li>SetComponent<T>(E,value) 更新组件的值。</T></li><li>RemoveComponent<T>(E) 从 E 中移除组件。</T></li><li>AddComponent<T>(E) 静静地什么也不做，因为组件已经存在。</T></li></ul><h3 id="使用可启用的组件"><a href="#使用可启用的组件" class="headerlink" title="使用可启用的组件"></a>使用可启用的组件</h3><p>您只能启用 IComponentData 和 IBufferElementData 组件。为此，实现 IEnableableComponent 接口。</p><p>当您使用可启用的组件时，目标实体不会更改其原型，ECS 不会移动任何数据，并且组件的现有值保持不变。这意味着您可以在工作线程上运行的作业上启用和禁用组件，而无需使用实体命令缓冲区或创建同步点。</p><p>但是，为防止出现竞争情况，对可启用组件具有写入权限的作业可能会导致主线程操作阻塞，直到作业完成，即使该作业未在任何实体上启用或禁用该组件。</p><p>在使用 CreateEntity() 创建的新实体上默认启用所有可启用的组件。从预制件实例化的实体继承预制件的启用或禁用状态。</p><h4 id="启用组件方法"><a href="#启用组件方法" class="headerlink" title="启用组件方法"></a>启用组件方法</h4><p>要使用可启用的组件，您可以在 EntityManager、ComponentLookup<T>、EntityCommandBuffer 和 ArchetypeChunk 上使用以下方法：</T></p><ul><li>IsComponentEnabled<T>(Entity e)：如果实体 e 具有组件 T 并且它已启用，则返回 true。如果实体 e 有组件 T，但它被禁用，则返回 false。断言实体 e 是否没有组件 T，或者 T 是否未实现 IEnableableComponent。</T></li><li>SetComponentEnabled<T>(Entity e, bool enable)：如果实体 e 有组件 T，则根据 enable 的值启用或禁用它。断言实体 e 是否没有组件 T，或者 T 是否未实现 IEnableableComponent。<br>例如：<figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">&#x2F;&#x2F; ... in a SystemBase OnUpdate()Entity e &#x3D; this.EntityManager.CreateEntity(typeof(Health));ComponentLookup&lt;Health&gt; healthLookup &#x3D; this.GetComponentLookup&lt;&gt;();&#x2F;&#x2F; truebool b &#x3D; healthLookup.IsComponentEnabled(e);&#x2F;&#x2F; disable the Health component of the entityhealthLookup.SetComponentEnabled(e, false);&#x2F;&#x2F; though disabled, the component can still be read and modifiedHealth h &#x3D; healthLookup(e);</code></pre></div></figure>您可以使用 ComponentLookup<T>.SetComponentEnabled(Entity,bool) 从工作线程安全地启用或禁用实体，因为不需要进行结构更改。该作业必须具有对组件 T 的写访问权。您应该避免启用或禁用另一个线程可能在运行中处理的实体上的组件，因为这通常会导致竞争条件。</T></T></li></ul><h4 id="查询启用组件"><a href="#查询启用组件" class="headerlink" title="查询启用组件"></a>查询启用组件</h4><p>禁用组件 T 的实体匹配查询，就好像它根本没有组件 T 一样。例如，如果实体 E 具有组件 T1（启用）、T2（禁用）和 T3（禁用）：</p><ul><li>它与同时需要 T1 和 T2 的查询不匹配</li><li>它匹配需要 T1 并排除 T2 的查询</li><li>它不匹配将 T2 和 T3 作为可选组件的查询，因为它没有启用这些组件中的至少一个。</li></ul><p>所有 EntityQuery 方法都会自动处理可启用的组件。例如，query.CalculateEntityCount() 计算与查询匹配的实体数，同时考虑启用和禁用它们的哪些组件。有两个例外：</p><ul><li>以 IgnoreFilter 结尾的方法名称将所有组件视为已启用。这些方法不需要同步点，因为只有结构变化会影响它们的结果。它们往往比尊重过滤的变体更有效。</li><li>使用 EntityQueryOptions.IgnoreComponentEnabledState 创建的查询在确定它们是否与查询匹配时忽略匹配原型中所有实体的当前启用&#x2F;禁用状态。</li></ul><p>以下是查询已使用 EntityManager.IsComponentEnabled 禁用的组件的示例：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public struct Health : IComponentData, IEnableableComponent&#123;    public float Value;&#125;&#x2F;&#x2F; ... in a SystemBase OnUpdate()Entity e1 &#x3D; this.EntityManager.CreateEntity(typeof(Health), typeof(Translation));Entity e2 &#x3D; this.EntityManager.CreateEntity(typeof(Health), typeof(Translation));&#x2F;&#x2F; true (components begin life enabled)bool b &#x3D; this.EntityManager.IsComponentEnabled&lt;Health&gt;(e1);&#x2F;&#x2F; disable the Health component on the first entitythis.EntityManager.SetComponentEnabled&lt;Health&gt;(e1, false);EntityQuery query &#x3D; new EntityQueryBuilder(Allocator.Temp).WithAll&lt;Health, Translation&gt;().Build(this);&#x2F;&#x2F; the returned array does not include the first entityvar entities &#x3D; query.ToEntityArray(Allocator.Temp);&#x2F;&#x2F; the returned array does not include the Health of the first entityvar healths &#x3D; query.ToComponentDataArray&lt;Health&gt;(Allocator.Temp);&#x2F;&#x2F; the returned array does not include the Translation of the first entityvar translations &#x3D; query.ToComponentDataArray&lt;Translation&gt;(Allocator.Temp);&#x2F;&#x2F; This query matches components whether they&#39;re enabled or disabledvar queryIgnoredEnableable &#x3D; new EntityQueryBuilder(Allocator.Temp).WithAll&lt;Health, Translation&gt;().WithOptions(EntityQueryOptions.IgnoreComponentEnabledState).Build(this);&#x2F;&#x2F; the returned array includes the Translations of both entitiesvar translationsAll &#x3D; queryIgnoreEnableable.ToComponentDataArray&lt;Translation&gt;(Allocator.Temp);</code></pre></div></figure><h4 id="异步操作"><a href="#异步操作" class="headerlink" title="异步操作"></a>异步操作</h4><p>为了安全且确定地处理可启用组件，所有同步 EntityQuery 操作（忽略过滤的操作除外）会自动等待任何正在运行的作业完成，这些作业对查询的可启用组件具有写访问权限。所有异步 EntityQuery 操作（以 Async 结尾的操作）也会自动插入对这些正在运行的作业的输入依赖项。</p><p>异步 EntityQuery 收集和分散操作，例如 EntityQuery.ToEntityArrayAsync() 安排一个作业来执行请求的操作。这些方法必须返回 NativeList 而不是 NativeArray，因为查询匹配的实体数量在作业运行之前是未知的，但容器必须立即返回给调用者。</p><p>此列表的初始容量根据可以匹配查询的最大实体数进行了保守调整，但其最终长度可能会更短。在异步收集或分散作业完成之前，对列表的任何读取或写入（包括其当前长度、容量或基指针）都会导致 JobsDebugger 安全错误。但是，您可以安全地将列表传递给依赖的后续作业。</p><h1 id="向实体添加组件"><a href="#向实体添加组件" class="headerlink" title="向实体添加组件"></a>向实体添加组件</h1><p>要将组件添加到实体，请使用实体所在世界的 EntityManager。您可以将组件添加到单个实体，或同时添加到多个实体。</p><p>将组件添加到实体是一种结构更改，这意味着实体移动到不同的块。这意味着您不能直接从作业中将组件添加到实体。相反，您必须使用 EntityCommandBuffer 来记录您稍后添加组件的意图。</p><h2 id="将组件添加到单个实体"><a href="#将组件添加到单个实体" class="headerlink" title="将组件添加到单个实体"></a>将组件添加到单个实体</h2><p>以下代码示例创建一个新实体，然后从主线程向该实体添加一个组件。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public partial struct AddComponentToSingleEntitySystemExample : ISystem&#123;    public void OnCreate(ref SystemState state)    &#123;        var entity &#x3D; state.EntityManager.CreateEntity();        state.EntityManager.AddComponent&lt;Rotation&gt;(entity);    &#125;    public void OnUpdate(ref SystemState state) &#123; &#125;    public void OnDestroy(ref SystemState state) &#123; &#125;&#125;</code></pre></div></figure><h2 id="将组件添加到多个实体"><a href="#将组件添加到多个实体" class="headerlink" title="将组件添加到多个实体"></a>将组件添加到多个实体</h2><p>以下代码示例获取带有附加 ComponentA 组件的每个实体，并从主线程向它们添加 ComponentB 组件。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">struct ComponentA : IComponentData &#123;&#125;struct ComponentB : IComponentData &#123;&#125;public partial struct AddComponentToMultipleEntitiesSystemExample : ISystem&#123;    public void OnCreate(ref SystemState state)    &#123;        var query &#x3D; state.GetEntityQuery(typeof(ComponentA));        state.EntityManager.AddComponent&lt;ComponentB&gt;(query);    &#125;    public void OnUpdate(ref SystemState state) &#123; &#125;    public void OnDestroy(ref SystemState state) &#123; &#125;&#125;</code></pre></div></figure><h1 id="从实体中移除组件"><a href="#从实体中移除组件" class="headerlink" title="从实体中移除组件"></a>从实体中移除组件</h1><p>要从实体中移除组件，请使用实体所在世界的 EntityManager。</p><div class="note note-warning">            <p>将组件添加到实体是一种结构更改，这意味着实体移动到不同的原型块。</p>          </div><h2 id="从主线程"><a href="#从主线程" class="headerlink" title="从主线程"></a>从主线程</h2><p>您可以直接从主线程中的实体中删除组件。以下代码示例获取每个带有附加 Rotation 组件的实体，然后删除 Rotation 组件。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public partial struct RemoveComponentSystemExample : ISystem&#123;    public void OnCreate(ref SystemState state)    &#123;        var query &#x3D; state.GetEntityQuery(typeof(Rotation));        state.EntityManager.RemoveComponent&lt;Rotation&gt;(query);    &#125;    public void OnUpdate(ref SystemState state) &#123; &#125;    public void OnDestroy(ref SystemState state) &#123; &#125;&#125;</code></pre></div></figure><h2 id="从一份工作"><a href="#从一份工作" class="headerlink" title="从一份工作"></a>从一份工作</h2><p>因为从实体中删除组件是一种结构更改，所以您不能直接在作业中执行此操作。相反，您必须使用 EntityCommandBuffer 来记录您稍后删除组件的意图。</p><h1 id="读取和写入组件值"><a href="#读取和写入组件值" class="headerlink" title="读取和写入组件值"></a>读取和写入组件值</h1><p>将组件添加到实体后，您的系统可以访问、读取和写入组件值。根据您的用例，您可以使用多种方法来实现此目的。</p><h2 id="访问单个组件"><a href="#访问单个组件" class="headerlink" title="访问单个组件"></a>访问单个组件</h2><p>有时您可能希望一次读取或写入一个实体的单个组件。为此，在主线程上，您可以让 EntityManager 读取或写入单个实体的组件值。 EntityManager 保留一个查找表以快速找到每个实体的块和块内的索引。</p><h2 id="访问多个组件"><a href="#访问多个组件" class="headerlink" title="访问多个组件"></a>访问多个组件</h2><p>对于大多数工作，您需要读取或写入一个块或一组块中所有实体的组件：</p><ul><li>ArchetypeChunk 直接读取和写入块的组件数组。</li><li>EntityQuery 有效地检索与查询匹配的组块集。</li><li>IJobEntity 使用作业在查询中遍历组件。</li></ul><h2 id="延迟组件值更改"><a href="#延迟组件值更改" class="headerlink" title="延迟组件值更改"></a>延迟组件值更改</h2><p>要推迟组件值的更改以备后用，请使用 EntityCommandBuffer 来记录您写入（而非读取）组件值的意图。这些更改只会在您稍后在主线程上播放 EntityCommandBuffer 时发生。</p>]]></content>
    
    
    
    <tags>
      
      <tag>Entities</tag>
      
      <tag>ECS</tag>
      
      <tag>DOTS</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>ECS 系统</title>
    <link href="/2022/11/16/ECS-systems-intro/"/>
    <url>/2022/11/16/ECS-systems-intro/</url>
    
    <content type="html"><![CDATA[<ul><li><a href="#%E7%B3%BB%E7%BB%9F">系统</a></li><li><a href="#%E4%BD%BF%E7%94%A8-SystemBase-%E5%88%9B%E5%BB%BA%E7%B3%BB%E7%BB%9F">使用 <code>SystemBase</code> 创建系统</a><ul><li><a href="#%E5%9B%9E%E8%B0%83%E6%96%B9%E6%B3%95%E9%A1%BA%E5%BA%8F">回调方法顺序</a></li></ul></li><li><a href="#%E8%BF%AD%E4%BB%A3%E6%95%B0%E6%8D%AE">迭代数据</a><ul><li><a href="#%E9%A2%9D%E5%A4%96%E8%B5%84%E6%BA%90">额外资源</a></li><li><a href="#%E4%BD%BF%E7%94%A8-Entities-ForEach-%E8%BF%AD%E4%BB%A3%E6%95%B0%E6%8D%AE">使用 <code>Entities.ForEach</code> 迭代数据</a></li><li><a href="#%E4%BD%BF%E7%94%A8-IJobEntity-%E8%BF%AD%E4%BB%A3%E6%95%B0%E6%8D%AE">使用 <code>IJobEntity</code> 迭代数据</a></li><li><a href="#%E4%BD%BF%E7%94%A8-IJobEntityBatch-%E9%81%8D%E5%8E%86%E6%88%90%E6%89%B9%E6%95%B0%E6%8D%AE">使用 <code>IJobEntityBatch</code> 遍历成批数据</a></li><li><a href="#%E6%89%8B%E5%8A%A8%E8%BF%AD%E4%BB%A3%E6%95%B0%E6%8D%AE">手动迭代数据</a></li></ul></li><li><a href="#%E7%B3%BB%E7%BB%9F%E6%9B%B4%E6%96%B0%E9%A1%BA%E5%BA%8F">系统更新顺序</a><ul><li><a href="#%E7%BB%84%E4%BB%B6%E7%B3%BB%E7%BB%9F%E7%BB%84">组件系统组</a></li><li><a href="#%E7%B3%BB%E7%BB%9F%E6%8E%92%E5%BA%8F%E5%B1%9E%E6%80%A7">系统排序属性</a></li><li><a href="#%E9%BB%98%E8%AE%A4%E7%B3%BB%E7%BB%9F%E7%BB%84">默认系统组</a></li><li><a href="#%E5%A4%9A%E4%B8%AA%E4%B8%96%E7%95%8C">多个世界</a></li></ul></li><li><a href="#%E4%BD%BF%E7%94%A8%E4%BD%9C%E4%B8%9A%E5%9C%A8%E5%A4%9A%E4%B8%AA%E7%BA%BF%E7%A8%8B%E4%B8%8A%E8%B0%83%E5%BA%A6%E6%95%B0%E6%8D%AE">使用作业在多个线程上调度数据</a><ul><li><a href="#%E5%B7%A5%E4%BD%9C%E6%89%A9%E5%B1%95">工作扩展</a></li><li><a href="#%E9%80%9A%E7%94%A8%E8%81%8C%E4%BD%8D">通用职位</a></li><li><a href="#%E4%BD%BF%E7%94%A8-Job-WithCode-%E5%AE%89%E6%8E%92%E5%90%8E%E5%8F%B0%E4%BD%9C%E4%B8%9A">使用 Job.WithCode 安排后台作业</a></li><li><a href="#%E4%BD%9C%E4%B8%9A%E4%BE%9D%E8%B5%96">作业依赖</a></li></ul></li><li><a href="#%E4%BD%BF%E7%94%A8-EntityQuery-%E6%9F%A5%E8%AF%A2%E6%95%B0%E6%8D%AE">使用 EntityQuery 查询数据</a><ul><li><a href="#%E5%88%9B%E5%BB%BA%E5%AE%9E%E4%BD%93%E6%9F%A5%E8%AF%A2">创建实体查询</a></li><li><a href="#%E5%AE%9A%E4%B9%89%E8%BF%87%E6%BB%A4%E5%99%A8">定义过滤器</a></li><li><a href="#%E6%8C%89%E5%90%AF%E7%94%A8%E7%BB%84%E4%BB%B6%E8%BF%87%E6%BB%A4">按启用组件过滤</a></li><li><a href="#%E5%90%88%E5%B9%B6%E6%9F%A5%E8%AF%A2">合并查询</a></li><li><a href="#%E6%89%A7%E8%A1%8C%E6%9F%A5%E8%AF%A2">执行查询</a></li><li><a href="#%E7%BC%96%E8%BE%91%E5%99%A8%E4%B8%AD%E7%9A%84%E6%9F%A5%E8%AF%A2">编辑器中的查询</a></li></ul></li><li><a href="#%E4%BD%BF%E7%94%A8-EntityCommandBuffer-%E5%AE%89%E6%8E%92%E6%95%B0%E6%8D%AE%E6%9B%B4%E6%94%B9">使用 EntityCommandBuffer 安排数据更改</a><ul><li><a href="#EntityCommandBuffer-%E6%96%B9%E6%B3%95">EntityCommandBuffer 方法</a></li><li><a href="#%E5%9C%A8%E5%8D%95%E7%BA%BF%E7%A8%8B%E4%BD%9C%E4%B8%9A%E4%B8%AD%E4%BD%BF%E7%94%A8-EntityCommandBuffer">在单线程作业中使用 EntityCommandBuffer</a></li><li><a href="#%E5%9C%A8%E5%B9%B6%E8%A1%8C%E4%BD%9C%E4%B8%9A%E4%B8%AD%E4%BD%BF%E7%94%A8-EntityCommandBuffer">在并行作业中使用 EntityCommandBuffer</a></li><li><a href="#%E9%87%8D%E7%94%A8-EntityCommandBuffer-%E5%AE%9E%E4%BE%8B">重用 EntityCommandBuffer 实例</a></li><li><a href="#%E5%A4%9A%E9%87%8D%E5%9B%9E%E6%94%BE">多重回放</a></li><li><a href="#%E5%9C%A8%E4%B8%BB%E7%BA%BF%E7%A8%8B%E4%B8%8A%E4%BD%BF%E7%94%A8-EntityCommandBuffer">在主线程上使用 EntityCommandBuffer</a></li><li><a href="#%E4%BD%BF%E7%94%A8-EntityCommandBufferSystem-%E8%87%AA%E5%8A%A8%E6%92%AD%E6%94%BE%E5%92%8C%E5%A4%84%E7%90%86%E5%91%BD%E4%BB%A4%E7%BC%93%E5%86%B2%E5%8C%BA">使用 EntityCommandBufferSystem 自动播放和处理命令缓冲区</a></li><li><a href="#%E5%BB%B6%E8%BF%9F%E5%AE%9E%E4%BD%93">延迟实体</a></li><li><a href="#%E5%9C%A8-Entities-ForEach-%E6%96%B9%E6%B3%95%E4%B8%AD%E4%BD%BF%E7%94%A8%E5%91%BD%E4%BB%A4%E7%BC%93%E5%86%B2%E5%8C%BA">在 <code>Entities.ForEach</code> 方法中使用命令缓冲区</a></li></ul></li><li><a href="#%E6%9F%A5%E6%89%BE%E4%BB%BB%E6%84%8F%E6%95%B0%E6%8D%AE">查找任意数据</a><ul><li><a href="#%E5%9C%A8%E7%B3%BB%E7%BB%9F%E4%B8%AD%E6%9F%A5%E6%89%BE%E5%AE%9E%E4%BD%93%E6%95%B0%E6%8D%AE">在系统中查找实体数据</a></li><li><a href="#%E5%9C%A8%E4%BD%9C%E4%B8%9A%E4%B8%AD%E6%9F%A5%E6%89%BE%E5%AE%9E%E4%BD%93%E6%95%B0%E6%8D%AE">在作业中查找实体数据</a></li><li><a href="#%E6%95%B0%E6%8D%AE%E8%AE%BF%E9%97%AE%E9%94%99%E8%AF%AF">数据访问错误</a></li></ul></li><li><a href="#%E5%86%99%E5%85%A5%E7%BB%84">写入组</a><ul><li><a href="#%E4%BD%BF%E7%94%A8%E5%86%99%E7%BB%84">使用写组</a></li><li><a href="#%E7%BC%96%E5%86%99%E7%BB%84%E7%A4%BA%E4%BE%8B">编写组示例</a></li><li><a href="#%E5%88%9B%E5%BB%BA%E5%86%99%E5%85%A5%E7%BB%84">创建写入组</a></li><li><a href="#%E5%90%AF%E7%94%A8%E5%86%99%E7%BB%84%E8%BF%87%E6%BB%A4">启用写组过滤</a></li><li><a href="#%E8%A6%86%E7%9B%96%E5%8F%A6%E4%B8%80%E4%B8%AA%E4%BD%BF%E7%94%A8%E5%86%99%E5%85%A5%E7%BB%84%E7%9A%84%E7%B3%BB%E7%BB%9F">覆盖另一个使用写入组的系统</a></li><li><a href="#%E6%89%A9%E5%B1%95%E5%8F%A6%E4%B8%80%E4%B8%AA%E4%BD%BF%E7%94%A8%E5%86%99%E7%BB%84%E7%9A%84%E7%B3%BB%E7%BB%9F">扩展另一个使用写组的系统</a></li></ul></li><li><a href="#%E7%89%88%E6%9C%AC%E5%8F%B7">版本号</a><ul><li><a href="#%E7%89%88%E6%9C%AC%E5%8F%B7%E7%BB%93%E6%9E%84">版本号结构</a></li><li><a href="#%E5%AE%9E%E4%BD%93%E7%89%88%E6%9C%AC%E5%8F%B7">实体版本号</a></li><li><a href="#%E4%B8%96%E7%95%8C%E7%89%88%E6%9C%AC%E5%8F%B7">世界版本号</a></li><li><a href="#%E4%BD%9C%E4%B8%9A%E7%BB%84%E4%BB%B6%E7%B3%BB%E7%BB%9F%E7%89%88%E6%9C%AC%E5%8F%B7">作业组件系统版本号</a></li><li><a href="#%E9%9D%9E%E5%85%B1%E4%BA%AB%E7%BB%84%E4%BB%B6%E7%89%88%E6%9C%AC%E5%8F%B7">非共享组件版本号</a></li><li><a href="#%E5%85%B1%E4%BA%AB%E7%BB%84%E4%BB%B6%E7%89%88%E6%9C%AC%E5%8F%B7">共享组件版本号</a></li></ul></li></ul><h1 id="系统"><a href="#系统" class="headerlink" title="系统"></a>系统</h1><p>系统是每帧在主线程上运行一次的代码单元。系统被组织成系统组的层次结构，您可以使用这些系统组来组织系统更新的顺序。有关 ECS 中系统基础知识的更多信息，请参阅系统概念。</p><span id="more"></span><table><thead><tr><th>标题</th><th>描述</th></tr></thead><tbody><tr><td>使用<code>SystemBase</code>创建系统</td><td>有关如何使用<code>SystemBase</code>创建系统的信息。</td></tr><tr><td>迭代数据</td><td>描述了可以迭代系统中数据的各种方法。</td></tr><tr><td>系统更新顺序</td><td>有关系统更新顺序以及如何使用系统组控制更新顺序的信息。</td></tr><tr><td>使用作业在多个线程上调度数据</td><td>有关如何在系统中使用作业的信息。</td></tr><tr><td>使用<code>EntityQuery</code>查询实体数据</td><td>有关使用<code>EntityQuery</code>查询实体数据的信息。</td></tr><tr><td>使用<code>EntityCommandBuffer</code>调度数据更改</td><td>使用命令缓冲区来延迟对数据的更改。</td></tr><tr><td>查找任意数据</td><td>有关如何查找任意实体数据的信息。</td></tr><tr><td>写入组</td><td>使用写入组覆盖系统的数据。</td></tr><tr><td>版本号</td><td>使用版本号来检测潜在的变化。</td></tr></tbody></table><h1 id="使用-SystemBase-创建系统"><a href="#使用-SystemBase-创建系统" class="headerlink" title="使用 SystemBase 创建系统"></a>使用 <code>SystemBase</code> 创建系统</h1><p>要创建托管系统，请实现抽象类 <code>SystemBase</code>。</p><p>您必须使用 <code>OnUpdate</code> 系统事件回调，来添加您的系统必须在每一帧执行的工作。<code>ComponentSystemBase</code> 命名空间中的所有其他回调方法都是可选的。</p><p>所有系统事件都在主线程上运行。最佳做法是使用 <code>OnUpdate</code> 方法来安排作业来执行大部分工作。要从系统安排作业，您可以使用以下机制之一：</p><ul><li><code>Entities.ForEach</code>：遍历组件数据。</li><li><code>Job.WithCode</code>：将 lambda 表达式作为单个后台作业执行。</li><li><code>IJobEntity</code>：迭代多个系统中的组件数据。</li><li><code>IJobEntityBatch</code>：按原型块迭代数据。</li></ul><p>以下示例说明了使用 <code>Entities.ForEach</code> 来实现一个系统，该系统根据一个组件的值更新另一个组件：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public struct Position : IComponentData&#123;    public float3 Value;&#125;public struct Velocity : IComponentData&#123;    public float3 Value;&#125;[RequireMatchingQueriesForUpdate]public partial class ECSSystem : SystemBase&#123;    protected override void OnUpdate()    &#123;        &#x2F;&#x2F; &#96;ForEach&#96; 中捕获的局部变量        float dT &#x3D; SystemAPI.Time.DeltaTime;        Entities            .WithName(&quot;Update_Displacement&quot;)            .ForEach(                (ref Position position, in Velocity velocity) &#x3D;&gt;                &#123;                    position &#x3D; new Position()                    &#123;                        Value &#x3D; position.Value + velocity.Value * dT                    &#125;;                &#125;            )            .ScheduleParallel();    &#125;&#125;</code></pre></div></figure><h2 id="回调方法顺序"><a href="#回调方法顺序" class="headerlink" title="回调方法顺序"></a>回调方法顺序</h2><p><code>SystemBase</code> 中有几个回调方法，Unity 在系统创建过程中的不同点调用，您可以使用它们来安排系统必须在每一帧执行的工作：</p><ul><li><code>OnCreate</code>：创建系统时调用。</li><li><code>OnStartRunning</code>：在第一次调用 <code>OnUpdate</code> 之前以及系统恢复运行时调用。</li><li><code>OnUpdate</code>：只要系统有工作要做，就会在每一帧调用。有关确定系统何时有工作要做的因素的更多信息，请参阅 <code>ShouldRunSystem</code>。</li><li><code>OnStopRunning</code>：在 <code>OnDestroy</code> 之前调用。每当系统停止运行时也会调用，如果没有实体与系统的 <code>EntityQuery</code> 匹配，或者如果系统的 <code>Enabled</code> 属性设置为 <code>false</code>，就会发生这种情况。</li><li><code>OnDestroy</code>：系统被销毁时调用。</li></ul><p>下图说明了系统的事件顺序：<br><img src="https://docs.unity3d.com/Packages/com.unity.entities@1.0/manual/images/SystemEventOrder.png" alt="回调方法顺序"><br>父系统组的 <code>OnUpdate</code> 方法触发其组中所有系统的 <code>OnUpdate</code> 方法。有关系统如何更新的更多信息，请参阅系统的更新顺序。</p><h1 id="迭代数据"><a href="#迭代数据" class="headerlink" title="迭代数据"></a>迭代数据</h1><p>迭代数据是创建系统时需要执行的最常见任务之一。系统通常处理一组实体，从一个或多个组件读取数据，执行计算，然后将结果写入另一个组件。</p><p>迭代实体和组件的最有效方法是在按顺序处理组件的作业中。这利用了所有可用内核和数据局部性的处理能力来避免 CPU 缓存未命中。</p><p>本节介绍如何通过以下方式迭代实体数据：</p><table><thead><tr><th>标题</th><th>描述</th></tr></thead><tbody><tr><td>使用 <code>Entities.ForEach</code> 迭代数据</td><td>如何使用 <code>SystemBase.Entities.ForEach</code> 逐个实体地处理组件数据。</td></tr><tr><td>使用 <code>IJobEntity</code> 迭代数据</td><td>如何使用 <code>IJobEntity</code> 编写一次并创建多个计划。</td></tr><tr><td>遍历成批数据</td><td>如何使用 <code>IJobEntityBatch</code> 遍历包含匹配实体的原型块。</td></tr><tr><td>手动迭代数据</td><td>如何手动迭代实体或原型块。</td></tr></tbody></table><h2 id="额外资源"><a href="#额外资源" class="headerlink" title="额外资源"></a>额外资源</h2><p>您还可以使用 <code>EntityQuery</code> 类来构建数据视图，其中仅包含给定算法或过程所需的特定数据。上面列表中的许多迭代方法显式或内部使用 <code>EntityQuery</code>。有关详细信息，请参阅使用<code>EntityQuery</code>查询实体数据。</p><h2 id="使用-Entities-ForEach-迭代数据"><a href="#使用-Entities-ForEach-迭代数据" class="headerlink" title="使用 Entities.ForEach 迭代数据"></a>使用 <code>Entities.ForEach</code> 迭代数据</h2><p>如果您使用 <code>SystemBase</code> 类来创建您的系统，则可以使用 <code>Entities.ForEach</code> 构造来定义和执行针对实体及其组件的算法。在编译时，Unity 将每个 <code>ForEach()</code> 调用转换为生成的作业。</p><p>您向 <code>Entities.ForEach</code> 传递一个 lambda 表达式，Unity 会根据 lambda 参数类型生成一个实体查询。当生成的作业运行时，Unity 会为每个与查询匹配的实体调用一次 lambda。 <code>ForEachLambdaJobDescription</code> 表示此生成的作业。</p><h3 id="定义一个-lambda-表达式"><a href="#定义一个-lambda-表达式" class="headerlink" title="定义一个 lambda 表达式"></a>定义一个 lambda 表达式</h3><p>当您定义 <code>Entities.ForEach</code> lambda 表达式时，您可以声明 <code>SystemBase</code> 类在执行该方法时用于传递有关当前实体的信息的参数。</p><p>典型的 lambda 表达式如下所示：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">Entities.ForEach(    (        Entity entity,        int entityInQueryIndex,        &#96;ref&#96; ObjectPosition translation,        in Movement move    ) &#x3D;&gt; &#123; &#x2F;* .. *&#x2F;&#125;)</code></pre></div></figure><p>您最多可以将八个参数传递给 <code>Entities.ForEach</code> lambda 表达式。如果需要传递更多参数，可以定义自定义委托。有关详细信息，请参阅本文档中有关自定义委托的部分。</p><p>使用标准委托时，必须按以下顺序对参数进行分组：</p><ol><li>按值传递的参数（无参数修饰符）</li><li>可写参数（<code>ref</code>参数修饰符）</li><li>只读参数（<code>in</code>参数修饰符）</li></ol><p>您必须在所有组件上使用 <code>ref</code> 或 <code>in</code> 参数修改关键字。如果你不这样做，Unity 传递给你的方法的组件结构是一个副本而不是引用。这意味着它会为只读参数占用额外的内存，并且当函数返回后复制的结构超出范围时，您对组件所做的任何更改都会被静默抛出。</p><p>如果 lambda 表达式不遵循此顺序，并且您还没有创建合适的委托，则编译器会提供类似于以下内容的错误：</p><div class="note note-danger">            <p>error CS1593: Delegate ‘Invalid_ForEach_Signature_See_ForEach_Documentation_For_Rules_And_Restrictions’ does not take N arguments</p>          </div><p>此错误消息将参数数量作为问题，即使问题是参数顺序也是如此。</p><h4 id="自定义代理"><a href="#自定义代理" class="headerlink" title="自定义代理"></a>自定义代理</h4><p>如果要在 <code>ForEach</code> lambda 表达式中使用八个以上的参数，则必须声明自己的委托类型和 <code>ForEach</code> 重载。这允许您使用无限数量的参数，并以您想要的任何顺序放置 <code>ref</code>、<code>in</code> 和 <code>value</code> 参数。</p><p>您还可以在参数列表中的任意位置声明三个命名参数 <code>entity</code>、<code>entityInQueryIndex</code> 和 <code>nativeThreadIndex</code>。不要对这些参数使用 <code>ref</code> 或 <code>in</code> 修饰符。</p><p>以下示例显示 12 个参数，并在 lambda 表达式中使用<code>entity</code>参数：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">static class BringYourOwnDelegate&#123;    &#x2F;&#x2F; 声明采用 12 个参数的委托。 T0 用于 Entity 参数    public delegate void CustomForEachDelegate&lt;T0, T1, T2, T3, T4, T5, T6, T7, T8, T9, T10, T11&gt;        (T0 t0, in T1 t1, in T2 t2, in T3 t3, in T4 t4, in T5 t5,         in T6 t6, in T7 t7, in T8 t8, in T9 t9, in T10 t10, in T11 t11);    &#x2F;&#x2F; 声明函数重载    public static TDescription ForEach&lt;TDescription, T0, T1, T2, T3, T4, T5, T6, T7, T8, T9, T10, T11&gt;        (this TDescription description, CustomForEachDelegate&lt;T0, T1, T2, T3, T4, T5, T6, T7, T8, T9, T10, T11&gt; codeToRun)        where TDescription : struct, Unity.Entities.CodeGeneratedJobForEach.ISupportForEachWithUniversalDelegate &#x3D;&gt;        LambdaForEachDescriptionConstructionMethods.ThrowCodeGenException&lt;TDescription&gt;();&#125;&#x2F;&#x2F; 使用自定义委托和重载的系统[RequireMatchingQueriesForUpdate]public partial class MayParamsSystem : SystemBase&#123;    protected override void OnUpdate()    &#123;        Entities.ForEach(                (Entity entity0,                    in Data1 d1,                    in Data2 d2,                    in Data3 d3,                    in Data4 d4,                    in Data5 d5,                    in Data6 d6,                    in Data7 d7,                    in Data8 d8,                    in Data9 d9,                    in Data10 d10,                    in Data11 d11                    ) &#x3D;&gt; &#123;&#x2F;* .. *&#x2F;&#125;)            .Run();    &#125;&#125;</code></pre></div></figure><h4 id="组件参数"><a href="#组件参数" class="headerlink" title="组件参数"></a>组件参数</h4><p>要访问与实体关联的组件，您必须将该组件类型的参数传递给 lambda 表达式。编译器会自动将传递给 lambda 表达式的所有组件作为必需组件添加到实体查询中。</p><p>要更新组件值，您必须使用参数列表中的 <code>ref</code> 关键字将其传递给 lambda 表达式。如果没有 <code>ref</code> 关键字，Unity 将对组件的临时副本进行任何修改。</p><p>要声明传递给 lambda 表达式的只读组件，请使用参数列表中的 <code>in</code> 关键字。</p><p>当您使用 <code>ref</code> 时，Unity 会将当前块中的组件标记为已更改，即使 lambda 表达式实际上并未修改它们。为了提高效率，您应该始终使用 <code>in</code> 关键字将您的 lambda 表达式未修改的组件声明为只读。</p><p>以下示例将 <code>Source</code> 组件参数作为只读传递给作业，并将 <code>Destination</code> 组件参数作为可写传递：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">Entities.ForEach(    (ref Destination outputData,        in Source inputData) &#x3D;&gt;    &#123;        outputData.Value &#x3D; inputData.Value;    &#125;)    .ScheduleParallel();</code></pre></div></figure><div class="note note-danger">            <p>您不能将块组件传递给 <code>Entities.ForEach</code> lambda 表达式。</p>          </div><p>对于动态缓冲区，使用 <code>DynamicBuffer&lt;T&gt;</code> 而不是缓冲区中存储的组件类型：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">[RequireMatchingQueriesForUpdate]public partial class BufferSum : SystemBase&#123;    private EntityQuery query;    &#x2F;&#x2F; 安排两个作业之间的依赖关系    protected override void OnUpdate()    &#123;        &#x2F;&#x2F; 这里可以访问query变量，        &#x2F;&#x2F; 因为我们在下面的entities.ForEach中使用了WithStoreEntityQueryInField(query)        int entitiesInQuery &#x3D; query.CalculateEntityCount();        &#x2F;&#x2F; 创建一个原生数组来保存中间和        &#x2F;&#x2F; (每个实体一个元素)        NativeArray&lt;int&gt; intermediateSums            &#x3D; new NativeArray&lt;int&gt;(entitiesInQuery, Allocator.TempJob);        &#x2F;&#x2F; 安排第一个作业以添加所有缓冲区元素        Entities            .ForEach((int entityInQueryIndex, in DynamicBuffer&lt;IntBufferData&gt; buffer) &#x3D;&gt;        &#123;            for (int i &#x3D; 0; i &lt; buffer.Length; i++)            &#123;                intermediateSums[entityInQueryIndex] +&#x3D; buffer[i].Value;            &#125;        &#125;)            .WithStoreEntityQueryInField(ref query)            .WithName(&quot;IntermediateSums&quot;)            .ScheduleParallel(); &#x2F;&#x2F; 为每个实体块并行执行        &#x2F;&#x2F; 安排第二个工作，依赖于第一个        Job.WithCode(() &#x3D;&gt;        &#123;            int result &#x3D; 0;            for (int i &#x3D; 0; i &lt; intermediateSums.Length; i++)            &#123;                result +&#x3D; intermediateSums[i];            &#125;            &#x2F;&#x2F;不兼容burst:            Debug.Log(&quot;Final sum is &quot; + result);        &#125;)            .WithDisposeOnCompletion(intermediateSums)            .WithoutBurst()            .WithName(&quot;FinalSum&quot;)            .Schedule(); &#x2F;&#x2F; 在单个后台线程上执行    &#125;&#125;</code></pre></div></figure><h4 id="命名参数"><a href="#命名参数" class="headerlink" title="命名参数"></a>命名参数</h4><p>您还可以将以下命名的参数传递给 <code>Entities.ForEach</code> lambda 表达式，Unity 根据作业正在处理的实体为其分配值。</p><table><thead><tr><th>参数</th><th>函数</th></tr></thead><tbody><tr><td>Entity entity</td><td>当前实体的实体实例。只要类型是 Entity，您就可以将参数命名为任何名称。</td></tr><tr><td>int entityInQueryIndex</td><td>实体在查询选择的所有实体列表中的索引。当您有一个需要为每个实体填充唯一值的native array时，请使用实体索引值。您可以使用 entityInQueryIndex 作为该数组中的索引。您应该使用 entityInQueryIndex 作为 sortKey 将命令添加到并发实体命令缓冲区。</td></tr><tr><td>int nativeThreadIndex</td><td>执行 lambda 表达式当前迭代的线程的唯一索引。当您使用 Run() 执行 lambda 表达式时，nativeThreadIndex 始终为零。不要使用 nativeThreadIndex 作为并发实体命令缓冲区的 sortKey；使用 entityInQueryIndex 代替。</td></tr><tr><td>EntityCommands commands</td><td>只要类型是 EntityCommands，您就可以将此参数命名为任何名称。仅将此参数与 WithDeferredPlaybackSystem<T>() 或 WithImmediatePlayback() 结合使用。 EntityCommands 类型包含几个方法，这些方法反映了 EntityCommandBuffer 类型中的对应方法。如果您在 Entities.ForEach() 中使用 EntityCommands 实例，编译器会在适当的地方创建额外的代码来处理实体命令缓冲区的创建、调度、播放和处置，在这些代码上调用 EntityCommands 方法的对应方法。</T></td></tr></tbody></table><h3 id="执行-Entities-ForEach-lambda-表达式"><a href="#执行-Entities-ForEach-lambda-表达式" class="headerlink" title="执行 Entities.ForEach lambda 表达式"></a>执行 <code>Entities.ForEach</code> lambda 表达式</h3><p>您可以通过以下方式执行作业 lambda 表达式：</p><ul><li>使用 Schedule() 和 ScheduleParallel() 来安排作业</li><li>使用 Run() 立即在主线程上执行作业。<br>以下示例说明了使用 <code>Entities.ForEach</code> 读取 <code>Velocity</code> 组件并写入 <code>ObjectPosition</code> 组件的 <code>SystemBase</code> 实现：<figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">[RequireMatchingQueriesForUpdate]partial class ApplyVelocitySystem : SystemBase&#123;    protected override void OnUpdate()    &#123;        Entities.ForEach((ref ObjectPosition translation,            in Velocity velocity) &#x3D;&gt;            &#123;                translation.Value +&#x3D; velocity.Value;            &#125;)            .Schedule();    &#125;&#125;</code></pre></div></figure></li></ul><h3 id="选择实体"><a href="#选择实体" class="headerlink" title="选择实体"></a>选择实体</h3><p><code>Entities.ForEach</code> 有自己的机制来定义用于选择要处理的实体的实体查询。该查询会自动包含您用作 lambda 表达式参数的任何组件。</p><p>您还可以使用 <code>WithAll</code>、<code>WithAny</code> 和 <code>WithNone</code> 子句进一步细化 <code>Entities.ForEach</code> 选择的实体。有关查询选项的完整列表，请参阅 <code>SystemBase.Entities</code>。</p><p>以下示例使用这些子句根据这些参数选择实体：</p><ul><li>该实体具有组件：<code>Destination</code>、<code>Source</code> 和 <code>LocalToWorld</code></li><li>实体至少具有以下组件之一：<code>ObjectRotation</code>、<code>ObjectPosition</code> 或 <code>ObjectUniformScale</code></li><li>该实体没有 <code>ObjectNonUniformScale</code> 组件。<figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">Entities.WithAll&lt;LocalToWorld&gt;()    .WithAny&lt;ObjectRotation, ObjectPosition, ObjectUniformScale&gt;()    .WithNone&lt;ObjectNonUniformScale&gt;()    .ForEach((ref Destination outputData, in Source inputData) &#x3D;&gt;    &#123;        &#x2F;* do some work *&#x2F;    &#125;)    .Schedule();</code></pre></div></figure>在此示例中，在 lambda 表达式中仅访问 <code>Destination</code> 和 <code>Source</code> 组件，因为它们是参数列表中的唯一组件。</li></ul><h3 id="访问-EntityQuery-对象"><a href="#访问-EntityQuery-对象" class="headerlink" title="访问 EntityQuery 对象"></a>访问 EntityQuery 对象</h3><p><code>Entities.ForEach</code> 使用 <code>OnCreate</code> 创建一个 <code>EntityQuery</code>，您可以随时使用它的副本，甚至在调用 <code>Entities.ForEach</code> 之前。</p><p>要访问此实体查询，请使用带有 <code>ref</code> 参数修饰符的 <code>WithStoreEntityQueryInField(ref query)</code>。此方法将对查询的引用分配给您提供的字段。但是，此 <code>EntityQuery</code> 没有 <code>Entities.ForEach</code> 调用设置的任何过滤器。</p><p>以下示例说明如何访问为 <code>Entities.ForEach</code> 构造隐式创建的 <code>EntityQuery</code> 对象。它使用 <code>EntityQuery</code> 对象来调用 <code>CalculateEntityCount()</code> 方法，并使用此计数创建一个具有足够空间的native array，来为查询选择的每个实体存储一个值：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">private EntityQuery query;protected override void OnUpdate()&#123;    int dataCount &#x3D; query.CalculateEntityCount();    NativeArray&lt;float&gt; dataSquared        &#x3D; new NativeArray&lt;float&gt;(dataCount, Allocator.Temp);    Entities        .WithStoreEntityQueryInField(ref query)        .ForEach((int entityInQueryIndex, in Data data) &#x3D;&gt;        &#123;            dataSquared[entityInQueryIndex] &#x3D; data.Value * data.Value;        &#125;)        .ScheduleParallel();    Job        .WithCode(() &#x3D;&gt;    &#123;        &#x2F;&#x2F;使用 dataSquared 数组...        var v &#x3D; dataSquared[dataSquared.Length - 1];    &#125;)        .WithDisposeOnCompletion(dataSquared)        .Schedule();&#125;</code></pre></div></figure><h3 id="访问可选组件"><a href="#访问可选组件" class="headerlink" title="访问可选组件"></a>访问可选组件</h3><p><code>Entities.ForEach</code> lambda 表达式不支持使用 <code>WithAny&lt;T,U&gt;</code> 查询和访问可选组件。</p><p>如果要读取或写入可选组件，请将 <code>Entities.ForEach</code> 构造拆分为可选组件的每个组合的多个作业。例如，如果您有两个可选组件，则需要三个 <code>ForEach</code> 结构：一个包含第一个可选组件，一个包含第二个可选组件，一个包含两个组件。另一种选择是按块使用 <code>IJobChunkiterate</code>。有关详细信息，请参阅按批次迭代数据。</p><h3 id="更改过滤"><a href="#更改过滤" class="headerlink" title="更改过滤"></a>更改过滤</h3><p>您可以使用 <code>WithChangeFilter&lt;T&gt;</code> 来启用<strong>更改过滤</strong>，只有在当前 <code>SystemBase</code> 实例上次运行后实体中的另一个组件发生更改时，它才会处理组件。更改过滤器中的组件类型必须在 lambda 表达式参数列表中，或者是 <code>WithAll&lt;T&gt;</code> 语句的一部分。例如：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">Entities    .WithChangeFilter&lt;Source&gt;()    .ForEach((ref Destination outputData,        in Source inputData) &#x3D;&gt;        &#123;            &#x2F;* Do work *&#x2F;        &#125;)    .ScheduleParallel();</code></pre></div></figure><p>实体查询最多支持对两种组件类型进行更改过滤。</p><p>Unity 在原型块级别应用更改过滤。如果任何代码访问具有写访问权限的块中的组件，那么 Unity 会将该原型块中的组件类型标记为已更改，即使代码没有更改任何数据。</p><h3 id="共享组件过滤"><a href="#共享组件过滤" class="headerlink" title="共享组件过滤"></a>共享组件过滤</h3><p>Unity 将具有共享组件的实体与其他具有相同共享组件值的实体分组。要选择具有特定共享组件值的实体组，请使用 <code>WithSharedComponentFilter</code> 方法。</p><p>以下示例选择了全部按 <code>Cohort:ISharedComponentData</code> 分组的实体。此示例中的 lambda 表达式根据实体的Cohort设置 <code>DisplayColor:IComponentData</code> 组件：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">[RequireMatchingQueriesForUpdate]public partial class ColorCycleJob : SystemBase&#123;    protected unsafe override void OnUpdate()    &#123;        EntityManager.GetAllUniqueSharedComponents&lt;Cohort&gt;(out var cohorts, Allocator.Temp);        for (int i&#x3D;0; i&lt;cohorts.Length; i++)        &#123;            var cohort &#x3D; cohorts[i];            DisplayColor newColor &#x3D; ColorTable.GetNextColor(cohort.Value);            Entities.WithSharedComponentFilter(cohort)                .ForEach((ref DisplayColor color) &#x3D;&gt; &#123; color &#x3D; newColor; &#125;)                .ScheduleParallel();        &#125;    &#125;&#125;</code></pre></div></figure><p>该示例使用 <code>EntityManager</code> 获取所有唯一同类Cohort值的队列数组。然后它为每个队列安排一个 lambda 作业，并将新颜色作为捕获变量传递给 lambda 表达式。</p><h3 id="捕获变量"><a href="#捕获变量" class="headerlink" title="捕获变量"></a>捕获变量</h3><p>您可以捕获 <code>Entities.ForEach</code> lambda 表达式的局部变量。当您调用其中一种 Schedule 方法而不是 Run 来使用作业来执行 lambda 表达式时，对捕获的变量以及如何使用它们有一些限制：</p><ul><li>您只能捕获 NativeContainer 和 blittable 类型。</li><li>作业只能写入作为NativeContainer的捕获变量。要返回单个值，请创建一个包含一个元素的native array。</li></ul><p>如果您要读取NativeContainer，但不写入它，请始终使用 <code>WithReadOnly(variable)</code> 指定只读访问权限。有关为捕获的变量设置属性的详细信息，请参阅 <code>SystemBase.Entities</code>。 <code>Entities.ForEach</code> 将这些作为方法提供，因为 C# 语言不允许局部变量的属性。</p><p>要在 <code>Entities.ForEach</code> 运行后处理捕获的NativeContainer或包含NativeContainer的类型，请使用 <code>WithDisposeOnCompletion(variable)</code>。如果您在 <code>Run()</code> 中调用它，它会在 lambda 表达式运行后立即处理这些类型。如果您在 <code>Schedule()</code> 和 <code>ScheduleParallel()</code> 中调用它，它会安排它们稍后与作业一起处理，并返回 JobHandle。</p><div class="note note-info">            <p>当您使用 <code>Run()</code> 执行该方法时，您可以写入不是NativeContainer的捕获变量。但是，您仍应尽可能使用 blittable 类型，以便可以使用 Burst 编译该方法。</p>          </div><h3 id="支持的功能"><a href="#支持的功能" class="headerlink" title="支持的功能"></a>支持的功能</h3><p>除了使用 <code>Run()</code> 在主线程上执行 lambda 表达式。您还可以使用 <code>Schedule()</code> 将其作为单个作业执行，或使用 <code>ScheduleParallel()</code> 将其作为并行作业执行。这些不同的执行方法对您访问数据的方式有不同的限制。此外，Burst 编译器使用 C# 语言的一个受限子集，因此如果要在该子集之外使用 C# 功能，则需要指定 <code>WithoutBurst()</code>。这包括访问托管类型。</p><p>下表显示了 <code>Entities.ForEach</code> 中支持哪些用于 <code>SystemBase</code> 中的不同的调度方法：</p><table><thead><tr><th>支持的功能</th><th>Run</th><th>Schedule</th><th>ScheduleParallel</th></tr></thead><tbody><tr><td>捕获本地值类型</td><td>✓</td><td>✓</td><td>✓</td></tr><tr><td>捕获本地引用类型</td><td>Only WithoutBurst and not in ISystem</td><td></td><td></td></tr><tr><td>写入捕获的变量</td><td>✓</td><td></td><td></td></tr><tr><td>在系统类上使用字段</td><td>Only WithoutBurst</td><td></td><td></td></tr><tr><td>引用类型的方法</td><td>Only WithoutBurst and not in ISystem</td><td></td><td></td></tr><tr><td>共享组件</td><td>Only WithoutBurst and not in ISystem</td><td></td><td></td></tr><tr><td>托管组件</td><td>Only WithoutBurst and not in ISystem</td><td></td><td></td></tr><tr><td>结构变化</td><td>Only WithStructuralChanges and not in ISystem</td><td></td><td></td></tr><tr><td>SystemBase.GetComponent</td><td>✓</td><td>✓</td><td>✓</td></tr><tr><td>SystemBase.SetComponent</td><td>✓</td><td>✓</td><td></td></tr><tr><td>GetComponentDataFromEntity</td><td>✓</td><td>✓</td><td>Only as ReadOnly</td></tr><tr><td>HasComponent</td><td>✓</td><td>✓</td><td>✓</td></tr><tr><td>WithDisposeOnCompletion</td><td>✓</td><td>✓</td><td>✓</td></tr><tr><td>WithScheduleGranularity</td><td></td><td></td><td>✓</td></tr><tr><td>WithDeferredPlaybackSystem</td><td>✓</td><td>✓</td><td>✓</td></tr><tr><td>WithImmediatePlayback</td><td>✓</td><td></td><td></td></tr><tr><td>HasBuffer</td><td>✓</td><td>✓</td><td>✓</td></tr><tr><td>SystemBase.GetStorageInfoFromEntity</td><td>✓</td><td>✓</td><td>✓</td></tr><tr><td>SystemBase.Exists</td><td>✓</td><td>✓</td><td>✓</td></tr></tbody></table><div class="note note-waring">            <p><code>WithStructuralChanges()</code> 会禁用突发。如果您想获得高性能 <code>Entities.ForEach</code>，请不要使用此选项。如果要使用此选项，请使用 <code>EntityCommandBuffer</code>。</p>          </div><p><code>Entities.ForEach</code> 构造使用 Roslyn 源代码生成器将您为构造编写的代码转换为正确的 ECS 代码。这种翻译意味着您可以表达算法的意图，而无需包含复杂的样板代码。但是，这意味着一些常见的代码编写方式是不允许的。</p><p>不支持以下功能：</p><ul><li><code>.With</code> 调用中的动态代码</li><li>使用ref修饰 <code>SharedComponent</code> 参数</li><li>嵌套 <code>Entities.ForEach</code> lambda 表达式</li><li>调用存储在变量、字段或方法中的委托</li><li>具有 lambda 参数类型的 <code>SetComponent</code></li><li>具有可写 lambda 参数的 <code>GetComponent</code></li><li>lambdas 中的泛型参数</li><li>在具有泛型参数的系统中</li></ul><h3 id="依赖关系"><a href="#依赖关系" class="headerlink" title="依赖关系"></a>依赖关系</h3><p>默认情况下，系统使用其 <code>Dependency</code> 属性来管理其与 ECS 相关的依赖项。默认情况下，系统按照它们在 <code>OnUpdate()</code> 函数中出现的顺序将使用 <code>Entities.ForEach</code> 和 <code>Job.WithCode</code> 创建的每个作业添加到依赖作业句柄。您还可以将 <code>JobHandle</code> 传递给您的 <code>Schedule</code> 方法以手动管理作业依赖性，然后返回生成的依赖性。有关详细信息，请参阅依赖项文档。</p><p>有关作业依赖性的更多一般信息，请参阅作业依赖性。</p><h2 id="使用-IJobEntity-迭代数据"><a href="#使用-IJobEntity-迭代数据" class="headerlink" title="使用 IJobEntity 迭代数据"></a>使用 <code>IJobEntity</code> 迭代数据</h2><p>当您在多个系统中使用不同的调用进行数据转换时，要遍历 <code>ComponentData</code>，您可以使用 <code>IJobEntity</code>，它类似于 <code>Entities.ForEach</code>。</p><p>它创建一个 <code>IJobEntityBatch</code> 作业，因此您只需考虑要转换的数据。</p><h3 id="IJobEntity-和-Entities-ForEach-的比较"><a href="#IJobEntity-和-Entities-ForEach-的比较" class="headerlink" title="IJobEntity 和 Entities.ForEach 的比较"></a>IJobEntity 和 <code>Entities.ForEach</code> 的比较</h3><p><code>IJobEntity</code> 相对于 <code>Entities.ForEach</code> 的优势在于您可以编写一次代码并在多个系统中重复使用它，而不是仅一次。</p><p>这是一个 <code>Entities.ForEach</code> 示例：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">[RequireMatchingQueriesForUpdate]public partial class BoidForEachSystem : SystemBase&#123;    EntityQuery m_BoidQuery;    EntityQuery m_ObstacleQuery;    EntityQuery m_TargetQuery;    protected override void OnUpdate()    &#123;        &#x2F;&#x2F; 计算各个查询中的实体数量。        var boidCount &#x3D; m_BoidQuery.CalculateEntityCount();        var obstacleCount &#x3D; m_ObstacleQuery.CalculateEntityCount();        var targetCount &#x3D; m_TargetQuery.CalculateEntityCount();        &#x2F;&#x2F; 分配数组，以存储与相应的查询匹配的实体数量相等的数据。        var cellSeparation &#x3D; CollectionHelper.CreateNativeArray&lt;float3, RewindableAllocator&gt;(boidCount, ref World.UpdateAllocator);        var copyTargetPositions &#x3D; CollectionHelper.CreateNativeArray&lt;float3, RewindableAllocator&gt;(targetCount, ref World.UpdateAllocator);        var copyObstaclePositions &#x3D; CollectionHelper.CreateNativeArray&lt;float3, RewindableAllocator&gt;(obstacleCount, ref World.UpdateAllocator);        &#x2F;&#x2F; 安排各个数组的作业与各个查询一起存储。        Entities            .WithSharedComponentFilter(new BoidSetting&#123;num&#x3D;1&#125;)            .ForEach((int entityInQueryIndex, in LocalToWorld localToWorld) &#x3D;&gt;            &#123;                cellSeparation[entityInQueryIndex] &#x3D; localToWorld.Position;            &#125;)            .ScheduleParallel();        Entities            .WithAll&lt;BoidTarget&gt;()            .WithStoreEntityQueryInField(ref m_TargetQuery)            .ForEach((int entityInQueryIndex, in LocalToWorld localToWorld) &#x3D;&gt;            &#123;                copyTargetPositions[entityInQueryIndex] &#x3D; localToWorld.Position;            &#125;)            .ScheduleParallel();        Entities            .WithAll&lt;BoidObstacle&gt;()            .WithStoreEntityQueryInField(ref m_ObstacleQuery)            .ForEach((int entityInQueryIndex, in LocalToWorld localToWorld) &#x3D;&gt;            &#123;                copyObstaclePositions[entityInQueryIndex] &#x3D; localToWorld.Position;            &#125;)            .ScheduleParallel();    &#125;&#125;</code></pre></div></figure><p>可以改写如下：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">[RequireMatchingQueriesForUpdate]public partial class BoidJobEntitySystem : SystemBase&#123;    EntityQuery m_BoidQuery;    EntityQuery m_ObstacleQuery;    EntityQuery m_TargetQuery;    protected override void OnUpdate()    &#123;        &#x2F;&#x2F; 计算各个查询中的实体数量。        var boidCount &#x3D; m_BoidQuery.CalculateEntityCount();        var obstacleCount &#x3D; m_ObstacleQuery.CalculateEntityCount();        var targetCount &#x3D; m_TargetQuery.CalculateEntityCount();        &#x2F;&#x2F; 分配数组，以存储与相应的查询匹配的实体数量相等的数据。        var cellSeparation &#x3D; CollectionHelper.CreateNativeArray&lt;float3, RewindableAllocator&gt;(boidCount, ref World.UpdateAllocator);        var copyTargetPositions &#x3D; CollectionHelper.CreateNativeArray&lt;float3, RewindableAllocator&gt;(targetCount, ref World.UpdateAllocator);        var copyObstaclePositions &#x3D; CollectionHelper.CreateNativeArray&lt;float3, RewindableAllocator&gt;(obstacleCount, ref World.UpdateAllocator);        &#x2F;&#x2F; 为各自的数组安排作业，以与各自的查询一起存储。        new CopyPositionsJob &#123; copyPositions &#x3D; cellSeparation&#125;.ScheduleParallel(m_BoidQuery);        new CopyPositionsJob &#123; copyPositions &#x3D; copyTargetPositions&#125;.ScheduleParallel(m_TargetQuery);        new CopyPositionsJob &#123; copyPositions &#x3D; copyObstaclePositions&#125;.ScheduleParallel(m_ObstacleQuery);    &#125;    protected override void OnCreate()    &#123;        &#x2F;&#x2F; 获取相应的查询，其中包括前面描述的“CopyPositionsJob”所需的组件。        m_BoidQuery &#x3D; GetEntityQuery(typeof(LocalToWorld));        m_BoidQuery.SetSharedComponentFilter(new BoidSetting&#123;num&#x3D;1&#125;);        m_ObstacleQuery &#x3D; GetEntityQuery(typeof(LocalToWorld), typeof(BoidObstacle));        m_TargetQuery &#x3D; GetEntityQuery(typeof(LocalToWorld), typeof(BoidTarget));;    &#125;&#125;</code></pre></div></figure><h3 id="创建-IJobEntity-作业"><a href="#创建-IJobEntity-作业" class="headerlink" title="创建 IJobEntity 作业"></a>创建 IJobEntity 作业</h3><p>要创建 <code>IJobEntity</code> 作业，请编写一个使用 <code>IJobEntity</code> 接口的结构，并实现您自己的自定义 <code>Execute</code> 方法。</p><p>使用 <code>partial</code> 关键字是因为源代码生成创建了一个结构，该结构在 <code>project/Temp/GeneratedCode/.....</code> 中找到的单独文件中实现 <code>IJobEntityBatch</code>。</p><p>以下示例每帧向每个 <code>SampleComponent</code> 值加1。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public struct SampleComponent : IComponentData &#123; public float Value; &#125;public partial struct ASampleJob : IJobEntity&#123;    &#x2F;&#x2F; 每个SampleComponent值+1    void Execute(ref SampleComponent sample)    &#123;        sample.Value +&#x3D; 1f;    &#125;&#125;public partial class ASample : SystemBase&#123;    protected override void OnUpdate()    &#123;        &#x2F;&#x2F; 调度作业        new ASampleJob().ScheduleParallel();    &#125;&#125;</code></pre></div></figure><h3 id="指定查询"><a href="#指定查询" class="headerlink" title="指定查询"></a>指定查询</h3><p>您可以通过以下方式指定 IJobEntity 的查询：</p><ul><li>手动创建查询，以指定不同的调用要求。</li><li>让已实施的 <code>IJobEntity</code> 为您完成，基于其给定的执行参数，以及使用属性 <code>[WithAll(params Type)]</code>、<code>[WithAny(params Type)]</code>、<code>[WithNone(params Type)]</code>、<code>[ WithChangeFilter(params Type)]</code> 和 <code>[WithEntityQueryOptions((params EntityQueryOptions)]</code>。</li></ul><p>以下示例显示了这两个选项：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">partial struct QueryJob : IJobEntity&#123;    &#x2F;&#x2F; 迭代所有 SampleComponents 并增加它们的值    public void Execute(ref SampleComponent sample)    &#123;        sample.Value +&#x3D; 1;    &#125;&#125;[RequireMatchingQueriesForUpdate]public partial class QuerySystem : SystemBase&#123;    &#x2F;&#x2F; 与 QueryJob 匹配的查询，为 &#96;BoidTarget&#96; 指定    EntityQuery query_boidtarget;    &#x2F;&#x2F; 与 QueryJob 匹配的查询，为 &#96;BoidObstacle&#96; 指定    EntityQuery query_boidobstacle;    protected override void OnCreate()    &#123;        &#x2F;&#x2F; 包含在“QueryJob”中找到的所有执行参数的查询 - 以及其他用户指定的组件“BoidTarget”。        query_boidtarget &#x3D; GetEntityQuery(ComponentType.ReadWrite&lt;SampleComponent&gt;(),ComponentType.ReadOnly&lt;BoidTarget&gt;());        &#x2F;&#x2F; 包含在“QueryJob”中找到的所有执行参数的查询 - 以及其他用户指定的组件“BoidObstacle”。        query_boidobstacle &#x3D; GetEntityQuery(ComponentType.ReadWrite&lt;SampleComponent&gt;(),ComponentType.ReadOnly&lt;BoidObstacle&gt;());    &#125;    protected override void OnUpdate()    &#123;        &#x2F;&#x2F; Uses the BoidTarget query        new QueryJob().ScheduleParallel(query_boidtarget);        &#x2F;&#x2F; Uses the BoidObstacle query        new QueryJob().ScheduleParallel(query_boidobstacle);        &#x2F;&#x2F; Uses query created automatically that matches parameters found in &#96;QueryJob&#96;.        new QueryJob().ScheduleParallel();    &#125;&#125;</code></pre></div></figure><h4 id="属性"><a href="#属性" class="headerlink" title="属性"></a>属性</h4><p>因为 IJobEntity 类似于作业，所以您可以使用对作业起作用的所有属性：</p><ul><li><code>Unity.Burst.BurstCompile</code></li><li><code>Unity.Collections.DeallocateOnJobCompletion</code></li><li><code>Unity.Collections.NativeDisableParallelForRestriction</code></li><li><code>Unity.Burst.BurstDiscard</code></li><li><code>Unity.Collections.LowLevel.Unsafe.NativeSetThreadIndex</code></li><li><code>Unity.Burst.NoAlia</code></li></ul><p>IJobEntity 还具有您可以使用的其他属性：</p><table><thead><tr><th>属性</th><th>描述</th></tr></thead><tbody><tr><td><code>Unity.Entities.WithAll(params Type[])</code></td><td>在作业结构上设置。缩小查询范围，使实体必须匹配提供的所有类型。</td></tr><tr><td><code>Unity.Entities.WithAny(params Type[])</code></td><td>在作业结构上设置。缩小查询范围，使实体必须匹配所提供的任何类型。</td></tr><tr><td><code>Unity.Entities.WithNone(params Type[])</code></td><td>在作业结构上设置。缩小查询范围，使实体不必匹配所提供的任何类型。</td></tr><tr><td><code>Unity.Entities.WithChangeFilter(params Type[])</code></td><td>在作业结构上设置或附加到执行中的参数。缩小查询范围，以便实体必须在给定组件的原型块中进行更改。</td></tr><tr><td><code>Unity.Entities.WithEntityQueryOptions(params EntityQueryOptions[])</code></td><td>在作业结构上设置。更改查询范围以使用描述的 EntityQueryOptions。</td></tr><tr><td><code>Unity.Entities.EntityInQueryIndex</code></td><td>在 Execute 中设置 int 参数以获取查询中的当前索引，用于当前实体迭代。这与 <code>Entities.ForEach</code> 中的 entityInQueryIndex 相同。</td></tr></tbody></table><p>以下是 <code>EntityInQueryIndex</code> 的示例：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">[BurstCompile]partial struct CopyPositionsJob : IJobEntity&#123;    public NativeArray&lt;float3&gt; copyPositions;    &#x2F;&#x2F; 遍历所有 &#96;LocalToWorld&#96; 并将它们的位置存储在 &#96;copyPositions&#96; 中。    public void Execute([EntityInQueryIndex] int entityInQueryIndex, in LocalToWorld localToWorld)    &#123;        copyPositions[entityInQueryIndex] &#x3D; localToWorld.Position;    &#125;&#125;[RequireMatchingQueriesForUpdate]public partial class EntityInQuerySystem : SystemBase&#123;    &#x2F;&#x2F; 此查询应匹配 &#96;CopyPositionsJob&#96; 参数    EntityQuery query;    protected override void OnCreate()    &#123;        &#x2F;&#x2F; 获取匹配 &#96;CopyPositionsJob&#96; 参数的查询        query &#x3D; GetEntityQuery(ComponentType.ReadOnly&lt;LocalToWorld&gt;());    &#125;    protected override void OnUpdate()    &#123;        &#x2F;&#x2F; 获取一个native array，该数组的大小等于查询找到的实体数量。        var positions &#x3D; new NativeArray&lt;float3&gt;(query.CalculateEntityCount(), World.UpdateAllocator.ToAllocator);        &#x2F;&#x2F; 在并行线程上为此数组安排作业。        new CopyPositionsJob&#123;copyPositions &#x3D; positions&#125;.ScheduleParallel();        &#x2F;&#x2F; 处理作业找到的位置数组。        positions.Dispose(Dependency);    &#125;&#125;</code></pre></div></figure><h4 id="执行参数"><a href="#执行参数" class="headerlink" title="执行参数"></a>执行参数</h4><p>以下是您可以在 <code>IJobEntity</code> 中使用的所有受支持 <code>Execute</code> 参数的列表：</p><table><thead><tr><th>参数</th><th>描述</th></tr></thead><tbody><tr><td><code>IComponentData</code></td><td>标记为 <code>ref</code> 用于读写访问，或标记为对 <code>ComponentData</code> 的只读访问。</td></tr><tr><td><code>ICleanupComponentData</code></td><td>标记为 <code>ref</code> 用于读写访问，或标记为对 <code>ComponentData</code> 的只读访问。</td></tr><tr><td><code>ISharedComponent</code></td><td>标记为只读访问 <code>SharedComponentData</code>。如果这是托管的，你不能突发编译或安排它。使用 <code>.Run</code> 代替。</td></tr><tr><td>Managed components</td><td>使用值副本进行读写访问，或使用 <code>in</code> 标记对托管组件进行只读访问。例如，UnityEngine.Transform。将托管组件标记为 <code>ref</code> 是错误的，您不能对其进行突发编译或调度。使用 .Run 代替。</td></tr><tr><td><code>Entity</code></td><td>获取当前实体。这只是一个值副本，所以不要用 <code>ref</code> 或 <code>in</code> 标记。</td></tr><tr><td><code>DynamicBuffer&lt;T&gt;</code></td><td>获取动态缓冲区。用 <code>ref</code> 标记为读写访问，用 <code>in</code> 标记为只读访问。</td></tr><tr><td><code>IAspect</code></td><td>获取方面。方面充当参考，因此您无法分配它们。但是，您可以使用 <code>ref</code> 和 value-copy 将其标记为可读写，使用 <code>in</code> 其标记为只读访问。</td></tr><tr><td><code>int</code></td><td>支持三种整数：</td></tr><tr><td></td><td>使用属性 [Unity.Entities.ChunkIndexInQuery] 标记 <code>int</code> 以获取查询中的当前原型块索引。</td></tr><tr><td></td><td>使用属性 [Unity.Entities.EntityIndexInChunk] 标记 <code>int</code> 以获取当前原型块中的当前实体索引。您可以添加 <code>EntityIndexInChunk</code> 和 <code>ChunkIndexInQuery</code> 以获得每个实体的唯一标识符。</td></tr><tr><td></td><td>使用属性 [Unity.Entities.EntityInQueryIndex] 标记 <code>int</code> 以获取查询的打包索引。这对性能有影响，使用 <code>EntityQuery.CalculateBaseEntityIndexArray[Async]</code>。</td></tr></tbody></table><h2 id="使用-IJobEntityBatch-遍历成批数据"><a href="#使用-IJobEntityBatch-遍历成批数据" class="headerlink" title="使用 IJobEntityBatch 遍历成批数据"></a>使用 <code>IJobEntityBatch</code> 遍历成批数据</h2><p>在系统内实现 IJobEntityBatch 或 IJobEntityBatchWithIndex 以在实体批次中迭代数据。</p><p>当您在系统的 OnUpdate 函数中计划 IJobEntityBatch 作业时，系统会使用您传递给计划函数的实体查询来识别应该传递给该作业的块。该作业会为这些块中的每批实体调用一次您的 Execute 函数。默认情况下，批处理大小是一个完整的块，但您可以在调度作业时将批处理大小设置为块的一部分。无论批次大小如何，给定批次中的实体始终存储在同一块中。在作业的执行函数中，您可以逐个实体地迭代每个批次中的数据。</p><p>当您需要批次集中所有实体的索引值时，请使用 <code>IJobEntityBatchWithIndex</code>。否则，<code>IJobEntityBatch</code> 效率更高，因为它不需要计算这些索引。</p><p>要实施批处理作业：</p><ol><li><p>使用 EntityQuery 查询数据以确定要处理的实体。</p></li><li><p>使用 IJobEntityBatch 或 IJobEntityBatchWithIndex 定义作业结构。</p></li><li><p>声明您的作业访问的数据。在作业结构中，包括用于标识作业必须直接访问的组件类型的 ComponentTypeHandle 对象的字段。此外，指定作业是读取还是写入这些组件。您还可以包含标识您要查找的实体数据的字段，这些实体不属于查询的一部分，以及用于非实体数据的字段。</p></li><li><p>编写作业结构的执行函数来转换您的数据。获取作业读取或写入的组件的 NativeArray 实例，然后迭代当前批处理以执行所需的工作。</p></li><li><p>在系统 OnUpdate 函数中安排作业，将标识要处理的实体的实体查询传递给调度函数。</p></li></ol><div class="note note-info">            <p>与使用 <code>Entities.ForEach</code> 相比，使用 <code>IJobEntityBatch</code> 或 <code>IJobEntityBatchWithIndex</code> 进行迭代更复杂并且需要更多的代码设置，并且只应在必要或更有效时使用。</p>          </div><p>有关详细信息，ECS 示例存储库包含一个简单的 HelloCube 示例，该示例演示了如何使用 IJobEntityBatch。</p><h3 id="使用-EntityQuery-查询数据"><a href="#使用-EntityQuery-查询数据" class="headerlink" title="使用 EntityQuery 查询数据"></a>使用 EntityQuery 查询数据</h3><p><code>EntityQuery</code> 定义了 <code>EntityArchetype</code> 必须包含的组件类型集，系统才能处理其关联的块和实体。原型可以有额外的组件，但它必须至少有查询定义的组件。您还可以排除包含特定类型组件的原型。</p><p>将选择您的作业应处理的实体的查询传递给您用于安排作业的计划方法。</p><p>有关定义查询的信息，请参阅使用 <code>EntityQuery</code> 查询数据。</p><div class="note note-info">            <p>不要在 <code>EntityQuery</code> 中包含完全可选的组件。要处理可选组件，请使用 <code>IJobEntityBatch.Execute</code> 中的 <code>ArchetypeChunk.Has</code> 方法来确定当前 <code>ArchetypeChunk</code> 是否具有可选组件。因为同一批次中的所有实体都具有相同的组件，所以您只需要检查每个批次是否存在可选组件一次，而不是每个实体一次。</p>          </div><h3 id="定义作业结构"><a href="#定义作业结构" class="headerlink" title="定义作业结构"></a>定义作业结构</h3><p>作业结构由执行要执行的工作的执行函数和声明执行函数使用的数据的字段组成。</p><p>典型的 <code>IJobEntityBatch</code> 作业结构如下所示：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public struct UpdateTranslationFromVelocityJob : IJobEntityBatch&#123;    public ComponentTypeHandle&lt;VelocityVector&gt; velocityTypeHandle;    public ComponentTypeHandle&lt;ObjectPosition&gt; translationTypeHandle;    public float DeltaTime;    [BurstCompile]    public void Execute(ArchetypeChunk batchInChunk, int batchIndex)    &#123;        NativeArray&lt;VelocityVector&gt; velocityVectors &#x3D;            batchInChunk.GetNativeArray(velocityTypeHandle);        NativeArray&lt;ObjectPosition&gt; translations &#x3D;            batchInChunk.GetNativeArray(translationTypeHandle);        for(int i &#x3D; 0; i &lt; batchInChunk.Count; i++)        &#123;            float3 translation &#x3D; translations[i].Value;            float3 velocity &#x3D; velocityVectors[i].Value;            float3 newTranslation &#x3D; translation + velocity * DeltaTime;            translations[i] &#x3D; new ObjectPosition() &#123; Value &#x3D; newTranslation &#125;;        &#125;    &#125;&#125;</code></pre></div></figure><p>此示例访问实体的两个组件 VelocityVector 和 Translation 的数据，并根据自上次更新以来经过的时间计算新的平移。</p><h4 id="IJobEntityBatch-与-IJobEntityBatchWithIndex"><a href="#IJobEntityBatch-与-IJobEntityBatchWithIndex" class="headerlink" title="IJobEntityBatch 与 IJobEntityBatchWithIndex"></a>IJobEntityBatch 与 IJobEntityBatchWithIndex</h4><p><code>IJobEntityBatch</code> 和 <code>IJobEntityBatchWithIndex</code> 之间的唯一区别是 <code>IJobEntityBatchWithIndex</code> 在对批处理调用 <code>Execute</code> 函数时传递一个 <code>indexOfFirstEntityInQuery</code> 参数。该参数为当前batch中第一个实体在实体查询选中的所有实体列表中的索引。</p><p>当您需要每个实体的单独索引时，请使用 <code>IJobEntityBatchWithIndex</code>。例如，如果您为每个实体计算一个唯一的结果，您可以使用此索引将每个结果写入native array的不同元素。如果您不使用 <code>indexOfFirstEntityInQuery</code> 值，请改用 <code>IJobEntityBatch</code>，以避免计算索引值的开销。</p><div class="note note-info">            <p>当您向 <code>[EntityCommandBuffer.ParallelWriter]</code> 添加命令时，您可以使用 <code>batchIndex</code> 参数作为命令缓冲区函数的 <code>sortKey</code> 参数。您不需要仅使用 <code>IJobEntityBatchWithIndex</code> 来为每个实体获取唯一的排序键。两种作业类型都可用的 <code>batchIndex</code> 参数可用于此目的。</p>          </div><h4 id="声明您的工作访问的数据"><a href="#声明您的工作访问的数据" class="headerlink" title="声明您的工作访问的数据"></a>声明您的工作访问的数据</h4><p>作业结构中的字段声明可用于 Execute 函数的数据。这些领域分为四大类：</p><ul><li><p>ComponentTypeHandle 字段 —— 组件句柄字段允许您的 Execute 函数访问存储在当前块中的实体组件和缓冲区。请参阅访问实体组件和缓冲区数据。</p></li><li><p>ComponentLookup、BufferLookup 字段 —— 这些“来自实体的数据”字段允许您的 Execute 函数查找任何实体的数据，无论它存储在何处。 （这种类型的随机访问是访问数据效率最低的方式，只应在必要时使用。）请参阅查找其他实体的数据。</p></li><li><p>其他字段 —— 您可以根据需要为您的结构声明其他字段。您可以在每次安排作业时设置此类字段的值。请参阅访问其他数据。</p></li><li><p>输出字段 —— 除了更新作业中的可写实体组件或缓冲区外，您还可以写入为作业结构声明的NativeContainer字段。此类字段必须是原生容器，例如 NativeArray；您不能使用其他数据类型。</p></li></ul><h5 id="访问实体组件和缓冲区数据"><a href="#访问实体组件和缓冲区数据" class="headerlink" title="访问实体组件和缓冲区数据"></a>访问实体组件和缓冲区数据</h5><p>访问存储在查询中实体之一的组件中的数据是三个步骤的过程：</p><p>首先，您必须在作业结构上定义一个 ComponentTypeHandle 字段，将 T 设置为组件的数据类型。例如：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public ComponentTypeHandle&lt;ObjectPosition&gt; translationTypeHandle;</code></pre></div></figure><p>接下来，您在作业的 Execute 方法中使用此句柄字段来访问包含该类型组件数据的数组（作为 NativeArray）。该数组包含批次中每个实体的一个元素：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">NativeArray&lt;ObjectPosition&gt; translations &#x3D; batchInChunk.GetNativeArray(translationTypeHandle);</code></pre></div></figure><p>最后，当您安排作业时（在系统的 OnUpdate 方法中，您使用 ComponentSystemBase.GetComponentTypeHandle 函数为类型句柄字段分配一个值：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">&#x2F;&#x2F; &quot;this&quot; is your &#96;SystemBase&#96; subclassupdateFromVelocityJob.translationTypeHandle &#x3D; this.GetComponentTypeHandle&lt;ObjectPosition&gt;(false);</code></pre></div></figure><p>每次安排作业时，始终设置作业的组件句柄字段。不要缓存类型句柄并在以后使用它。</p><p>批次中的每个组件数据数组都是对齐的，以便给定索引对应于所有数组中的相同实体。换句话说，如果您的作业使用一个实体的两个组件，请在两个数据数组中使用相同的数组索引来访问同一实体的数据。</p><p>您可以使用 ComponentTypeHandle 变量来访问您未包含在 EntityQuery 中的组件类型。但是，您必须检查以确保当前批次包含该组件，然后再尝试访问它。使用 Has 函数检查当前批次是否包含特定组件类型：</p><p>ComponentTypeHandle 字段是 ECS 作业安全系统的一部分，可防止在读取和写入作业中的数据时出现竞争条件。始终设置 GetComponentTypeHandle 函数的 isReadOnly 参数以准确反映组件在作业中的访问方式。</p><h5 id="查找其他实体的数据"><a href="#查找其他实体的数据" class="headerlink" title="查找其他实体的数据"></a>查找其他实体的数据</h5><p>通过 EntityQuery 和 IJobEntityBatch 作业（或 Entities.ForEach）访问组件数据几乎总是访问数据的最有效方式。但是，通常情况下您需要以随机访问方式查找数据，例如，当一个实体依赖于另一个实体中的数据时。要执行这种类型的数据查找，您必须通过作业结构将不同类型的句柄传递给您的作业：</p><p>ComponentLookup – 访问具有该组件类型的任何实体的组件</p><p>BufferLookup – 访问具有该缓冲区类型的任何实体的缓冲区</p><p>这些类型为组件和缓冲区提供类似数组的接口，由 Entity 对象索引。除了由于随机数据访问而相对低效之外，以这种方式查找数据还会增加您遇到工作安全系统建立的保障措施的机会。例如，如果您尝试根据另一个实体的变换设置一个实体的变换，作业安全系统无法判断这是否安全，因为您可以通过 ComponentLookup 对象访问所有变换。您可能正在写入您正在读取的相同数据，从而造成竞争条件。</p><p>要使用 ComponentLookup 和 BufferLookup，请在作业结构上声明一个类型为 ComponentLookup 或 BufferLookup 的字段，并在调度作业之前设置该字段的值。</p><p>有关详细信息，请参阅查找数据。</p><h5 id="访问其他数据"><a href="#访问其他数据" class="headerlink" title="访问其他数据"></a>访问其他数据</h5><p>如果在执行作业时需要其他信息，可以在作业结构上定义一个字段，然后在 Execute 方法中访问该字段。您只能在安排作业时设置该值，并且该值对于所有批次都保持不变。</p><p>例如，如果您正在更新移动对象，您很可能需要传入自上次更新以来经过的时间。为此，您可以定义一个名为 DeltaTime 的字段，在 OnUpdate 中设置它的值并在作业执行函数中使用该值。在为新帧安排作业之前，您将在每一帧计算并为 DeltaTime 字段分配一个新值。</p><h4 id="编写执行函数"><a href="#编写执行函数" class="headerlink" title="编写执行函数"></a>编写执行函数</h4><p>编写作业结构的执行函数，将数据从输入状态转换为所需的输出状态。</p><p>IJobEntityBatch.Execute 方法的签名是：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">void Execute(ArchetypeChunk batchInChunk, int batchIndex)</code></pre></div></figure><p>对于 IJobEntityBatchWithIndex.Execute，签名是：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">void Execute(ArchetypeChunk batchInChunk, int batchIndex, int indexOfFirstEntityInQuery)</code></pre></div></figure><h5 id="batchInChunk-参数"><a href="#batchInChunk-参数" class="headerlink" title="batchInChunk 参数"></a>batchInChunk 参数</h5><p>batchInChunk 参数提供包含此作业迭代的实体和组件的 ArchetypeChunk 实例。因为一个块只能包含一个原型，所以一个块中的所有实体都具有相同的组件集。默认情况下，此对象将所有实体包含在一个块中；但是，如果您使用 ScheduleParallel 安排作业，则可以指定一个批次仅包含块中实体数的一小部分。</p><p>使用 batchInChunk 参数获取访问组件数据所需的 NativeArray 实例。 （您还必须声明一个具有相应组件类型句柄的字段——并在安排作业时设置该字段。）</p><h5 id="batchIndex-参数"><a href="#batchIndex-参数" class="headerlink" title="batchIndex 参数"></a>batchIndex 参数</h5><p>batchIndex 参数是当前批次在为当前作业创建的所有批次列表中的索引。作业中的批次不一定按索引顺序处理。</p><p>您可以在以下情况下使用 batchIndex 值：您有一个NativeContainer，每个批次有一个元素，您希望将在执行函数中计算的值写入其中。使用 batchIndex 作为此容器的数组索引。</p><p>如果您使用并行写入实体命令缓冲区，请将 batchIndex 参数作为 sortKey 参数传递给命令缓冲区函数。</p><h5 id="indexOfFirstEntityInQuery-参数"><a href="#indexOfFirstEntityInQuery-参数" class="headerlink" title="indexOfFirstEntityInQuery 参数"></a>indexOfFirstEntityInQuery 参数</h5><p>IJobEntityBatchWithIndex Execute 函数有一个名为 indexofFirstEntityInQuery 的附加参数。如果您将查询选择的实体描绘成一个列表，则 indexOfFirstEntityInQuery 将是当前批次中第一个实体的该列表的索引。作业中的批次不一定按索引顺序处理。</p><h5 id="可选组件"><a href="#可选组件" class="headerlink" title="可选组件"></a>可选组件</h5><p>如果您的实体查询中有 Any 过滤器或完全可选的组件根本没有出现在查询中，您可以使用 ArchetypeChunk.Has 函数在使用之前测试当前块是否包含这些组件之一：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">&#x2F;&#x2F; If entity has Rotation and LocalToWorld components,&#x2F;&#x2F; slerp to align to the velocity vectorif (batchInChunk.Has&lt;Rotation&gt;(rotationTypeHandle) &amp;&amp;    batchInChunk.Has&lt;LocalToWorld&gt;(l2wTypeHandle))&#123;    NativeArray&lt;Rotation&gt; rotations        &#x3D; batchInChunk.GetNativeArray(rotationTypeHandle);    NativeArray&lt;LocalToWorld&gt; transforms        &#x3D; batchInChunk.GetNativeArray(l2wTypeHandle);    &#x2F;&#x2F; By putting the loop inside the check for the    &#x2F;&#x2F; optional components, we can check once per batch    &#x2F;&#x2F; rather than once per entity.    for (int i &#x3D; 0; i &lt; batchInChunk.Count; i++)    &#123;        float3 direction &#x3D; math.normalize(velocityVectors[i].Value);        float3 up &#x3D; transforms[i].Up;        quaternion rotation &#x3D; rotations[i].Value;        quaternion look &#x3D; quaternion.LookRotation(direction, up);        quaternion newRotation &#x3D; math.slerp(rotation, look, DeltaTime);        rotations[i] &#x3D; new Rotation() &#123; Value &#x3D; newRotation &#125;;    &#125;&#125;</code></pre></div></figure><h3 id="安排工作"><a href="#安排工作" class="headerlink" title="安排工作"></a>安排工作</h3><p>要运行 IJobEntityBatch 作业，您必须创建作业结构的实例，设置结构字段，然后安排作业。当您在 <code>SystemBase</code> 实现的 OnUpdate 函数中执行此操作时，系统会安排作业在每一帧运行。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">[RequireMatchingQueriesForUpdate]public partial class UpdateTranslationFromVelocitySystem : SystemBase&#123;    EntityQuery query;    protected override void OnCreate()    &#123;        &#x2F;&#x2F; Set up the query        var description &#x3D; new EntityQueryDesc()        &#123;            All &#x3D; new ComponentType[]                   &#123;ComponentType.ReadWrite&lt;ObjectPosition&gt;(),                    ComponentType.ReadOnly&lt;VelocityVector&gt;()&#125;        &#125;;        query &#x3D; this.GetEntityQuery(description);    &#125;    protected override void OnUpdate()    &#123;        &#x2F;&#x2F; Instantiate the job struct        var updateFromVelocityJob            &#x3D; new UpdateTranslationFromVelocityJob();        &#x2F;&#x2F; Set the job component type handles        &#x2F;&#x2F; &quot;this&quot; is your &#96;SystemBase&#96; subclass        updateFromVelocityJob.translationTypeHandle            &#x3D; this.GetComponentTypeHandle&lt;ObjectPosition&gt;(false);        updateFromVelocityJob.velocityTypeHandle            &#x3D; this.GetComponentTypeHandle&lt;VelocityVector&gt;(true);        &#x2F;&#x2F; Set other data need in job, such as time        updateFromVelocityJob.DeltaTime &#x3D; World.Time.DeltaTime;        &#x2F;&#x2F; Schedule the job        this.Dependency            &#x3D; updateFromVelocityJob.ScheduleParallel(query, this.Dependency);    &#125;&#125;</code></pre></div></figure><p>当您调用 GetComponentTypeHandle 函数来设置组件类型变量时，请确保将作业读取但不写入的组件的 isReadOnly 参数设置为 true。正确设置这些参数会对 ECS 框架安排作业的效率产生重大影响。这些访问模式设置必须与其在结构定义和 EntityQuery 中的等效设置相匹配。</p><p>不要在系统类变量中缓存 GetComponentTypeHandle 的返回值。您必须在每次系统运行时调用该函数，并将更新后的值传递给作业。</p><h4 id="调度选项"><a href="#调度选项" class="headerlink" title="调度选项"></a>调度选项</h4><p>您可以在安排作业时通过选择适当的功能来控制作业的执行方式：</p><ul><li><p>运行——立即在当前（主）线程上执行作业。 Run 还会完成当前作业所依赖的任何计划作业。批量大小始终为 1（整个块）。</p></li><li><p>Schedule——安排作业在当前作业所依赖的任何计划作业之后在工作线程上运行。为实体查询选择的每个块调用一次作业执行函数。块按顺序处理。批量大小始终为 1。</p></li><li><p>ScheduleParallel——与 Schedule 类似，不同之处在于您可以指定批处理大小，并且这些批处理是并行处理的（假设工作线程可用）而不是顺序处理。</p></li></ul><h4 id="设置批量大小"><a href="#设置批量大小" class="headerlink" title="设置批量大小"></a>设置批量大小</h4><p>要设置批量大小，请使用 ScheduleParallel 方法来安排作业并将 batchesPerChunk 参数设置为正整数。使用值 1 将批处理大小设置为完整块。</p><p>用于调度作业的查询选择的每个块都分为 batchesPerChunk 指定的批次数。来自同一块的每个批次包含大致相同数量的实体；然而，来自不同块的批次可能包含非常不同数量的实体。最大批处理大小为 1，这意味着每个块中的所有实体都在对 Execute 函数的一次调用中一起处理。来自不同块的实体永远不能包含在同一批中。</p><div class="note note-info">            <p>通常，使用 batchesPerChunk 设置为 1 来在对 Execute 的单个调用中处理块中的所有实体是最有效的。然而，情况并非总是如此。例如，如果您的 Execute 函数执行的实体数量较少且算法成本较高，则可以通过使用较小的实体批次从并行处理中获得额外的好处。</p>          </div><h3 id="跳过实体不变的块"><a href="#跳过实体不变的块" class="headerlink" title="跳过实体不变的块"></a>跳过实体不变的块</h3><p>如果您只需要在组件值更改时更新实体，则可以将该组件类型添加到为作业选择实体和块的 EntityQuery 的更改过滤器中。例如，如果您有一个系统读取两个组件并且只需要在前两个组件中的一个发生更改时更新第三个组件，则可以按如下方式使用 EntityQuery：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">EntityQuery query;protected override void OnCreate()&#123;    query &#x3D; GetEntityQuery(        new ComponentType[]        &#123;            ComponentType.ReadOnly&lt;InputA&gt;(),            ComponentType.ReadOnly&lt;InputB&gt;(),            ComponentType.ReadWrite&lt;Output&gt;()        &#125;    );    query.SetChangedVersionFilter(            new ComponentType[]            &#123;                typeof(InputA),                typeof(InputB)            &#125;        );&#125;</code></pre></div></figure><p>EntityQuery 更改过滤器最多支持两个组件。如果您想检查更多或者您没有使用 EntityQuery，您可以手动进行检查。要进行此检查，请使用 ArchetypeChunk.DidChange 函数将组件的块更改版本与系统的 LastSystemVersion 进行比较。如果此函数返回 false，则您可以完全跳过当前块，因为自上次系统运行以来该类型的组件均未更改。</p><p>您必须使用结构字段将 LastSystemVersion 从系统传递到作业中，如下所示：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">struct UpdateOnChangeJob : IJobEntityBatch&#123;    public ComponentTypeHandle&lt;InputA&gt; InputATypeHandle;    public ComponentTypeHandle&lt;InputB&gt; InputBTypeHandle;    [ReadOnly] public ComponentTypeHandle&lt;Output&gt; OutputTypeHandle;    public uint LastSystemVersion;    [BurstCompile]    public void Execute(ArchetypeChunk batchInChunk, int batchIndex)    &#123;        var inputAChanged &#x3D; batchInChunk.DidChange(InputATypeHandle, LastSystemVersion);        var inputBChanged &#x3D; batchInChunk.DidChange(InputBTypeHandle, LastSystemVersion);        &#x2F;&#x2F; If neither component changed, skip the current batch        if (!(inputAChanged || inputBChanged))            return;        var inputAs &#x3D; batchInChunk.GetNativeArray(InputATypeHandle);        var inputBs &#x3D; batchInChunk.GetNativeArray(InputBTypeHandle);        var outputs &#x3D; batchInChunk.GetNativeArray(OutputTypeHandle);        for (var i &#x3D; 0; i &lt; outputs.Length; i++)        &#123;            outputs[i] &#x3D; new Output &#123; Value &#x3D; inputAs[i].Value + inputBs[i].Value &#125;;        &#125;    &#125;&#125;</code></pre></div></figure><p>与所有作业结构字段一样，您必须在安排作业之前分配其值：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">[RequireMatchingQueriesForUpdate]public partial class UpdateDataOnChangeSystem : &#96;SystemBase&#96; &#123;    EntityQuery query;    protected override void OnUpdate()    &#123;        var job &#x3D; new UpdateOnChangeJob();        job.LastSystemVersion &#x3D; this.LastSystemVersion;        job.InputATypeHandle &#x3D; GetComponentTypeHandle&lt;InputA&gt;(true);        job.InputBTypeHandle &#x3D; GetComponentTypeHandle&lt;InputB&gt;(true);        job.OutputTypeHandle &#x3D; GetComponentTypeHandle&lt;Output&gt;(false);        this.Dependency &#x3D; job.ScheduleParallel(query, this.Dependency);    &#125;    protected override void OnCreate()    &#123;        query &#x3D; GetEntityQuery(            new ComponentType[]            &#123;                ComponentType.ReadOnly&lt;InputA&gt;(),                ComponentType.ReadOnly&lt;InputB&gt;(),                ComponentType.ReadWrite&lt;Output&gt;()            &#125;        );    &#125;&#125;</code></pre></div></figure><div class="note note-info">            <p>为了提高效率，更改版本适用于整个块而不是单个实体。如果另一个能够写入该类型组件的作业访问块，则 ECS 会增加该组件的更改版本，并且 DidChange 函数返回 true。即使声明对组件的写访问权限的作业实际上并未更改组件值，ECS 也会增加更改版本。 （这是在读取组件数据而不更新它时应始终只读的原因之一。）</p>          </div><h2 id="手动迭代数据"><a href="#手动迭代数据" class="headerlink" title="手动迭代数据"></a>手动迭代数据</h2><p>如果您需要以一种不适合迭代 EntityQuery 中所有块的简化模型的方式管理块，您可以在native array中显式手动请求所有原型块，并使用 IJobParallelFor 等作业处理它们.下面是一个例子：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public class RotationSpeedSystem : SystemBase&#123;   [BurstCompile]   struct RotationSpeedJob : IJobParallelFor   &#123;       [DeallocateOnJobCompletion] public NativeArray&lt;ArchetypeChunk&gt; Chunks;       public ArchetypeChunkComponentType&lt;RotationQuaternion&gt; RotationType;       [ReadOnly] public ArchetypeChunkComponentType&lt;RotationSpeed&gt; RotationSpeedType;       public float DeltaTime;       public void Execute(int chunkIndex)       &#123;           var chunk &#x3D; Chunks[chunkIndex];           var chunkRotation &#x3D; chunk.GetNativeArray(RotationType);           var chunkSpeed &#x3D; chunk.GetNativeArray(RotationSpeedType);           var instanceCount &#x3D; chunk.Count;           for (int i &#x3D; 0; i &lt; instanceCount; i++)           &#123;               var rotation &#x3D; chunkRotation[i];               var speed &#x3D; chunkSpeed[i];               rotation.Value &#x3D; math.mul(math.normalize(rotation.Value), quaternion.AxisAngle(math.up(), speed.RadiansPerSecond * DeltaTime));               chunkRotation[i] &#x3D; rotation;           &#125;       &#125;   &#125;   EntityQuery m_Query;      protected override void OnCreate()   &#123;       var queryDesc &#x3D; new EntityQueryDesc       &#123;           All &#x3D; new ComponentType[]&#123; typeof(RotationQuaternion), ComponentType.ReadOnly&lt;RotationSpeed&gt;() &#125;       &#125;;       m_Query &#x3D; GetEntityQuery(queryDesc);   &#125;   protected override void OnUpdate()   &#123;       var rotationType &#x3D; GetArchetypeChunkComponentType&lt;RotationQuaternion&gt;();       var rotationSpeedType &#x3D; GetArchetypeChunkComponentType&lt;RotationSpeed&gt;(true);       var chunks &#x3D; m_Query.ToArchetypeChunkArray(Allocator.TempJob);       var rotationsSpeedJob &#x3D; new RotationSpeedJob       &#123;           Chunks &#x3D; chunks,           RotationType &#x3D; rotationType,           RotationSpeedType &#x3D; rotationSpeedType,           DeltaTime &#x3D; Time.deltaTime       &#125;;       this.Dependency rotationsSpeedJob.Schedule(chunks.Length,32, this.Dependency);   &#125;&#125;</code></pre></div></figure><h3 id="如何手动迭代数据"><a href="#如何手动迭代数据" class="headerlink" title="如何手动迭代数据"></a>如何手动迭代数据</h3><p>您可以使用 EntityManager 类手动遍历实体或原型块，但这效率不高。您应该只使用这些迭代方法来测试或调试您的代码，或者在您拥有一组受控实体的孤立世界中。</p><p>例如，以下代码片段遍历活动世界中的所有实体：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">var entityManager &#x3D; World.Active.EntityManager;var allEntities &#x3D; entityManager.GetAllEntities();foreach (var entity in allEntities)&#123;   &#x2F;&#x2F;...&#125;allEntities.Dispose();</code></pre></div></figure><p>此代码段遍历活动世界中的所有块：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">var entityManager &#x3D; World.Active.EntityManager;var allChunks &#x3D; entityManager.GetAllChunks();foreach (var chunk in allChunks)&#123;   &#x2F;&#x2F;...&#125;allChunks.Dispose();</code></pre></div></figure><h1 id="系统更新顺序"><a href="#系统更新顺序" class="headerlink" title="系统更新顺序"></a>系统更新顺序</h1><p>要指定系统的更新顺序，您可以使用 <code>ComponentSystemGroup</code> 类。要将系统置于组中，请在系统的类声明中使用 <code>UpdateInGroup</code> 属性。然后，您可以使用 <code>UpdateBefore</code> 或 <code>UpdateAfter</code> 属性来指定系统必须更新的顺序。</p><p>有一组默认系统组，您可以使用它们在框架的正确阶段更新系统。您可以将一个组嵌套在另一个组中，以便您组中的所有系统都在正确的阶段更新，并根据其组内的顺序进行更新。</p><h2 id="组件系统组"><a href="#组件系统组" class="headerlink" title="组件系统组"></a>组件系统组</h2><p><code>ComponentSystemGroup</code> 类表示 Unity 必须按特定顺序一起更新的相关组件系统的列表。 <code>ComponentSystemGroup</code> 继承自 <code>ComponentSystemBase</code>，因此您可以相对于其他系统对其进行排序，并且它具有 <code>OnUpdate()</code> 方法。这也意味着您可以将一个 <code>ComponentSystemGroup</code> 嵌套在另一个 <code>ComponentSystemGroup</code> 中，并形成层次结构。</p><p>默认情况下，当您在 <code>ComponentSystemGroup</code> 中调用 <code>Update()</code> 方法时，它会在其已排序的成员系统列表中的每个系统上调用 <code>Update()</code>。如果任何成员系统是系统组，它们将递归更新自己的成员。生成的系统排序遵循树的深度优先遍历。</p><h2 id="系统排序属性"><a href="#系统排序属性" class="headerlink" title="系统排序属性"></a>系统排序属性</h2><p>您可以在系统上使用以下属性来确定其更新顺序：</p><table><thead><tr><th>属性</th><th>描述</th></tr></thead><tbody><tr><td><code>UpdateInGroup</code></td><td>指定此系统应属于的 <code>ComponentSystemGroup</code>。如果不设置该属性，Unity 会自动将其添加到默认世界的 <code>SimulationSystemGroup</code> 中。有关详细信息，请参阅默认系统组部分。</td></tr><tr><td><code>UpdateBefore</code> <code>UpdateAfter</code></td><td>排序系统相对于其他系统。为这些属性指定的系统类型必须是同一组的成员。 Unity 在包含两个系统的适当的最深组中处理跨组边界的排序。例如，如果 <code>CarSystem</code> 在 <code>CarGroup</code> 中，而 <code>TruckSystem</code> 在 <code>TruckGroup</code> 中，并且 <code>CarGroup</code> 和 <code>TruckGroup</code> 都是 <code>VehicleGroup</code> 的成员，那么 <code>CarGroup</code> 和 <code>TruckGroup</code> 的顺序隐式决定了 <code>CarSystem</code> 和 <code>TruckSystem</code> 的相对顺序。您无需明确订购系统。</td></tr><tr><td><code>DisableAutoCreation</code></td><td>阻止 Unity 在默认世界初始化期间创建系统。您必须明确地创建和更新系统。但是，您可以将带有此标记的系统添加到 <code>ComponentSystemGroup</code> 的更新列表中，它会像该列表中的其他系统一样自动更新。</td></tr></tbody></table><p>如果您将 <code>DisableAutoCreation</code> 属性添加到组件系统或系统组，Unity 不会创建它或将其添加到默认系统组。要手动创建系统，请使用 <code>World.GetOrCreateSystem&lt;MySystem&gt;()</code> 并从主线程调用 <code>MySystem.Update()</code> 来更新它。您可以使用它在 Unity 播放器循环中的其他位置插入系统，例如，如果您有一个应该在帧中稍后或更早运行的系统。</p><h2 id="默认系统组"><a href="#默认系统组" class="headerlink" title="默认系统组"></a>默认系统组</h2><p>默认世界包含 <code>ComponentSystemGroup</code> 实例的层次结构。 Unity 播放器循环中有三个根级系统组：</p><ul><li><code>InitializationSystemGroup</code>：在播放器循环的初始化阶段结束时更新。</li><li><code>SimulationSystemGroup</code>：在播放器循环的更新阶段结束时更新。</li><li><code>PresentationSystemGroup</code>：在播放器循环的 <code>PreLateUpdate</code> 阶段结束时更新。</li></ul><p>默认系统组也有一些预定义的成员系统：</p><p><strong>InitializationSystemGroup</strong>:</p><ul><li>BeginInitializationEntityCommandBufferSystem</li><li>CopyInitialTransformFromGameObjectSystem</li><li>SubSceneLiveConversionSystem</li><li>SubSceneStreamingSystem</li><li>EndInitializationEntityCommandBufferSystem</li></ul><p><strong>SimulationSystemGroup</strong>:</p><ul><li>BeginSimulationEntityCommandBufferSystem</li><li>TransformSystemGroup<ul><li>ParentSystem</li><li>CopyTransformFromGameObjectSystem</li><li>TRSToLocalToWorldSystem</li><li>TRSToLocalToParentSystem</li><li>LocalToParentSystem</li><li>CopyTransformToGameObjectSystem</li></ul></li><li>LateSimulationSystemGroup</li><li>EndSimulationEntityCommandBufferSystem</li></ul><p><strong>PresentationSystemGroup</strong>:</p><ul><li>BeginPresentationEntityCommandBufferSystem</li><li>CreateMissingRenderBoundsFromMeshRenderer</li><li>RenderingSystemBootstrap</li><li>RenderBoundsUpdateSystem</li><li>RenderMeshSystem</li><li>LODGroupSystemV1</li><li>LodRequirementsUpdateSystem</li><li>EndPresentationEntityCommandBufferSystem</li></ul><p>请注意，此列表的具体内容可能会发生变化。</p><h2 id="多个世界"><a href="#多个世界" class="headerlink" title="多个世界"></a>多个世界</h2><p>您可以创建多个世界，也可以在多个世界中实例化相同的组件系统类。您还可以从更新顺序中的不同点以不同的速率更新每个实例。</p><p>您无法手动更新给定世界中的每个系统，但您可以控制在哪个世界中创建哪些系统，以及将它们添加到哪些现有系统组中。</p><p>例如，您可以创建一个实例化 <code>SystemX</code> 和 <code>SystemY</code> 的自定义世界，并将 <code>SystemX</code> 添加到默认世界的 <code>SimulationSystemGroup</code>，并将 <code>SystemY</code> 添加到默认世界的 <code>PresentationSystemGroup</code>。这些系统可以像往常一样相对于它们的同级组对自己进行排序，Unity 会更新它们以及相应的组。</p><p>您还可以使用 <code>ICustomBootstrap</code> 接口来管理多个世界中的系统：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public interface ICustomBootstrap&#123;    &#x2F;&#x2F; 返回应由默认引导程序处理的系统。    &#x2F;&#x2F; 如果返回 null，则根本不会创建默认世界。    &#x2F;&#x2F; 空列表创建默认世界和入口点    List&lt;Type&gt; Initialize(List&lt;Type&gt; systems);&#125;</code></pre></div></figure><p>当您实现此接口时，它会在默认世界初始化之前将组件系统类型的完整列表传递给 <code>Initialize()</code> 方法。自定义引导程序可以遍历此列表并在您定义的世界中创建系统。您可以从 <code>Initialize()</code> 方法返回系统列表，Unity 创建它们作为默认世界初始化的一部分。</p><p>例如，这是自定义 <code>MyCustomBootstrap.Initialize()</code> 实现的典型过程：</p><ol><li>创建任何其他世界及其顶级 <code>ComponentSystemGroups</code>。</li><li>对于系统类型列表中的每个类型：<ol><li>向上搜索 <code>ComponentSystemGroup</code> 层次结构以找到此系统类型的顶级组。</li><li>如果它是在步骤 1 中创建的组之一，则在该世界中创建系统并使用 <code>group.AddSystemToUpdateList()</code> 将其添加到层次结构中。</li><li>如果不是，则将此类型附加到列表以返回到 <code>DefaultWorldInitialization</code>。</li></ol></li><li>在新的顶级组上调用 <code>group.SortSystemUpdateList()</code>。<ol><li>可选择将它们添加到默认世界组之一</li></ol></li><li>将未处理系统的列表返回给 <code>DefaultWorldInitialization</code>。</li></ol><div class="note note-info">            <p>ECS 框架通过反射找到您的 <code>ICustomBootstrap</code> 实现。</p>          </div><h1 id="使用作业在多个线程上调度数据"><a href="#使用作业在多个线程上调度数据" class="headerlink" title="使用作业在多个线程上调度数据"></a>使用作业在多个线程上调度数据</h1><p>实体包和 Unity 的 DOTS 架构广泛使用 C# 作业系统。只要有可能，您应该在系统代码中使用作业。</p><p><code>SystemBase</code> 类提供 <code>Entities.ForEach</code> 和 <code>Job.WithCode</code> 以将应用程序的逻辑实现为多线程代码。在更复杂的情况下，您可以使用 <code>IJobEntityBatch</code> 的 <code>Schedule()</code> 和 <code>ScheduleParallel()</code> 方法在主线程之外转换数据。 <code>Entities.ForEach</code> 使用起来最简单，通常需要较少的代码来实现。</p><p>ECS 按照您的系统所在的顺序在主线程上安排作业。当您安排作业时，ECS 会跟踪哪些作业读取和写入哪些组件。读取组件的作业依赖于写入同一组件的任何先前计划的作业，反之亦然。作业调度程序使用作业依赖关系来确定哪些作业可以并行运行，哪些作业必须按顺序运行。</p><p>例如，以下系统更新位置：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">using Unity.Burst;using Unity.Collections;using Unity.Entities;using Unity.Jobs;using Unity.Transforms;public class MovementSpeedSystem : SystemBase&#123;    &#x2F;&#x2F; OnUpdate runs on the main thread.    protected override void OnUpdate()    &#123;        Entities            .ForEach((ref Translation position, in MovementSpeed speed) &#x3D;&gt;                &#123;                    float3 displacement &#x3D; speed.Value * dt;                    position &#x3D; new Translation()&#123;                            Value &#x3D; position.Value + displacement                        &#125;;                &#125;)            .ScheduleParallel();    &#125;&#125;</code></pre></div></figure><h2 id="工作扩展"><a href="#工作扩展" class="headerlink" title="工作扩展"></a>工作扩展</h2><p>Unity C# 作业系统允许您在多个线程上运行代码。该系统提供调度、并行处理和多线程安全。作业系统是一个核心 Unity 模块，它提供通用接口和类来创建和运行作业，无论您是否使用 Entities 包。</p><p>这些接口包括：</p><ul><li><code>IJob</code>：创建一个在任何线程或内核上运行的作业，由作业系统调度程序确定。</li><li><code>IJobParallelFor</code>：创建一个可以在多个线程上并行运行的作业，以处理 <code>NativeContainer</code> 的元素。</li><li><code>IJobExtensions</code>：提供运行 <code>IJob</code> 作业的扩展方法。</li><li><code>IJobParallelForExtensions</code>：提供运行 <code>IJobParallelFor</code> 作业的扩展方法。</li><li><code>JobHandle</code>：访问计划作业的句柄。您还可以使用 <code>JobHandle</code> 实例来指定作业之间的依赖关系。</li></ul><p>有关作业系统的概述，请参阅 Unity 用户手册中的 C# 作业系统。</p><p>Jobs 包扩展了作业系统以支持 ECS。它包含：</p><ul><li><code>IJobParallelForDeferExtensions</code></li><li><code>IJobFilter</code></li><li><code>JobParallelIndexListExtensions</code></li><li><code>Job​Struct​Produce&lt;T&gt;</code></li></ul><h2 id="通用职位"><a href="#通用职位" class="headerlink" title="通用职位"></a>通用职位</h2><p>在 C# 中，您可以使用继承和接口使一段代码适用于一系列类型。例如：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">&#x2F;&#x2F; 该方法不仅限于一种输入，而是任何实现 IBlendable 的类型。void foo(IBlendable a) &#123;...&#125;</code></pre></div></figure><p>在 Burst 编译器使用的高性能 C# (HPC#) 中，您不能使用托管类型或虚拟方法调用，因此泛型是使一段代码在一系列类型上运行的选项：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">&#x2F;&#x2F; 他的方法可以对任何 IBlendable 结构进行操作（并且可以调用IBlendable 方法），但不需要托管对象或虚拟方法调用。void foo&lt;T&gt;(T a) where T : struct, IBlendable &#123;...&#125;</code></pre></div></figure><p>您必须在 HPC# 中编写作业，因此对于在一系列类型上运行的作业，它必须是通用的：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">[BurstCompile()]public struct BlendJob&lt;T&gt; : IJob    where T : struct, IBlendable&#123;    public NativeReference&lt;T&gt; blendable;    public void Execute()     &#123;        var val &#x3D; blendable.Value;        val.Blend();        blendable.Value &#x3D; val;    &#125;&#125;</code></pre></div></figure><h3 id="从-Burst-编译代码中调度通用作业"><a href="#从-Burst-编译代码中调度通用作业" class="headerlink" title="从 Burst 编译代码中调度通用作业"></a>从 Burst 编译代码中调度通用作业</h3><p>要从 Burst 编译代码安排通用作业，您需要作业具体专业化的反射数据。不幸的是，Unity 不会为所有具体的特化自动生成此反射，因此在某些情况下您必须手动注册它们：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">&#x2F;&#x2F; 此程序集属性允许同一程序集中的突发编译代码为 MyJob 安排具体的专业化 &lt;int, float&gt;。[assembly: RegisterGenericJobType(typeof(MyJob&lt;int, float&gt;))]</code></pre></div></figure><p>如果您尝试安排未在程序集中注册具体专业化的作业，则 Unity 会抛出异常。</p><p>注册类型的程序集无关紧要。例如，如果一个作业类型只在程序集 Foo 中注册，您也可以在程序集 Bar 中安排它。</p><p>如果您多次重复注册相同的具体专业化，则不会将其视为错误。</p><h3 id="具体作业类型的自动注册"><a href="#具体作业类型的自动注册" class="headerlink" title="具体作业类型的自动注册"></a>具体作业类型的自动注册</h3><p>当您直接实例化通用作业的具体特化时，Unity 会自动在程序集中注册该特化：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">&#x2F;&#x2F; Registers specialization &lt;int, float&gt; for MyJob in the assembly.var job &#x3D; new MyJob&lt;int, float&gt;();</code></pre></div></figure><p>但是，在间接实例化具体特化时，Unity 不会自动注册它：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">void makeJob&lt;T&gt;()&#123;    new MyJob&lt;T, float&gt;().Schedule();   &#125;void foo()&#123;    makeJob&lt;int&gt;();    &#x2F;&#x2F; does NOT register MyJob&lt;int, float&gt;&#125;</code></pre></div></figure><p>但是，如果您将通用作业作为返回类型或输出参数包含在签名中，则 Unity 会自动注册它：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">MyJob&lt;T, float&gt; makeJob&lt;T&gt;()&#123;    var j &#x3D; new MyJob&lt;T, float&gt;()    j.Schedule();       return j;&#125;void foo()&#123;    makeJob&lt;int&gt;();    &#x2F;&#x2F; registers MyJob&lt;int, float&gt;&#125;</code></pre></div></figure><p>您可以通过多级通用方法调用使用此间接注册作品：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">MyJob&lt;T, float&gt; makeJob&lt;T&gt;()&#123;    var j &#x3D; new MyJob&lt;T, float&gt;()    j.Schedule();       return j;&#125;void foo&lt;T&gt;()&#123;    makeJob&lt;T&gt;();    &#125;void bar()&#123;    foo&lt;int&gt;();       &#x2F;&#x2F; registers MyJob&lt;int, float&gt;&#125;</code></pre></div></figure><p>您还可以将通用作业嵌套在另一个类或结构中：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">struct BlendJobWrapper&lt;T&gt; where T : struct, IBlendable&#123;    public T blendable;    [BurstCompile()]    public struct BlendJob : IJob    &#123;        public T blendable;        public void Execute() &#123;...&#125;    &#125;    public JobHandle Schedule(JobHandle dep &#x3D; new JobHandle())    &#123;        return new BlendJob &#123; blendable &#x3D; blendable &#125;.Schedule(dep);    &#125;&#125;</code></pre></div></figure><p>在前面的示例中，如果 <code>BlendJobWrapper&lt;foo&gt;</code> 是自动或手动注册的，那么 <code>BlendJob&lt;foo&gt;</code> 也会有效注册。仅围绕一个通用作业的包装器类型并不能解决任何问题，但是当您同时使用多个通用作业时，这些包装器类型允许更优雅的作业创建和调度。</p><h3 id="作业化分拣"><a href="#作业化分拣" class="headerlink" title="作业化分拣"></a>作业化分拣</h3><p><code>NativeSortExtension</code> 类具有排序方法，包括使用作业进行排序的方法：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public unsafe static JobHandle Sort&lt;T, U&gt;(T* array, int length, U comp, JobHandle deps)    where T : unmanaged    where U : IComparer&lt;T&gt;&#123;    if (length &#x3D;&#x3D; 0)        return inputDeps;    var segmentSortJob &#x3D; new SegmentSort&lt;T, U&gt; &#123; Data &#x3D; array, Comp &#x3D; comp, Length &#x3D; length, SegmentWidth &#x3D; 1024 &#125;;    var segmentSortMergeJob &#x3D; new SegmentSortMerge&lt;T, U&gt; &#123; Data &#x3D; array, Comp &#x3D; comp, Length &#x3D; length, SegmentWidth &#x3D; 1024 &#125;;    var segmentCount &#x3D; (length + 1023) &#x2F; 1024;    var workerSegmentCount &#x3D; segmentCount &#x2F; math.max(1, JobsUtility.MaxJobThreadCount);    var handle &#x3D; segmentSortJob.Schedule(segmentCount, workerSegmentCount, deps);    return segmentSortMergeJob.Schedule(segmentSortJobHandle);&#125;</code></pre></div></figure><p>在此示例中，排序分为两个作业：第一个作业将数组拆分为多个子部分，然后分别对它们进行并行排序。第二个作业等待第一个，然后将这些排序的子部分合并为最终的排序结果。</p><p>但是，此方法不会自动注册两个通用作业 SegmentSort 和 SegmentSortMerge 的具体特化，因为这两种类型都未用作方法的返回类型或输出参数。</p><p>一种解决方案是将两个作业都放入参数中：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public unsafe static JobHandle Sort&lt;T, U&gt;(T* array, int length, U comp, JobHandle deps        out SegmentSort&lt;T, U&gt; segmentSortJob, out SegmentSortMerge&lt;T, U&gt; segmentSortMergeJob)    where T : unmanaged    where U : IComparer&lt;T&gt;&#123;    if (length &#x3D;&#x3D; 0)        return inputDeps;    segmentSortJob &#x3D; new SegmentSort&lt;T, U&gt; &#123; Data &#x3D; array, Comp &#x3D; comp, Length &#x3D; length, SegmentWidth &#x3D; 1024 &#125;;    segmentSortMergeJob &#x3D; new SegmentSortMerge&lt;T, U&gt; &#123; Data &#x3D; array, Comp &#x3D; comp, Length &#x3D; length, SegmentWidth &#x3D; 1024 &#125;;    var segmentCount &#x3D; (length + 1023) &#x2F; 1024;    var workerSegmentCount &#x3D; segmentCount &#x2F; math.max(1, JobsUtility.MaxJobThreadCount);    var handle &#x3D; segmentSortJob.Schedule(segmentCount, workerSegmentCount, deps);    return segmentSortMergeJob.Schedule(segmentSortJobHandle);&#125;</code></pre></div></figure><p>然而，这解决了注册问题，但是您随后必须传递参数以获得您可能不想要的两个作业结构。</p><p>更好的解决方案是将两种作业类型包装在一个包装器类型中：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">unsafe struct SortJob&lt;T, U&gt; :    where T : unamanged    where U : IComparer&lt;T&gt;&#123;    public T* data;    public U comparer;    public int length;    unsafe struct SegmentSort : IJobParallelFor    &#123;        [NativeDisableUnsafePtrRestriction]        public T* data;        public U comp;        public int length;        public int segmentWidth;        public void Execute(int index) &#123;...&#125;    &#125;    unsafe struct SegmentSortMerge : IJob    &#123;        [NativeDisableUnsafePtrRestriction]        public T* data;        public U comp;        public int length;        public int segmentWidth;        public void Execute() &#123;...&#125;    &#125;    public JobHandle Schedule(JobHandle dep &#x3D; new JobHandle())    &#123;        if (length &#x3D;&#x3D; 0)            return inputDeps;        var segmentSortJob &#x3D; new SegmentSort&lt;T, U&gt; &#123; Data &#x3D; array, Comp &#x3D; comp, Length &#x3D; length, SegmentWidth &#x3D; 1024 &#125;;        var segmentSortMergeJob &#x3D; new SegmentSortMerge&lt;T, U&gt; &#123; Data &#x3D; array, Comp &#x3D; comp, Length &#x3D; length, SegmentWidth &#x3D; 1024 &#125;;        var segmentCount &#x3D; (length + 1023) &#x2F; 1024;        var workerSegmentCount &#x3D; segmentCount &#x2F; math.max(1, JobsUtility.MaxJobThreadCount);        var handle &#x3D; segmentSortJob.Schedule(segmentCount, workerSegmentCount, deps);        return segmentSortMergeJob.Schedule(segmentSortJobHandle);    &#125;&#125;</code></pre></div></figure><p>在这种安排中，您可以创建 SortJob 的实例并调用其 Schedule() 方法，而不是调用 Sort() 方法。通过对 SortJob 进行具体实例化，您还可以自动注册所需的 SegmentSort 和 SegmentSortMerge 具体特化。</p><p>这种嵌套通用作业的模式启用了一个方便的 API，可以将相关的通用作业集安排在一起。</p><h2 id="使用-Job-WithCode-安排后台作业"><a href="#使用-Job-WithCode-安排后台作业" class="headerlink" title="使用 Job.WithCode 安排后台作业"></a>使用 Job.WithCode 安排后台作业</h2><p>SystemBase 类中的 Job.WithCode 构造将方法作为单个后台作业运行。您还可以在主线程上运行 Job.WithCode 并利用 Burst 编译来加快执行速度。</p><h3 id="使用-Job-WithCode"><a href="#使用-Job-WithCode" class="headerlink" title="使用 Job.WithCode"></a>使用 Job.WithCode</h3><p>以下示例使用一个 Job.WithCode lambda 表达式用随机数填充native array，并使用另一个作业将这些数字加在一起：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public partial class RandomSumJob : SystemBase&#123;    private uint seed &#x3D; 1;    protected override void OnUpdate()    &#123;        Random randomGen &#x3D; new Random(seed++);        NativeArray&lt;float&gt; randomNumbers            &#x3D; new NativeArray&lt;float&gt;(500, Allocator.TempJob);        Job.WithCode(() &#x3D;&gt;        &#123;            for (int i &#x3D; 0; i &lt; randomNumbers.Length; i++)            &#123;                randomNumbers[i] &#x3D; randomGen.NextFloat();            &#125;        &#125;).Schedule();        &#x2F;&#x2F; To get data out of a job, you must use a NativeArray        &#x2F;&#x2F; even if there is only one value        NativeArray&lt;float&gt; result            &#x3D; new NativeArray&lt;float&gt;(1, Allocator.TempJob);        Job.WithCode(() &#x3D;&gt;        &#123;            for (int i &#x3D; 0; i &lt; randomNumbers.Length; i++)            &#123;                result[0] +&#x3D; randomNumbers[i];            &#125;        &#125;).Schedule();        &#x2F;&#x2F; This completes the scheduled jobs to get the result immediately, but for        &#x2F;&#x2F; better efficiency you should schedule jobs early in the frame with one        &#x2F;&#x2F; system and get the results late in the frame with a different system.        this.CompleteDependency();        UnityEngine.Debug.Log(&quot;The sum of &quot;            + randomNumbers.Length + &quot; numbers is &quot; + result[0]);        randomNumbers.Dispose();        result.Dispose();    &#125;&#125;</code></pre></div></figure><p>要运行并行作业，请实施 IJobFor。您可以使用 ScheduleParallel() 在系统的 OnUpdate() 函数中安排并行作业。</p><h3 id="捕获变量-1"><a href="#捕获变量-1" class="headerlink" title="捕获变量"></a>捕获变量</h3><p>您不能将参数传递给 Job.WithCode lambda 表达式或返回值。相反，您必须在系统的 OnUpdate() 函数中捕获局部变量。</p><p>如果您使用 Schedule() 来安排您的作业在 Unity 的作业系统中运行，则还有其他限制：</p><ul><li>您必须将捕获的变量声明为 NativeArray、NativeContainer或 blittable 类型。</li><li>要返回数据，您必须将返回值写入捕获的native array，即使数据是单个值也是如此。但是，如果您使用 Run() 来执行作业，则可以写入任何捕获的变量。<br>Job.WithCode 有一组方法将只读和安全属性应用于捕获的NativeContainer的变量。例如，您可以使用 WithReadOnly 将对变量的访问限制为只读。您还可以使用 WithDisposeOnCompletion 在作业完成后自动释放容器。有关详细信息，请参阅 Job.WithCode 文档的捕获变量部分。</li></ul><h3 id="执行-Job-WithCode-lambda-表达式"><a href="#执行-Job-WithCode-lambda-表达式" class="headerlink" title="执行 Job.WithCode lambda 表达式"></a>执行 Job.WithCode lambda 表达式</h3><p>要执行 Job.WithCode lambda 表达式，您可以使用以下命令：</p><ul><li>Schedule()：将方法作为单个非并行作业执行。安排作业在后台线程上运行代码并更好地利用所有可用的 CPU 资源。您可以显式地将 JobHandle 传递给 Schedule()，或者，如果您不传递任何依赖项，系统会假定当前系统的 Dependency 属性表示作业的依赖项。或者，如果作业没有依赖项，您可以传入一个新的 JobHandle。</li><li>Run()：在主线程上执行方法。您可以 Burst 编译 Job.WithCode，因此如果您使用 Run() 来执行代码，即使它在主线程上运行也会更快。当你调用 Run() 时，Unity 会自动完成 Job.WithCode 构造的所有依赖。</li></ul><h3 id="依赖关系-1"><a href="#依赖关系-1" class="headerlink" title="依赖关系"></a>依赖关系</h3><p>默认情况下，系统使用其 Dependency 属性来管理其依赖项。系统按照它们在 OnUpdate() 方法中出现的顺序将您创建的每个 <code>Entities.ForEach</code> 和 Job.WithCode 作业添加到依赖作业句柄。</p><p>要手动管理作业依赖性，请将 JobHandle 传递给 Schedule 方法，然后返回生成的依赖性。有关详细信息，请参阅依赖项 API 文档。</p><p>有关作业依赖性的一般信息，请参阅有关作业依赖性的文档。</p><h2 id="作业依赖"><a href="#作业依赖" class="headerlink" title="作业依赖"></a>作业依赖</h2><p>Unity根据系统读写的ECS组件分析各个系统的数据依赖关系。如果在帧中较早更新的系统读取较晚系统写入的数据，或写入较晚系统读取的数据，则第二个系统依赖于第一个系统。为了防止竞争条件，作业调度程序确保系统依赖的所有作业在运行该系统的作业之前已经完成。</p><h3 id="作业依赖更新顺序"><a href="#作业依赖更新顺序" class="headerlink" title="作业依赖更新顺序"></a>作业依赖更新顺序</h3><p>系统的 Dependency 属性是一个 JobHandle，表示系统的 ECS 相关依赖项。在 OnUpdate() 之前，Dependency 属性反映系统对先前作业的传入依赖项。默认情况下，系统会根据您在系统中安排作业时每个作业读取和写入的组件来更新依赖属性。</p><h4 id="覆盖默认顺序"><a href="#覆盖默认顺序" class="headerlink" title="覆盖默认顺序"></a>覆盖默认顺序</h4><p>要覆盖此默认行为，请使用 <code>Entities.ForEach</code> 和 Job.WithCode 的重载版本，它们将作业依赖项作为参数并将更新的依赖项作为 JobHandle 返回。当您使用这些构造的显式版本时，ECS 不会自动将作业句柄与系统的 Dependency 属性组合在一起。您必须在需要时手动组合它们。</p><p>Dependency 属性不跟踪作业可能对通过 NativeArray 或其他类似容器传递的数据的依赖关系。如果您在一个作业中编写 NativeArray，并在另一个作业中读取该数组，则必须手动将第一个作业的 JobHandle 添加为第二个作业的依赖项。您可以使用 JobHandle.CombineDependencies 来执行此操作。</p><h4 id="Entities-ForEach-的作业依赖顺序"><a href="#Entities-ForEach-的作业依赖顺序" class="headerlink" title="Entities.ForEach 的作业依赖顺序"></a><code>Entities.ForEach</code> 的作业依赖顺序</h4><p>当您调用 Entities.ForEach.Run() 时，作业计划程序会在开始 <code>ForEach</code> 迭代之前完成系统依赖的所有计划作业。如果您还使用 WithStructuralChanges() 作为构造的一部分，则作业调度程序将完成所有正在运行和已调度的作业。结构更改还会使对组件数据的任何直接引用无效。有关详细信息，请参阅有关结构更改的文档。</p><h3 id="更多资源"><a href="#更多资源" class="headerlink" title="更多资源"></a>更多资源</h3><ul><li>JobHandle 和依赖项</li><li>Unity的工作系统</li></ul><h1 id="使用-EntityQuery-查询数据-1"><a href="#使用-EntityQuery-查询数据-1" class="headerlink" title="使用 EntityQuery 查询数据"></a>使用 EntityQuery 查询数据</h1><p>EntityQuery 查找具有一组指定组件类型的原型。然后它将原型的块收集到一个系统可以处理的数组中。</p><p>例如，如果查询匹配组件类型 A 和 B，则查询会收集具有这两种组件类型的所有原型的块，而不管这些原型可能具有的任何其他组件类型。因此，具有组件类型 A、B 和 C 的原型将匹配查询。</p><p>您可以使用 EntityQuery 执行以下操作：</p><p>运行作业以处理选定的实体和组件<br>获取包含所有选定实体的 NativeArray<br>按组件类型获取所选组件的 NativeArray<br>EntityQuery 返回的实体和组件数组是并行的。这意味着相同的索引值始终适用于任何数组中的相同实体。</p><h2 id="创建实体查询"><a href="#创建实体查询" class="headerlink" title="创建实体查询"></a>创建实体查询</h2><p>要创建实体查询，您可以将组件类型传递给 EntityQueryBuilder 帮助器类型。以下示例定义了一个 EntityQuery，它查找所有同时具有 ObjectRotation 和 ObjectRotationSpeed 组件的实体：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">EntityQuery query &#x3D; new EntityQueryBuilder(Allocator.Temp)    .WithAllRW&lt;ObjectRotation&gt;()    .WithAll&lt;ObjectRotationSpeed&gt;()    .Build(this);</code></pre></div></figure><p>查询使用 EntityQueryBuilder.WithAllRW<T> 来显示系统写入 ObjectRotation。如果可能，您应该始终指定只读访问权限，因为对数据的读取访问权限的限制较少。这有助于作业调度程序更有效地执行作业。</T></p><h3 id="指定系统选择的原型"><a href="#指定系统选择的原型" class="headerlink" title="指定系统选择的原型"></a>指定系统选择的原型</h3><p>查询将仅匹配包含您指定组件的原型。可以使用三种不同的 EntityQueryBuilder 方法指定组件：</p><ul><li>WithAll<T>()：这些组件是必需的。为了匹配查询，原型必须包含查询的所有必需组件。</T></li><li>WithAny<T>()：这些组件是可选的。为了匹配查询，原型必须至少包含一个查询的可选组件。</T></li><li>WithNone<T>()：排除这些组件。为了匹配查询，原型不得包含任何查询的排除组件。<br>例如，以下查询包括包含 ObjectRotation 和 ObjectRotationSpeed 组件的原型，但不包括包含 Static 组件的任何原型：<figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">EntityQuery query &#x3D; new EntityQueryBuilder(Allocator.Temp)    .WithAllRW&lt;ObjectRotation&gt;()    .WithAll&lt;ObjectRotationSpeed&gt;()    .WithNone&lt;Static&gt;()    .Build(this);</code></pre></div></figure></T></li></ul><div class="note note-info">            <p>要处理可选组件，请使用 ArchetypeChunk.Has<T> 方法来确定块是否包含可选组件。这是因为同一块中的所有实体都具有相同的组件，因此您只需检查每个块是否存在可选组件一次：而不是每个实体一次。</T></p>          </div><p>您可以使用 EntityQueryBuilder.WithOptions() 来查找专门的原型。例如：</p><ul><li>IncludePrefab：包括包含 Prefab 标签组件的原型。</li><li>IncludeDisabledEntities：包括包含 Disabled 标签组件的原型。</li><li>FilterWriteGroup：仅包含 WriteGroup 中明确包含在查询中的组件的实体。排除具有来自同一 WriteGroup 的任何其他组件的实体。<br>有关选项的完整列表，请参阅 EntityQueryOptions。</li></ul><h4 id="按写入组过滤"><a href="#按写入组过滤" class="headerlink" title="按写入组过滤"></a>按写入组过滤</h4><p>在以下示例中，LuigiComponent 和 MarioComponent 是基于 CharacterComponent 组件的同一 WriteGroup 中的组件。此查询使用需要 CharacterComponent 和 MarioComponent 的 FilterWriteGroup 选项：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public struct CharacterComponent : IComponentData &#123; &#125;[WriteGroup(typeof(CharacterComponent))]public struct LuigiComponent : IComponentData &#123; &#125;[WriteGroup(typeof(CharacterComponent))]public struct MarioComponent : IComponentData &#123; &#125;[RequireMatchingQueriesForUpdate]public partial class ECSSystem : SystemBase&#123;    protected override void OnCreate()    &#123;        var query &#x3D; new EntityQueryBuilder(Allocator.Temp)            .WithAllRW&lt;CharacterComponent&gt;()            .WithAll&lt;MarioComponent&gt;()            .WithOptions(EntityQueryOptions.FilterWriteGroup)            .Build(this);    &#125;    protected override void OnUpdate()    &#123;        throw new NotImplementedException();    &#125;&#125;</code></pre></div></figure><p>此查询排除任何同时具有 LuigiComponent 和 MarioComponent 的实体，因为 LuigiComponent 未明确包含在查询中。</p><p>这比 None 字段更有效，因为您不需要更改其他系统使用的查询，只要它们也使用写组。</p><p>您可以使用写入组来扩展现有系统。例如，如果您在另一个系统中将 CharacterComponent 和 LuigiComponent 定义为不受您控制的库的一部分，则可以将 MarioComponent 与 LuigiComponent 放在同一写入组中，以更改 CharacterComponent 的更新方式。然后，对于您添加到 MarioComponent 的任何实体，系统都会更新 CharacterComponent，但原始系统不会更新它。对于没有 MarioComponent 的实体，原始系统像以前一样更新 CharacterComponent。有关详细信息，请参阅有关写入组的文档。</p><h2 id="定义过滤器"><a href="#定义过滤器" class="headerlink" title="定义过滤器"></a>定义过滤器</h2><p>要进一步对实体进行排序，您可以使用过滤器根据以下内容排除实体：</p><p>共享组件过滤器：根据共享组件的特定值过滤实体集。<br>更改过滤器：根据特定组件类型的值是否已更改来过滤实体集。<br>在您对查询对象调用 ResetFilter 之前，您设置的过滤器一直有效。</p><p>要忽略查询的活动块过滤器，请使用名称以 IgnoreFilter 结尾的 EntityQuery 方法。这些方法通常比过滤等效方法更有效。例如，请参阅 IsEmpty 与 IsEmptyIgnoreFilter。</p><h3 id="使用共享组件过滤器"><a href="#使用共享组件过滤器" class="headerlink" title="使用共享组件过滤器"></a>使用共享组件过滤器</h3><p>要使用共享组件筛选器，请在 EntityQuery 中包含共享组件以及任何其他需要的组件，然后调用 SetSharedComponentFilter 方法。然后传入包含要选择的值的相同 ISharedComponent 类型的结构。所有值都必须匹配。您最多可以向过滤器添加两个不同的共享组件。</p><p>您可以随时更改筛选器，但如果您更改筛选器，它不会更改您从组 ToComponentDataArray<T> 或 ToEntityArray 方法接收的任何现有实体或组件数组。您必须重新创建这些数组。</T></p><p>以下示例定义了一个名为 SharedGrouping 的共享组件和一个仅处理组字段设置为 1 的实体的系统。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">struct SharedGrouping : ISharedComponentData&#123;    public int Group;&#125;[RequireMatchingQueriesForUpdate]partial class ImpulseSystem : SystemBase&#123;    EntityQuery query;    protected override void OnCreate()    &#123;        query &#x3D; new EntityQueryBuilder(Allocator.Temp)            .WithAllRW&lt;ObjectPosition&gt;()            .WithAll&lt;Displacement, SharedGrouping&gt;()            .Build(this);    &#125;    protected override void OnUpdate()    &#123;        &#x2F;&#x2F; Only iterate over entities that have the SharedGrouping data set to 1        query.SetSharedComponentFilter(new SharedGrouping &#123; Group &#x3D; 1 &#125;);        var positions &#x3D; query.ToComponentDataArray&lt;ObjectPosition&gt;(Allocator.Temp);        var displacements &#x3D; query.ToComponentDataArray&lt;Displacement&gt;(Allocator.Temp);        for (int i &#x3D; 0; i &lt; positions.Length; i++)            positions[i] &#x3D; new ObjectPosition            &#123;                Value &#x3D; positions[i].Value + displacements[i].Value            &#125;;    &#125;&#125;</code></pre></div></figure><h3 id="使用更改过滤器"><a href="#使用更改过滤器" class="headerlink" title="使用更改过滤器"></a>使用更改过滤器</h3><p>如果您只需要在组件值更改时更新实体，请使用 SetChangedVersionFilter 方法将该组件添加到 EntityQuery 过滤器。例如，以下 EntityQuery 仅包含来自另一个系统已写入 Translation 组件的块的实体：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">EntityQuery query;protected override void OnCreate()&#123;    query &#x3D; new EntityQueryBuilder(Allocator.Temp)        .WithAllRW&lt;LocalToWorld&gt;()        .WithAll&lt;ObjectPosition&gt;()        .Build(this);    query.SetChangedVersionFilter(typeof(ObjectPosition));&#125;</code></pre></div></figure><p>为了提高效率，更改过滤器适用于整个原型块，而不是单个实体。更改过滤器还只检查声明对组件进行写访问的系统是否已运行，而不检查它是否更改了任何数据。例如，如果可以写入该组件类型的另一个作业访问该块，则更改过滤器将包括该块中的所有实体。这就是为什么您应该始终声明对不需要修改的组件的只读访问权限。</p><h2 id="按启用组件过滤"><a href="#按启用组件过滤" class="headerlink" title="按启用组件过滤"></a>按启用组件过滤</h2><p>启用组件允许在运行时启用和禁用单个实体上的组件。禁用实体上的组件不会将该实体移动到新的原型中，但出于 EntityQuery 匹配的目的，实体将被视为没有组件。具体来说：</p><p>如果一个实体禁用了组件 T，它将不会匹配需要组件 T 的查询（使用 WithAll<T>()）。<br>如果实体禁用了组件 T，它将匹配排除组件 T 的查询（使用 WithNone<T>()）。<br>大多数 EntityQuery 操作（例如 ToEntityArray 和 CalculateEntityCount）会自动过滤掉其可启用组件会导致它们与查询不匹配的实体。要禁用此过滤，请使用这些操作的 IgnoreFilter 变体，或在查询创建时传递 EntityQueryOptions.IgnoreComponentEnabledState。</T></T></p><p>有关更多详细信息，请参阅启用的组件文档。</p><h2 id="合并查询"><a href="#合并查询" class="headerlink" title="合并查询"></a>合并查询</h2><p>要有效地将多个查询合并为一个，您可以创建一个包含多个查询描述的查询。生成的查询与匹配任何提供的查询描述的原型相匹配。本质上，组合查询匹配查询描述的并集。以下示例选择包含 ObjectRotation 组件或 ObjectRotationSpeed 组件（或两者）的任何原型：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">EntityQuery query &#x3D; new EntityQueryBuilder(Allocator.Temp)    .WithAllRW&lt;ObjectRotation&gt;()    &#x2F;&#x2F; Start a new query description    .AddAdditionalQuery()    .WithAllRW&lt;ObjectRotationSpeed&gt;()    .Build(this);</code></pre></div></figure><h2 id="执行查询"><a href="#执行查询" class="headerlink" title="执行查询"></a>执行查询</h2><p>通常，您在安排使用它的作业时执行实体查询。您还可以调用返回实体、组件或原型块数组的 EntityQuery 方法之一：</p><p>ToEntityArray：返回所选实体的数组。<br>ToComponentDataArray：返回所选实体的 T 类型组件的数组。<br>CreateArchetypeChunkArray：返回包含所选实体的所有块。因为查询对原型、共享组件值和更改过滤器进行操作，这些对于块中的所有实体都是相同的，所以存储在返回的块集中的实体集与 ToEntityArray 返回的实体集相同。<br>上述方法的异步版本也可用，它安排一个作业来收集请求的数据。其中一些变体必须返回 NativeList 而不是 NativeArray 才能支持可启用的组件。请参阅 ToEntityListAsync、ToComponentDataListAsync 和 CreateArchetypeChunkArrayAsync。</p><h2 id="编辑器中的查询"><a href="#编辑器中的查询" class="headerlink" title="编辑器中的查询"></a>编辑器中的查询</h2><p>在编辑器中，以下图标代表查询： 。当您使用特定的实体窗口和检查器时，您会看到这一点。您还可以使用“查询”窗口查看与所选查询匹配的组件和实体。</p><h1 id="使用-EntityCommandBuffer-安排数据更改"><a href="#使用-EntityCommandBuffer-安排数据更改" class="headerlink" title="使用 EntityCommandBuffer 安排数据更改"></a>使用 EntityCommandBuffer 安排数据更改</h1><p>要对实体数据更改进行排队而不是立即执行更改，您可以使用 EntityCommandBuffer 结构，它创建一个线程安全的命令缓冲区。如果您想在作业完成时推迟任何结构更改，这将很有用。</p><h2 id="EntityCommandBuffer-方法"><a href="#EntityCommandBuffer-方法" class="headerlink" title="EntityCommandBuffer 方法"></a>EntityCommandBuffer 方法</h2><p>您可以使用 EntityCommandBuffer 中的方法来记录命令，这些方法反映了 EntityManager 的一些方法，例如：</p><ul><li>CreateEntity(EntityArchetype)：创建具有指定原型的新实体。</li><li>DestroyEntity(Entity)：销毁实体。</li><li>SetComponent<T>(Entity, T)：为实体上类型 T 的组件设置值。</T></li><li>AddComponent<T>(Entity)：将类型 T 的组件添加到实体。</T></li><li>RemoveComponent<T>(EntityQuery)：从匹配查询的所有实体中移除类型 T 的组件。<br>Unity 仅在调用主线程上的 Playback 方法时才将更改记录在 EntityCommandBuffer 中。如果您尝试在播放后记录任何对命令缓冲区的进一步更改，Unity 会抛出异常。</T></li></ul><p>EntityCommandBuffer 有一个作业安全句柄，类似于NativeContainer。如果您尝试对使用命令缓冲区的未完成计划作业执行以下任何操作，安全检查会抛出异常：</p><ul><li>通过其 AddComponent、Playback、Dispose 或其他方法访问 EntityCommandBuffer。</li><li>安排另一个访问相同 EntityCommandBuffer 的作业，除非新作业依赖于已安排的作业。</li></ul><h2 id="在单线程作业中使用-EntityCommandBuffer"><a href="#在单线程作业中使用-EntityCommandBuffer" class="headerlink" title="在单线程作业中使用 EntityCommandBuffer"></a>在单线程作业中使用 EntityCommandBuffer</h2><p>Unity 无法在作业中执行结构更改，因此您可以使用实体的命令缓冲区来推迟结构更改，直到 Unity 完成作业。例如：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">&#x2F;&#x2F; ... in a system update&#x2F;&#x2F; You don&#39;t specify a size because the buffer will grow as needed.EntityCommandBuffer ecb &#x3D; new EntityCommandBuffer(Allocator.TempJob);&#x2F;&#x2F; The ECB is captured by the &#96;ForEach&#96; job.&#x2F;&#x2F; Until completed, the job owns the ECB&#39;s job safety handle.Entities    .ForEach((Entity e, in FooComp foo) &#x3D;&gt;    &#123;        if (foo.Value &gt; 0)        &#123;            &#x2F;&#x2F; Record a command that will later add            &#x2F;&#x2F; BarComp to the entity.            ecb.AddComponent&lt;BarComp&gt;(e);        &#125;    &#125;).Schedule();this.Dependency.Complete();&#x2F;&#x2F; Now that the job is completed, you can enact the changes.&#x2F;&#x2F; Note that Playback can only be called on the main thread.ecb.Playback(this.EntityManager);&#x2F;&#x2F; You are responsible for disposing of any ECB you create.ecb.Dispose();</code></pre></div></figure><h2 id="在并行作业中使用-EntityCommandBuffer"><a href="#在并行作业中使用-EntityCommandBuffer" class="headerlink" title="在并行作业中使用 EntityCommandBuffer"></a>在并行作业中使用 EntityCommandBuffer</h2><p>如果要在并行作业中使用实体命令缓冲区，请使用 EntityCommandBuffer.ParallelWriter，它以线程安全的方式并发记录到命令缓冲区：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">EntityCommandBuffer ecb &#x3D; new EntityCommandBuffer(Allocator.TempJob);&#x2F;&#x2F; Methods of this writer record commands to &#x2F;&#x2F; the EntityCommandBuffer in a thread-safe way.EntityCommandBuffer.ParallelWriter parallelEcb &#x3D; ecb.AsParallelWriter();</code></pre></div></figure><blockquote><p>note<br>只有记录需要线程安全才能并发。回放在主线程上始终是单线程的。</p></blockquote><h3 id="确定性回放"><a href="#确定性回放" class="headerlink" title="确定性回放"></a>确定性回放</h3><p>因为命令的记录是跨线程拆分的，所以记录命令的顺序取决于作业调度，因此是不确定的。</p><p>确定性并不总是必不可少的，但产生确定性结果的代码更容易调试。还有一些网络场景需要在不同机器上获得一致的结果。但是，确定性会对性能产生影响，因此您可能需要在某些项目中接受不确定性。</p><p>您无法避免不确定的录制顺序，但可以通过以下方式确定命令的播放顺序：</p><ol><li>每个命令记录一个“排序键”int 作为第一个参数传递给每个命令方法。您必须调用 lambda 参数 entityInQueryIndex，否则 <code>Entities.ForEach</code> 将无法识别 int。</li><li>在播放时，在执行命令之前按命令的排序键对命令进行排序。<br>只要记录的排序键独立于调度，排序就可以确定播放顺序。</li></ol><p>在并行作业中，每个实体所需的排序键是一个数字，它与作业查询中的该实体具有固定且唯一的关联。</p><p>并行作业中提供的 entityInQueryIndex 值满足这些条件。在与作业查询匹配的原型块列表中，实体具有以下索引：</p><ul><li>第一个块的第一个实体有 entityInQueryIndex 0</li><li>第一个块的第二个实体有 entityInQueryIndex 1</li><li>第二个块的第一个实体有一个 entityInQueryIndex，它是第一个块的计数</li><li>第三个块的第一个实体有一个 entityInQueryIndex，它是前两个块的计数之和</li></ul><p>entityInQueryIndex 始终遵循此模式。</p><p>以下示例代码显示了并行作业中使用的实体命令缓冲区：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">&#x2F;&#x2F; ... in a system updateEntityCommandBuffer ecb &#x3D; new EntityCommandBuffer(Allocator.TempJob);&#x2F;&#x2F; We need to write to the ECB concurrently across threads.EntityCommandBuffer.ParallelWriter ecbParallel &#x3D; ecb.AsParallelWriter();&#x2F;&#x2F; The entityInQueryIndex is unique for each entity and will be&#x2F;&#x2F; consistent for each particular entity regardless of scheduling.Entities    .ForEach((Entity e, int entityInQueryIndex, in FooComp foo) &#x3D;&gt; &#123;        if (foo.Value &gt; 0)        &#123;            &#x2F;&#x2F; The first arg is the &#39;sort key&#39; recorded with the command.            ecbParallel.AddComponent&lt;BarComp&gt;(entityInQueryIndex, e);        &#125;    &#125;).Schedule();&#x2F;&#x2F; Playback is single-threaded as normal.this.Dependency.Complete();&#x2F;&#x2F; To ensure deterministic playback order,&#x2F;&#x2F; the commands are first sorted by their sort keys.ecb.Playback(this.EntityManager);ecb.Dispose();</code></pre></div></figure><h2 id="重用-EntityCommandBuffer-实例"><a href="#重用-EntityCommandBuffer-实例" class="headerlink" title="重用 EntityCommandBuffer 实例"></a>重用 EntityCommandBuffer 实例</h2><p>最好的做法是为每个作业提供自己的命令缓冲区。这是因为与在单个命令缓冲区中记录相同的命令相比，将一组命令记录到多个命令缓冲区的开销很小。</p><p>但是，您可以在非并行作业中重用相同的 EntityCommandBuffer，只要这些作业在调度中不重叠即可。如果您在并行作业中重复使用 EntityCommandBuffer 实例，这可能会导致播放中命令的意外排序顺序，除非每个作业的排序键位于不同的范围内。</p><h2 id="多重回放"><a href="#多重回放" class="headerlink" title="多重回放"></a>多重回放</h2><p>如果多次调用 Playback 方法，它会抛出异常。为避免这种情况，请使用 PlaybackPolicy.MultiPlayback 选项创建一个 EntityCommandBuffer 实例：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">&#x2F;&#x2F; ... in a system updateEntityCommandBuffer ecb &#x3D;        new EntityCommandBuffer(Allocator.TempJob, PlaybackPolicy.MultiPlayback);&#x2F;&#x2F; ... record commandsecb.Playback(this.EntityManager);&#x2F;&#x2F; Additional playbacks are OK because this ECB is MultiPlayback.ecb.Playback(this.EntityManager);ecb.Dispose();</code></pre></div></figure><p>如果您想重复生成一组实体，则多重播放很有用。为此，使用 EntityCommandBuffer 创建并配置一组新实体，然后重复播放以重新生成另一组匹配的实体。</p><h2 id="在主线程上使用-EntityCommandBuffer"><a href="#在主线程上使用-EntityCommandBuffer" class="headerlink" title="在主线程上使用 EntityCommandBuffer"></a>在主线程上使用 EntityCommandBuffer</h2><p>您可以在主线程上记录命令缓冲区更改。这在以下情况下很有用：</p><ul><li>延迟您的更改。</li><li>多次回放一组更改。</li><li>在一个统一的地方回放很多变化。这比将更改散布在框架的不同部分更有效。</li></ul><p>每个结构更改操作都会触发一个同步点，这意味着该操作必须等待部分或所有计划作业完成。如果将结构更改组合到命令缓冲区中，则帧的同步点会更少。</p><h2 id="使用-EntityCommandBufferSystem-自动播放和处理命令缓冲区"><a href="#使用-EntityCommandBufferSystem-自动播放和处理命令缓冲区" class="headerlink" title="使用 EntityCommandBufferSystem 自动播放和处理命令缓冲区"></a>使用 EntityCommandBufferSystem 自动播放和处理命令缓冲区</h2><p>您可以使用 EntityCommandBufferSystem 回放和处理命令缓冲区，而不是自己手动执行。去做这个：</p><ol><li>获取要进行播放的 EntityCommandBuffer 系统的实例。</li><li>通过系统创建一个 EntityCommandBuffer 实例。</li><li>安排一个将命令写入 EntityCommandBuffer 的作业。</li><li>注册系统完成的预定作业。</li></ol><p>例如：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">&#x2F;&#x2F; ... in a system&#x2F;&#x2F; Assume an EntityCommandBufferSystem exists named FooECBSystem.EntityCommandBufferSystem sys &#x3D;        this.World.GetExistingSystemManaged&lt;FooECBSystem&gt;();&#x2F;&#x2F; Create a command buffer that will be played back&#x2F;&#x2F; and disposed by MyECBSystem.EntityCommandBuffer ecb &#x3D; sys.CreateCommandBuffer();&#x2F;&#x2F; A &#96;ForEach&#96; with no argument to Schedule implicitly&#x2F;&#x2F; assigns its returned JobHandle to this.DependencyEntities    .ForEach((Entity e, in FooComp foo) &#x3D;&gt; &#123;        &#x2F;&#x2F; ... record to the ECB    &#125;).Schedule();&#x2F;&#x2F; Register the job so that it gets completed by the ECB system.sys.AddJobHandleForProducer(this.Dependency);</code></pre></div></figure><div class="note note-waring">            <p>不要手动回放和处置 EntityCommandBufferSystem 创建的 EntityCommandBuffer。 EntityCommandBufferSystem 为您完成这两件事。</p>          </div><p>在每次更新中，一个 EntityCommandBufferSystem：</p><ol><li>完成所有已注册的作业，以确保他们已完成录制）。</li><li>以创建它们的相同顺序播放通过系统创建的所有实体命令缓冲区。</li><li>处理 EntityCommandBuffer。</li></ol><h3 id="默认-EntityCommandBufferSystem-系统"><a href="#默认-EntityCommandBufferSystem-系统" class="headerlink" title="默认 EntityCommandBufferSystem 系统"></a>默认 EntityCommandBufferSystem 系统</h3><p>默认世界具有以下默认 EntityCommandBufferSystem 系统：</p><ul><li>BeginInitializationEntityCommandBufferSystem</li><li>EndInitializationEntityCommandBufferSystem</li><li>BeginSimulationEntityCommandBufferSystem</li><li>EndSimulationEntityCommandBufferSystem</li><li>BeginPresentationEntityCommandBufferSystem</li></ul><p>因为在 Unity 将渲染数据交给渲染器之后，帧中不会发生结构变化，所以没有 EndPresentationEntityCommandBufferSystem 系统。您可以改用 BeginInitializationEntityCommandBufferSystem：一帧的结尾是下一帧的开始。</p><p>这些更新在标准系统组的开头和结尾。有关详细信息，请参阅有关系统更新顺序的文档。</p><p>对于大多数用例，默认系统应该足够了，但如果需要，您可以创建自己的 EntityCommandBufferSystem：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">&#x2F;&#x2F; You should specify where exactly in the frame&#x2F;&#x2F; that the ECB system should update.[UpdateInGroup(typeof(SimulationSystemGroup))][UpdateAfter(typeof(FooSystem))]public class MyECBSystem : EntityCommandBufferSystem &#123;    &#x2F;&#x2F; This class is intentionally empty. There is generally no    &#x2F;&#x2F; reason to put any code in an EntityCommandBufferSystem.&#125;</code></pre></div></figure><h2 id="延迟实体"><a href="#延迟实体" class="headerlink" title="延迟实体"></a>延迟实体</h2><p>EntityCommandBuffer 方法 CreateEntity 和 Instantiate 记录创建实体的命令。这些方法只记录命令，不创建实体。因此，它们返回带有负索引的实体值，代表尚不存在的占位符实体。这些占位符实体值仅在同一 ECB 的记录命令中有意义。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">&#x2F;&#x2F; ... in a systemEntityCommandBuffer ecb &#x3D; new EntityCommandBuffer(Allocator.TempJob);Entity placeholderEntity &#x3D; ecb.CreateEntity();&#x2F;&#x2F; Valid to use placeholderEntity in later commands of same ECB.ecb.AddComponent&lt;FooComp&gt;(placeholderEntity);&#x2F;&#x2F; The real entity is created, and&#x2F;&#x2F; FooComp is added to the real entity.ecb.Playback(this.EntityManager);&#x2F;&#x2F; Exception! The placeholderEntity has no meaning outside&#x2F;&#x2F; the ECB which created it, even after playback.this.EntityManager.AddComponent&lt;BarComp&gt;(placeholderEntity);ecb.Dispose();</code></pre></div></figure><p>AddComponent、SetComponent 或 SetBuffer 命令中记录的值可能具有 Entity 字段。在回放中，Unity 将这些组件或缓冲区中的任何占位符实体值重新映射到相应的实际实体。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">&#x2F;&#x2F; ... in a systemEntityCommandBuffer ecb &#x3D; new EntityCommandBuffer(Allocator.TempJob);&#x2F;&#x2F; For all entities with a FooComp component...Entities    .WithAll&lt;FooComp&gt;()    .ForEach((Entity e) &#x3D;&gt;    &#123;        &#x2F;&#x2F; In playback, an actual entity will be created        &#x2F;&#x2F; that corresponds to this placeholder entity.        Entity placeholderEntity &#x3D; ecb.CreateEntity();        &#x2F;&#x2F; (Assume BarComp has an Entity field called TargetEnt.)        BarComp bar &#x3D; new BarComp &#123; TargetEnt &#x3D; placeholderEntity &#125;;        &#x2F;&#x2F; In playback, TargetEnt will be assigned the        &#x2F;&#x2F; actual Entity that corresponds to placeholderEntity.        ecb.AddComponent(e, bar);    &#125;).Run();&#x2F;&#x2F; After playback, each entity with FooComp now has a&#x2F;&#x2F; BarComp component whose TargetEnt references a new entity.ecb.Playback(this.EntityManager);ecb.Dispose();</code></pre></div></figure><h2 id="在-Entities-ForEach-方法中使用命令缓冲区"><a href="#在-Entities-ForEach-方法中使用命令缓冲区" class="headerlink" title="在 Entities.ForEach 方法中使用命令缓冲区"></a>在 <code>Entities.ForEach</code> 方法中使用命令缓冲区</h2><p>要在 <code>Entities.ForEach</code> 方法中使用命令缓冲区，请将 EntityCommandBuffer 参数传递给 lambda 表达式本身。仅支持一小部分 EntityCommandBuffer 方法，它们具有 [SupportedInEntitiesForEach] 属性：</p><ul><li>Entity Instantiate(实体实体)</li><li>void DestroyEntity(实体实体)</li><li>void AddComponent<T>(Entity e, T component) where T : unmanaged, IComponentData</T></li><li>void SetComponent<T>(Entity e, T component) where T : unmanaged, IComponentData</T></li><li>void RemoveComponent<T>(实体 e)<br>例如，以下代码执行此操作：</T></li></ul><ol><li>它检查每个实体以查看其 HealthLevel 是否为 0。</li><li>如果为真，它会记录一条销毁实体的命令。</li><li>它还指定 EndSimulationEntityCommandBufferSystem 应该回放命令。<figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public struct HealthLevel : IComponentData&#123;    public int Value;&#125;Entities    .WithDeferredPlaybackSystem&lt;EndSimulationEntityCommandBufferSystem&gt;    .ForEach(        (Entity entity, EntityCommandBuffer buffer, HealthLevel healthLevel) &#x3D;&gt;         &#123;            if (healthLevel &#x3D;&#x3D; 0)            &#123;                buffer.DestroyEntity(entity);            &#125;        &#125;    ).ScheduleParallel();</code></pre></div></figure>当您在 <code>ForEach()</code> 函数中使用这些方法中的任何一种时，编译器会在运行时生成创建、填充、回放和处理 EntityCommandBuffer 实例或 EntityCommandBuffer.ParallelWriter 实例所需的代码（如果调用了 ScheduleParallel()） .</li></ol><p>在 <code>ForEach()</code> 之外调用这些方法会导致异常。</p><h3 id="在-Entities-ForEach-中回放-EntityCommandBuffer"><a href="#在-Entities-ForEach-中回放-EntityCommandBuffer" class="headerlink" title="在 Entities.ForEach 中回放 EntityCommandBuffer"></a>在 <code>Entities.ForEach</code> 中回放 EntityCommandBuffer</h3><p>要将 EntityCommandBuffer 参数传递给 <code>ForEach()</code> 函数，您还必须调用以下方法之一来指定何时回放命令：</p><ul><li>延迟回放：调用 WithDeferredPlaybackSystem<T>()，其中 T 标识回放命令的实体命令缓冲区系统。它必须是从 EntityCommandBufferSystem 派生的类型。</T></li><li>立即回放：在 <code>ForEach()</code> 函数完成所有迭代后立即调用 WithImmediatePlayback() 执行实体命令。您只能将 WithImmediatePlayback() 与 Run() 一起使用。</li></ul><p>编译器自动生成代码来创建和处理任何 EntityCommandBuffer 实例。</p><h1 id="查找任意数据"><a href="#查找任意数据" class="headerlink" title="查找任意数据"></a>查找任意数据</h1><p>访问和修改数据的最有效方法是使用具有实体查询和作业的系统。这以最高效的方式利用 CPU 资源，内存缓存未命中率最低。理想情况下，您应该使用最有效、最快的路径来执行大量数据转换。但是，有时您可能需要在程序的任意位置访问任意实体的任意组件。</p><p>您可以在实体的 IComponentData 及其动态缓冲区中查找数据。您查找数据的方式取决于您的代码是使用 Entities.ForEach、IJobEntityBatch 作业，还是在系统中执行的主线程上的其他一些方法。</p><h2 id="在系统中查找实体数据"><a href="#在系统中查找实体数据" class="headerlink" title="在系统中查找实体数据"></a>在系统中查找实体数据</h2><p>要从系统的 <code>Entities.ForEach</code> 或 Job.WithCode 方法中查找存储在任意实体组件中的数据，请使用 GetComponent<T>(Entity)</T></p><p>例如，以下代码使用 GetComponent<T>(Entity) 获取 Target 组件，该组件具有标识要定位的实体的实体字段。然后它将跟踪实体旋转到它们的目标：</T></p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">[RequireMatchingQueriesForUpdate]public partial class TrackingSystem : SystemBase&#123;    protected override void OnUpdate()    &#123;        float deltaTime &#x3D; SystemAPI.Time.DeltaTime;        Entities            .ForEach((ref Rotation orientation,            in LocalToWorld transform,            in Target target) &#x3D;&gt;            &#123;                &#x2F;&#x2F; Check to make sure the target Entity still exists and has                &#x2F;&#x2F; the needed component                if (!HasComponent&lt;LocalToWorld&gt;(target.entity))                    return;                &#x2F;&#x2F; Look up the entity data                LocalToWorld targetTransform &#x3D; GetComponent&lt;LocalToWorld&gt;(target.entity);                float3 targetPosition &#x3D; targetTransform.Position;                &#x2F;&#x2F; Calculate the rotation                float3 displacement &#x3D; targetPosition - transform.Position;                float3 upReference &#x3D; new float3(0, 1, 0);                quaternion lookRotation &#x3D; quaternion.LookRotationSafe(displacement, upReference);                orientation.Value &#x3D; math.slerp(orientation.Value, lookRotation, deltaTime);            &#125;)            .ScheduleParallel();    &#125;&#125;</code></pre></div></figure><p>如果要访问存储在动态缓冲区中的数据，还需要在 <code>SystemBase</code> 的 OnUpdate 方法中声明一个 BufferLookup 类型的局部变量。然后，您可以在 lambda 表达式中捕获局部变量。例如：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public struct BufferData : IBufferElementData&#123;    public float Value;&#125;[RequireMatchingQueriesForUpdate]public partial class BufferLookupSystem : SystemBase&#123;    protected override void OnUpdate()    &#123;        BufferLookup&lt;BufferData&gt; buffersOfAllEntities            &#x3D; this.GetBufferLookup&lt;BufferData&gt;(true);        Entities            .ForEach((ref Rotation orientation,            in LocalToWorld transform,            in Target target) &#x3D;&gt;            &#123;                &#x2F;&#x2F; Check to make sure the target Entity with this buffer type still exists                if (!buffersOfAllEntities.HasBuffer(target.entity))                    return;                &#x2F;&#x2F; Get a reference to the buffer                DynamicBuffer&lt;BufferData&gt; bufferOfOneEntity &#x3D; buffersOfAllEntities[target.entity];                &#x2F;&#x2F; Use the data in the buffer                float avg &#x3D; 0;                for (var i &#x3D; 0; i &lt; bufferOfOneEntity.Length; i++)                &#123;                    avg +&#x3D; bufferOfOneEntity[i].Value;                &#125;                if (bufferOfOneEntity.Length &gt; 0)                    avg &#x2F;&#x3D; bufferOfOneEntity.Length;            &#125;)            .ScheduleParallel();    &#125;&#125;</code></pre></div></figure><h2 id="在作业中查找实体数据"><a href="#在作业中查找实体数据" class="headerlink" title="在作业中查找实体数据"></a>在作业中查找实体数据</h2><p>要在 IJobEntityBatch 等作业结构中随机访问组件数据，请使用以下类型之一：</p><ul><li>组件查找</li><li>缓冲区查找<br>这些类型获得一个类似于数组的组件接口，由 Entity 对象索引。</li></ul><p>要使用它们，请声明一个类型为 ComponentLookup 或 BufferLookup 的字段，设置该字段的值，然后安排作业。</p><p>例如，您可以使用 ComponentLookup 字段来查找实体的世界位置：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">[ReadOnly]public ComponentLookup&lt;LocalToWorld&gt; EntityPositions;</code></pre></div></figure><div class="note note-info">            <p>此声明使用 ReadOnly 属性。您应该始终将 ComponentLookup 对象声明为只读，除非您想写入您访问的组件。</p>          </div><p>以下示例说明了如何设置数据字段和安排作业：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">protected override void OnUpdate()&#123;    var job &#x3D; new ChaserSystemJob();    &#x2F;&#x2F; Set non-ECS data fields    job.deltaTime &#x3D; SystemAPI.Time.DeltaTime;    &#x2F;&#x2F; Schedule the job using Dependency property    Dependency &#x3D; job.ScheduleParallel(query, this.Dependency);&#125;</code></pre></div></figure><p>要查找组件的值，请在作业的 Execute 方法中使用实体对象：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">                float3 targetPosition &#x3D; entityPosition.Position;if !ENABLE_TRANSFORM_V1                float3 chaserPosition &#x3D; transform.Value.Position;else                float3 chaserPosition &#x3D; position.Value;endif                float3 displacement &#x3D; targetPosition - chaserPosition;                float3 newPosition &#x3D; chaserPosition + displacement * deltaTime;if !ENABLE_TRANSFORM_V1                transform.Value.Position &#x3D; newPosition;else                position &#x3D; new Translation &#123; Value &#x3D; newPosition &#125;;endif</code></pre></div></figure><p>以下完整示例显示了一个系统，该系统将具有目标字段的实体移动到目标的当前位置：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">    [RequireMatchingQueriesForUpdate]    public partial class MoveTowardsEntitySystem : SystemBase    &#123;        private EntityQuery query;        [BurstCompile]        private partial struct MoveTowardsJob : IJobEntity        &#123;            &#x2F;&#x2F; Read-only data stored (potentially) in other chunks            [ReadOnly]            public ComponentLookup&lt;LocalToWorld&gt; EntityPositions;            &#x2F;&#x2F; Non-entity data            public float deltaTime;if !ENABLE_TRANSFORM_V1            public void Execute(ref LocalToWorldTransform transform, in Target target, in LocalToWorld entityPosition)else            public void Execute(Translation position, in Target target, in LocalToWorld entityPosition)endif            &#123;                &#x2F;&#x2F; Get the target Entity object                Entity targetEntity &#x3D; target.entity;                &#x2F;&#x2F; Check that the target still exists                if (!EntityPositions.HasComponent(targetEntity))                    return;                &#x2F;&#x2F; Update translation to move the chasing enitity toward the target                float3 targetPosition &#x3D; entityPosition.Position;if !ENABLE_TRANSFORM_V1                float3 chaserPosition &#x3D; transform.Value.Position;                float3 displacement &#x3D; targetPosition - chaserPosition;                transform.Value.Position &#x3D; chaserPosition + displacement * deltaTime;else                float3 chaserPosition &#x3D; position.Value;                float3 displacement &#x3D; targetPosition - chaserPosition;                position &#x3D; new Translation                &#123;                    Value &#x3D; chaserPosition + displacement * deltaTime                &#125;;endif            &#125;        &#125;        protected override void OnCreate()        &#123;            &#x2F;&#x2F; Select all entities that have Translation and Target Component            query &#x3D; this.GetEntityQuery                (if !ENABLE_TRANSFORM_V1                    typeof(LocalToWorldTransform),else                    typeof(Translation),endif                    ComponentType.ReadOnly&lt;Target&gt;()                );        &#125;        protected override void OnUpdate()        &#123;            &#x2F;&#x2F; Create the job            var job &#x3D; new MoveTowardsJob();            &#x2F;&#x2F; Set the component data lookup field            job.EntityPositions &#x3D; GetComponentLookup&lt;LocalToWorld&gt;(true);            &#x2F;&#x2F; Set non-ECS data fields            job.deltaTime &#x3D; SystemAPI.Time.DeltaTime;            &#x2F;&#x2F; Schedule the job using Dependency property            Dependency &#x3D; job.ScheduleParallel(query, Dependency);        &#125;    &#125;</code></pre></div></figure><h2 id="数据访问错误"><a href="#数据访问错误" class="headerlink" title="数据访问错误"></a>数据访问错误</h2><p>如果您查找的数据与您要在作业中读取和写入的数据重叠，则随机访问可能会导致竞争条件。</p><p>如果您确定要直接读取或写入的实体数据与要随机读取或写入的特定实体数据之间没有重叠，则可以使用 NativeDisableParallelForRestriction 属性标记访问器对象。</p><h1 id="写入组"><a href="#写入组" class="headerlink" title="写入组"></a>写入组</h1><p>写入组为一个系统提供了一种覆盖另一个系统的机制，即使您无法更改另一个系统。</p><p>一种常见的 ECS 模式是系统读取一组输入组件并将其写入另一个组件作为其输出。但是，您可能希望覆盖系统的输出，并使用基于不同输入集的不同系统来更新输出组件。</p><p>目标组件类型的写入组由 ECS 将 WriteGroup 属性应用于的所有其他组件类型组成，并以该目标组件类型作为参数。作为系统创建者，您可以使用写入组，以便您的系统用户可以排除您的系统将以其他方式选择和处理的实体。这种过滤机制允许系统用户根据自己的逻辑为排除的实体更新组件，同时让您的系统在其余部分照常运行。</p><h2 id="使用写组"><a href="#使用写组" class="headerlink" title="使用写组"></a>使用写组</h2><p>要使用写入组，您必须对系统中的查询使用写入组过滤器选项。这从查询中排除所有实体，这些实体具有来自查询中可写的任何组件的写入组的组件。</p><p>要覆盖使用写入组的系统，请将您自己的组件类型标记为该系统输出组件的写入组的一部分。原始系统忽略任何具有您的组件的实体，您可以使用您自己的系统更新这些实体的数据。</p><h2 id="编写组示例"><a href="#编写组示例" class="headerlink" title="编写组示例"></a>编写组示例</h2><p>在此示例中，您使用外部包根据角色的健康状况为游戏中的所有角色着色。为此，包中有两个组件：HealthComponent 和 ColorComponent；</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public struct HealthComponent : IComponentData&#123;   public int Value;&#125;public struct ColorComponent : IComponentData&#123;   public float4 Value;&#125;</code></pre></div></figure><p>包中还有两个系统：</p><ol><li>ComputeColorFromHealthSystem，它从 HealthComponent 读取并写入 ColorComponent</li><li>RenderWithColorComponent，从 ColorComponent 读取<br>为了表示玩家何时使用能量提升并且他们的角色变得无敌，您将 InvincibleTagComponent 附加到角色的实体。在这种情况下，角色的颜色应该更改为单独的不同颜色，而上面的示例不适用。</li></ol><p>您可以创建自己的系统来覆盖 ColorComponent 值，但理想情况下 ComputeColorFromHealthSystem 不会计算实体的颜色。它应该忽略任何具有 InvincibleTagComponent 的实体。当屏幕上有成千上万的玩家时，这就变得更加重要。</p><p>这个系统来自另一个不知道 InvincibleTagComponent 的包，所以这是写组有用的时候。当您知道它计算的值无论如何都会被覆盖时，它允许系统忽略查询中的实体。您需要做两件事来支持这一点：</p><ol><li><p>将 InvincibleTagComponent 标记为 ColorComponent 写入组的一部分：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">[WriteGroup(typeof(ColorComponent))]struct InvincibleTagComponent : IComponentData &#123;&#125;</code></pre></div></figure><p>ColorComponent 的写入组由所有组件类型组成，这些组件类型具有以 typeof(ColorComponent) 作为参数的 WriteGroup 属性。</p></li><li><p>ComputeColorFromHealthSystem 必须明确支持写组。为此，系统需要为其所有查询指定 EntityQueryOptions.FilterWriteGroup 选项，如下所示：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">...protected override void OnUpdate() &#123;Entities    .WithName(&quot;ComputeColor&quot;)    .WithEntityQueryOptions(EntityQueryOptions.FilterWriteGroup) &#x2F;&#x2F; support write groups    .ForEach((ref ColorComponent color, in HealthComponent health) &#x3D;&gt; &#123;        &#x2F;&#x2F; compute color here    &#125;).ScheduleParallel();&#125;...</code></pre></div></figure><p>执行时，会发生以下情况：</p></li><li><p>系统检测到你写入 ColorComponent 因为它是一个引用参数</p></li><li><p>它查找 ColorComponent 的写入组并在其中找到 InvincibleTagComponent</p></li><li><p>它排除了所有具有 InvincibleTagComponent 的实体</p></li></ol><p>好处是，这允许系统根据系统未知的类型排除实体，并且可能存在于不同的包中。</p><div class="note note-info">            <p>有关更多示例，请参阅 Unity.Transforms 代码，它为其更新的每个组件使用写入组，包括 LocalToWorld。</p>          </div><h2 id="创建写入组"><a href="#创建写入组" class="headerlink" title="创建写入组"></a>创建写入组</h2><p>要创建写入组，请将 WriteGroup 属性添加到写入组中每个组件类型的声明中。 WriteGroup 属性采用一个参数，即组中组件用于更新的组件类型。单个组件可以是多个写入组的成员。</p><p>例如，如果您的系统在实体上存在组件 A 或 B 时写入组件 W，那么您可以为 W 定义一个写入组，如下所示：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public struct W : IComponentData&#123;   public int Value;&#125;[WriteGroup(typeof(W))]public struct A : IComponentData&#123;   public int Value;&#125;[WriteGroup(typeof(W))]public struct B : IComponentData&#123;   public int Value;&#125;</code></pre></div></figure><p>您没有将写入组的目标（上例中的组件 W）添加到它自己的写入组。</p><h2 id="启用写组过滤"><a href="#启用写组过滤" class="headerlink" title="启用写组过滤"></a>启用写组过滤</h2><p>要启用写入组过滤，请在您的作业上设置 FilterWriteGroups 标志：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public class AddingSystem : SystemBase&#123;   protected override void OnUpdate() &#123;      Entities          &#x2F;&#x2F; support write groups by setting EntityQueryOptions         .WithEntityQueryOptions(EntityQueryOptions.FilterWriteGroup)          .ForEach((ref W w, in B b) &#x3D;&gt; &#123;            &#x2F;&#x2F; perform computation here         &#125;).ScheduleParallel();&#125;&#125;</code></pre></div></figure><p>对于查询描述对象，在创建查询时设置标志：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public class AddingSystem : SystemBase&#123;   private EntityQuery m_Query;   protected override void OnCreate()   &#123;       var queryDescription &#x3D; new EntityQueryDesc       &#123;           All &#x3D; new ComponentType[] &#123;              ComponentType.ReadWrite&lt;W&gt;(),              ComponentType.ReadOnly&lt;B&gt;()           &#125;,           Options &#x3D; EntityQueryOptions.FilterWriteGroup       &#125;;       m_Query &#x3D; GetEntityQuery(queryDescription);   &#125;   &#x2F;&#x2F; Define IJobEntityBatch struct and schedule...&#125;</code></pre></div></figure><p>当您在查询中启用写入组过滤时，该查询会将可写组件的写入组中的所有组件添加到查询的 None 列表中，除非您明确将它们添加到 All 或 Any 列表中。因此，如果查询明确需要来自特定写入组的实体上的每个组件，则查询只会选择该实体。如果实体具有来自该写入组的一个或多个附加组件，则查询将拒绝它。</p><p>在上面的示例代码中，查询：</p><ul><li>排除具有组件 A 的任何实体，因为 W 是可写的并且 A 是 W 的写入组的一部分。</li><li>不排除具有组件 B 的任何实体。即使 B 是 W 的写入组的一部分，它也在 All 列表中明确指定。</li></ul><h2 id="覆盖另一个使用写入组的系统"><a href="#覆盖另一个使用写入组的系统" class="headerlink" title="覆盖另一个使用写入组的系统"></a>覆盖另一个使用写入组的系统</h2><p>如果系统在其查询中使用写组过滤，您可以使用自己的系统覆盖该系统并写入那些组件。要覆盖系统，请将您自己的组件添加到其他系统写入的组件的写入组中。</p><p>因为写组过滤排除了查询未明确要求的写组中的任何组件，所以其他系统会忽略任何具有您的组件的实体。</p><p>例如，如果您想通过指定旋转的角度和轴来设置实体的方向，您可以创建一个组件和一个系统来将角度和轴值转换为四元数并将其写入 Unity.Transforms.Rotation零件。</p><p>为了防止 Unity.Transforms 系统更新 Rotation，无论除您之外的其他组件是否存在，您都可以将您的组件放在 Rotation 的写入组中：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">using System;using Unity.Collections;using Unity.Entities;using Unity.Transforms;using Unity.Mathematics;[Serializable][WriteGroup(typeof(Rotation))]public struct RotationAngleAxis : IComponentData&#123;   public float Angle;   public float3 Axis;&#125;</code></pre></div></figure><p>然后，您可以无竞争地使用 RotationAngleAxis 组件更新任何实体：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">using Unity.Burst;using Unity.Entities;using Unity.Jobs;using Unity.Collections;using Unity.Mathematics;using Unity.Transforms;public class RotationAngleAxisSystem : SystemBase&#123;   protected override void OnUpdate()   &#123;      Entities.ForEach((ref Rotation destination, in RotationAngleAxis source) &#x3D;&gt;      &#123;         destination.Value              &#x3D; quaternion.AxisAngle(math.normalize(source.Axis), source.Angle);      &#125;).ScheduleParallel();   &#125;&#125;</code></pre></div></figure><h2 id="扩展另一个使用写组的系统"><a href="#扩展另一个使用写组的系统" class="headerlink" title="扩展另一个使用写组的系统"></a>扩展另一个使用写组的系统</h2><p>如果你想扩展另一个系统而不是覆盖它，或者如果你想让未来的系统覆盖或扩展你的系统，那么你可以在你自己的系统上启用写组过滤。但是，当您这样做时，默认情况下两个系统都不会处理任何组件组合。您必须明确查询和处理每个组合。</p><p>在前面的示例中，它定义了一个写入组，其中包含以组件 W 为目标的组件 A 和 B。如果将名为 C 的新组件添加到写入组，那么知道 C 的新系统可以查询包含的实体C，这些实体是否也有组件 A 或 B 并不重要。</p><p>但是，如果新系统还启用了写组过滤，那就不再是这样了。如果您只需要组件 C，则写入组过滤会排除任何具有 A 或 B 的实体。相反，您必须显式查询每个有意义的组件组合。</p><div class="note note-info">            <p>您可以在适当的时候使用查询的 Any 子句。</p>          </div><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">var query &#x3D; new EntityQueryDesc&#123;    All &#x3D; new ComponentType[] &#123;       ComponentType.ReadOnly&lt;C&gt;(),        ComponentType.ReadWrite&lt;W&gt;()    &#125;,    Any &#x3D; new ComponentType[] &#123;       ComponentType.ReadOnly&lt;A&gt;(),        ComponentType.ReadOnly&lt;B&gt;()    &#125;,    Options &#x3D; EntityQueryOptions.FilterWriteGroup&#125;;</code></pre></div></figure><p>如果有任何实体包含未明确提及的写入组中的组件组合，则写入写入组目标的系统及其过滤器不会处理它们。但是，如果存在任何这些类型的实体，则很可能是程序中的逻辑错误，它们不应该存在。</p><h1 id="版本号"><a href="#版本号" class="headerlink" title="版本号"></a>版本号</h1><p>您可以使用 ECS 架构各部分的版本号（也称为世代）来检测潜在的变化并实施有效的优化策略，例如在数据自应用程序的最后一帧以来未发生变化时跳过处理。对实体执行快速版本检查以提高应用程序的性能非常有用。</p><p>此页面概述了 ECS 使用的所有不同版本号，以及导致它们更改的条件。</p><h2 id="版本号结构"><a href="#版本号结构" class="headerlink" title="版本号结构"></a>版本号结构</h2><p>所有版本号都是 32 位有符号整数。它们总是增加，除非它们环绕：有符号整数溢出是 C# 中定义的行为。这意味着要比较版本号，您应该使用（不）相等运算符，而不是关系运算符。</p><p>例如，检查 VersionB 是否比 VersionA 更新的正确方法是使用以下内容：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">bool VersionBIsMoreRecent &#x3D; (VersionB - VersionA) &gt; 0;</code></pre></div></figure><p>无法保证版本号增加多少。</p><h2 id="实体版本号"><a href="#实体版本号" class="headerlink" title="实体版本号"></a>实体版本号</h2><p>EntityId 包含索引和版本号。因为 ECS 回收索引，所以每次实体销毁实体时它都会增加 EntityManager 中的版本号。如果在 EntityManager 中查找 EntityId 时版本号不匹配，则意味着引用的实体不再存在。</p><p>例如，在您通过 EntityId 获取一个单位正在跟踪的敌人的位置之前，您可以调用 ComponentDataFromEntity.Exists。这使用版本号来检查实体是否仍然存在。</p><h2 id="世界版本号"><a href="#世界版本号" class="headerlink" title="世界版本号"></a>世界版本号</h2><p>ECS每创建或销毁一个管理器（如系统），就会增加一个世界的版本号。</p><h2 id="作业组件系统版本号"><a href="#作业组件系统版本号" class="headerlink" title="作业组件系统版本号"></a>作业组件系统版本号</h2><p>EntityDataManager.GlobalVersion 在每次作业组件系统更新之前都会增加。</p><p>您应该将此版本号与 System.LastSystemVersion 结合使用。这会在每次作业组件系统更新后获取 EntityDataManager.GlobalVersion 的值。</p><p>您应该将此版本号与 Chunk.ChangeVersion[] 结合使用。</p><h3 id="块-ChangeVersion"><a href="#块-ChangeVersion" class="headerlink" title="块.ChangeVersion"></a>块.ChangeVersion</h3><p>对于原型中的每个组件类型，此数组包含 EntityDataManager.GlobalVersion 的值，在组件数组最后一次被访问为在此块中可写时。这并不能保证任何事情都发生了变化，只是它可能已经发生了变化。</p><p>您不能以可写方式访问共享组件，即使也为这些组件存储了版本号：它没有任何用处。</p><p>当您在 <code>Entities.ForEach</code> 构造中使用 WithChangeFilter() 方法时，ECS 将该特定组件的 Chunk.ChangeVersion 与 System.LastSystemVersion 进行比较，并且它仅处理其组件数组在系统上次开始运行后被访问为可写的块。</p><p>例如，如果保证一组单位的生命值数量自上一帧以来没有变化，则可以跳过检查这些单位是否应更新其损坏模型。</p><h2 id="非共享组件版本号"><a href="#非共享组件版本号" class="headerlink" title="非共享组件版本号"></a>非共享组件版本号</h2><p>对于每个非共享组件类型，每当涉及该类型的迭代器变得无效时，ECS 都会增加 EntityManager.m_ComponentTypeOrderVersion[] 版本号。换句话说，任何可能修改该类型数组（不是实例）的东西。</p><p>例如，如果您有特定组件标识的静态对象和每个块的边界框，则只需在该组件的类型顺序版本更改时更新这些边界框。</p><h2 id="共享组件版本号"><a href="#共享组件版本号" class="headerlink" title="共享组件版本号"></a>共享组件版本号</h2><p>当存储在引用共享组件的块中的实体发生任何结构更改时，SharedComponentDataManager.m_S​​haredComponentVersion[] 版本号会增加。</p><p>例如，如果您为每个共享组件保留一个实体计数，您可以依靠该版本号仅在相应版本号更改时重做每个计数。</p>]]></content>
    
    
    
    <tags>
      
      <tag>Entities</tag>
      
      <tag>ECS</tag>
      
      <tag>DOTS</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>ECS 概念</title>
    <link href="/2022/11/16/ECS-concepts-intro/"/>
    <url>/2022/11/16/ECS-concepts-intro/</url>
    
    <content type="html"><![CDATA[<h1 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h1><p>Entities package使用<strong>实体组件系统</strong>(<strong>ECS</strong>)架构来组织代码和数据。实体是唯一标识符，就像游戏对象的轻量级的非托管替代品。实体相当与一个ID，用来关联包含实体数据的各个组件。与游戏对象不同，实体不包含代码：它们是由您创建的系统来处理的数据单元。</p><span id="more"></span><table><thead><tr><th>标题</th><th>描述</th></tr></thead><tbody><tr><td>Entity</td><td>实体是不包含代码的游戏对象的轻量级替代品。</td></tr><tr><td>Component</td><td>组件包含有关单个实体的数据。</td></tr><tr><td>System</td><td>向系统添加代码以处理实体和组件。</td></tr><tr><td>World</td><td>世界将实体组织成孤立的群体。</td></tr><tr><td>Archetype</td><td>原型是一个或多个实体可能具有的组件的独特组合。</td></tr><tr><td>Structural changes</td><td>结构更改是会影响应用程序性能的资源密集型操作。</td></tr></tbody></table><h1 id="实体"><a href="#实体" class="headerlink" title="实体"></a>实体</h1><p>实体表示程序中具有自己数据集的离散事物，例如角色、视觉效果、UI 元素，甚至是网络事务等抽象事物。实体类似于非托管(unmanaged)的轻量级游戏对象，代表程序的特定元素。但是，实体仅充当将各个唯一组件关联在一起的 ID，而不包含任何代码或充当其关联组件的容器。</p><p>实体的集合存储在一个世界中，世界的 <code>EntityManager</code> 管理世界中的所有实体。 <code>EntityManager</code> 包含可用于创建、销毁和修改该世界中的实体的方法。其中包括以下常用方法：</p><table><thead><tr><th>方法</th><th>描述</th></tr></thead><tbody><tr><td>CreateEntity</td><td>创建一个新实体。</td></tr><tr><td>Instantiate</td><td>复制现有实体并从该副本创建新实体。</td></tr><tr><td>DestroyEntity</td><td>销毁现有实体。</td></tr><tr><td>AddComponent</td><td>将组件添加到现有实体。</td></tr><tr><td>RemoveComponent</td><td>从现有实体中删除组件。</td></tr><tr><td>GetComponent</td><td>获取实体组件的值。</td></tr><tr><td>SetComponent</td><td>覆盖实体组件的值。</td></tr></tbody></table><div class="note note-info">            <p>当您创建或销毁实体时，这是一种结构更改，会影响应用程序的性能。有关详细信息，请参阅有关结构更改的文档</p>          </div><p>实体没有类型，但您可以根据与其关联的组件类型对实体进行分类。 <code>EntityManager</code> 跟踪现有全部实体上组件的唯一组合。这些独特的组合称为原型（Archetype）。有关原型如何工作的更多信息，请参阅有关原型概念的文档。</p><h2 id="编辑器中的实体"><a href="#编辑器中的实体" class="headerlink" title="编辑器中的实体"></a>编辑器中的实体</h2><p>在编辑器中，以下图标代表实体：<img src="https://docs.unity3d.com/Packages/com.unity.entities@1.0/manual/images/editor-entity-icon.png" style="display: inline;"> 。当您使用特定的实体窗口和检查器时，您会看到这一点。</p><h1 id="组件"><a href="#组件" class="headerlink" title="组件"></a>组件</h1><p>在实体组件系统 (ECS) 体系结构中，组件包含系统可以读取或写入的实体数据。</p><p>使用没有方法的<code>IComponentData</code>接口将一个结构体标记为组件类型。此组件类型只能包含非托管数据，并且它们可以包含方法，但最佳实践是它们只有纯数据。如果你想创建一个托管组件，你将它定义为一个类。有关详细信息，请参阅托管组件。</p><p>对于不同的目的，有不同类型的组件。根据您希望如何管理项目中的数据，某些组件允许对应用程序的性能进行更精细的控制。有关详细信息，请参阅组件类型。</p><p>一组独特的实体的组件称为原型。 ECS 架构按原型将组件数据存储在称为块的 16KiB 内存块中。有关 ECS 如何存储组件数据的更多信息，请参阅原型概念文档。</p><h1 id="系统"><a href="#系统" class="headerlink" title="系统"></a>系统</h1><p>系统提供将组件数据从当前状态转换为下一个状态的逻辑。例如，系统可能会通过速度乘以自上次更新以来的时间间隔来更新所有移动实体的位置。</p><p>系统每帧在主线程上运行一次。系统被组织成被称为系统组的层次结构，您可以使用这些系统组来组织系统更新的顺序。</p><p>您可以创建非托管或托管系统。要定义托管系统，请创建一个继承自<code>SystemBase</code>的类。要定义非托管系统，请创建一个继承自<code>ISystem</code>的结构。</p><p><code>ISystem</code>和<code>SystemBase</code>都有三种方法，您可以覆盖<code>OnUpdate</code>、<code>OnCreate</code>和<code>OnDestroy</code>。系统的<code>OnUpdate</code>方法每帧执行一次。</p><p>一个系统只能处理一个世界中的实体，所以一个系统与一个特定的世界相关联。您可以使用<code>World</code>属性返回系统附加到的世界。</p><p>默认情况下，一个自动引导过程会为每个系统和系统组创建一个实例。这个引导将创建一个默认世界，其中包含三个系统组：<code>InitializationSystemGroup</code>、<code>SimulationSystemGroup</code>和<code>PresentationSystemGroup</code>。默认情况下系统的一个实例将被添加到<code>SimulationSystemGroup</code>。您可以使用<code>[UpdateInGroup]</code>属性来覆盖此行为。</p><p>要禁用自动引导过程，请使用脚本定义<code>#UNITY_DISABLE_AUTOMATIC_SYSTEM_BOOTSTRAP</code>。</p><h2 id="系统类型"><a href="#系统类型" class="headerlink" title="系统类型"></a>系统类型</h2><p>您可以使用多种类型的系统：</p><ul><li><code>SystemBase</code>: 为托管系统提供基类。</li><li><code>ISystem</code>: 为非托管系统提供接口。</li><li><code>EntityCommandBufferSystem</code>: 为其他系统提供实体命令缓冲区实例。这允许您将结构更改组合在一起以提高应用程序的性能。</li><li><code>ComponentSystemGroup</code>: 为系统提供嵌套组织和更新顺序。</li></ul><h2 id="系统组"><a href="#系统组" class="headerlink" title="系统组"></a>系统组</h2><p>一个系统组可以有系统和其他系统组作为它的子系统。系统组有一个可以覆盖的更新方法，基本的更新方法将按排序顺序更新组的子级。</p><p>每次将组添加到系统组时，它都会重新排序系统更新顺序。要控制系统组的更新顺序，请将<code>UpdateBefore</code>或<code>UpdateAfter</code>属性添加到系统以指定它应该在之前或之后更新哪些系统。这些属性仅适用于同一系统组的子系统。例如：</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">&#x2F;&#x2F; 如果 PeachSystem 和 DaisySystem 是同一组的子级，&#x2F;&#x2F; 则实体组件系统按排序顺序将 PeachSystem 放在 DaisySystem 之前的某处。[UpdateBefore(typeof(DaisySystem))]public partial class PeachSystem : SystemBase &#123; &#125;</code></pre></div></figure><p>要创建系统组，请创建一个继承自<code>ComponentSystemGroup</code>的类。因为系统属于一个世界，所以必须使用<code>World.GetOrCreateSystem</code>创建一个系统。要将系统添加到组，请使用<code>group.AddSystemToUpdateList</code>。您可以将其他系统组添加到现有系统组。</p><p>有关详细信息，请参阅有关系统更新顺序的文档。</p><h2 id="检查系统"><a href="#检查系统" class="headerlink" title="检查系统"></a>检查系统</h2><p>您可以使用“Systems window”窗口检查每个世界中系统的更新顺序，并查看系统组的完整层次结构。有关详细信息，请参阅有关系统窗口参考的文档。</p><h3 id="编辑器中的系统"><a href="#编辑器中的系统" class="headerlink" title="编辑器中的系统"></a>编辑器中的系统</h3><p>在编辑器中，以下图标代表不同类型的系统。当您使用特定的实体窗口和检查器时，您会看到这一点。</p><table><thead><tr><th>图标</th><th>含义</th></tr></thead><tbody><tr><td><img src="https://docs.unity3d.com/Packages/com.unity.entities@1.0/manual/images/editor-system-group.png"></td><td>一个系统组</td></tr><tr><td><img src="https://docs.unity3d.com/Packages/com.unity.entities@1.0/manual/images/editor-system.png"></td><td>一个系统</td></tr><tr><td><img src="https://docs.unity3d.com/Packages/com.unity.entities@1.0/manual/images/editor-system-start-step.png"></td><td>一个带有<code>OrderFirst</code>参数的，设置为在开头执行的<code>实体命令缓冲区系统</code>。</td></tr><tr><td><img src="https://docs.unity3d.com/Packages/com.unity.entities@1.0/manual/images/editor-system-end-step.png"></td><td>一个带有<code>OrderLast</code>参数的，设置为在末尾执行的<code>实体命令缓冲区系统</code>。</td></tr></tbody></table><h1 id="世界"><a href="#世界" class="headerlink" title="世界"></a>世界</h1><p>世界是实体的集合。实体的 ID 号仅在其自己的世界中是唯一的。一个世界有一个<code>EntityManager</code>结构体，你可以用它来创建、销毁和修改世界中的实体。</p><p>一个世界拥有一组系统，这些系统通常只访问同一个世界中的实体。此外，世界中具有相同组件类型集的一组实体一起存储在原型中，原型决定了你程序中的组件在内存中的组织方式。</p><h2 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h2><p>默认情况下，当您进入播放模式时，Unity 会创建一个世界实例并将每个系统添加到这个默认世界。</p><p>如果您更喜欢手动将系统添加到默认世界，请创建一个实现<code>ICustomBootstrap</code>接口的类。</p><p>如果你想完全手动控制引导，使用这些定义来禁用默认的世界创建：</p><p>-<code>#UNITY_DISABLE_AUTOMATIC_SYSTEM_BOOTSTRAP_RUNTIME_WORLD</code>：禁用默认运行时世界的生成。<br>-<code>#UNITY_DISABLE_AUTOMATIC_SYSTEM_BOOTSTRAP_EDITOR_WORLD</code>：禁用默认编辑器世界的生成。<br>-<code>#UNITY_DISABLE_AUTOMATIC_SYSTEM_BOOTSTRAP</code>：禁用两个默认世界的生成。</p><p>然后，您的代码负责创建您的世界和系统，并将您的世界的更新插入到 Unity 可编写脚本的 PlayerLoop 中。</p><p>Unity 使用<code>WorldFlags</code>在编辑器中创建专门的世界。</p><h2 id="时间考量"><a href="#时间考量" class="headerlink" title="时间考量"></a>时间考量</h2><p>世界控制其中系统的<code>Time</code>属性的值。系统的<code>Time</code>属性是当前世界时间的别名。</p><p>默认情况下，Unity 为每个世界创建一个<code>TimeData</code>实体，由<code>UpdateWorldTimeSystem</code>实例更新。这反映了自上一帧以来经过的时间。</p><p><code>FixedStepSimulationSystemGroup</code>中的系统处理时间的方式与其他系统组不同。固定步长模拟组中的系统以固定间隔更新，而不是在当前增量时间更新一次，并且如果固定间隔是帧时间的足够小的一部分，则每帧可能更新不止一次。</p><p>如果您需要对世界中的时间进行更多控制，可以使用<code>World.SetTime</code>直接指定一个时间值。您还可以使用<code>PushTime</code>临时更改世界时间，使用<code>PopTime</code>返回到之前的时间（在时间栈中）。</p><h1 id="原型"><a href="#原型" class="headerlink" title="原型"></a>原型</h1><p>原型是世界中具有相同的唯一组件类型组合的所有实体的唯一标识符。例如，一个世界中具有组件类型 A 和 B 的所有实体共享一个原型。所有具有组件类型 A、B 和 C 的实体共享一个不同的原型，所有具有组件类型 A 和 Z 的实体共享另一个原型。</p><p>当您从实体中添加或删除组件类型时，世界的<code>EntityManager</code>会将实体移动到适当的原型。例如，如果实体具有组件类型 A、B 和 C，并且您删除了它的 B组件，<code>EntityManager</code>会将实体移动到具有组件类型 A 和 C 的原型。如果不存在这样的原型，则<code>EntityManager</code>会创建它。</p><div class="note note-danger">            <p>频繁移动实体会占用大量资源并降低应用程序的性能。有关详细信息，请参阅有关结构变更概念的文档。</p>          </div><p>基于原型的实体架构意味着通过组件类型查询实体是高效的。例如，如果您想要查找具有组件类型 A 和 B 的所有实体，您可以找到具有这些组件类型的所有原型，这比扫描所有单个实体的性能更高。世界中的现有原型集往往会在程序生命周期的早期稳定下来，因此您可以缓存查询以获得更快的性能。</p><p>原型只有在它的世界被销毁时才会被销毁。</p><h2 id="原型块-Archetype-chunks"><a href="#原型块-Archetype-chunks" class="headerlink" title="原型块 Archetype chunks"></a>原型块 Archetype chunks</h2><p>所有具有相同原型的实体和组件都存储在称为块的统一内存块中。每个块由 16KiB 组成，它们可以存储的实体数量取决于块原型中组件的数量和大小。<code>EntityManager</code>根据需要创建和销毁块。</p><p>块包含每个组件类型的数组，以及用于存储实体 ID 的附加数组。例如，在具有组件类型 A 和 B 的原型中，每个块都具有三个数组：一个数组用于 A 组件值，一个数组用于 B 组件值，一个数组用于实体 ID。</p><p>块的数组是紧密打包的：块的第一个实体存储在这些数组的索引 0 处，块的第二个实体存储在索引 1 处，后续实体存储在连续的索引中。当一个新的实体被添加到块中时，它被存储在第一个可用的索引中。当一个实体从块中移除时（因为它被破坏或被移动到另一个原型），块的最后一个实体被移动以填补空白。</p><p>将实体添加到原型时，如果原型的现有块已满，则<code>EntityManager</code>会创建一个新块。当最后一个实体从块中移除时，<code>EntityManager</code>会销毁该块。</p><h2 id="编辑器中的原型"><a href="#编辑器中的原型" class="headerlink" title="编辑器中的原型"></a>编辑器中的原型</h2><p>原型窗口列出了项目中所有世界的原型，并显示了每个原型的已分配和未使用内存量。</p><p>在编辑器中，以下图标代表原型：<img src="https://docs.unity3d.com/Packages/com.unity.entities@1.0/manual/images/editor-archetype-icon.png" style="display: inline;">。</p><h1 id="结构更改"><a href="#结构更改" class="headerlink" title="结构更改"></a>结构更改</h1><p>导致 Unity 重新组织内存块或内存中块的内容的操作称为结构更改。了解哪些操作是结构更改很重要，因为它们可能是资源密集型的，您只能在主线程上执行它们；不是来自jobs。</p><p>以下操作被视为结构更改：</p><ul><li>创建或销毁实体。</li><li>添加或删除组件。</li><li>设置一个共享组件值。</li></ul><h2 id="创建一个实体"><a href="#创建一个实体" class="headerlink" title="创建一个实体"></a>创建一个实体</h2><p>当您创建一个实体时，Unity 要么将实体添加到现有块中，要么如果没有块可用于实体的原型，则创建一个新块并将实体添加到其中。</p><h2 id="销毁一个实体"><a href="#销毁一个实体" class="headerlink" title="销毁一个实体"></a>销毁一个实体</h2><p>当您销毁一个实体时，Unity 会将该实体从其块中移除。如果移除实体在块中留下空隙，Unity 会移动块中的最后一个实体来填补空隙。如果删除实体使块为空，Unity 会释放块。</p><h2 id="添加或删除组件"><a href="#添加或删除组件" class="headerlink" title="添加或删除组件"></a>添加或删除组件</h2><p>在实体中添加或删除组件时，您会更改实体的原型。 Unity 将每个实体存储在与实体原型匹配的块中。这意味着如果您更改实体的原型，Unity 必须将该实体移动到另一个块。如果不存在合适的块，Unity 会创建一个新块。如果移动使前一个块有间隙或留空，Unity 将移动块中的最后一个实体以分别填充间隙或释放块。</p><h2 id="设置一个共享组件值"><a href="#设置一个共享组件值" class="headerlink" title="设置一个共享组件值"></a>设置一个共享组件值</h2><p>当您设置实体的共享组件的值时，Unity 会将实体移动到与新共享组件值匹配的块中。如果不存在合适的块，Unity 会创建一个新块。如果移动使前一个块有间隙或为空，Unity 将移动块中的最后一个实体以填充间隙或分别释放块。</p><div class="note note-info">            <p>设置常规组件值不是结构更改，因为它不需要 Unity 移动实体。</p>          </div><h2 id="同步点"><a href="#同步点" class="headerlink" title="同步点"></a>同步点</h2><p>您不能直接在作业中进行结构更改，因为它可能会使其他已安排的作业无效，并创建一个同步点。</p><p>一个同步点（sync point）是程序执行中的一个点，它等待到目前为止已经调度的所有作业完成。同步点会限制您在一段时间内使用作业系统中可用的所有工作线程的能力。因此，您的目标应该是避免同步点。 ECS 中数据的结构变化是产生同步点的主要原因。</p><p>结构更改不仅要求同步点，而且还会使对任何组件数据的所有直接引用无效。这包括<code>DynamicBuffer</code>的实例和提供对组件（例如<code>ComponentSystemBase.GetComponentDataFromEntity</code>）的直接访问的方法的结果。</p><h3 id="避免同步点"><a href="#避免同步点" class="headerlink" title="避免同步点"></a>避免同步点</h3><p>您可以使用实体命令缓冲区来排队缓冲结构更改，而不是立即执行它们。您可以在帧的稍后时间执行存储在实体命令缓冲区中的命令。这将跨帧分布的多个同步点减少为单个同步点。</p><p>每个标准<code>ComponentSystemGroup</code>实例都提供<code>EntityCommandBufferSystem</code>作为组中更新的第一个和最后一个系统。如果您从这些标准系统之一获取实体命令缓冲区对象，则所有结构更改都发生在帧中的同一点，从而产生一个同步点。您还可以使用实体命令缓冲区来记录作业中的结构更改，而不是仅在主线程上进行结构更改。</p><p>如果您不能为任务使用实体命令缓冲区，请将所有进行结构更改的系统按系统执行顺序分组在一起。两个都进行结构更改的系统如果按顺序更新，则只会创建一个同步点。</p>]]></content>
    
    
    
    <tags>
      
      <tag>Entities</tag>
      
      <tag>ECS</tag>
      
      <tag>DOTS</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Entities ECS DOTS 中文文档</title>
    <link href="/2022/11/15/ECS/"/>
    <url>/2022/11/15/ECS/</url>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>Entity 1.0版本发布了，很多接口和概念都不同与之前的版本了。苦于没有更方便的中文资料去查询，总体机翻并稍微修改了一下，贴在下面，以备后用。</p><span id="more"></span><h1 id="总目录"><a href="#总目录" class="headerlink" title="总目录"></a>总目录</h1><ul><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E7%B3%BB%E7%BB%9F">系统</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E4%BD%BF%E7%94%A8-SystemBase-%E5%88%9B%E5%BB%BA%E7%B3%BB%E7%BB%9F">使用 <code>SystemBase</code> 创建系统</a><ul><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E5%9B%9E%E8%B0%83%E6%96%B9%E6%B3%95%E9%A1%BA%E5%BA%8F">回调方法顺序</a></li></ul></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E8%BF%AD%E4%BB%A3%E6%95%B0%E6%8D%AE">迭代数据</a><ul><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E9%A2%9D%E5%A4%96%E8%B5%84%E6%BA%90">额外资源</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E4%BD%BF%E7%94%A8-Entities-ForEach-%E8%BF%AD%E4%BB%A3%E6%95%B0%E6%8D%AE">使用 <code>Entities.ForEach</code> 迭代数据</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E4%BD%BF%E7%94%A8-IJobEntity-%E8%BF%AD%E4%BB%A3%E6%95%B0%E6%8D%AE">使用 <code>IJobEntity</code> 迭代数据</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E4%BD%BF%E7%94%A8-IJobEntityBatch-%E9%81%8D%E5%8E%86%E6%88%90%E6%89%B9%E6%95%B0%E6%8D%AE">使用 <code>IJobEntityBatch</code> 遍历成批数据</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E6%89%8B%E5%8A%A8%E8%BF%AD%E4%BB%A3%E6%95%B0%E6%8D%AE">手动迭代数据</a></li></ul></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E7%B3%BB%E7%BB%9F%E6%9B%B4%E6%96%B0%E9%A1%BA%E5%BA%8F">系统更新顺序</a><ul><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E7%BB%84%E4%BB%B6%E7%B3%BB%E7%BB%9F%E7%BB%84">组件系统组</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E7%B3%BB%E7%BB%9F%E6%8E%92%E5%BA%8F%E5%B1%9E%E6%80%A7">系统排序属性</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E9%BB%98%E8%AE%A4%E7%B3%BB%E7%BB%9F%E7%BB%84">默认系统组</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E5%A4%9A%E4%B8%AA%E4%B8%96%E7%95%8C">多个世界</a></li></ul></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E4%BD%BF%E7%94%A8%E4%BD%9C%E4%B8%9A%E5%9C%A8%E5%A4%9A%E4%B8%AA%E7%BA%BF%E7%A8%8B%E4%B8%8A%E8%B0%83%E5%BA%A6%E6%95%B0%E6%8D%AE">使用作业在多个线程上调度数据</a><ul><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E5%B7%A5%E4%BD%9C%E6%89%A9%E5%B1%95">工作扩展</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E9%80%9A%E7%94%A8%E8%81%8C%E4%BD%8D">通用职位</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E4%BD%BF%E7%94%A8-Job-WithCode-%E5%AE%89%E6%8E%92%E5%90%8E%E5%8F%B0%E4%BD%9C%E4%B8%9A">使用 Job.WithCode 安排后台作业</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E4%BD%9C%E4%B8%9A%E4%BE%9D%E8%B5%96">作业依赖</a></li></ul></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E4%BD%BF%E7%94%A8-EntityQuery-%E6%9F%A5%E8%AF%A2%E6%95%B0%E6%8D%AE">使用 EntityQuery 查询数据</a><ul><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E5%88%9B%E5%BB%BA%E5%AE%9E%E4%BD%93%E6%9F%A5%E8%AF%A2">创建实体查询</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E5%AE%9A%E4%B9%89%E8%BF%87%E6%BB%A4%E5%99%A8">定义过滤器</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E6%8C%89%E5%90%AF%E7%94%A8%E7%BB%84%E4%BB%B6%E8%BF%87%E6%BB%A4">按启用组件过滤</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E5%90%88%E5%B9%B6%E6%9F%A5%E8%AF%A2">合并查询</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E6%89%A7%E8%A1%8C%E6%9F%A5%E8%AF%A2">执行查询</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E7%BC%96%E8%BE%91%E5%99%A8%E4%B8%AD%E7%9A%84%E6%9F%A5%E8%AF%A2">编辑器中的查询</a></li></ul></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E4%BD%BF%E7%94%A8-EntityCommandBuffer-%E5%AE%89%E6%8E%92%E6%95%B0%E6%8D%AE%E6%9B%B4%E6%94%B9">使用 EntityCommandBuffer 安排数据更改</a><ul><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#EntityCommandBuffer-%E6%96%B9%E6%B3%95">EntityCommandBuffer 方法</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E5%9C%A8%E5%8D%95%E7%BA%BF%E7%A8%8B%E4%BD%9C%E4%B8%9A%E4%B8%AD%E4%BD%BF%E7%94%A8-EntityCommandBuffer">在单线程作业中使用 EntityCommandBuffer</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E5%9C%A8%E5%B9%B6%E8%A1%8C%E4%BD%9C%E4%B8%9A%E4%B8%AD%E4%BD%BF%E7%94%A8-EntityCommandBuffer">在并行作业中使用 EntityCommandBuffer</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E9%87%8D%E7%94%A8-EntityCommandBuffer-%E5%AE%9E%E4%BE%8B">重用 EntityCommandBuffer 实例</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E5%A4%9A%E9%87%8D%E5%9B%9E%E6%94%BE">多重回放</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E5%9C%A8%E4%B8%BB%E7%BA%BF%E7%A8%8B%E4%B8%8A%E4%BD%BF%E7%94%A8-EntityCommandBuffer">在主线程上使用 EntityCommandBuffer</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E4%BD%BF%E7%94%A8-EntityCommandBufferSystem-%E8%87%AA%E5%8A%A8%E6%92%AD%E6%94%BE%E5%92%8C%E5%A4%84%E7%90%86%E5%91%BD%E4%BB%A4%E7%BC%93%E5%86%B2%E5%8C%BA">使用 EntityCommandBufferSystem 自动播放和处理命令缓冲区</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E5%BB%B6%E8%BF%9F%E5%AE%9E%E4%BD%93">延迟实体</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E5%9C%A8-Entities-ForEach-%E6%96%B9%E6%B3%95%E4%B8%AD%E4%BD%BF%E7%94%A8%E5%91%BD%E4%BB%A4%E7%BC%93%E5%86%B2%E5%8C%BA">在 <code>Entities.ForEach</code> 方法中使用命令缓冲区</a></li></ul></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E6%9F%A5%E6%89%BE%E4%BB%BB%E6%84%8F%E6%95%B0%E6%8D%AE">查找任意数据</a><ul><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E5%9C%A8%E7%B3%BB%E7%BB%9F%E4%B8%AD%E6%9F%A5%E6%89%BE%E5%AE%9E%E4%BD%93%E6%95%B0%E6%8D%AE">在系统中查找实体数据</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E5%9C%A8%E4%BD%9C%E4%B8%9A%E4%B8%AD%E6%9F%A5%E6%89%BE%E5%AE%9E%E4%BD%93%E6%95%B0%E6%8D%AE">在作业中查找实体数据</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E6%95%B0%E6%8D%AE%E8%AE%BF%E9%97%AE%E9%94%99%E8%AF%AF">数据访问错误</a></li></ul></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E5%86%99%E5%85%A5%E7%BB%84">写入组</a><ul><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E4%BD%BF%E7%94%A8%E5%86%99%E7%BB%84">使用写组</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E7%BC%96%E5%86%99%E7%BB%84%E7%A4%BA%E4%BE%8B">编写组示例</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E5%88%9B%E5%BB%BA%E5%86%99%E5%85%A5%E7%BB%84">创建写入组</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E5%90%AF%E7%94%A8%E5%86%99%E7%BB%84%E8%BF%87%E6%BB%A4">启用写组过滤</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E8%A6%86%E7%9B%96%E5%8F%A6%E4%B8%80%E4%B8%AA%E4%BD%BF%E7%94%A8%E5%86%99%E5%85%A5%E7%BB%84%E7%9A%84%E7%B3%BB%E7%BB%9F">覆盖另一个使用写入组的系统</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E6%89%A9%E5%B1%95%E5%8F%A6%E4%B8%80%E4%B8%AA%E4%BD%BF%E7%94%A8%E5%86%99%E7%BB%84%E7%9A%84%E7%B3%BB%E7%BB%9F">扩展另一个使用写组的系统</a></li></ul></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E7%89%88%E6%9C%AC%E5%8F%B7">版本号</a><ul><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E7%89%88%E6%9C%AC%E5%8F%B7%E7%BB%93%E6%9E%84">版本号结构</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E5%AE%9E%E4%BD%93%E7%89%88%E6%9C%AC%E5%8F%B7">实体版本号</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E4%B8%96%E7%95%8C%E7%89%88%E6%9C%AC%E5%8F%B7">世界版本号</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E4%BD%9C%E4%B8%9A%E7%BB%84%E4%BB%B6%E7%B3%BB%E7%BB%9F%E7%89%88%E6%9C%AC%E5%8F%B7">作业组件系统版本号</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E9%9D%9E%E5%85%B1%E4%BA%AB%E7%BB%84%E4%BB%B6%E7%89%88%E6%9C%AC%E5%8F%B7">非共享组件版本号</a></li><li><a href="https://hexo.home.kuanmi.top/2022/11/16/ECS-systems-intro/#%E5%85%B1%E4%BA%AB%E7%BB%84%E4%BB%B6%E7%89%88%E6%9C%AC%E5%8F%B7">共享组件版本号</a></li></ul></li></ul><h1 id="目录"><a href="#目录" class="headerlink" title="目录"></a>目录</h1><ul><li><a href="#%E5%89%8D%E8%A8%80">前言</a></li><li><a href="#%E7%9B%AE%E5%BD%95">目录</a></li><li><a href="#%E6%A6%82%E5%BF%B5">概念</a><ul><li><a href="#%E5%AE%9E%E4%BD%93">实体</a><ul><li><a href="#%E7%BC%96%E8%BE%91%E5%99%A8%E4%B8%AD%E7%9A%84%E5%AE%9E%E4%BD%93">编辑器中的实体</a></li></ul></li><li><a href="#%E7%BB%84%E4%BB%B6">组件</a></li><li><a href="#%E7%B3%BB%E7%BB%9F">系统</a><ul><li><a href="#%E7%B3%BB%E7%BB%9F%E7%B1%BB%E5%9E%8B">系统类型</a></li><li><a href="#%E7%B3%BB%E7%BB%9F%E7%BB%84">系统组</a></li><li><a href="#%E6%A3%80%E6%9F%A5%E7%B3%BB%E7%BB%9F">检查系统</a><ul><li><a href="#%E7%BC%96%E8%BE%91%E5%99%A8%E4%B8%AD%E7%9A%84%E7%B3%BB%E7%BB%9F">编辑器中的系统</a></li></ul></li></ul></li><li><a href="#%E4%B8%96%E7%95%8C">世界</a><ul><li><a href="#%E5%88%9D%E5%A7%8B%E5%8C%96">初始化</a></li><li><a href="#%E6%97%B6%E9%97%B4%E8%80%83%E9%87%8F">时间考量</a></li></ul></li><li><a href="#%E5%8E%9F%E5%9E%8B">原型</a><ul><li><a href="#%E5%8E%9F%E5%9E%8B%E5%9D%97-archetype-chunks">原型块 Archetype chunks</a></li><li><a href="#%E7%BC%96%E8%BE%91%E5%99%A8%E4%B8%AD%E7%9A%84%E5%8E%9F%E5%9E%8B">编辑器中的原型</a></li></ul></li><li><a href="#%E7%BB%93%E6%9E%84%E5%8F%98%E5%8C%96">结构变化</a><ul><li><a href="#%E5%88%9B%E5%BB%BA%E4%B8%80%E4%B8%AA%E5%AE%9E%E4%BD%93">创建一个实体</a></li><li><a href="#%E9%94%80%E6%AF%81%E4%B8%80%E4%B8%AA%E5%AE%9E%E4%BD%93">销毁一个实体</a></li><li><a href="#%E6%B7%BB%E5%8A%A0%E6%88%96%E5%88%A0%E9%99%A4%E7%BB%84%E4%BB%B6">添加或删除组件</a></li><li><a href="#%E8%AE%BE%E7%BD%AE%E4%B8%80%E4%B8%AA%E5%85%B1%E4%BA%AB%E7%BB%84%E4%BB%B6%E5%80%BC">设置一个共享组件值</a></li><li><a href="#%E5%90%8C%E6%AD%A5%E7%82%B9">同步点</a><ul><li><a href="#%E9%81%BF%E5%85%8D%E5%90%8C%E6%AD%A5%E7%82%B9">避免同步点</a></li></ul></li></ul></li></ul></li><li><a href="#%E5%AE%9E%E7%8E%B0%E7%B3%BB%E7%BB%9F">实现系统</a><ul><li><a href="#%E4%BD%BF%E7%94%A8systembase%E5%88%9B%E5%BB%BA%E7%B3%BB%E7%BB%9F">使用<code>SystemBase</code>创建系统</a><ul><li><a href="#%E5%9B%9E%E8%B0%83%E6%96%B9%E6%B3%95%E9%A1%BA%E5%BA%8F">回调方法顺序</a></li></ul></li><li><a href="#%E8%BF%AD%E4%BB%A3%E6%95%B0%E6%8D%AE">迭代数据</a><ul><li><a href="#%E4%BD%BF%E7%94%A8-entitiesforeach-%E8%BF%AD%E4%BB%A3%E6%95%B0%E6%8D%AE">使用 <code>Entities.ForEach</code> 迭代数据</a><ul><li><a href="#%E5%AE%9A%E4%B9%89%E4%B8%80%E4%B8%AA-lambda-%E8%A1%A8%E8%BE%BE%E5%BC%8F">定义一个 lambda 表达式</a><ul><li><a href="#%E8%87%AA%E5%AE%9A%E4%B9%89%E4%BB%A3%E7%90%86">自定义代理</a></li><li><a href="#%E7%BB%84%E4%BB%B6%E5%8F%82%E6%95%B0">组件参数</a></li><li><a href="#%E5%91%BD%E5%90%8D%E5%8F%82%E6%95%B0">命名参数</a></li></ul></li><li><a href="#%E6%89%A7%E8%A1%8C-entitiesforeach-lambda-%E8%A1%A8%E8%BE%BE%E5%BC%8F">执行 Entities.ForEach lambda 表达式</a></li><li><a href="#%E9%80%89%E6%8B%A9%E5%AE%9E%E4%BD%93">选择实体</a></li><li><a href="#%E8%AE%BF%E9%97%AE-entityquery-%E5%AF%B9%E8%B1%A1">访问 EntityQuery 对象</a></li><li><a href="#%E8%AE%BF%E9%97%AE%E5%8F%AF%E9%80%89%E7%BB%84%E4%BB%B6">访问可选组件</a></li><li><a href="#%E6%9B%B4%E6%94%B9%E8%BF%87%E6%BB%A4">更改过滤</a></li><li><a href="#%E5%85%B1%E4%BA%AB%E7%BB%84%E4%BB%B6%E8%BF%87%E6%BB%A4">共享组件过滤</a></li><li><a href="#%E6%8D%95%E8%8E%B7%E5%8F%98%E9%87%8F">捕获变量</a></li><li><a href="#%E6%94%AF%E6%8C%81%E7%9A%84%E5%8A%9F%E8%83%BD">支持的功能</a></li><li><a href="#%E4%BE%9D%E8%B5%96%E5%85%B3%E7%B3%BB">依赖关系</a></li></ul></li><li><a href="#%E4%BD%BF%E7%94%A8-ijobentity-%E8%BF%AD%E4%BB%A3%E6%95%B0%E6%8D%AE">使用 <code>IJobEntity</code> 迭代数据</a><ul><li><a href="#ijobentity-%E5%92%8C-entitiesforeach-%E7%9A%84%E6%AF%94%E8%BE%83">IJobEntity 和 Entities.ForEach 的比较</a></li><li><a href="#%E5%88%9B%E5%BB%BA-ijobentity-%E4%BD%9C%E4%B8%9A">创建 IJobEntity 作业</a></li><li><a href="#%E6%8C%87%E5%AE%9A%E6%9F%A5%E8%AF%A2">指定查询</a><ul><li><a href="#%E5%B1%9E%E6%80%A7">属性</a></li><li><a href="#%E6%89%A7%E8%A1%8C%E5%8F%82%E6%95%B0">执行参数</a></li></ul></li></ul></li><li><a href="#%E9%81%8D%E5%8E%86%E6%88%90%E6%89%B9%E6%95%B0%E6%8D%AE">遍历成批数据</a><ul><li><a href="#%E4%BD%BF%E7%94%A8-entityquery-%E6%9F%A5%E8%AF%A2%E6%95%B0%E6%8D%AE">使用 EntityQuery 查询数据</a></li><li><a href="#%E5%AE%9A%E4%B9%89%E4%BD%9C%E4%B8%9A%E7%BB%93%E6%9E%84">定义作业结构</a><ul><li><a href="#ijobentitybatch-%E4%B8%8E-ijobentitybatchwithindex">IJobEntityBatch 与 IJobEntityBatchWithIndex</a></li><li><a href="#%E5%A3%B0%E6%98%8E%E6%82%A8%E7%9A%84%E5%B7%A5%E4%BD%9C%E8%AE%BF%E9%97%AE%E7%9A%84%E6%95%B0%E6%8D%AE">声明您的工作访问的数据</a><ul><li><a href="#%E8%AE%BF%E9%97%AE%E5%AE%9E%E4%BD%93%E7%BB%84%E4%BB%B6%E5%92%8C%E7%BC%93%E5%86%B2%E5%8C%BA%E6%95%B0%E6%8D%AE">访问实体组件和缓冲区数据</a></li><li><a href="#%E6%9F%A5%E6%89%BE%E5%85%B6%E4%BB%96%E5%AE%9E%E4%BD%93%E7%9A%84%E6%95%B0%E6%8D%AE">查找其他实体的数据</a></li><li><a href="#%E8%AE%BF%E9%97%AE%E5%85%B6%E4%BB%96%E6%95%B0%E6%8D%AE">访问其他数据</a></li></ul></li><li><a href="#%E7%BC%96%E5%86%99%E6%89%A7%E8%A1%8C%E5%87%BD%E6%95%B0">编写执行函数</a><ul><li><a href="#batchinchunk-%E5%8F%82%E6%95%B0">batchInChunk 参数</a></li><li><a href="#batchindex-%E5%8F%82%E6%95%B0">batchIndex 参数</a></li><li><a href="#indexoffirstentityinquery-%E5%8F%82%E6%95%B0">indexOfFirstEntityInQuery 参数</a></li><li><a href="#%E5%8F%AF%E9%80%89%E7%BB%84%E4%BB%B6">可选组件</a></li></ul></li></ul></li><li><a href="#%E5%AE%89%E6%8E%92%E5%B7%A5%E4%BD%9C">安排工作</a><ul><li><a href="#%E8%B0%83%E5%BA%A6%E9%80%89%E9%A1%B9">调度选项</a></li><li><a href="#%E8%AE%BE%E7%BD%AE%E6%89%B9%E9%87%8F%E5%A4%A7%E5%B0%8F">设置批量大小</a></li></ul></li><li><a href="#%E8%B7%B3%E8%BF%87%E5%AE%9E%E4%BD%93%E4%B8%8D%E5%8F%98%E7%9A%84%E5%9D%97">跳过实体不变的块</a></li></ul></li><li><a href="#%E6%89%8B%E5%8A%A8%E8%BF%AD%E4%BB%A3%E6%95%B0%E6%8D%AE">手动迭代数据</a><ul><li><a href="#%E5%A6%82%E4%BD%95%E6%89%8B%E5%8A%A8%E8%BF%AD%E4%BB%A3%E6%95%B0%E6%8D%AE">如何手动迭代数据</a></li></ul></li></ul></li><li><a href="#%E7%B3%BB%E7%BB%9F%E6%9B%B4%E6%96%B0%E9%A1%BA%E5%BA%8F">系统更新顺序</a><ul><li><a href="#%E7%BB%84%E4%BB%B6%E7%B3%BB%E7%BB%9F%E7%BB%84">组件系统组</a></li><li><a href="#%E7%B3%BB%E7%BB%9F%E6%8E%92%E5%BA%8F%E5%B1%9E%E6%80%A7">系统排序属性</a></li><li><a href="#%E9%BB%98%E8%AE%A4%E7%B3%BB%E7%BB%9F%E7%BB%84">默认系统组</a></li><li><a href="#%E5%A4%9A%E4%B8%AA%E4%B8%96%E7%95%8C">多个世界</a></li></ul></li><li><a href="#%E4%BD%BF%E7%94%A8%E4%BD%9C%E4%B8%9A%E5%9C%A8%E5%A4%9A%E4%B8%AA%E7%BA%BF%E7%A8%8B%E4%B8%8A%E8%B0%83%E5%BA%A6%E6%95%B0%E6%8D%AE">使用作业在多个线程上调度数据</a><ul><li><a href="#%E5%B7%A5%E4%BD%9C%E6%89%A9%E5%B1%95">工作扩展</a></li><li><a href="#%E9%80%9A%E7%94%A8%E8%81%8C%E4%BD%8D">通用职位</a><ul><li><a href="#%E4%BB%8E-burst-%E7%BC%96%E8%AF%91%E4%BB%A3%E7%A0%81%E4%B8%AD%E8%B0%83%E5%BA%A6%E9%80%9A%E7%94%A8%E4%BD%9C%E4%B8%9A">从 Burst 编译代码中调度通用作业</a></li><li><a href="#%E5%85%B7%E4%BD%93%E4%BD%9C%E4%B8%9A%E7%B1%BB%E5%9E%8B%E7%9A%84%E8%87%AA%E5%8A%A8%E6%B3%A8%E5%86%8C">具体作业类型的自动注册</a></li><li><a href="#%E4%BD%9C%E4%B8%9A%E5%8C%96%E5%88%86%E6%8B%A3">作业化分拣</a></li></ul></li><li><a href="#%E4%BD%BF%E7%94%A8-jobwithcode-%E5%AE%89%E6%8E%92%E5%90%8E%E5%8F%B0%E4%BD%9C%E4%B8%9A">使用 Job.WithCode 安排后台作业</a><ul><li><a href="#%E4%BD%BF%E7%94%A8-jobwithcode">使用 Job.WithCode</a></li><li><a href="#%E6%8D%95%E8%8E%B7%E5%8F%98%E9%87%8F-1">捕获变量</a></li><li><a href="#%E6%89%A7%E8%A1%8C-jobwithcode-lambda-%E8%A1%A8%E8%BE%BE%E5%BC%8F">执行 Job.WithCode lambda 表达式</a></li><li><a href="#%E4%BE%9D%E8%B5%96%E5%85%B3%E7%B3%BB-1">依赖关系</a></li></ul></li><li><a href="#%E4%BD%9C%E4%B8%9A%E4%BE%9D%E8%B5%96">作业依赖</a><ul><li><a href="#%E4%BD%9C%E4%B8%9A%E4%BE%9D%E8%B5%96%E6%9B%B4%E6%96%B0%E9%A1%BA%E5%BA%8F">作业依赖更新顺序</a><ul><li><a href="#%E8%A6%86%E7%9B%96%E9%BB%98%E8%AE%A4%E9%A1%BA%E5%BA%8F">覆盖默认顺序</a></li><li><a href="#entitiesforeach-%E7%9A%84%E4%BD%9C%E4%B8%9A%E4%BE%9D%E8%B5%96%E9%A1%BA%E5%BA%8F">Entities.ForEach 的作业依赖顺序</a></li></ul></li><li><a href="#%E6%9B%B4%E5%A4%9A%E8%B5%84%E6%BA%90">更多资源</a></li></ul></li></ul></li><li><a href="#%E4%BD%BF%E7%94%A8-entityquery-%E6%9F%A5%E8%AF%A2%E6%95%B0%E6%8D%AE-1">使用 EntityQuery 查询数据</a><ul><li><a href="#%E5%88%9B%E5%BB%BA%E5%AE%9E%E4%BD%93%E6%9F%A5%E8%AF%A2">创建实体查询</a><ul><li><a href="#%E6%8C%87%E5%AE%9A%E7%B3%BB%E7%BB%9F%E9%80%89%E6%8B%A9%E7%9A%84%E5%8E%9F%E5%9E%8B">指定系统选择的原型</a><ul><li><a href="#%E6%8C%89%E5%86%99%E5%85%A5%E7%BB%84%E8%BF%87%E6%BB%A4">按写入组过滤</a></li></ul></li></ul></li><li><a href="#%E5%AE%9A%E4%B9%89%E8%BF%87%E6%BB%A4%E5%99%A8">定义过滤器</a><ul><li><a href="#%E4%BD%BF%E7%94%A8%E5%85%B1%E4%BA%AB%E7%BB%84%E4%BB%B6%E8%BF%87%E6%BB%A4%E5%99%A8">使用共享组件过滤器</a></li><li><a href="#%E4%BD%BF%E7%94%A8%E6%9B%B4%E6%94%B9%E8%BF%87%E6%BB%A4%E5%99%A8">使用更改过滤器</a></li></ul></li><li><a href="#%E6%8C%89%E5%90%AF%E7%94%A8%E7%BB%84%E4%BB%B6%E8%BF%87%E6%BB%A4">按启用组件过滤</a></li><li><a href="#%E5%90%88%E5%B9%B6%E6%9F%A5%E8%AF%A2">合并查询</a></li><li><a href="#%E6%89%A7%E8%A1%8C%E6%9F%A5%E8%AF%A2">执行查询</a></li><li><a href="#%E7%BC%96%E8%BE%91%E5%99%A8%E4%B8%AD%E7%9A%84%E6%9F%A5%E8%AF%A2">编辑器中的查询</a></li></ul></li><li><a href="#%E4%BD%BF%E7%94%A8-entitycommandbuffer-%E5%AE%89%E6%8E%92%E6%95%B0%E6%8D%AE%E6%9B%B4%E6%94%B9">使用 EntityCommandBuffer 安排数据更改</a><ul><li><a href="#entitycommandbuffer-%E6%96%B9%E6%B3%95">EntityCommandBuffer 方法</a></li><li><a href="#%E5%9C%A8%E5%8D%95%E7%BA%BF%E7%A8%8B%E4%BD%9C%E4%B8%9A%E4%B8%AD%E4%BD%BF%E7%94%A8-entitycommandbuffer">在单线程作业中使用 EntityCommandBuffer</a></li><li><a href="#%E5%9C%A8%E5%B9%B6%E8%A1%8C%E4%BD%9C%E4%B8%9A%E4%B8%AD%E4%BD%BF%E7%94%A8-entitycommandbuffer">在并行作业中使用 EntityCommandBuffer</a><ul><li><a href="#%E7%A1%AE%E5%AE%9A%E6%80%A7%E5%9B%9E%E6%94%BE">确定性回放</a></li></ul></li><li><a href="#%E9%87%8D%E7%94%A8-entitycommandbuffer-%E5%AE%9E%E4%BE%8B">重用 EntityCommandBuffer 实例</a></li><li><a href="#%E5%A4%9A%E9%87%8D%E5%9B%9E%E6%94%BE">多重回放</a></li><li><a href="#%E5%9C%A8%E4%B8%BB%E7%BA%BF%E7%A8%8B%E4%B8%8A%E4%BD%BF%E7%94%A8-entitycommandbuffer">在主线程上使用 EntityCommandBuffer</a></li><li><a href="#%E4%BD%BF%E7%94%A8-entitycommandbuffersystem-%E8%87%AA%E5%8A%A8%E6%92%AD%E6%94%BE%E5%92%8C%E5%A4%84%E7%90%86%E5%91%BD%E4%BB%A4%E7%BC%93%E5%86%B2%E5%8C%BA">使用 EntityCommandBufferSystem 自动播放和处理命令缓冲区</a><ul><li><a href="#%E9%BB%98%E8%AE%A4-entitycommandbuffersystem-%E7%B3%BB%E7%BB%9F">默认 EntityCommandBufferSystem 系统</a></li></ul></li><li><a href="#%E5%BB%B6%E8%BF%9F%E5%AE%9E%E4%BD%93">延迟实体</a></li><li><a href="#%E5%9C%A8-entitiesforeach-%E6%96%B9%E6%B3%95%E4%B8%AD%E4%BD%BF%E7%94%A8%E5%91%BD%E4%BB%A4%E7%BC%93%E5%86%B2%E5%8C%BA">在 Entities.ForEach 方法中使用命令缓冲区</a><ul><li><a href="#%E5%9C%A8-entitiesforeach-%E4%B8%AD%E5%9B%9E%E6%94%BE-entitycommandbuffer">在 Entities.forEach 中回放 EntityCommandBuffer</a></li></ul></li></ul></li><li><a href="#%E6%9F%A5%E6%89%BE%E4%BB%BB%E6%84%8F%E6%95%B0%E6%8D%AE">查找任意数据</a><ul><li><a href="#%E5%9C%A8%E7%B3%BB%E7%BB%9F%E4%B8%AD%E6%9F%A5%E6%89%BE%E5%AE%9E%E4%BD%93%E6%95%B0%E6%8D%AE">在系统中查找实体数据</a></li><li><a href="#%E5%9C%A8%E4%BD%9C%E4%B8%9A%E4%B8%AD%E6%9F%A5%E6%89%BE%E5%AE%9E%E4%BD%93%E6%95%B0%E6%8D%AE">在作业中查找实体数据</a></li><li><a href="#%E6%95%B0%E6%8D%AE%E8%AE%BF%E9%97%AE%E9%94%99%E8%AF%AF">数据访问错误</a></li></ul></li><li><a href="#%E5%86%99%E5%85%A5%E7%BB%84">写入组</a><ul><li><a href="#%E4%BD%BF%E7%94%A8%E5%86%99%E7%BB%84">使用写组</a></li><li><a href="#%E7%BC%96%E5%86%99%E7%BB%84%E7%A4%BA%E4%BE%8B">编写组示例</a></li><li><a href="#%E5%88%9B%E5%BB%BA%E5%86%99%E5%85%A5%E7%BB%84">创建写入组</a></li><li><a href="#%E5%90%AF%E7%94%A8%E5%86%99%E7%BB%84%E8%BF%87%E6%BB%A4">启用写组过滤</a></li><li><a href="#%E8%A6%86%E7%9B%96%E5%8F%A6%E4%B8%80%E4%B8%AA%E4%BD%BF%E7%94%A8%E5%86%99%E5%85%A5%E7%BB%84%E7%9A%84%E7%B3%BB%E7%BB%9F">覆盖另一个使用写入组的系统</a></li><li><a href="#%E6%89%A9%E5%B1%95%E5%8F%A6%E4%B8%80%E4%B8%AA%E4%BD%BF%E7%94%A8%E5%86%99%E7%BB%84%E7%9A%84%E7%B3%BB%E7%BB%9F">扩展另一个使用写组的系统</a></li></ul></li><li><a href="#%E7%89%88%E6%9C%AC%E5%8F%B7">版本号</a><ul><li><a href="#%E7%89%88%E6%9C%AC%E5%8F%B7%E7%BB%93%E6%9E%84">版本号结构</a></li><li><a href="#%E5%AE%9E%E4%BD%93%E7%89%88%E6%9C%AC%E5%8F%B7">实体版本号</a></li><li><a href="#%E4%B8%96%E7%95%8C%E7%89%88%E6%9C%AC%E5%8F%B7">世界版本号</a></li><li><a href="#%E4%BD%9C%E4%B8%9A%E7%BB%84%E4%BB%B6%E7%B3%BB%E7%BB%9F%E7%89%88%E6%9C%AC%E5%8F%B7">作业组件系统版本号</a><ul><li><a href="#%E5%9D%97changeversion">块.ChangeVersion</a></li></ul></li><li><a href="#%E9%9D%9E%E5%85%B1%E4%BA%AB%E7%BB%84%E4%BB%B6%E7%89%88%E6%9C%AC%E5%8F%B7">非共享组件版本号</a></li><li><a href="#%E5%85%B1%E4%BA%AB%E7%BB%84%E4%BB%B6%E7%89%88%E6%9C%AC%E5%8F%B7">共享组件版本号</a></li></ul></li></ul></li><li><a href="#%E7%BB%84%E4%BB%B6%E7%9A%84%E4%BD%BF%E7%94%A8">组件的使用</a><ul><li><a href="#%E7%BB%84%E4%BB%B6%E7%B1%BB%E5%9E%8B">组件类型</a></li><li><a href="#%E5%90%91%E5%AE%9E%E4%BD%93%E6%B7%BB%E5%8A%A0%E7%BB%84%E4%BB%B6">向实体添加组件</a></li><li><a href="#%E4%BB%8E%E5%AE%9E%E4%BD%93%E4%B8%AD%E7%A7%BB%E9%99%A4%E7%BB%84%E4%BB%B6">从实体中移除组件</a></li><li><a href="#%E8%AF%BB%E5%8F%96%E5%92%8C%E5%86%99%E5%85%A5%E7%BB%84%E4%BB%B6%E5%80%BC">读取和写入组件值</a></li></ul></li><li><a href="#%E5%AE%9E%E4%BD%93%E5%8C%85%E4%B8%AD%E7%9A%84%E8%84%9A%E6%9C%AC">实体包中的脚本</a><ul><li><a href="#%E4%BD%BF%E7%94%A8-baking-%E8%BD%AC%E6%8D%A2%E6%95%B0%E6%8D%AE">使用 Baking 转换数据</a></li><li><a href="#%E4%BD%BF%E7%94%A8%E6%96%B9%E9%9D%A2%E7%BB%84%E7%BB%87%E4%BB%A3%E7%A0%81">使用方面组织代码</a></li><li><a href="#%E8%BD%AC%E6%8D%A2%E7%B3%BB%E7%BB%9F">转换系统</a></li><li><a href="#blob-%E8%B5%84%E4%BA%A7">Blob 资产</a></li><li><a href="#%E5%9C%A8%E8%BF%90%E8%A1%8C%E6%97%B6%E5%8A%A0%E8%BD%BD%E5%9C%BA%E6%99%AF">在运行时加载场景</a></li></ul></li><li><a href="#%E5%9C%A8%E7%BC%96%E8%BE%91%E5%99%A8%E4%B8%AD%E5%B7%A5%E4%BD%9C">在编辑器中工作</a><ul><li><a href="#%E5%AE%9E%E4%BD%93%E9%A6%96%E9%80%89%E9%A1%B9%E5%8F%82%E8%80%83">实体首选项参考</a></li><li><a href="#%E4%BD%BF%E7%94%A8%E5%88%9B%E4%BD%9C%E5%92%8C%E8%BF%90%E8%A1%8C%E6%97%B6%E6%95%B0%E6%8D%AE">使用创作和运行时数据</a></li><li><a href="#%E5%AE%9E%E4%BD%93%E7%AA%97%E5%8F%A3">实体窗口</a></li><li><a href="#%E5%AE%9E%E4%BD%93inspectors">实体Inspectors</a></li></ul></li><li><a href="#%E6%80%A7%E8%83%BD%E5%92%8C%E8%B0%83%E8%AF%95">性能和调试</a><ul><li><a href="#entities-profiler-modules">Entities Profiler modules</a></li><li><a href="#entities-journaling">Entities Journaling</a></li></ul></li></ul><h1 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h1><table><thead><tr><th>标题</th><th>描述</th></tr></thead><tbody><tr><td>Entity</td><td>实体是不包含代码的游戏对象的轻量级替代品。</td></tr><tr><td>Component</td><td>组件包含有关单个实体的数据。</td></tr><tr><td>System</td><td>向系统添加代码以处理实体和组件。</td></tr><tr><td>World</td><td>世界将实体组织成孤立的群体。</td></tr><tr><td>Archetype</td><td>原型是一个或多个实体可能具有的组件的独特组合。</td></tr><tr><td>Structural changes</td><td>结构更改是会影响应用程序性能的资源密集型操作。</td></tr></tbody></table><h2 id="实体"><a href="#实体" class="headerlink" title="实体"></a>实体</h2><p>实体表示程序中具有自己数据集的离散事物，例如角色、视觉效果、UI 元素，甚至是网络事务等抽象事物。实体类似于非托管(unmanaged)的轻量级游戏对象，代表程序的特定元素。但是，实体仅充当将各个唯一组件关联在一起的 ID，而不包含任何代码或充当其关联组件的容器。</p><p>实体的集合存储在一个世界中，世界的 EntityManager 管理世界中的所有实体。 EntityManager 包含可用于创建、销毁和修改该世界中的实体的方法。其中包括以下常用方法：</p><table><thead><tr><th>方法</th><th>描述</th></tr></thead><tbody><tr><td>CreateEntity</td><td>创建一个新实体。</td></tr><tr><td>Instantiate</td><td>复制现有实体并从该副本创建新实体。</td></tr><tr><td>DestroyEntity</td><td>销毁现有实体。</td></tr><tr><td>AddComponent</td><td>将组件添加到现有实体。</td></tr><tr><td>RemoveComponent</td><td>从现有实体中删除组件。</td></tr><tr><td>GetComponent</td><td>检索实体组件的值。</td></tr><tr><td>SetComponent</td><td>覆盖实体组件的值。</td></tr></tbody></table><div class="note note-info">            <p>当您创建或销毁实体时，这是一种结构更改，会影响应用程序的性能。有关详细信息，请参阅有关结构更改的文档</p>          </div><p>实体没有类型，但您可以根据与其关联的组件类型对实体进行分类。 EntityManager 跟踪现有实体上组件的唯一组合。这些独特的组合称为原型（Archetype）。有关原型如何工作的更多信息，请参阅有关原型概念的文档。</p><h3 id="编辑器中的实体"><a href="#编辑器中的实体" class="headerlink" title="编辑器中的实体"></a>编辑器中的实体</h3><p>在编辑器中，以下图标代表实体：<img src="https://docs.unity3d.com/Packages/com.unity.entities@1.0/manual/images/editor-entity-icon.png" style="display: inline;"> 。当您使用特定的实体窗口和检查器时，您会看到这一点。</p><h2 id="组件"><a href="#组件" class="headerlink" title="组件"></a>组件</h2><p>在实体组件系统 (ECS) 体系结构中，组件包含系统可以读取或写入的实体数据。</p><p>使用没有方法的<code>IComponentData</code>接口将一个结构体标记为组件类型。此组件类型只能包含非托管数据，并且它们可以包含方法，但最佳实践是它们只有纯数据。如果你想创建一个托管组件，你将它定义为一个类。有关详细信息，请参阅托管组件。</p><p>有不同类型的组件用于不同的目的。根据您希望如何管理项目中的数据，某些组件允许对应用程序的性能进行更精细的控制。有关详细信息，请参阅组件类型。</p><p>一组独特的实体的组件称为原型。 ECS 架构按原型将组件数据存储在称为块的 16KiB 内存块中。有关 ECS 如何存储组件数据的更多信息，请参阅原型概念文档。</p><h2 id="系统"><a href="#系统" class="headerlink" title="系统"></a>系统</h2><p>系统提供将组件数据从当前状态转换为下一个状态的逻辑。例如，系统可能会通过速度乘以自上次更新以来的时间间隔来更新所有移动实体的位置。</p><p>系统每帧在主线程上运行一次。系统被组织成被称为系统组的层次结构，您可以使用这些系统组来组织系统更新的顺序。</p><p>您可以创建非托管或托管系统。要定义托管系统，请创建一个继承自<code>SystemBase</code>的类。要定义非托管系统，请创建一个继承自<code>ISystem</code>的结构。</p><p><code>ISystem</code>和<code>SystemBase</code>都有三种方法，您可以覆盖<code>OnUpdate</code>、<code>OnCreate</code>和<code>OnDestroy</code>。系统的<code>OnUpdate</code>方法每帧执行一次。</p><p>一个系统只能处理一个世界中的实体，所以一个系统与一个特定的世界相关联。您可以使用<code>World</code>属性返回系统附加到的世界。</p><p>默认情况下，自动引导过程会为每个系统和系统组创建一个实例。引导创建一个默认世界，其中包含三个系统组：<code>InitializationSystemGroup</code>、<code>SimulationSystemGroup</code>和<code>PresentationSystemGroup</code>。默认情况下系统的一个实例被添加到<code>SimulationSystemGroup</code>。您可以使用<code>[UpdateInGroup]</code>属性来覆盖此行为。</p><p>要禁用自动引导过程，请使用脚本定义<code>#UNITY_DISABLE_AUTOMATIC_SYSTEM_BOOTSTRAP</code>。</p><h3 id="系统类型"><a href="#系统类型" class="headerlink" title="系统类型"></a>系统类型</h3><p>您可以使用多种类型的系统：<br> -<code>SystemBase</code>: 为托管系统提供基类。<br> -<code>ISystem</code>: 为非托管系统提供接口。<br> -<code>EntityCommandBufferSystem</code>: 为其他系统提供实体命令缓冲区实例。这允许您将结构更改组合在一起以提高应用程序的性能。<br> -<code>ComponentSystemGroup</code>: 为系统提供嵌套组织和更新顺序。</p><h3 id="系统组"><a href="#系统组" class="headerlink" title="系统组"></a>系统组</h3><p>一个系统组可以有系统和其他系统组作为它的子系统。系统组有一个可以覆盖的更新方法，基本方法按排序顺序更新组的子级。</p><p>每次将组添加到系统组时，它都会重新排序系统更新顺序。要控制系统组的更新顺序，请将<code>UpdateBefore</code>或<code>UpdateAfter</code>属性添加到系统以指定它应该在之前或之后更新哪些系统。这些属性仅适用于同一系统组的子系统。例如：</p><figure><div class="code-wrapper"><pre class="line-numbers language-C#" data-language="C#"><code class="language-C#">&#x2F;&#x2F; 如果 PeachSystem 和 DaisySystem 是同一组的子级，&#x2F;&#x2F; 则实体组件系统按排序顺序将 PeachSystem 放在 DaisySystem 之前的某处。    [UpdateBefore(typeof(DaisySystem))]    public partial class PeachSystem : SystemBase &#123; &#125;</code></pre></div></figure><p>要创建系统组，请创建一个继承自<code>ComponentSystemGroup</code>的类。因为系统属于一个世界，所以必须使用<code>World.GetOrCreateSystem</code>创建一个系统。要将系统添加到组，请使用<code>group.AddSystemToUpdateList</code>。您可以将其他系统组添加到现有系统组。</p><p>有关详细信息，请参阅有关系统更新顺序的文档。</p><h3 id="检查系统"><a href="#检查系统" class="headerlink" title="检查系统"></a>检查系统</h3><p>您可以使用“Systems”窗口检查每个世界中系统的更新顺序，并查看系统组的完整层次结构。有关详细信息，请参阅有关系统窗口参考的文档。</p><h4 id="编辑器中的系统"><a href="#编辑器中的系统" class="headerlink" title="编辑器中的系统"></a>编辑器中的系统</h4><p>在编辑器中，以下图标代表不同类型的系统。当您使用特定的实体窗口和检查器时，您会看到这一点。</p><table><thead><tr><th>图标</th><th>含义</th></tr></thead><tbody><tr><td><img src="https://docs.unity3d.com/Packages/com.unity.entities@1.0/manual/images/editor-system-group.png" style="display: inline;"></td><td>一个系统组</td></tr><tr><td><img src="https://docs.unity3d.com/Packages/com.unity.entities@1.0/manual/images/editor-system.png" style="display: inline;"></td><td>一个系统</td></tr><tr><td><img src="https://docs.unity3d.com/Packages/com.unity.entities@1.0/manual/images/editor-system-start-step.png" style="display: inline;"></td><td>一个带有<code>OrderFirst</code>参数的，设置为在开头执行的<code>实体命令缓冲区系统</code>。</td></tr><tr><td><img src="https://docs.unity3d.com/Packages/com.unity.entities@1.0/manual/images/editor-system-end-step.png" style="display: inline;"></td><td>一个带有<code>OrderLast</code>参数的，设置为在末尾执行的<code>实体命令缓冲区系统</code>。</td></tr></tbody></table><h2 id="世界"><a href="#世界" class="headerlink" title="世界"></a>世界</h2><p>世界是实体的集合。实体的 ID 号仅在其自己的世界中是唯一的。一个世界有一个<code>EntityManager</code>结构体，你可以用它来创建、销毁和修改世界中的实体。</p><p>一个世界拥有一组系统，这些系统通常只访问同一个世界中的实体。此外，世界中具有相同组件类型集的一组实体一起存储在原型中，原型决定了你程序中的组件在内存中的组织方式。</p><h3 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h3><p>默认情况下，当您进入播放模式时，Unity 会创建一个世界实例并将每个系统添加到这个默认世界。</p><p>如果您更喜欢手动将系统添加到默认世界，请创建一个实现<code>ICustomBootstrap</code>接口的类。</p><p>如果你想完全手动控制引导，使用这些定义来禁用默认的世界创建：</p><p>-<code>#UNITY_DISABLE_AUTOMATIC_SYSTEM_BOOTSTRAP_RUNTIME_WORLD</code>：禁用默认运行时世界的生成。<br>-<code>#UNITY_DISABLE_AUTOMATIC_SYSTEM_BOOTSTRAP_EDITOR_WORLD</code>：禁用默认编辑器世界的生成。<br>-<code>#UNITY_DISABLE_AUTOMATIC_SYSTEM_BOOTSTRAP</code>：禁用两个默认世界的生成。</p><p>然后，您的代码负责创建您的世界和系统，并将您的世界的更新插入到 Unity 可编写脚本的 PlayerLoop 中。</p><p>Unity 使用<code>WorldFlags</code>在编辑器中创建专门的世界。</p><h3 id="时间考量"><a href="#时间考量" class="headerlink" title="时间考量"></a>时间考量</h3><p>世界控制其中系统的<code>Time</code>属性的值。系统的<code>Time</code>属性是当前世界时间的别名。</p><p>默认情况下，Unity 为每个世界创建一个<code>TimeData</code>实体，由<code>UpdateWorldTimeSystem</code>实例更新。这反映了自上一帧以来经过的时间。</p><p><code>FixedStepSimulationSystemGroup</code>中的系统处理时间的方式与其他系统组不同。固定步长模拟组中的系统以固定间隔更新，而不是在当前增量时间更新一次，并且如果固定间隔是帧时间的足够小的一部分，则每帧可能更新不止一次。</p><p>如果您需要对世界中的时间进行更多控制，可以使用<code>World.SetTime</code>直接指定一个时间值。您还可以使用<code>PushTime</code>临时更改世界时间，使用<code>PopTime</code>返回到之前的时间（在时间栈中）。</p><h2 id="原型"><a href="#原型" class="headerlink" title="原型"></a>原型</h2><p>原型是世界中所有实体的唯一标识符，这些实体具有相同的唯一组件类型组合。例如，一个世界中具有组件类型 A 和 B 的所有实体共享一个原型。所有具有组件类型 A、B 和 C 的实体共享一个不同的原型，所有具有组件类型 A 和 Z 的实体共享另一个原型。</p><p>当您从实体中添加或删除组件类型时，世界的<code>EntityManager</code>会将实体移动到适当的原型。例如，如果实体具有组件类型 A、B 和 C，并且您删除了它的 B<code>组件，EntityManager</code>会将实体移动到具有组件类型 A 和 C 的原型。如果不存在这样的原型，则<code>EntityManager</code>会创建它。</p><div class="note note-warning">            <p>频繁移动实体会占用大量资源并降低应用程序的性能。有关详细信息，请参阅有关结构变更概念的文档。</p>          </div><p>基于原型的实体组织意味着通过组件类型查询实体是高效的。例如，如果您想要查找具有组件类型 A 和 B 的所有实体，您可以找到具有这些组件类型的所有原型，这比扫描所有单个实体的性能更高。世界中的现有原型集往往会在程序生命周期的早期稳定下来，因此您可以缓存查询以获得更快的性能。</p><p>原型只有在它的世界被销毁时才会被销毁。</p><h3 id="原型块-Archetype-chunks"><a href="#原型块-Archetype-chunks" class="headerlink" title="原型块 Archetype chunks"></a>原型块 Archetype chunks</h3><p>所有具有相同原型的实体和组件都存储在称为块的统一内存块中。每个块由 16KiB 组成，它们可以存储的实体数量取决于块原型中组件的数量和大小。<code>EntityManager</code>根据需要创建和销毁块。</p><p>块包含每个组件类型的数组，以及用于存储实体 ID 的附加数组。例如，在具有组件类型 A 和 B 的原型中，每个块都具有三个数组：一个数组用于 A 组件值，一个数组用于 B 组件值，一个数组用于实体 ID。</p><p>块的数组是紧密打包的：块的第一个实体存储在这些数组的索引 0 处，块的第二个实体存储在索引 1 处，后续实体存储在连续的索引中。当一个新的实体被添加到块中时，它被存储在第一个可用的索引中。当一个实体从块中移除时（因为它被破坏或被移动到另一个原型），块的最后一个实体被移动以填补空白。</p><p>将实体添加到原型时，如果原型的现有块已满，则<code>EntityManager</code>会创建一个新块。当最后一个实体从块中移除时，<code>EntityManager</code>会销毁该块。</p><h3 id="编辑器中的原型"><a href="#编辑器中的原型" class="headerlink" title="编辑器中的原型"></a>编辑器中的原型</h3><p>原型窗口列出了项目中所有世界的原型，并显示了每个原型的已分配和未使用内存量。</p><p>在编辑器中，以下图标代表原型：<img src="https://docs.unity3d.com/Packages/com.unity.entities@1.0/manual/images/editor-archetype-icon.png" style="display: inline;">。</p><h2 id="结构变化"><a href="#结构变化" class="headerlink" title="结构变化"></a>结构变化</h2><p>导致 Unity 重新组织内存块或内存中块的内容的操作称为结构更改。了解哪些操作是结构更改很重要，因为它们可能是资源密集型的，您只能在主线程上执行它们；不是来自jobs。</p><p>以下操作被视为结构更改：</p><ul><li>创建或销毁实体。</li><li>添加或删除组件。</li><li>设置一个共享组件值。</li></ul><h3 id="创建一个实体"><a href="#创建一个实体" class="headerlink" title="创建一个实体"></a>创建一个实体</h3><p>当您创建一个实体时，Unity 要么将实体添加到现有块中，要么如果没有块可用于实体的原型，则创建一个新块并将实体添加到其中。</p><h3 id="销毁一个实体"><a href="#销毁一个实体" class="headerlink" title="销毁一个实体"></a>销毁一个实体</h3><p>当您销毁一个实体时，Unity 会将该实体从其块中移除。如果移除实体在块中留下空隙，Unity 会移动块中的最后一个实体来填补空隙。如果删除实体使块为空，Unity 会释放块。</p><h3 id="添加或删除组件"><a href="#添加或删除组件" class="headerlink" title="添加或删除组件"></a>添加或删除组件</h3><p>在实体中添加或删除组件时，您会更改实体的原型。 Unity 将每个实体存储在与实体原型匹配的块中。这意味着如果您更改实体的原型，Unity 必须将该实体移动到另一个块。如果不存在合适的块，Unity 会创建一个新块。如果移动使前一个块有间隙或留空，Unity 将移动块中的最后一个实体以分别填充间隙或释放块。</p><h3 id="设置一个共享组件值"><a href="#设置一个共享组件值" class="headerlink" title="设置一个共享组件值"></a>设置一个共享组件值</h3><p>当您设置实体的共享组件的值时，Unity 会将实体移动到与新共享组件值匹配的块中。如果不存在合适的块，Unity 会创建一个新块。如果移动使前一个块有间隙或为空，Unity 将移动块中的最后一个实体以填充间隙或分别释放块。</p><div class="note note-info">            <p>设置常规组件值不是结构更改，因为它不需要 Unity 移动实体。</p>          </div><h3 id="同步点"><a href="#同步点" class="headerlink" title="同步点"></a>同步点</h3><p>您不能直接在作业中进行结构更改，因为它可能会使其他已安排的作业无效，并创建一个同步点。</p><p>一个同步点（sync point）是程序执行中的一个点，它等待到目前为止已经调度的所有作业完成。同步点会限制您在一段时间内使用作业系统中可用的所有工作线程的能力。因此，您的目标应该是避免同步点。 ECS 中数据的结构变化是产生同步点的主要原因。</p><p>结构更改不仅要求同步点，而且还会使对任何组件数据的所有直接引用无效。这包括<code>DynamicBuffer</code>的实例和提供对组件（例如<code>ComponentSystemBase.GetComponentDataFromEntity</code>）的直接访问的方法的结果。</p><h4 id="避免同步点"><a href="#避免同步点" class="headerlink" title="避免同步点"></a>避免同步点</h4><p>您可以使用实体命令缓冲区来排队缓冲结构更改，而不是立即执行它们。您可以在帧的稍后时间执行存储在实体命令缓冲区中的命令。这将跨帧分布的多个同步点减少为单个同步点。</p><p>每个标准<code>ComponentSystemGroup</code>实例都提供<code>EntityCommandBufferSystem</code>作为组中更新的第一个和最后一个系统。如果您从这些标准系统之一获取实体命令缓冲区对象，则所有结构更改都发生在帧中的同一点，从而产生一个同步点。您还可以使用实体命令缓冲区来记录作业中的结构更改，而不是仅在主线程上进行结构更改。</p><p>如果您不能为任务使用实体命令缓冲区，请将所有进行结构更改的系统按系统执行顺序分组在一起。两个都进行结构更改的系统如果按顺序更新，则只会创建一个同步点。</p><h1 id="实现系统"><a href="#实现系统" class="headerlink" title="实现系统"></a>实现系统</h1><p>系统是每帧在主线程上运行一次的代码单元。系统被组织成系统组的层次结构，您可以使用这些系统组来组织系统更新的顺序。有关 ECS 中系统基础知识的更多信息，请参阅系统概念。</p><table><thead><tr><th>标题</th><th>描述</th></tr></thead><tbody><tr><td>使用<code>SystemBase</code>创建系统</td><td>有关如何使用<code>SystemBase</code>创建系统的信息。</td></tr><tr><td>迭代数据</td><td>描述了可以迭代系统中数据的各种方法。</td></tr><tr><td>系统更新顺序</td><td>有关系统更新顺序以及如何使用系统组控制更新顺序的信息。</td></tr><tr><td>使用作业在多个线程上调度数据</td><td>有关如何在系统中使用作业的信息。</td></tr><tr><td>使用<code>EntityQuery</code>查询实体数据</td><td>有关使用<code>EntityQuery</code>查询实体数据的信息。</td></tr><tr><td>使用<code>EntityCommandBuffer</code>调度数据更改</td><td>使用命令缓冲区来延迟对数据的更改。</td></tr><tr><td>查找任意数据</td><td>有关如何查找任意实体数据的信息。</td></tr><tr><td>写入组</td><td>使用写入组覆盖系统的数据。</td></tr><tr><td>版本号</td><td>使用版本号来检测潜在的变化。</td></tr></tbody></table><h2 id="使用SystemBase创建系统"><a href="#使用SystemBase创建系统" class="headerlink" title="使用SystemBase创建系统"></a>使用<code>SystemBase</code>创建系统</h2><p>要创建托管系统，请实现抽象类 <code>SystemBase</code>。</p><p>您必须使用 <code>OnUpdate</code> 系统事件回调来添加您的系统必须在每一帧执行的工作。 <code>ComponentSystemBase</code> 命名空间中的所有其他回调方法都是可选的。</p><p>所有系统事件都在主线程上运行。最佳做法是使用 <code>OnUpdate</code> 方法来安排作业来执行大部分工作。要从系统安排作业，您可以使用以下机制之一：</p><ul><li><code>Entities.ForEach</code>：遍历组件数据。</li><li><code>Job.WithCode</code>：将 lambda 表达式作为单个后台作业执行。</li><li><code>IJobEntity</code>：迭代多个系统中的组件数据。</li><li><code>IJobEntityBatch</code>：按原型块迭代数据。<br>以下示例说明了使用 <code>Entities.ForEach</code> 来实现一个系统，该系统根据一个组件的值更新另一个组件：<figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">public struct Position : IComponentData&#123;    public float3 Value;&#125;public struct Velocity : IComponentData&#123;    public float3 Value;&#125;[RequireMatchingQueriesForUpdate]public partial class ECSSystem : SystemBase&#123;    protected override void OnUpdate()    &#123;        &#x2F;&#x2F; Local variable captured in ForEach        float dT &#x3D; SystemAPI.Time.DeltaTime;        Entities            .WithName(&quot;Update_Displacement&quot;)            .ForEach(                (ref Position position, in Velocity velocity) &#x3D;&gt;                &#123;                    position &#x3D; new Position()                    &#123;                        Value &#x3D; position.Value + velocity.Value * dT                    &#125;;                &#125;            )            .ScheduleParallel();    &#125;&#125;</code></pre></div></figure></li></ul><h3 id="回调方法顺序"><a href="#回调方法顺序" class="headerlink" title="回调方法顺序"></a>回调方法顺序</h3><p><code>SystemBase</code> 中有几个回调方法，Unity 在系统创建过程中的不同点调用，您可以使用它们来安排系统必须在每一帧执行的工作：</p><ul><li><code>OnCreate</code>：创建系统时调用。</li><li><code>OnStartRunning</code>：在第一次调用 <code>OnUpdate</code> 之前以及系统恢复运行时调用。</li><li><code>OnUpdate</code>：只要系统有工作要做，就会在每一帧调用。有关确定系统何时有工作要做的因素的更多信息，请参阅 <code>ShouldRunSystem</code>。</li><li><code>OnStopRunning</code>：在 <code>OnDestroy</code> 之前调用。每当系统停止运行时也会调用，如果没有实体与系统的 <code>EntityQuery</code> 匹配，或者如果系统的 <code>Enabled</code> 属性设置为 <code>false</code>，就会发生这种情况。</li><li><code>OnDestroy</code>：系统被销毁时调用。</li></ul><p>下图说明了系统的事件顺序：<br><img src="https://docs.unity3d.com/Packages/com.unity.entities@1.0/manual/images/SystemEventOrder.png" alt="回调方法顺序"><br>父系统组的 <code>OnUpdate</code> 方法触发其组中所有系统的 <code>OnUpdate</code> 方法。有关系统如何更新的更多信息，请参阅系统的更新顺序。</p><h2 id="迭代数据"><a href="#迭代数据" class="headerlink" title="迭代数据"></a>迭代数据</h2><p>迭代数据是创建系统时需要执行的最常见任务之一。系统通常处理一组实体，从一个或多个组件读取数据，执行计算，然后将结果写入另一个组件。</p><p>迭代实体和组件的最有效方法是在按顺序处理组件的作业中。这利用了所有可用内核和数据局部性的处理能力来避免 CPU 缓存未命中。</p><p>本节介绍如何通过以下方式迭代实体数据：</p><table><thead><tr><th>标题</th><th>描述</th></tr></thead><tbody><tr><td>使用 <code>Entities.ForEach</code> 迭代数据</td><td>如何使用 <code>SystemBase.Entities.ForEach</code> 逐个实体地处理组件数据。</td></tr><tr><td>使用 <code>IJobEntity</code> 迭代数据</td><td>如何使用 <code>IJobEntity</code> 编写一次并创建多个计划。</td></tr><tr><td>遍历成批数据</td><td>如何使用 <code>IJobEntityBatch</code> 遍历包含匹配实体的原型块。</td></tr><tr><td>手动迭代数据</td><td>如何手动迭代实体或原型块。</td></tr></tbody></table><p>您还可以使用 EntityQuery 类来构建数据视图，其中仅包含给定算法或过程所需的特定数据。上面列表中的许多迭代方法显式或内部使用 EntityQuery。有关详细信息，请参阅使用实体查询查询实体数据。</p><h3 id="使用-Entities-ForEach-迭代数据"><a href="#使用-Entities-ForEach-迭代数据" class="headerlink" title="使用 Entities.ForEach 迭代数据"></a>使用 <code>Entities.ForEach</code> 迭代数据</h3><p>如果您使用 SystemBase 类来创建您的系统，则可以使用 Entities.ForEach 构造来定义和执行针对实体及其组件的算法。在编译时，Unity 将每个 ForEach() 调用转换为生成的作业。</p><p>您向 Entities.ForEach 传递一个 lambda 表达式，Unity 会根据 lambda 参数类型生成一个实体查询。当生成的作业运行时，Unity 会为每个与查询匹配的实体调用一次 lambda。 ForEachLambdaJobDescription 表示此生成的作业。</p><h4 id="定义一个-lambda-表达式"><a href="#定义一个-lambda-表达式" class="headerlink" title="定义一个 lambda 表达式"></a>定义一个 lambda 表达式</h4><p>当您定义 Entities.ForEach lambda 表达式时，您可以声明 SystemBase 类在执行该方法时用于传递有关当前实体的信息的参数。</p><p>典型的 lambda 表达式如下所示：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">Entities.ForEach(    (Entity entity,        int entityInQueryIndex,        ref ObjectPosition translation,        in Movement move) &#x3D;&gt; &#123; &#x2F;* .. *&#x2F;&#125;)</code></pre></div></figure><p>您最多可以将八个参数传递给 Entities.ForEach lambda 表达式。如果需要传递更多参数，可以定义自定义委托。有关详细信息，请参阅本文档中有关自定义委托的部分。</p><p>使用标准委托时，必须按以下顺序对参数进行分组：</p><ol><li>按值传递的参数（无参数修饰符）</li><li>可写参数（ref参数修饰符）</li><li>只读参数（在参数修饰符中）<br>您必须在所有组件上使用 ref 或 in 参数修改关键字。如果你不这样做，Unity 传递给你的方法的组件结构是一个副本而不是引用。这意味着它会为只读参数占用额外的内存，并且当函数返回后复制的结构超出范围时，您对组件所做的任何更改都会被静默抛出。</li></ol><p>如果 lambda 表达式不遵循此顺序，并且您还没有创建合适的委托，则编译器会提供类似于以下内容的错误：</p><p>error CS1593: Delegate ‘Invalid_ForEach_Signature_See_ForEach_Documentation_For_Rules_And_Restrictions’ does not take N arguments</p><p>此错误消息将参数数量作为问题，即使问题是参数顺序也是如此。</p><h5 id="自定义代理"><a href="#自定义代理" class="headerlink" title="自定义代理"></a>自定义代理</h5><p>如果要在 ForEach lambda 表达式中使用八个以上的参数，则必须声明自己的委托类型和 ForEach 重载。这允许您使用无限数量的参数，并以您想要的任何顺序放置 ref、in 和 value 参数。</p><p>您还可以在参数列表中的任意位置声明三个命名参数 entity、entityInQueryIndex 和 nativeThreadIndex。不要对这些参数使用 ref 或 in 修饰符。</p><p>以下示例显示 12 个参数，并在 lambda 表达式中使用实体参数：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">static class BringYourOwnDelegate&#123;    &#x2F;&#x2F; Declare the delegate that takes 12 parameters. T0 is used for the Entity argument    public delegate void CustomForEachDelegate&lt;T0, T1, T2, T3, T4, T5, T6, T7, T8, T9, T10, T11&gt;        (T0 t0, in T1 t1, in T2 t2, in T3 t3, in T4 t4, in T5 t5,         in T6 t6, in T7 t7, in T8 t8, in T9 t9, in T10 t10, in T11 t11);    &#x2F;&#x2F; Declare the function overload    public static TDescription ForEach&lt;TDescription, T0, T1, T2, T3, T4, T5, T6, T7, T8, T9, T10, T11&gt;        (this TDescription description, CustomForEachDelegate&lt;T0, T1, T2, T3, T4, T5, T6, T7, T8, T9, T10, T11&gt; codeToRun)        where TDescription : struct, Unity.Entities.CodeGeneratedJobForEach.ISupportForEachWithUniversalDelegate &#x3D;&gt;        LambdaForEachDescriptionConstructionMethods.ThrowCodeGenException&lt;TDescription&gt;();&#125;&#x2F;&#x2F; A system that uses the custom delegate and overload[RequireMatchingQueriesForUpdate]public partial class MayParamsSystem : SystemBase&#123;    protected override void OnUpdate()    &#123;        Entities.ForEach(                (Entity entity0,                    in Data1 d1,                    in Data2 d2,                    in Data3 d3,                    in Data4 d4,                    in Data5 d5,                    in Data6 d6,                    in Data7 d7,                    in Data8 d8,                    in Data9 d9,                    in Data10 d10,                    in Data11 d11                    ) &#x3D;&gt; &#123;&#x2F;* .. *&#x2F;&#125;)            .Run();    &#125;&#125;</code></pre></div></figure><h5 id="组件参数"><a href="#组件参数" class="headerlink" title="组件参数"></a>组件参数</h5><p>要访问与实体关联的组件，您必须将该组件类型的参数传递给 lambda 表达式。编译器会自动将传递给 lambda 表达式的所有组件作为必需组件添加到实体查询中。</p><p>要更新组件值，您必须使用参数列表中的 ref 关键字将其传递给 lambda 表达式。如果没有 ref 关键字，Unity 将对组件的临时副本进行任何修改。</p><p>要声明传递给 lambda 表达式的只读组件，请使用参数列表中的 in 关键字。</p><p>当您使用 ref 时，Unity 会将当前块中的组件标记为已更改，即使 lambda 表达式实际上并未修改它们。为了提高效率，您应该始终使用 in 关键字将您的 lambda 表达式未修改的组件声明为只读。</p><p>以下示例将 Source 组件参数作为只读传递给作业，并将 Destination 组件参数作为可写传递：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">Entities.ForEach(    (ref Destination outputData,        in Source inputData) &#x3D;&gt;    &#123;        outputData.Value &#x3D; inputData.Value;    &#125;)    .ScheduleParallel();</code></pre></div></figure><div class="note note-waring">            <p>You can’t pass chunk components to the Entities.ForEach lambda expression.</p>          </div><p>对于动态缓冲区，使用 DynamicBuffer<T> 而不是缓冲区中存储的组件类型：</T></p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">[RequireMatchingQueriesForUpdate]public partial class BufferSum : SystemBase&#123;    private EntityQuery query;    &#x2F;&#x2F;Schedules the two jobs with a dependency between them    protected override void OnUpdate()    &#123;        &#x2F;&#x2F;The query variable can be accessed here because we are        &#x2F;&#x2F;using WithStoreEntityQueryInField(query) in the entities.ForEach below        int entitiesInQuery &#x3D; query.CalculateEntityCount();        &#x2F;&#x2F;Create a native array to hold the intermediate sums        &#x2F;&#x2F;(one element per entity)        NativeArray&lt;int&gt; intermediateSums            &#x3D; new NativeArray&lt;int&gt;(entitiesInQuery, Allocator.TempJob);        &#x2F;&#x2F;Schedule the first job to add all the buffer elements        Entities            .ForEach((int entityInQueryIndex, in DynamicBuffer&lt;IntBufferData&gt; buffer) &#x3D;&gt;        &#123;            for (int i &#x3D; 0; i &lt; buffer.Length; i++)            &#123;                intermediateSums[entityInQueryIndex] +&#x3D; buffer[i].Value;            &#125;        &#125;)            .WithStoreEntityQueryInField(ref query)            .WithName(&quot;IntermediateSums&quot;)            .ScheduleParallel(); &#x2F;&#x2F; Execute in parallel for each chunk of entities        &#x2F;&#x2F;Schedule the second job, which depends on the first        Job.WithCode(() &#x3D;&gt;        &#123;            int result &#x3D; 0;            for (int i &#x3D; 0; i &lt; intermediateSums.Length; i++)            &#123;                result +&#x3D; intermediateSums[i];            &#125;            &#x2F;&#x2F;Not burst compatible:            Debug.Log(&quot;Final sum is &quot; + result);        &#125;)            .WithDisposeOnCompletion(intermediateSums)            .WithoutBurst()            .WithName(&quot;FinalSum&quot;)            .Schedule(); &#x2F;&#x2F; Execute on a single, background thread    &#125;&#125;</code></pre></div></figure><h5 id="命名参数"><a href="#命名参数" class="headerlink" title="命名参数"></a>命名参数</h5><p>您还可以将以下命名参数传递给 Entities.ForEach lambda 表达式，Unity 根据作业正在处理的实体为其分配值。</p><table><thead><tr><th>参数</th><th>函数</th></tr></thead><tbody><tr><td>Entity entity</td><td>当前实体的实体实例。只要类型是 Entity，您就可以将参数命名为任何名称。</td></tr><tr><td>int entityInQueryIndex</td><td>实体在查询选择的所有实体列表中的索引。当您有一个需要为每个实体填充唯一值的本机数组时，请使用实体索引值。您可以使用 entityInQueryIndex 作为该数组中的索引。您应该使用 entityInQueryIndex 作为 sortKey 将命令添加到并发实体命令缓冲区。</td></tr><tr><td>int nativeThreadIndex</td><td>执行 lambda 表达式当前迭代的线程的唯一索引。当您使用 Run() 执行 lambda 表达式时，nativeThreadIndex 始终为零。不要使用 nativeThreadIndex 作为并发实体命令缓冲区的 sortKey；使用 entityInQueryIndex 代替。</td></tr><tr><td>EntityCommands commands</td><td>只要类型是 EntityCommands，您就可以将此参数命名为任何名称。仅将此参数与 WithDeferredPlaybackSystem<T>() 或 WithImmediatePlayback() 结合使用。 EntityCommands 类型包含几个方法，这些方法反映了 EntityCommandBuffer 类型中的对应方法。如果您在 Entities.ForEach() 中使用 EntityCommands 实例，编译器会在适当的地方创建额外的代码来处理实体命令缓冲区的创建、调度、播放和处置，在这些代码上调用 EntityCommands 方法的对应方法。</T></td></tr></tbody></table><h4 id="执行-Entities-ForEach-lambda-表达式"><a href="#执行-Entities-ForEach-lambda-表达式" class="headerlink" title="执行 Entities.ForEach lambda 表达式"></a>执行 Entities.ForEach lambda 表达式</h4><p>您可以通过以下方式执行作业 lambda 表达式：</p><ul><li>使用 Schedule() 和 ScheduleParallel() 来安排作业</li><li>使用 Run() 立即在主线程上执行作业。<br>以下示例说明了使用 Entities.ForEach 读取 Velocity 组件并写入 ObjectPosition 组件的 SystemBase 实现：<figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">[RequireMatchingQueriesForUpdate]partial class ApplyVelocitySystem : SystemBase&#123;    protected override void OnUpdate()    &#123;        Entities.ForEach((ref ObjectPosition translation,            in Velocity velocity) &#x3D;&gt;            &#123;                translation.Value +&#x3D; velocity.Value;            &#125;)            .Schedule();    &#125;&#125;</code></pre></div></figure></li></ul><h4 id="选择实体"><a href="#选择实体" class="headerlink" title="选择实体"></a>选择实体</h4><p>Entities.ForEach 有自己的机制来定义用于选择要处理的实体的实体查询。该查询会自动包含您用作 lambda 表达式参数的任何组件。</p><p>您还可以使用 WithAll、WithAny 和 WithNone 子句进一步细化 Entities.ForEach 选择的实体。有关查询选项的完整列表，请参阅 SystemBase.Entities。</p><p>以下示例使用这些子句根据这些参数选择实体：</p><ul><li>该实体具有组件、Destination、Source 和 LocalToWorld</li><li>实体至少具有以下组件之一：ObjectRotation、ObjectPosition 或 ObjectUniformScale</li><li>该实体没有 ObjectNonUniformScale 组件。<figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">Entities.WithAll&lt;LocalToWorld&gt;()    .WithAny&lt;Rotation, ObjectPosition, ObjectUniformScale&gt;()    .WithNone&lt;ObjectNonUniformScale&gt;()    .ForEach((ref Destination outputData, in Source inputData) &#x3D;&gt;    &#123;        &#x2F;* do some work *&#x2F;    &#125;)    .Schedule();</code></pre></div></figure>在此示例中，在 lambda 表达式中仅访问 Destination 和 Source 组件，因为它们是参数列表中的唯一组件。</li></ul><h4 id="访问-EntityQuery-对象"><a href="#访问-EntityQuery-对象" class="headerlink" title="访问 EntityQuery 对象"></a>访问 EntityQuery 对象</h4><p>Entities.ForEach 在 OnCreate 中创建一个 EntityQuery，您可以随时使用它的副本，甚至在调用 Entities.ForEach 之前。</p><p>要访问此实体查询，请使用带有 ref 参数修饰符的 WithStoreEntityQueryInField(ref query)。此方法将对查询的引用分配给您提供的字段。但是，此 EntityQuery 没有 Entities.ForEach 调用设置的任何过滤器。</p><p>以下示例说明如何访问为 Entities.ForEach 构造隐式创建的 EntityQuery 对象。它使用 EntityQuery 对象调用 CalculateEntityCount() 方法并使用此计数创建一个具有足够空间的本机数组来为查询选择的每个实体存储一个值：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">private EntityQuery query;protected override void OnUpdate()&#123;    int dataCount &#x3D; query.CalculateEntityCount();    NativeArray&lt;float&gt; dataSquared        &#x3D; new NativeArray&lt;float&gt;(dataCount, Allocator.Temp);    Entities        .WithStoreEntityQueryInField(ref query)        .ForEach((int entityInQueryIndex, in Data data) &#x3D;&gt;        &#123;            dataSquared[entityInQueryIndex] &#x3D; data.Value * data.Value;        &#125;)        .ScheduleParallel();    Job        .WithCode(() &#x3D;&gt;    &#123;        &#x2F;&#x2F;Use dataSquared array...        var v &#x3D; dataSquared[dataSquared.Length - 1];    &#125;)        .WithDisposeOnCompletion(dataSquared)        .Schedule();&#125;</code></pre></div></figure><h4 id="访问可选组件"><a href="#访问可选组件" class="headerlink" title="访问可选组件"></a>访问可选组件</h4><p>Entities.ForEach lambda 表达式不支持使用 WithAny&lt;T,U&gt; 查询和访问可选组件。</p><p>如果要读取或写入可选组件，请将 Entities.ForEach 构造拆分为可选组件的每个组合的多个作业。例如，如果您有两个可选组件，则需要三个 ForEach 结构：一个包含第一个可选组件，一个包含第二个可选组件，一个包含两个组件。另一种选择是按块使用 IJobChunkiterate。有关详细信息，请参阅按批次迭代数据。</p><h4 id="更改过滤"><a href="#更改过滤" class="headerlink" title="更改过滤"></a>更改过滤</h4><p>您可以使用 WithChangeFilter<T> 来启用更改过滤，只有在当前 SystemBase 实例上次运行后实体中的另一个组件发生更改时，它才会处理组件。更改过滤器中的组件类型必须在 lambda 表达式参数列表中，或者是 WithAll<T> 语句的一部分。例如：</T></T></p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">Entities    .WithChangeFilter&lt;Source&gt;()    .ForEach((ref Destination outputData,        in Source inputData) &#x3D;&gt;        &#123;            &#x2F;* Do work *&#x2F;        &#125;)    .ScheduleParallel();</code></pre></div></figure><p>实体查询最多支持对两种组件类型进行更改过滤。</p><p>Unity 在原型块级别应用更改过滤。如果任何代码访问具有写访问权限的块中的组件，那么 Unity 会将该原型块中的组件类型标记为已更改，即使代码没有更改任何数据。</p><h4 id="共享组件过滤"><a href="#共享组件过滤" class="headerlink" title="共享组件过滤"></a>共享组件过滤</h4><p>Unity 将具有共享组件的实体与其他具有相同共享组件值的实体分组。要选择具有特定共享组件值的实体组，请使用 WithSharedComponentFilter 方法。</p><p>以下示例选择按 Cohort ISharedComponentData 分组的实体。此示例中的 lambda 表达式根据实体的群组设置 DisplayColor IComponentData 组件：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">[RequireMatchingQueriesForUpdate]public partial class ColorCycleJob : SystemBase&#123;    protected unsafe override void OnUpdate()    &#123;        EntityManager.GetAllUniqueSharedComponents&lt;Cohort&gt;(out var cohorts, Allocator.Temp);        for (int i&#x3D;0; i&lt;cohorts.Length; i++)        &#123;            var cohort &#x3D; cohorts[i];            DisplayColor newColor &#x3D; ColorTable.GetNextColor(cohort.Value);            Entities.WithSharedComponentFilter(cohort)                .ForEach((ref DisplayColor color) &#x3D;&gt; &#123; color &#x3D; newColor; &#125;)                .ScheduleParallel();        &#125;    &#125;&#125;</code></pre></div></figure><p>该示例使用 EntityManager 获取所有唯一同类群组值。然后它为每个队列安排一个 lambda 作业，并将新颜色作为捕获变量传递给 lambda 表达式。</p><h4 id="捕获变量"><a href="#捕获变量" class="headerlink" title="捕获变量"></a>捕获变量</h4><p>您可以捕获 Entities.ForEach lambda 表达式的局部变量。当您调用其中一种 Schedule 方法而不是 Run 来使用作业来执行 lambda 表达式时，对捕获的变量以及如何使用它们有一些限制：</p><ul><li>您只能捕获本机容器和 blittable 类型。</li><li>作业只能写入作为本机容器的捕获变量。要返回单个值，请创建一个包含一个元素的本机数组。</li></ul><p>如果您读取本机容器，但不写入它，请始终使用 WithReadOnly(variable) 指定只读访问权限。有关为捕获的变量设置属性的详细信息，请参阅 SystemBase.Entities。 Entities.ForEach 将这些作为方法提供，因为 C# 语言不允许局部变量的属性。</p><p>要在 Entities.ForEach 运行后处理捕获的本机容器或包含本机容器的类型，请使用 WithDisposeOnCompletion(variable)。如果您在 Run() 中调用它，它会在 lambda 表达式运行后立即处理这些类型。如果您在 Schedule() 和 ScheduleParallel() 中调用它，它会安排它们稍后与作业一起处理，并返回 JobHandle。</p><div class="note note-info">            <p>当您使用 Run() 执行该方法时，您可以写入不是本机容器的捕获变量。但是，您仍应尽可能使用 blittable 类型，以便可以使用 Burst 编译该方法。</p>          </div><h4 id="支持的功能"><a href="#支持的功能" class="headerlink" title="支持的功能"></a>支持的功能</h4><p>使用 Run() 在主线程上执行 lambda 表达式。您还可以使用 Schedule() 将其作为单个作业执行，或使用 ScheduleParallel() 将其作为并行作业执行。这些不同的执行方法对您访问数据的方式有不同的限制。此外，Burst 编译器使用 C# 语言的一个受限子集，因此如果要在该子集之外使用 C# 功能，则需要指定 WithoutBurst()。这包括访问托管类型。</p><p>下表显示了 Entities.ForEach 中支持哪些功能，用于 SystemBase 中可用的不同调度方法：</p><table><thead><tr><th>支持的功能</th><th>Run</th><th>Schedule</th><th>ScheduleParallel</th></tr></thead><tbody><tr><td>Capture local value type</td><td>✓</td><td>✓</td><td>✓</td></tr><tr><td>Capture local reference type</td><td>Only WithoutBurst and not in ISystem</td><td></td><td></td></tr><tr><td>Writing to captured variables</td><td>✓</td><td></td><td></td></tr><tr><td>Use field on the system class</td><td>Only WithoutBurst</td><td></td><td></td></tr><tr><td>Methods on reference types</td><td>Only WithoutBurst and not in ISystem</td><td></td><td></td></tr><tr><td>Shared Components</td><td>Only WithoutBurst and not in ISystem</td><td></td><td></td></tr><tr><td>Managed Components</td><td>Only WithoutBurst and not in ISystem</td><td></td><td></td></tr><tr><td>Structural changes</td><td>Only WithStructuralChanges and not in ISystem</td><td></td><td></td></tr><tr><td>SystemBase.GetComponent</td><td>✓</td><td>✓</td><td>✓</td></tr><tr><td>SystemBase.SetComponent</td><td>✓</td><td>✓</td><td></td></tr><tr><td>GetComponentDataFromEntity</td><td>✓</td><td>✓</td><td>Only as ReadOnly</td></tr><tr><td>HasComponent</td><td>✓</td><td>✓</td><td>✓</td></tr><tr><td>WithDisposeOnCompletion</td><td>✓</td><td>✓</td><td>✓</td></tr><tr><td>WithScheduleGranularity</td><td></td><td></td><td>✓</td></tr><tr><td>WithDeferredPlaybackSystem</td><td>✓</td><td>✓</td><td>✓</td></tr><tr><td>WithImmediatePlayback</td><td>✓</td><td></td><td></td></tr><tr><td>HasBuffer</td><td>✓</td><td>✓</td><td>✓</td></tr><tr><td>SystemBase.GetStorageInfoFromEntity</td><td>✓</td><td>✓</td><td>✓</td></tr><tr><td>SystemBase.Exists</td><td>✓</td><td>✓</td><td>✓</td></tr></tbody></table><div class="note note-waring">            <p>WithStructuralChanges() 禁用突发。如果您想获得高性能 Entities.ForEach，请不要使用此选项。如果要使用此选项，请使用 EntityCommandBuffer。</p>          </div><p>Entities.ForEach 构造使用 Roslyn 源代码生成器将您为构造编写的代码转换为正确的 ECS 代码。这种翻译意味着您可以表达算法的意图，而无需包含复杂的样板代码。但是，这意味着一些常见的代码编写方式是不允许的。</p><p>不支持以下功能：</p><ul><li>.With 调用中的动态代码</li><li>SharedComponent 参数由 ref</li><li>嵌套 Entities.ForEach lambda 表达式</li><li>使用存储在变量、字段或方法中的委托进行调用</li><li>具有 lambda 参数类型的 SetComponent</li><li>具有可写 lambda 参数的 GetComponent</li><li>lambdas 中的通用参数</li><li>在具有通用参数的系统中</li></ul><h4 id="依赖关系"><a href="#依赖关系" class="headerlink" title="依赖关系"></a>依赖关系</h4><p>默认情况下，系统使用其 Dependency 属性来管理其与 ECS 相关的依赖项。默认情况下，系统按照它们在 OnUpdate() 函数中出现的顺序将使用 Entities.ForEach 和 Job.WithCode 创建的每个作业添加到依赖作业句柄。您还可以将 JobHandle 传递给您的 Schedule 方法以手动管理作业依赖性，然后返回生成的依赖性。有关详细信息，请参阅依赖项文档。</p><p>有关作业依赖性的更多一般信息，请参阅作业依赖性。</p><h3 id="使用-IJobEntity-迭代数据"><a href="#使用-IJobEntity-迭代数据" class="headerlink" title="使用 IJobEntity 迭代数据"></a>使用 <code>IJobEntity</code> 迭代数据</h3><p>当您在多个系统中使用不同的调用进行数据转换时，要遍历 ComponentData，您可以使用 IJobEntity，它类似于 Entities.ForEach。</p><p>它创建一个 IJobEntityBatch 作业，因此您只需考虑要转换的数据。</p><h4 id="IJobEntity-和-Entities-ForEach-的比较"><a href="#IJobEntity-和-Entities-ForEach-的比较" class="headerlink" title="IJobEntity 和 Entities.ForEach 的比较"></a>IJobEntity 和 Entities.ForEach 的比较</h4><p>IJobEntity 相对于 Entities.ForEach 的优势在于您可以编写一次代码并在多个系统中重复使用它，而不是仅一次。</p><p>这是一个 Entities.ForEach 示例：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">[RequireMatchingQueriesForUpdate]public partial class BoidForEachSystem : SystemBase&#123;    EntityQuery m_BoidQuery;    EntityQuery m_ObstacleQuery;    EntityQuery m_TargetQuery;    protected override void OnUpdate()    &#123;        &#x2F;&#x2F; Calculate amount of entities in respective queries.        var boidCount &#x3D; m_BoidQuery.CalculateEntityCount();        var obstacleCount &#x3D; m_ObstacleQuery.CalculateEntityCount();        var targetCount &#x3D; m_TargetQuery.CalculateEntityCount();        &#x2F;&#x2F; Allocate arrays to store data equal to the amount of entities matching respective queries.        var cellSeparation &#x3D; CollectionHelper.CreateNativeArray&lt;float3, RewindableAllocator&gt;(boidCount, ref World.UpdateAllocator);        var copyTargetPositions &#x3D; CollectionHelper.CreateNativeArray&lt;float3, RewindableAllocator&gt;(targetCount, ref World.UpdateAllocator);        var copyObstaclePositions &#x3D; CollectionHelper.CreateNativeArray&lt;float3, RewindableAllocator&gt;(obstacleCount, ref World.UpdateAllocator);        &#x2F;&#x2F; Schedule job for respective arrays to be stored with respective queries.        Entities            .WithSharedComponentFilter(new BoidSetting&#123;num&#x3D;1&#125;)            .ForEach((int entityInQueryIndex, in LocalToWorld localToWorld) &#x3D;&gt;            &#123;                cellSeparation[entityInQueryIndex] &#x3D; localToWorld.Position;            &#125;)            .ScheduleParallel();        Entities            .WithAll&lt;BoidTarget&gt;()            .WithStoreEntityQueryInField(ref m_TargetQuery)            .ForEach((int entityInQueryIndex, in LocalToWorld localToWorld) &#x3D;&gt;            &#123;                copyTargetPositions[entityInQueryIndex] &#x3D; localToWorld.Position;            &#125;)            .ScheduleParallel();        Entities            .WithAll&lt;BoidObstacle&gt;()            .WithStoreEntityQueryInField(ref m_ObstacleQuery)            .ForEach((int entityInQueryIndex, in LocalToWorld localToWorld) &#x3D;&gt;            &#123;                copyObstaclePositions[entityInQueryIndex] &#x3D; localToWorld.Position;            &#125;)            .ScheduleParallel();    &#125;&#125;</code></pre></div></figure><p>可以改写如下：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">[RequireMatchingQueriesForUpdate]public partial class BoidJobEntitySystem : SystemBase&#123;    EntityQuery m_BoidQuery;    EntityQuery m_ObstacleQuery;    EntityQuery m_TargetQuery;    protected override void OnUpdate()    &#123;        &#x2F;&#x2F; Calculate amount of entities in respective queries.        var boidCount &#x3D; m_BoidQuery.CalculateEntityCount();        var obstacleCount &#x3D; m_ObstacleQuery.CalculateEntityCount();        var targetCount &#x3D; m_TargetQuery.CalculateEntityCount();        &#x2F;&#x2F; Allocate arrays to store data equal to the amount of entities matching respective queries.        var cellSeparation &#x3D; CollectionHelper.CreateNativeArray&lt;float3, RewindableAllocator&gt;(boidCount, ref World.UpdateAllocator);        var copyTargetPositions &#x3D; CollectionHelper.CreateNativeArray&lt;float3, RewindableAllocator&gt;(targetCount, ref World.UpdateAllocator);        var copyObstaclePositions &#x3D; CollectionHelper.CreateNativeArray&lt;float3, RewindableAllocator&gt;(obstacleCount, ref World.UpdateAllocator);        &#x2F;&#x2F; Schedule job for respective arrays to be stored with respective queries.        new CopyPositionsJob &#123; copyPositions &#x3D; cellSeparation&#125;.ScheduleParallel(m_BoidQuery);        new CopyPositionsJob &#123; copyPositions &#x3D; copyTargetPositions&#125;.ScheduleParallel(m_TargetQuery);        new CopyPositionsJob &#123; copyPositions &#x3D; copyObstaclePositions&#125;.ScheduleParallel(m_ObstacleQuery);    &#125;    protected override void OnCreate()    &#123;        &#x2F;&#x2F; Get respective queries, that includes components required by &#96;CopyPositionsJob&#96; described earlier.        m_BoidQuery &#x3D; GetEntityQuery(typeof(LocalToWorld));        m_BoidQuery.SetSharedComponentFilter(new BoidSetting&#123;num&#x3D;1&#125;);        m_ObstacleQuery &#x3D; GetEntityQuery(typeof(LocalToWorld), typeof(BoidObstacle));        m_TargetQuery &#x3D; GetEntityQuery(typeof(LocalToWorld), typeof(BoidTarget));;    &#125;&#125;</code></pre></div></figure><h4 id="创建-IJobEntity-作业"><a href="#创建-IJobEntity-作业" class="headerlink" title="创建 IJobEntity 作业"></a>创建 IJobEntity 作业</h4><p>要创建 IJobEntity 作业，请编写一个使用 IJobEntity 接口的结构，并实现您自己的自定义 Execute 方法。</p><p>使用 partial 关键字是因为源代码生成创建了一个结构，该结构在 project&#x2F;Temp&#x2F;GeneratedCode&#x2F;….. 中找到的单独文件中实现 IJobEntityBatch。</p><p>以下示例每帧向每个 SampleComponent 添加一个。</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">public struct SampleComponent : IComponentData &#123; public float Value; &#125;public partial struct ASampleJob : IJobEntity&#123;    &#x2F;&#x2F; Adds one to every SampleComponent value    void Execute(ref SampleComponent sample)    &#123;        sample.Value +&#x3D; 1f;    &#125;&#125;public partial class ASample : SystemBase&#123;    protected override void OnUpdate()    &#123;        &#x2F;&#x2F; Schedules the job        new ASampleJob().ScheduleParallel();    &#125;&#125;</code></pre></div></figure><h4 id="指定查询"><a href="#指定查询" class="headerlink" title="指定查询"></a>指定查询</h4><p>您可以通过以下方式指定 IJobEntity 的查询：</p><ul><li>手动创建查询，以指定不同的调用要求。</li><li>让已实施的 IJobEntity 为您完成，基于其给定的执行参数，以及使用属性 [WithAll(params Type)]、[WithAny(params Type)]、[WithNone(params Type)]、[ WithChangeFilter(params Type)] 和 [WithEntityQueryOptions((params EntityQueryOptions)]。<br>以下示例显示了这两个选项：<figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">partial struct QueryJob : IJobEntity&#123;    &#x2F;&#x2F; Iterates over all SampleComponents and increments their value    public void Execute(ref SampleComponent sample)    &#123;        sample.Value +&#x3D; 1;    &#125;&#125;[RequireMatchingQueriesForUpdate]public partial class QuerySystem : SystemBase&#123;    &#x2F;&#x2F; Query that matches QueryJob, specified for &#96;BoidTarget&#96;    EntityQuery query_boidtarget;    &#x2F;&#x2F; Query that matches QueryJob, specified for &#96;BoidObstacle&#96;    EntityQuery query_boidobstacle;    protected override void OnCreate()    &#123;        &#x2F;&#x2F; Query that contains all of Execute params found in &#96;QueryJob&#96; - as well as additional user specified component &#96;BoidTarget&#96;.        query_boidtarget &#x3D; GetEntityQuery(ComponentType.ReadWrite&lt;SampleComponent&gt;(),ComponentType.ReadOnly&lt;BoidTarget&gt;());        &#x2F;&#x2F; Query that contains all of Execute params found in &#96;QueryJob&#96; - as well as additional user specified component &#96;BoidObstacle&#96;.        query_boidobstacle &#x3D; GetEntityQuery(ComponentType.ReadWrite&lt;SampleComponent&gt;(),ComponentType.ReadOnly&lt;BoidObstacle&gt;());    &#125;    protected override void OnUpdate()    &#123;        &#x2F;&#x2F; Uses the BoidTarget query        new QueryJob().ScheduleParallel(query_boidtarget);        &#x2F;&#x2F; Uses the BoidObstacle query        new QueryJob().ScheduleParallel(query_boidobstacle);        &#x2F;&#x2F; Uses query created automatically that matches parameters found in &#96;QueryJob&#96;.        new QueryJob().ScheduleParallel();    &#125;&#125;</code></pre></div></figure></li></ul><h5 id="属性"><a href="#属性" class="headerlink" title="属性"></a>属性</h5><p>因为 IJobEntity 类似于作业，所以您可以使用对作业起作用的所有属性：</p><ul><li>Unity.Burst.BurstCompile</li><li>Unity.Collections.DeallocateOnJobCompletion</li><li>Unity.Collections.NativeDisableParallelForRestriction</li><li>Unity.Burst.BurstDiscard</li><li>Unity.Collections.LowLevel.Unsafe.NativeSetThreadIndex</li><li>Unity.Collections.NativeDisableParallelForRestriction</li><li>Unity.Burst.NoAlia</li></ul><p>IJobEntity 还具有您可以使用的其他属性：</p><table><thead><tr><th>属性</th><th>描述</th></tr></thead><tbody><tr><td>Unity.Entities.WithAll(params Type[])</td><td>在作业结构上设置。缩小查询范围，使实体必须匹配提供的所有类型。</td></tr><tr><td>Unity.Entities.WithAny(params Type[])</td><td>在作业结构上设置。缩小查询范围，使实体必须匹配所提供的任何类型。</td></tr><tr><td>Unity.Entities.WithNone(params Type[])</td><td>在作业结构上设置。缩小查询范围，使实体不必匹配所提供的任何类型。</td></tr><tr><td>Unity.Entities.WithChangeFilter(params Type[])</td><td>在作业结构上设置或附加到执行中的参数。缩小查询范围，以便实体必须在给定组件的原型块中进行更改。</td></tr><tr><td>Unity.Entities.WithEntityQueryOptions(params EntityQueryOptions[])</td><td>在作业结构上设置。更改查询范围以使用描述的 EntityQueryOptions。</td></tr><tr><td>Unity.Entities.EntityInQueryIndex</td><td>在 Execute 中设置 int 参数以获取查询中的当前索引，用于当前实体迭代。这与 Entities.ForEach 中的 entityInQueryIndex 相同。</td></tr></tbody></table><p>以下是 EntityInQueryIndex 的示例：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">[BurstCompile]partial struct CopyPositionsJob : IJobEntity&#123;    public NativeArray&lt;float3&gt; copyPositions;    &#x2F;&#x2F; Iterates over all &#96;LocalToWorld&#96; and stores their position inside &#96;copyPositions&#96;.    public void Execute([EntityInQueryIndex] int entityInQueryIndex, in LocalToWorld localToWorld)    &#123;        copyPositions[entityInQueryIndex] &#x3D; localToWorld.Position;    &#125;&#125;[RequireMatchingQueriesForUpdate]public partial class EntityInQuerySystem : SystemBase&#123;    &#x2F;&#x2F; This query should match &#96;CopyPositionsJob&#96; parameters    EntityQuery query;    protected override void OnCreate()    &#123;        &#x2F;&#x2F; Get query that matches &#96;CopyPositionsJob&#96; parameters        query &#x3D; GetEntityQuery(ComponentType.ReadOnly&lt;LocalToWorld&gt;());    &#125;    protected override void OnUpdate()    &#123;        &#x2F;&#x2F; Get a native array equal to the size of the amount of entities found by the query.        var positions &#x3D; new NativeArray&lt;float3&gt;(query.CalculateEntityCount(), World.UpdateAllocator.ToAllocator);        &#x2F;&#x2F; Schedule job on parallel threads for this array.        new CopyPositionsJob&#123;copyPositions &#x3D; positions&#125;.ScheduleParallel();        &#x2F;&#x2F; Dispose the array of positions found by the job.        positions.Dispose(Dependency);    &#125;&#125;</code></pre></div></figure><h5 id="执行参数"><a href="#执行参数" class="headerlink" title="执行参数"></a>执行参数</h5><p>以下是您可以在 IJobEntity 中使用的所有受支持的执行参数的列表：</p><table><thead><tr><th>参数</th><th>描述</th></tr></thead><tbody><tr><td>IComponentData</td><td>标记为 ref 用于读写访问，或标记为对 ComponentData 的只读访问。</td></tr><tr><td>ICleanupComponentData</td><td>标记为 ref 用于读写访问，或标记为对 ComponentData 的只读访问。</td></tr><tr><td>ISharedComponent</td><td>标记为只读访问 SharedComponentData。如果这是托管的，你不能突发编译或安排它。使用 .Run 代替。</td></tr><tr><td>Managed components</td><td>使用值副本进行读写访问，或使用 in 标记对托管组件进行只读访问。例如，UnityEngine.Transform。将托管组件标记为 ref 是错误的，您不能对其进行突发编译或调度。使用 .Run 代替。</td></tr><tr><td>Entity</td><td>获取当前实体。这只是一个值副本，所以不要用 ref 或 in 标记。</td></tr><tr><td>DynamicBuffer<T></T></td><td>获取动态缓冲区。用 ref 标记为读写访问，用 in 标记为只读访问。</td></tr><tr><td>IAspect</td><td>获取方面。方面充当参考，因此您无法分配它们。但是，您可以使用 ref 和 value-copy 将其标记为可读写，并将其标记为只读访问。</td></tr><tr><td>int</td><td>支持三种整数：</td></tr><tr><td></td><td>使用属性 [Unity.Entities.ChunkIndexInQuery] 标记 int 以获取查询中的当前原型块索引。</td></tr><tr><td></td><td>使用属性 [Unity.Entities.EntityIndexInChunk] 标记 int 以获取当前原型块中的当前实体索引。您可以添加 EntityIndexInChunk 和 ChunkIndexInQuery 以获得每个实体的唯一标识符。</td></tr><tr><td></td><td>使用属性 [Unity.Entities.EntityInQueryIndex] 标记 int 以获取查询的打包索引。这对性能有影响，使用 EntityQuery.CalculateBaseEntityIndexArray[Async]。</td></tr></tbody></table><h3 id="遍历成批数据"><a href="#遍历成批数据" class="headerlink" title="遍历成批数据"></a>遍历成批数据</h3><p>在系统内实施 IJobEntityBatch 或 IJobEntityBatchWithIndex 以在实体批次中迭代数据。</p><p>当您在系统的 OnUpdate 函数中计划 IJobEntityBatch 作业时，系统会使用您传递给计划函数的实体查询来识别应该传递给该作业的块。该作业会为这些块中的每批实体调用一次您的 Execute 函数。默认情况下，批处理大小是一个完整的块，但您可以在调度作业时将批处理大小设置为块的一部分。无论批次大小如何，给定批次中的实体始终存储在同一块中。在作业的执行函数中，您可以逐个实体地迭代每个批次中的数据。</p><p>当您需要批次集中所有实体的索引值时，请使用 IJobEntityBatchWithIndex。否则，IJobEntityBatch 效率更高，因为它不需要计算这些索引。</p><p>要实施批处理作业：</p><ol><li><p>使用 EntityQuery 查询数据以确定要处理的实体。</p></li><li><p>使用 IJobEntityBatch 或 IJobEntityBatchWithIndex 定义作业结构。</p></li><li><p>声明您的作业访问的数据。在作业结构中，包括用于标识作业必须直接访问的组件类型的 ComponentTypeHandle 对象的字段。此外，指定作业是读取还是写入这些组件。您还可以包含标识您要查找的实体数据的字段，这些实体不属于查询的一部分，以及用于非实体数据的字段。</p></li><li><p>编写作业结构的执行函数来转换您的数据。获取作业读取或写入的组件的 NativeArray 实例，然后迭代当前批处理以执行所需的工作。</p></li><li><p>在系统 OnUpdate 函数中安排作业，将标识要处理的实体的实体查询传递给调度函数。</p></li></ol><div class="note note-info">            <p>与使用 Entities.ForEach 相比，使用 IJobEntityBatch 或 IJobEntityBatchWithIndex 进行迭代更复杂并且需要更多的代码设置，并且只应在必要或更有效时使用。</p>          </div><p>有关详细信息，ECS 示例存储库包含一个简单的 HelloCube 示例，该示例演示了如何使用 IJobEntityBatch。</p><h4 id="使用-EntityQuery-查询数据"><a href="#使用-EntityQuery-查询数据" class="headerlink" title="使用 EntityQuery 查询数据"></a>使用 EntityQuery 查询数据</h4><p>EntityQuery 定义了 EntityArchetype 必须包含的组件类型集，系统才能处理其关联的块和实体。原型可以有额外的组件，但它必须至少有查询定义的组件。您还可以排除包含特定类型组件的原型。</p><p>将选择您的作业应处理的实体的查询传递给您用于安排作业的计划方法。</p><p>有关定义查询的信息，请参阅使用 EntityQuery 查询数据。</p><div class="note note-info">            <p>不要在 EntityQuery 中包含完全可选的组件。要处理可选组件，请使用 IJobEntityBatch.Execute 中的 ArchetypeChunk.Has 方法来确定当前 ArchetypeChunk 是否具有可选组件。因为同一批次中的所有实体都具有相同的组件，所以您只需要检查每个批次是否存在可选组件一次，而不是每个实体一次。</p>          </div><h4 id="定义作业结构"><a href="#定义作业结构" class="headerlink" title="定义作业结构"></a>定义作业结构</h4><p>作业结构由执行要执行的工作的执行函数和声明执行函数使用的数据的字段组成。</p><p>典型的 IJobEntityBatch 作业结构如下所示：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">public struct UpdateTranslationFromVelocityJob : IJobEntityBatch&#123;    public ComponentTypeHandle&lt;VelocityVector&gt; velocityTypeHandle;    public ComponentTypeHandle&lt;ObjectPosition&gt; translationTypeHandle;    public float DeltaTime;    [BurstCompile]    public void Execute(ArchetypeChunk batchInChunk, int batchIndex)    &#123;        NativeArray&lt;VelocityVector&gt; velocityVectors &#x3D;            batchInChunk.GetNativeArray(velocityTypeHandle);        NativeArray&lt;ObjectPosition&gt; translations &#x3D;            batchInChunk.GetNativeArray(translationTypeHandle);        for(int i &#x3D; 0; i &lt; batchInChunk.Count; i++)        &#123;            float3 translation &#x3D; translations[i].Value;            float3 velocity &#x3D; velocityVectors[i].Value;            float3 newTranslation &#x3D; translation + velocity * DeltaTime;            translations[i] &#x3D; new ObjectPosition() &#123; Value &#x3D; newTranslation &#125;;        &#125;    &#125;&#125;</code></pre></div></figure><p>此示例访问实体的两个组件 VelocityVector 和 Translation 的数据，并根据自上次更新以来经过的时间计算新的平移。</p><h5 id="IJobEntityBatch-与-IJobEntityBatchWithIndex"><a href="#IJobEntityBatch-与-IJobEntityBatchWithIndex" class="headerlink" title="IJobEntityBatch 与 IJobEntityBatchWithIndex"></a>IJobEntityBatch 与 IJobEntityBatchWithIndex</h5><p>IJobEntityBatch 和 IJobEntityBatchWithIndex 之间的唯一区别是 IJobEntityBatchWithIndex 在对批处理调用 Execute 函数时传递一个 indexOfFirstEntityInQuery 参数。该参数为当前batch中第一个实体在实体查询选中的所有实体列表中的索引。</p><p>当您需要每个实体的单独索引时，请使用 IJobEntityBatchWithIndex。例如，如果您为每个实体计算一个唯一的结果，您可以使用此索引将每个结果写入本机数组的不同元素。如果您不使用 indexOfFirstEntityInQuery 值，请改用 IJobEntityBatch，以避免计算索引值的开销。</p><div class="note note-info">            <p>当您向 [EntityCommandBuffer.ParallelWriter] 添加命令时，您可以使用 batchIndex 参数作为命令缓冲区函数的 sortKey 参数。您不需要仅使用 IJobEntityBatchWithIndex 来为每个实体获取唯一的排序键。两种作业类型都可用的 batchIndex 参数可用于此目的。</p>          </div><h5 id="声明您的工作访问的数据"><a href="#声明您的工作访问的数据" class="headerlink" title="声明您的工作访问的数据"></a>声明您的工作访问的数据</h5><p>作业结构中的字段声明可用于 Execute 函数的数据。这些领域分为四大类：</p><ul><li><p>ComponentTypeHandle 字段——组件句柄字段允许您的 Execute 函数访问存储在当前块中的实体组件和缓冲区。请参阅访问实体组件和缓冲区数据。</p></li><li><p>ComponentLookup、BufferLookup 字段——这些“来自实体的数据”字段允许您的 Execute 函数查找任何实体的数据，无论它存储在何处。 （这种类型的随机访问是访问数据效率最低的方式，只应在必要时使用。）请参阅查找其他实体的数据。</p></li><li><p>其他字段——您可以根据需要为您的结构声明其他字段。您可以在每次安排作业时设置此类字段的值。请参阅访问其他数据。</p></li><li><p>输出字段——除了更新作业中的可写实体组件或缓冲区外，您还可以写入为作业结构声明的本机容器字段。此类字段必须是原生容器，例如 NativeArray；您不能使用其他数据类型。</p></li></ul><h6 id="访问实体组件和缓冲区数据"><a href="#访问实体组件和缓冲区数据" class="headerlink" title="访问实体组件和缓冲区数据"></a>访问实体组件和缓冲区数据</h6><p>访问存储在查询中实体之一的组件中的数据是三个步骤的过程：</p><p>首先，您必须在作业结构上定义一个 ComponentTypeHandle 字段，将 T 设置为组件的数据类型。例如：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">public ComponentTypeHandle&lt;ObjectPosition&gt; translationTypeHandle;</code></pre></div></figure><p>接下来，您在作业的 Execute 方法中使用此句柄字段来访问包含该类型组件数据的数组（作为 NativeArray）。该数组包含批次中每个实体的一个元素：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">NativeArray&lt;ObjectPosition&gt; translations &#x3D; batchInChunk.GetNativeArray(translationTypeHandle);</code></pre></div></figure><p>最后，当您安排作业时（在系统的 OnUpdate 方法中，您使用 ComponentSystemBase.GetComponentTypeHandle 函数为类型句柄字段分配一个值：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">&#x2F;&#x2F; &quot;this&quot; is your SystemBase subclassupdateFromVelocityJob.translationTypeHandle &#x3D; this.GetComponentTypeHandle&lt;ObjectPosition&gt;(false);</code></pre></div></figure><p>每次安排作业时，始终设置作业的组件句柄字段。不要缓存类型句柄并在以后使用它。</p><p>批次中的每个组件数据数组都是对齐的，以便给定索引对应于所有数组中的相同实体。换句话说，如果您的作业使用一个实体的两个组件，请在两个数据数组中使用相同的数组索引来访问同一实体的数据。</p><p>您可以使用 ComponentTypeHandle 变量来访问您未包含在 EntityQuery 中的组件类型。但是，您必须检查以确保当前批次包含该组件，然后再尝试访问它。使用 Has 函数检查当前批次是否包含特定组件类型：</p><p>ComponentTypeHandle 字段是 ECS 作业安全系统的一部分，可防止在读取和写入作业中的数据时出现竞争条件。始终设置 GetComponentTypeHandle 函数的 isReadOnly 参数以准确反映组件在作业中的访问方式。</p><h6 id="查找其他实体的数据"><a href="#查找其他实体的数据" class="headerlink" title="查找其他实体的数据"></a>查找其他实体的数据</h6><p>通过 EntityQuery 和 IJobEntityBatch 作业（或 Entities.ForEach）访问组件数据几乎总是访问数据的最有效方式。但是，通常情况下您需要以随机访问方式查找数据，例如，当一个实体依赖于另一个实体中的数据时。要执行这种类型的数据查找，您必须通过作业结构将不同类型的句柄传递给您的作业：</p><p>ComponentLookup – 访问具有该组件类型的任何实体的组件</p><p>BufferLookup – 访问具有该缓冲区类型的任何实体的缓冲区</p><p>这些类型为组件和缓冲区提供类似数组的接口，由 Entity 对象索引。除了由于随机数据访问而相对低效之外，以这种方式查找数据还会增加您遇到工作安全系统建立的保障措施的机会。例如，如果您尝试根据另一个实体的变换设置一个实体的变换，作业安全系统无法判断这是否安全，因为您可以通过 ComponentLookup 对象访问所有变换。您可能正在写入您正在读取的相同数据，从而造成竞争条件。</p><p>要使用 ComponentLookup 和 BufferLookup，请在作业结构上声明一个类型为 ComponentLookup 或 BufferLookup 的字段，并在调度作业之前设置该字段的值。</p><p>有关详细信息，请参阅查找数据。</p><h6 id="访问其他数据"><a href="#访问其他数据" class="headerlink" title="访问其他数据"></a>访问其他数据</h6><p>如果在执行作业时需要其他信息，可以在作业结构上定义一个字段，然后在 Execute 方法中访问该字段。您只能在安排作业时设置该值，并且该值对于所有批次都保持不变。</p><p>例如，如果您正在更新移动对象，您很可能需要传入自上次更新以来经过的时间。为此，您可以定义一个名为 DeltaTime 的字段，在 OnUpdate 中设置它的值并在作业执行函数中使用该值。在为新帧安排作业之前，您将在每一帧计算并为 DeltaTime 字段分配一个新值。</p><h5 id="编写执行函数"><a href="#编写执行函数" class="headerlink" title="编写执行函数"></a>编写执行函数</h5><p>编写作业结构的执行函数，将数据从输入状态转换为所需的输出状态。</p><p>IJobEntityBatch.Execute 方法的签名是：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">void Execute(ArchetypeChunk batchInChunk, int batchIndex)</code></pre></div></figure><p>对于 IJobEntityBatchWithIndex.Execute，签名是：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">void Execute(ArchetypeChunk batchInChunk, int batchIndex, int indexOfFirstEntityInQuery)</code></pre></div></figure><h6 id="batchInChunk-参数"><a href="#batchInChunk-参数" class="headerlink" title="batchInChunk 参数"></a>batchInChunk 参数</h6><p>batchInChunk 参数提供包含此作业迭代的实体和组件的 ArchetypeChunk 实例。因为一个块只能包含一个原型，所以一个块中的所有实体都具有相同的组件集。默认情况下，此对象将所有实体包含在一个块中；但是，如果您使用 ScheduleParallel 安排作业，则可以指定一个批次仅包含块中实体数的一小部分。</p><p>使用 batchInChunk 参数获取访问组件数据所需的 NativeArray 实例。 （您还必须声明一个具有相应组件类型句柄的字段——并在安排作业时设置该字段。）</p><h6 id="batchIndex-参数"><a href="#batchIndex-参数" class="headerlink" title="batchIndex 参数"></a>batchIndex 参数</h6><p>batchIndex 参数是当前批次在为当前作业创建的所有批次列表中的索引。作业中的批次不一定按索引顺序处理。</p><p>您可以在以下情况下使用 batchIndex 值：您有一个本机容器，每个批次有一个元素，您希望将在执行函数中计算的值写入其中。使用 batchIndex 作为此容器的数组索引。</p><p>如果您使用并行写入实体命令缓冲区，请将 batchIndex 参数作为 sortKey 参数传递给命令缓冲区函数。</p><h6 id="indexOfFirstEntityInQuery-参数"><a href="#indexOfFirstEntityInQuery-参数" class="headerlink" title="indexOfFirstEntityInQuery 参数"></a>indexOfFirstEntityInQuery 参数</h6><p>IJobEntityBatchWithIndex Execute 函数有一个名为 indexofFirstEntityInQuery 的附加参数。如果您将查询选择的实体描绘成一个列表，则 indexOfFirstEntityInQuery 将是当前批次中第一个实体的该列表的索引。作业中的批次不一定按索引顺序处理。</p><h6 id="可选组件"><a href="#可选组件" class="headerlink" title="可选组件"></a>可选组件</h6><p>如果您的实体查询中有 Any 过滤器或完全可选的组件根本没有出现在查询中，您可以使用 ArchetypeChunk.Has 函数在使用之前测试当前块是否包含这些组件之一：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">&#x2F;&#x2F; If entity has Rotation and LocalToWorld components,&#x2F;&#x2F; slerp to align to the velocity vectorif (batchInChunk.Has&lt;Rotation&gt;(rotationTypeHandle) &amp;&amp;    batchInChunk.Has&lt;LocalToWorld&gt;(l2wTypeHandle))&#123;    NativeArray&lt;Rotation&gt; rotations        &#x3D; batchInChunk.GetNativeArray(rotationTypeHandle);    NativeArray&lt;LocalToWorld&gt; transforms        &#x3D; batchInChunk.GetNativeArray(l2wTypeHandle);    &#x2F;&#x2F; By putting the loop inside the check for the    &#x2F;&#x2F; optional components, we can check once per batch    &#x2F;&#x2F; rather than once per entity.    for (int i &#x3D; 0; i &lt; batchInChunk.Count; i++)    &#123;        float3 direction &#x3D; math.normalize(velocityVectors[i].Value);        float3 up &#x3D; transforms[i].Up;        quaternion rotation &#x3D; rotations[i].Value;        quaternion look &#x3D; quaternion.LookRotation(direction, up);        quaternion newRotation &#x3D; math.slerp(rotation, look, DeltaTime);        rotations[i] &#x3D; new Rotation() &#123; Value &#x3D; newRotation &#125;;    &#125;&#125;</code></pre></div></figure><h4 id="安排工作"><a href="#安排工作" class="headerlink" title="安排工作"></a>安排工作</h4><p>要运行 IJobEntityBatch 作业，您必须创建作业结构的实例，设置结构字段，然后安排作业。当您在 SystemBase 实现的 OnUpdate 函数中执行此操作时，系统会安排作业在每一帧运行。</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">[RequireMatchingQueriesForUpdate]public partial class UpdateTranslationFromVelocitySystem : SystemBase&#123;    EntityQuery query;    protected override void OnCreate()    &#123;        &#x2F;&#x2F; Set up the query        var description &#x3D; new EntityQueryDesc()        &#123;            All &#x3D; new ComponentType[]                   &#123;ComponentType.ReadWrite&lt;ObjectPosition&gt;(),                    ComponentType.ReadOnly&lt;VelocityVector&gt;()&#125;        &#125;;        query &#x3D; this.GetEntityQuery(description);    &#125;    protected override void OnUpdate()    &#123;        &#x2F;&#x2F; Instantiate the job struct        var updateFromVelocityJob            &#x3D; new UpdateTranslationFromVelocityJob();        &#x2F;&#x2F; Set the job component type handles        &#x2F;&#x2F; &quot;this&quot; is your SystemBase subclass        updateFromVelocityJob.translationTypeHandle            &#x3D; this.GetComponentTypeHandle&lt;ObjectPosition&gt;(false);        updateFromVelocityJob.velocityTypeHandle            &#x3D; this.GetComponentTypeHandle&lt;VelocityVector&gt;(true);        &#x2F;&#x2F; Set other data need in job, such as time        updateFromVelocityJob.DeltaTime &#x3D; World.Time.DeltaTime;        &#x2F;&#x2F; Schedule the job        this.Dependency            &#x3D; updateFromVelocityJob.ScheduleParallel(query, this.Dependency);    &#125;&#125;</code></pre></div></figure><p>当您调用 GetComponentTypeHandle 函数来设置组件类型变量时，请确保将作业读取但不写入的组件的 isReadOnly 参数设置为 true。正确设置这些参数会对 ECS 框架安排作业的效率产生重大影响。这些访问模式设置必须与其在结构定义和 EntityQuery 中的等效设置相匹配。</p><p>不要在系统类变量中缓存 GetComponentTypeHandle 的返回值。您必须在每次系统运行时调用该函数，并将更新后的值传递给作业。</p><h5 id="调度选项"><a href="#调度选项" class="headerlink" title="调度选项"></a>调度选项</h5><p>您可以在安排作业时通过选择适当的功能来控制作业的执行方式：</p><ul><li><p>运行——立即在当前（主）线程上执行作业。 Run 还会完成当前作业所依赖的任何计划作业。批量大小始终为 1（整个块）。</p></li><li><p>Schedule——安排作业在当前作业所依赖的任何计划作业之后在工作线程上运行。为实体查询选择的每个块调用一次作业执行函数。块按顺序处理。批量大小始终为 1。</p></li><li><p>ScheduleParallel——与 Schedule 类似，不同之处在于您可以指定批处理大小，并且这些批处理是并行处理的（假设工作线程可用）而不是顺序处理。</p></li></ul><h5 id="设置批量大小"><a href="#设置批量大小" class="headerlink" title="设置批量大小"></a>设置批量大小</h5><p>要设置批量大小，请使用 ScheduleParallel 方法来安排作业并将 batchesPerChunk 参数设置为正整数。使用值 1 将批处理大小设置为完整块。</p><p>用于调度作业的查询选择的每个块都分为 batchesPerChunk 指定的批次数。来自同一块的每个批次包含大致相同数量的实体；然而，来自不同块的批次可能包含非常不同数量的实体。最大批处理大小为 1，这意味着每个块中的所有实体都在对 Execute 函数的一次调用中一起处理。来自不同块的实体永远不能包含在同一批中。</p><div class="note note-info">            <p>通常，使用 batchesPerChunk 设置为 1 来在对 Execute 的单个调用中处理块中的所有实体是最有效的。然而，情况并非总是如此。例如，如果您的 Execute 函数执行的实体数量较少且算法成本较高，则可以通过使用较小的实体批次从并行处理中获得额外的好处。</p>          </div><h4 id="跳过实体不变的块"><a href="#跳过实体不变的块" class="headerlink" title="跳过实体不变的块"></a>跳过实体不变的块</h4><p>如果您只需要在组件值更改时更新实体，则可以将该组件类型添加到为作业选择实体和块的 EntityQuery 的更改过滤器中。例如，如果您有一个系统读取两个组件并且只需要在前两个组件中的一个发生更改时更新第三个组件，则可以按如下方式使用 EntityQuery：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">EntityQuery query;protected override void OnCreate()&#123;    query &#x3D; GetEntityQuery(        new ComponentType[]        &#123;            ComponentType.ReadOnly&lt;InputA&gt;(),            ComponentType.ReadOnly&lt;InputB&gt;(),            ComponentType.ReadWrite&lt;Output&gt;()        &#125;    );    query.SetChangedVersionFilter(            new ComponentType[]            &#123;                typeof(InputA),                typeof(InputB)            &#125;        );&#125;</code></pre></div></figure><p>EntityQuery 更改过滤器最多支持两个组件。如果您想检查更多或者您没有使用 EntityQuery，您可以手动进行检查。要进行此检查，请使用 ArchetypeChunk.DidChange 函数将组件的块更改版本与系统的 LastSystemVersion 进行比较。如果此函数返回 false，则您可以完全跳过当前块，因为自上次系统运行以来该类型的组件均未更改。</p><p>您必须使用结构字段将 LastSystemVersion 从系统传递到作业中，如下所示：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">struct UpdateOnChangeJob : IJobEntityBatch&#123;    public ComponentTypeHandle&lt;InputA&gt; InputATypeHandle;    public ComponentTypeHandle&lt;InputB&gt; InputBTypeHandle;    [ReadOnly] public ComponentTypeHandle&lt;Output&gt; OutputTypeHandle;    public uint LastSystemVersion;    [BurstCompile]    public void Execute(ArchetypeChunk batchInChunk, int batchIndex)    &#123;        var inputAChanged &#x3D; batchInChunk.DidChange(InputATypeHandle, LastSystemVersion);        var inputBChanged &#x3D; batchInChunk.DidChange(InputBTypeHandle, LastSystemVersion);        &#x2F;&#x2F; If neither component changed, skip the current batch        if (!(inputAChanged || inputBChanged))            return;        var inputAs &#x3D; batchInChunk.GetNativeArray(InputATypeHandle);        var inputBs &#x3D; batchInChunk.GetNativeArray(InputBTypeHandle);        var outputs &#x3D; batchInChunk.GetNativeArray(OutputTypeHandle);        for (var i &#x3D; 0; i &lt; outputs.Length; i++)        &#123;            outputs[i] &#x3D; new Output &#123; Value &#x3D; inputAs[i].Value + inputBs[i].Value &#125;;        &#125;    &#125;&#125;</code></pre></div></figure><p>与所有作业结构字段一样，您必须在安排作业之前分配其值：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">[RequireMatchingQueriesForUpdate]public partial class UpdateDataOnChangeSystem : SystemBase &#123;    EntityQuery query;    protected override void OnUpdate()    &#123;        var job &#x3D; new UpdateOnChangeJob();        job.LastSystemVersion &#x3D; this.LastSystemVersion;        job.InputATypeHandle &#x3D; GetComponentTypeHandle&lt;InputA&gt;(true);        job.InputBTypeHandle &#x3D; GetComponentTypeHandle&lt;InputB&gt;(true);        job.OutputTypeHandle &#x3D; GetComponentTypeHandle&lt;Output&gt;(false);        this.Dependency &#x3D; job.ScheduleParallel(query, this.Dependency);    &#125;    protected override void OnCreate()    &#123;        query &#x3D; GetEntityQuery(            new ComponentType[]            &#123;                ComponentType.ReadOnly&lt;InputA&gt;(),                ComponentType.ReadOnly&lt;InputB&gt;(),                ComponentType.ReadWrite&lt;Output&gt;()            &#125;        );    &#125;&#125;</code></pre></div></figure><div class="note note-info">            <p>为了提高效率，更改版本适用于整个块而不是单个实体。如果另一个能够写入该类型组件的作业访问块，则 ECS 会增加该组件的更改版本，并且 DidChange 函数返回 true。即使声明对组件的写访问权限的作业实际上并未更改组件值，ECS 也会增加更改版本。 （这是在读取组件数据而不更新它时应始终只读的原因之一。）</p>          </div><h3 id="手动迭代数据"><a href="#手动迭代数据" class="headerlink" title="手动迭代数据"></a>手动迭代数据</h3><p>如果您需要以一种不适合迭代 EntityQuery 中所有块的简化模型的方式管理块，您可以在本机数组中显式手动请求所有原型块，并使用 IJobParallelFor 等作业处理它们.下面是一个例子：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">public class RotationSpeedSystem : SystemBase&#123;   [BurstCompile]   struct RotationSpeedJob : IJobParallelFor   &#123;       [DeallocateOnJobCompletion] public NativeArray&lt;ArchetypeChunk&gt; Chunks;       public ArchetypeChunkComponentType&lt;RotationQuaternion&gt; RotationType;       [ReadOnly] public ArchetypeChunkComponentType&lt;RotationSpeed&gt; RotationSpeedType;       public float DeltaTime;       public void Execute(int chunkIndex)       &#123;           var chunk &#x3D; Chunks[chunkIndex];           var chunkRotation &#x3D; chunk.GetNativeArray(RotationType);           var chunkSpeed &#x3D; chunk.GetNativeArray(RotationSpeedType);           var instanceCount &#x3D; chunk.Count;           for (int i &#x3D; 0; i &lt; instanceCount; i++)           &#123;               var rotation &#x3D; chunkRotation[i];               var speed &#x3D; chunkSpeed[i];               rotation.Value &#x3D; math.mul(math.normalize(rotation.Value), quaternion.AxisAngle(math.up(), speed.RadiansPerSecond * DeltaTime));               chunkRotation[i] &#x3D; rotation;           &#125;       &#125;   &#125;   EntityQuery m_Query;      protected override void OnCreate()   &#123;       var queryDesc &#x3D; new EntityQueryDesc       &#123;           All &#x3D; new ComponentType[]&#123; typeof(RotationQuaternion), ComponentType.ReadOnly&lt;RotationSpeed&gt;() &#125;       &#125;;       m_Query &#x3D; GetEntityQuery(queryDesc);   &#125;   protected override void OnUpdate()   &#123;       var rotationType &#x3D; GetArchetypeChunkComponentType&lt;RotationQuaternion&gt;();       var rotationSpeedType &#x3D; GetArchetypeChunkComponentType&lt;RotationSpeed&gt;(true);       var chunks &#x3D; m_Query.ToArchetypeChunkArray(Allocator.TempJob);       var rotationsSpeedJob &#x3D; new RotationSpeedJob       &#123;           Chunks &#x3D; chunks,           RotationType &#x3D; rotationType,           RotationSpeedType &#x3D; rotationSpeedType,           DeltaTime &#x3D; Time.deltaTime       &#125;;       this.Dependency rotationsSpeedJob.Schedule(chunks.Length,32, this.Dependency);   &#125;&#125;</code></pre></div></figure><h4 id="如何手动迭代数据"><a href="#如何手动迭代数据" class="headerlink" title="如何手动迭代数据"></a>如何手动迭代数据</h4><p>您可以使用 EntityManager 类手动遍历实体或原型块，但这效率不高。您应该只使用这些迭代方法来测试或调试您的代码，或者在您拥有一组受控实体的孤立世界中。</p><p>例如，以下代码片段遍历活动世界中的所有实体：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">var entityManager &#x3D; World.Active.EntityManager;var allEntities &#x3D; entityManager.GetAllEntities();foreach (var entity in allEntities)&#123;   &#x2F;&#x2F;...&#125;allEntities.Dispose();</code></pre></div></figure><p>此代码段遍历活动世界中的所有块：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">var entityManager &#x3D; World.Active.EntityManager;var allChunks &#x3D; entityManager.GetAllChunks();foreach (var chunk in allChunks)&#123;   &#x2F;&#x2F;...&#125;allChunks.Dispose();</code></pre></div></figure><h2 id="系统更新顺序"><a href="#系统更新顺序" class="headerlink" title="系统更新顺序"></a>系统更新顺序</h2><p>要指定系统的更新顺序，您可以使用 ComponentSystemGroup 类。要将系统置于组中，请在系统的类声明中使用 UpdateInGroup 属性。然后，您可以使用 UpdateBefore 或 UpdateAfter 属性来指定系统必须更新的顺序。</p><p>有一组默认系统组，您可以使用它们在框架的正确阶段更新系统。您可以将一个组嵌套在另一个组中，以便您组中的所有系统都在正确的阶段更新，并根据其组内的顺序进行更新。</p><h3 id="组件系统组"><a href="#组件系统组" class="headerlink" title="组件系统组"></a>组件系统组</h3><p>ComponentSystemGroup 类表示 Unity 必须按特定顺序一起更新的相关组件系统的列表。 ComponentSystemGroup 继承自 ComponentSystemBase，因此您可以相对于其他系统对其进行排序，并且它具有 OnUpdate() 方法。这也意味着您可以将一个 ComponentSystemGroup 嵌套在另一个 ComponentSystemGroup 中，并形成层次结构。</p><p>默认情况下，当您在 ComponentSystemGroup 中调用 Update() 方法时，它会在其已排序的成员系统列表中的每个系统上调用 Update()。如果任何成员系统是系统组，它们将递归更新自己的成员。生成的系统排序遵循树的深度优先遍历。</p><h3 id="系统排序属性"><a href="#系统排序属性" class="headerlink" title="系统排序属性"></a>系统排序属性</h3><p>您可以在系统上使用以下属性来确定其更新顺序：</p><table><thead><tr><th>属性</th><th>描述</th></tr></thead><tbody><tr><td>UpdateInGroup</td><td>指定此系统应属于的 ComponentSystemGroup。如果不设置该属性，Unity 会自动将其添加到默认世界的 SimulationSystemGroup 中。有关详细信息，请参阅默认系统组部分。</td></tr><tr><td>UpdateBefore UpdateAfter</td><td>Order 系统相对于其他系统。为这些属性指定的系统类型必须是同一组的成员。 Unity 在包含两个系统的适当的最深组中处理跨组边界的排序。例如，如果 CarSystem 在 CarGroup 中，而 TruckSystem 在 TruckGroup 中，并且 CarGroup 和 TruckGroup 都是 VehicleGroup 的成员，那么 CarGroup 和 TruckGroup 的顺序隐式决定了 CarSystem 和 TruckSystem 的相对顺序。您无需明确订购系统。</td></tr><tr><td>DisableAutoCreation</td><td>阻止 Unity 在默认世界初始化期间创建系统。您必须明确地创建和更新系统。但是，您可以将带有此标记的系统添加到 ComponentSystemGroup 的更新列表中，它会像该列表中的其他系统一样自动更新。</td></tr></tbody></table><p>如果您将 DisableAutoCreation 属性添加到组件系统或系统组，Unity 不会创建它或将其添加到默认系统组。要手动创建系统，请使用 World.GetOrCreateSystem<MySystem>() 并从主线程调用 MySystem.Update() 来更新它。您可以使用它在 Unity 播放器循环中的其他位置插入系统，例如，如果您有一个应该在帧中稍后或更早运行的系统。</MySystem></p><h3 id="默认系统组"><a href="#默认系统组" class="headerlink" title="默认系统组"></a>默认系统组</h3><p>默认世界包含 ComponentSystemGroup 实例的层次结构。 Unity 播放器循环中有三个根级系统组：</p><ul><li>InitializationSystemGroup：在播放器循环的初始化阶段结束时更新。</li><li>SimulationSystemGroup：在播放器循环的更新阶段结束时更新。</li><li>PresentationSystemGroup：在播放器循环的 PreLateUpdate 阶段结束时更新。<br>默认系统组也有一些预定义的成员系统：</li></ul><p><strong>InitializationSystemGroup</strong>:</p><ul><li>BeginInitializationEntityCommandBufferSystem</li><li>CopyInitialTransformFromGameObjectSystem</li><li>SubSceneLiveConversionSystem</li><li>SubSceneStreamingSystem</li><li>EndInitializationEntityCommandBufferSystem</li></ul><p><strong>SimulationSystemGroup</strong>:</p><ul><li>BeginSimulationEntityCommandBufferSystem</li><li>TransformSystemGroup<ul><li>ParentSystem</li><li>CopyTransformFromGameObjectSystem</li><li>TRSToLocalToWorldSystem</li><li>TRSToLocalToParentSystem</li><li>LocalToParentSystem</li><li>CopyTransformToGameObjectSystem</li></ul></li><li>LateSimulationSystemGroup</li><li>EndSimulationEntityCommandBufferSystem</li></ul><p><strong>PresentationSystemGroup</strong>:</p><ul><li>BeginPresentationEntityCommandBufferSystem</li><li>CreateMissingRenderBoundsFromMeshRenderer</li><li>RenderingSystemBootstrap</li><li>RenderBoundsUpdateSystem</li><li>RenderMeshSystem</li><li>LODGroupSystemV1</li><li>LodRequirementsUpdateSystem</li><li>EndPresentationEntityCommandBufferSystem<br>请注意，此列表的具体内容可能会发生变化。</li></ul><h3 id="多个世界"><a href="#多个世界" class="headerlink" title="多个世界"></a>多个世界</h3><p>您可以创建多个世界，也可以在多个世界中实例化相同的组件系统类。您还可以从更新顺序中的不同点以不同的速率更新每个实例。</p><p>您无法手动更新给定世界中的每个系统，但您可以控制在哪个世界中创建哪些系统，以及将它们添加到哪些现有系统组中。</p><p>例如，您可以创建一个实例化 SystemX 和 SystemY 的自定义世界，并将 SystemX 添加到默认世界的 SimulationSystemGroup，并将 SystemY 添加到默认世界的 PresentationSystemGroup。这些系统可以像往常一样相对于它们的同级组对自己进行排序，Unity 会更新它们以及相应的组。</p><p>您还可以使用 ICustomBootstrap 接口来管理多个世界中的系统：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">public interface ICustomBootstrap&#123;    &#x2F;&#x2F; 返回应由默认引导程序处理的系统。    &#x2F;&#x2F; 如果返回 null，则根本不会创建默认世界。    &#x2F;&#x2F; 空列表创建默认世界和入口点    List&lt;Type&gt; Initialize(List&lt;Type&gt; systems);&#125;</code></pre></div></figure><p>当您实现此接口时，它会在默认世界初始化之前将组件系统类型的完整列表传递给 Initialize() 方法。自定义引导程序可以遍历此列表并在您定义的世界中创建系统。您可以从 Initialize() 方法返回系统列表，Unity 创建它们作为默认世界初始化的一部分。</p><p>例如，这是自定义 MyCustomBootstrap.Initialize() 实现的典型过程：</p><ol><li>创建任何其他世界及其顶级 ComponentSystemGroups。</li><li>对于系统类型列表中的每个类型：<ol><li>向上搜索 ComponentSystemGroup 层次结构以找到此系统类型的顶级组。</li><li>如果它是在步骤 1 中创建的组之一，则在该世界中创建系统并使用 group.AddSystemToUpdateList() 将其添加到层次结构中。</li><li>如果不是，则将此类型附加到列表以返回到 DefaultWorldInitialization。</li></ol></li><li>在新的顶级组上调用 group.SortSystemUpdateList()。<ol><li>可选择将它们添加到默认世界组之一</li></ol></li><li>将未处理系统的列表返回给 DefaultWorldInitialization。</li></ol><div class="note note-info">            <p>ECS 框架通过反射找到您的 ICustomBootstrap 实现。</p>          </div><h2 id="使用作业在多个线程上调度数据"><a href="#使用作业在多个线程上调度数据" class="headerlink" title="使用作业在多个线程上调度数据"></a>使用作业在多个线程上调度数据</h2><p>实体包和 Unity 的 DOTS 架构广泛使用 C# 作业系统。只要有可能，您应该在系统代码中使用作业。</p><p>SystemBase 类提供 Entities.ForEach 和 Job.WithCode 以将应用程序的逻辑实现为多线程代码。在更复杂的情况下，您可以使用 IJobEntityBatch 的 Schedule() 和 ScheduleParallel() 方法在主线程之外转换数据。 Entities.ForEach 使用起来最简单，通常需要较少的代码来实现。</p><p>ECS 按照您的系统所在的顺序在主线程上安排作业。当您安排作业时，ECS 会跟踪哪些作业读取和写入哪些组件。读取组件的作业依赖于写入同一组件的任何先前计划的作业，反之亦然。作业调度程序使用作业依赖关系来确定哪些作业可以并行运行，哪些作业必须按顺序运行。</p><p>例如，以下系统更新位置：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">using Unity.Burst;using Unity.Collections;using Unity.Entities;using Unity.Jobs;using Unity.Transforms;public class MovementSpeedSystem : SystemBase&#123;    &#x2F;&#x2F; OnUpdate runs on the main thread.    protected override void OnUpdate()    &#123;        Entities            .ForEach((ref Translation position, in MovementSpeed speed) &#x3D;&gt;                &#123;                    float3 displacement &#x3D; speed.Value * dt;                    position &#x3D; new Translation()&#123;                            Value &#x3D; position.Value + displacement                        &#125;;                &#125;)            .ScheduleParallel();    &#125;&#125;</code></pre></div></figure><h3 id="工作扩展"><a href="#工作扩展" class="headerlink" title="工作扩展"></a>工作扩展</h3><p>Unity C# 作业系统允许您在多个线程上运行代码。该系统提供调度、并行处理和多线程安全。作业系统是一个核心 Unity 模块，它提供通用接口和类来创建和运行作业，无论您是否使用 Entities 包。</p><p>这些接口包括：</p><ul><li>IJob：创建一个在任何线程或内核上运行的作业，由作业系统调度程序确定。</li><li>IJobParallelFor：创建一个可以在多个线程上并行运行的作业，以处理 NativeContainer 的元素。</li><li>IJobExtensions：提供运行 IJob 作业的扩展方法。</li><li>IJobParallelForExtensions：提供运行 IJobParallelFor 作业的扩展方法。</li><li>JobHandle：访问计划作业的句柄。您还可以使用 JobHandle 实例来指定作业之间的依赖关系。<br>有关作业系统的概述，请参阅 Unity 用户手册中的 C# 作业系统。</li></ul><p>Jobs 包扩展了作业系统以支持 ECS。它包含：</p><ul><li>IJobParallelForDeferExtensions</li><li>IJobFilter</li><li>JobParallelIndexListExtensions</li><li>Job​Struct​Produce<T></T></li></ul><h3 id="通用职位"><a href="#通用职位" class="headerlink" title="通用职位"></a>通用职位</h3><p>在 C# 中，您可以使用继承和接口使一段代码适用于一系列类型。例如：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">&#x2F;&#x2F; The method is not limited to just one kind of &#x2F;&#x2F; input but rather any type which implements IBlendable.void foo(IBlendable a) &#123;...&#125;</code></pre></div></figure><p>在 Burst 编译器使用的高性能 C# (HPC#) 中，您不能使用托管类型或虚拟方法调用，因此泛型是使一段代码在一系列类型上运行的选项：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">&#x2F;&#x2F; This method can operate on any IBlendable struct (and can call the &#x2F;&#x2F; IBlendable methods) but requires no managed objects or virtual method calls.void foo&lt;T&gt;(T a) where T : struct, IBlendable &#123;...&#125;</code></pre></div></figure><p>您必须在 HPC# 中编写作业，因此对于在一系列类型上运行的作业，它必须是通用的：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">[BurstCompile()]public struct BlendJob&lt;T&gt; : IJob    where T : struct, IBlendable&#123;    public NativeReference&lt;T&gt; blendable;    public void Execute()     &#123;        var val &#x3D; blendable.Value;        val.Blend();        blendable.Value &#x3D; val;    &#125;&#125;</code></pre></div></figure><h4 id="从-Burst-编译代码中调度通用作业"><a href="#从-Burst-编译代码中调度通用作业" class="headerlink" title="从 Burst 编译代码中调度通用作业"></a>从 Burst 编译代码中调度通用作业</h4><p>要从 Burst 编译代码安排通用作业，您需要作业具体专业化的反射数据。不幸的是，Unity 不会为所有具体的特化自动生成此反射，因此在某些情况下您必须手动注册它们：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">&#x2F;&#x2F; This assembly attribute allows Burst-compiled &#x2F;&#x2F; code in the same assembly to schedule the&#x2F;&#x2F; concrete specialization &lt;int, float&gt; for MyJob. *&#x2F;[assembly: RegisterGenericJobType(typeof(MyJob&lt;int, float&gt;))]</code></pre></div></figure><p>如果您尝试安排未在程序集中注册具体专业化的作业，则 Unity 会抛出异常。</p><p>注册类型的程序集无关紧要。例如，如果一个作业类型只在程序集 Foo 中注册，您也可以在程序集 Bar 中安排它。</p><p>如果您多次重复注册相同的具体专业化，则不会将其视为错误。</p><h4 id="具体作业类型的自动注册"><a href="#具体作业类型的自动注册" class="headerlink" title="具体作业类型的自动注册"></a>具体作业类型的自动注册</h4><p>当您直接实例化通用作业的具体特化时，Unity 会自动在程序集中注册该特化：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">&#x2F;&#x2F; Registers specialization &lt;int, float&gt; for MyJob in the assembly.var job &#x3D; new MyJob&lt;int, float&gt;();</code></pre></div></figure><p>但是，在间接实例化具体特化时，Unity 不会自动注册它：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">void makeJob&lt;T&gt;()&#123;    new MyJob&lt;T, float&gt;().Schedule();   &#125;void foo()&#123;    makeJob&lt;int&gt;();    &#x2F;&#x2F; does NOT register MyJob&lt;int, float&gt;&#125;</code></pre></div></figure><p>但是，如果您将通用作业作为返回类型或输出参数包含在签名中，则 Unity 会自动注册它：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">MyJob&lt;T, float&gt; makeJob&lt;T&gt;()&#123;    var j &#x3D; new MyJob&lt;T, float&gt;()    j.Schedule();       return j;&#125;void foo()&#123;    makeJob&lt;int&gt;();    &#x2F;&#x2F; registers MyJob&lt;int, float&gt;&#125;</code></pre></div></figure><p>您可以通过多级通用方法调用使用此间接注册作品：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">MyJob&lt;T, float&gt; makeJob&lt;T&gt;()&#123;    var j &#x3D; new MyJob&lt;T, float&gt;()    j.Schedule();       return j;&#125;void foo&lt;T&gt;()&#123;    makeJob&lt;T&gt;();    &#125;void bar()&#123;    foo&lt;int&gt;();       &#x2F;&#x2F; registers MyJob&lt;int, float&gt;&#125;</code></pre></div></figure><p>您还可以将通用作业嵌套在另一个类或结构中：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">struct BlendJobWrapper&lt;T&gt; where T : struct, IBlendable&#123;    public T blendable;    [BurstCompile()]    public struct BlendJob : IJob    &#123;        public T blendable;        public void Execute() &#123;...&#125;    &#125;    public JobHandle Schedule(JobHandle dep &#x3D; new JobHandle())    &#123;        return new BlendJob &#123; blendable &#x3D; blendable &#125;.Schedule(dep);    &#125;&#125;</code></pre></div></figure><p>在前面的示例中，如果 BlendJobWrapper<foo> 是自动或手动注册的，那么 BlendJob<foo> 也会有效注册。仅围绕一个通用作业的包装器类型并不能解决任何问题，但是当您同时使用多个通用作业时，这些包装器类型允许更优雅的作业创建和调度。</foo></foo></p><h4 id="作业化分拣"><a href="#作业化分拣" class="headerlink" title="作业化分拣"></a>作业化分拣</h4><p>NativeSortExtension 类具有排序方法，包括使用作业进行排序的方法：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">public unsafe static JobHandle Sort&lt;T, U&gt;(T* array, int length, U comp, JobHandle deps)    where T : unmanaged    where U : IComparer&lt;T&gt;&#123;    if (length &#x3D;&#x3D; 0)        return inputDeps;    var segmentSortJob &#x3D; new SegmentSort&lt;T, U&gt; &#123; Data &#x3D; array, Comp &#x3D; comp, Length &#x3D; length, SegmentWidth &#x3D; 1024 &#125;;    var segmentSortMergeJob &#x3D; new SegmentSortMerge&lt;T, U&gt; &#123; Data &#x3D; array, Comp &#x3D; comp, Length &#x3D; length, SegmentWidth &#x3D; 1024 &#125;;    var segmentCount &#x3D; (length + 1023) &#x2F; 1024;    var workerSegmentCount &#x3D; segmentCount &#x2F; math.max(1, JobsUtility.MaxJobThreadCount);    var handle &#x3D; segmentSortJob.Schedule(segmentCount, workerSegmentCount, deps);    return segmentSortMergeJob.Schedule(segmentSortJobHandle);&#125;</code></pre></div></figure><p>在此示例中，排序分为两个作业：第一个作业将数组拆分为多个子部分，然后分别对它们进行并行排序。第二个作业等待第一个，然后将这些排序的子部分合并为最终的排序结果。</p><p>但是，此方法不会自动注册两个通用作业 SegmentSort 和 SegmentSortMerge 的具体特化，因为这两种类型都未用作方法的返回类型或输出参数。</p><p>一种解决方案是将两个作业都放入参数中：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">public unsafe static JobHandle Sort&lt;T, U&gt;(T* array, int length, U comp, JobHandle deps        out SegmentSort&lt;T, U&gt; segmentSortJob, out SegmentSortMerge&lt;T, U&gt; segmentSortMergeJob)    where T : unmanaged    where U : IComparer&lt;T&gt;&#123;    if (length &#x3D;&#x3D; 0)        return inputDeps;    segmentSortJob &#x3D; new SegmentSort&lt;T, U&gt; &#123; Data &#x3D; array, Comp &#x3D; comp, Length &#x3D; length, SegmentWidth &#x3D; 1024 &#125;;    segmentSortMergeJob &#x3D; new SegmentSortMerge&lt;T, U&gt; &#123; Data &#x3D; array, Comp &#x3D; comp, Length &#x3D; length, SegmentWidth &#x3D; 1024 &#125;;    var segmentCount &#x3D; (length + 1023) &#x2F; 1024;    var workerSegmentCount &#x3D; segmentCount &#x2F; math.max(1, JobsUtility.MaxJobThreadCount);    var handle &#x3D; segmentSortJob.Schedule(segmentCount, workerSegmentCount, deps);    return segmentSortMergeJob.Schedule(segmentSortJobHandle);&#125;</code></pre></div></figure><p>然而，这解决了注册问题，但是您随后必须传递参数以获得您可能不想要的两个作业结构。</p><p>更好的解决方案是将两种作业类型包装在一个包装器类型中：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">unsafe struct SortJob&lt;T, U&gt; :    where T : unamanged    where U : IComparer&lt;T&gt;&#123;    public T* data;    public U comparer;    public int length;    unsafe struct SegmentSort : IJobParallelFor    &#123;        [NativeDisableUnsafePtrRestriction]        public T* data;        public U comp;        public int length;        public int segmentWidth;        public void Execute(int index) &#123;...&#125;    &#125;    unsafe struct SegmentSortMerge : IJob    &#123;        [NativeDisableUnsafePtrRestriction]        public T* data;        public U comp;        public int length;        public int segmentWidth;        public void Execute() &#123;...&#125;    &#125;    public JobHandle Schedule(JobHandle dep &#x3D; new JobHandle())    &#123;        if (length &#x3D;&#x3D; 0)            return inputDeps;        var segmentSortJob &#x3D; new SegmentSort&lt;T, U&gt; &#123; Data &#x3D; array, Comp &#x3D; comp, Length &#x3D; length, SegmentWidth &#x3D; 1024 &#125;;        var segmentSortMergeJob &#x3D; new SegmentSortMerge&lt;T, U&gt; &#123; Data &#x3D; array, Comp &#x3D; comp, Length &#x3D; length, SegmentWidth &#x3D; 1024 &#125;;        var segmentCount &#x3D; (length + 1023) &#x2F; 1024;        var workerSegmentCount &#x3D; segmentCount &#x2F; math.max(1, JobsUtility.MaxJobThreadCount);        var handle &#x3D; segmentSortJob.Schedule(segmentCount, workerSegmentCount, deps);        return segmentSortMergeJob.Schedule(segmentSortJobHandle);    &#125;&#125;</code></pre></div></figure><p>在这种安排中，您可以创建 SortJob 的实例并调用其 Schedule() 方法，而不是调用 Sort() 方法。通过对 SortJob 进行具体实例化，您还可以自动注册所需的 SegmentSort 和 SegmentSortMerge 具体特化。</p><p>这种嵌套通用作业的模式启用了一个方便的 API，可以将相关的通用作业集安排在一起。</p><h3 id="使用-Job-WithCode-安排后台作业"><a href="#使用-Job-WithCode-安排后台作业" class="headerlink" title="使用 Job.WithCode 安排后台作业"></a>使用 Job.WithCode 安排后台作业</h3><p>SystemBase 类中的 Job.WithCode 构造将方法作为单个后台作业运行。您还可以在主线程上运行 Job.WithCode 并利用 Burst 编译来加快执行速度。</p><h4 id="使用-Job-WithCode"><a href="#使用-Job-WithCode" class="headerlink" title="使用 Job.WithCode"></a>使用 Job.WithCode</h4><p>以下示例使用一个 Job.WithCode lambda 表达式用随机数填充本机数组，并使用另一个作业将这些数字加在一起：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">public partial class RandomSumJob : SystemBase&#123;    private uint seed &#x3D; 1;    protected override void OnUpdate()    &#123;        Random randomGen &#x3D; new Random(seed++);        NativeArray&lt;float&gt; randomNumbers            &#x3D; new NativeArray&lt;float&gt;(500, Allocator.TempJob);        Job.WithCode(() &#x3D;&gt;        &#123;            for (int i &#x3D; 0; i &lt; randomNumbers.Length; i++)            &#123;                randomNumbers[i] &#x3D; randomGen.NextFloat();            &#125;        &#125;).Schedule();        &#x2F;&#x2F; To get data out of a job, you must use a NativeArray        &#x2F;&#x2F; even if there is only one value        NativeArray&lt;float&gt; result            &#x3D; new NativeArray&lt;float&gt;(1, Allocator.TempJob);        Job.WithCode(() &#x3D;&gt;        &#123;            for (int i &#x3D; 0; i &lt; randomNumbers.Length; i++)            &#123;                result[0] +&#x3D; randomNumbers[i];            &#125;        &#125;).Schedule();        &#x2F;&#x2F; This completes the scheduled jobs to get the result immediately, but for        &#x2F;&#x2F; better efficiency you should schedule jobs early in the frame with one        &#x2F;&#x2F; system and get the results late in the frame with a different system.        this.CompleteDependency();        UnityEngine.Debug.Log(&quot;The sum of &quot;            + randomNumbers.Length + &quot; numbers is &quot; + result[0]);        randomNumbers.Dispose();        result.Dispose();    &#125;&#125;</code></pre></div></figure><p>要运行并行作业，请实施 IJobFor。您可以使用 ScheduleParallel() 在系统的 OnUpdate() 函数中安排并行作业。</p><h4 id="捕获变量-1"><a href="#捕获变量-1" class="headerlink" title="捕获变量"></a>捕获变量</h4><p>您不能将参数传递给 Job.WithCode lambda 表达式或返回值。相反，您必须在系统的 OnUpdate() 函数中捕获局部变量。</p><p>如果您使用 Schedule() 来安排您的作业在 Unity 的作业系统中运行，则还有其他限制：</p><ul><li>您必须将捕获的变量声明为 NativeArray、本机容器或 blittable 类型。</li><li>要返回数据，您必须将返回值写入捕获的本机数组，即使数据是单个值也是如此。但是，如果您使用 Run() 来执行作业，则可以写入任何捕获的变量。<br>Job.WithCode 有一组方法将只读和安全属性应用于捕获的本机容器的变量。例如，您可以使用 WithReadOnly 将对变量的访问限制为只读。您还可以使用 WithDisposeOnCompletion 在作业完成后自动释放容器。有关详细信息，请参阅 Job.WithCode 文档的捕获变量部分。</li></ul><h4 id="执行-Job-WithCode-lambda-表达式"><a href="#执行-Job-WithCode-lambda-表达式" class="headerlink" title="执行 Job.WithCode lambda 表达式"></a>执行 Job.WithCode lambda 表达式</h4><p>要执行 Job.WithCode lambda 表达式，您可以使用以下命令：</p><ul><li>Schedule()：将方法作为单个非并行作业执行。安排作业在后台线程上运行代码并更好地利用所有可用的 CPU 资源。您可以显式地将 JobHandle 传递给 Schedule()，或者，如果您不传递任何依赖项，系统会假定当前系统的 Dependency 属性表示作业的依赖项。或者，如果作业没有依赖项，您可以传入一个新的 JobHandle。</li><li>Run()：在主线程上执行方法。您可以 Burst 编译 Job.WithCode，因此如果您使用 Run() 来执行代码，即使它在主线程上运行也会更快。当你调用 Run() 时，Unity 会自动完成 Job.WithCode 构造的所有依赖。</li></ul><h4 id="依赖关系-1"><a href="#依赖关系-1" class="headerlink" title="依赖关系"></a>依赖关系</h4><p>默认情况下，系统使用其 Dependency 属性来管理其依赖项。系统按照它们在 OnUpdate() 方法中出现的顺序将您创建的每个 Entities.ForEach 和 Job.WithCode 作业添加到依赖作业句柄。</p><p>要手动管理作业依赖性，请将 JobHandle 传递给 Schedule 方法，然后返回生成的依赖性。有关详细信息，请参阅依赖项 API 文档。</p><p>有关作业依赖性的一般信息，请参阅有关作业依赖性的文档。</p><h3 id="作业依赖"><a href="#作业依赖" class="headerlink" title="作业依赖"></a>作业依赖</h3><p>Unity根据系统读写的ECS组件分析各个系统的数据依赖关系。如果在帧中较早更新的系统读取较晚系统写入的数据，或写入较晚系统读取的数据，则第二个系统依赖于第一个系统。为了防止竞争条件，作业调度程序确保系统依赖的所有作业在运行该系统的作业之前已经完成。</p><h4 id="作业依赖更新顺序"><a href="#作业依赖更新顺序" class="headerlink" title="作业依赖更新顺序"></a>作业依赖更新顺序</h4><p>系统的 Dependency 属性是一个 JobHandle，表示系统的 ECS 相关依赖项。在 OnUpdate() 之前，Dependency 属性反映系统对先前作业的传入依赖项。默认情况下，系统会根据您在系统中安排作业时每个作业读取和写入的组件来更新依赖属性。</p><h5 id="覆盖默认顺序"><a href="#覆盖默认顺序" class="headerlink" title="覆盖默认顺序"></a>覆盖默认顺序</h5><p>要覆盖此默认行为，请使用 Entities.ForEach 和 Job.WithCode 的重载版本，它们将作业依赖项作为参数并将更新的依赖项作为 JobHandle 返回。当您使用这些构造的显式版本时，ECS 不会自动将作业句柄与系统的 Dependency 属性组合在一起。您必须在需要时手动组合它们。</p><p>Dependency 属性不跟踪作业可能对通过 NativeArray 或其他类似容器传递的数据的依赖关系。如果您在一个作业中编写 NativeArray，并在另一个作业中读取该数组，则必须手动将第一个作业的 JobHandle 添加为第二个作业的依赖项。您可以使用 JobHandle.CombineDependencies 来执行此操作。</p><h5 id="Entities-ForEach-的作业依赖顺序"><a href="#Entities-ForEach-的作业依赖顺序" class="headerlink" title="Entities.ForEach 的作业依赖顺序"></a>Entities.ForEach 的作业依赖顺序</h5><p>当您调用 Entities.ForEach.Run() 时，作业计划程序会在开始 ForEach 迭代之前完成系统依赖的所有计划作业。如果您还使用 WithStructuralChanges() 作为构造的一部分，则作业调度程序将完成所有正在运行和已调度的作业。结构更改还会使对组件数据的任何直接引用无效。有关详细信息，请参阅有关结构更改的文档。</p><h4 id="更多资源"><a href="#更多资源" class="headerlink" title="更多资源"></a>更多资源</h4><ul><li>JobHandle 和依赖项</li><li>Unity的工作系统</li></ul><h2 id="使用-EntityQuery-查询数据-1"><a href="#使用-EntityQuery-查询数据-1" class="headerlink" title="使用 EntityQuery 查询数据"></a>使用 EntityQuery 查询数据</h2><p>EntityQuery 查找具有一组指定组件类型的原型。然后它将原型的块收集到一个系统可以处理的数组中。</p><p>例如，如果查询匹配组件类型 A 和 B，则查询会收集具有这两种组件类型的所有原型的块，而不管这些原型可能具有的任何其他组件类型。因此，具有组件类型 A、B 和 C 的原型将匹配查询。</p><p>您可以使用 EntityQuery 执行以下操作：</p><p>运行作业以处理选定的实体和组件<br>获取包含所有选定实体的 NativeArray<br>按组件类型获取所选组件的 NativeArray<br>EntityQuery 返回的实体和组件数组是并行的。这意味着相同的索引值始终适用于任何数组中的相同实体。</p><h3 id="创建实体查询"><a href="#创建实体查询" class="headerlink" title="创建实体查询"></a>创建实体查询</h3><p>要创建实体查询，您可以将组件类型传递给 EntityQueryBuilder 帮助器类型。以下示例定义了一个 EntityQuery，它查找所有同时具有 ObjectRotation 和 ObjectRotationSpeed 组件的实体：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">EntityQuery query &#x3D; new EntityQueryBuilder(Allocator.Temp)    .WithAllRW&lt;ObjectRotation&gt;()    .WithAll&lt;ObjectRotationSpeed&gt;()    .Build(this);</code></pre></div></figure><p>查询使用 EntityQueryBuilder.WithAllRW<T> 来显示系统写入 ObjectRotation。如果可能，您应该始终指定只读访问权限，因为对数据的读取访问权限的限制较少。这有助于作业调度程序更有效地执行作业。</T></p><h4 id="指定系统选择的原型"><a href="#指定系统选择的原型" class="headerlink" title="指定系统选择的原型"></a>指定系统选择的原型</h4><p>查询将仅匹配包含您指定组件的原型。可以使用三种不同的 EntityQueryBuilder 方法指定组件：</p><ul><li>WithAll<T>()：这些组件是必需的。为了匹配查询，原型必须包含查询的所有必需组件。</T></li><li>WithAny<T>()：这些组件是可选的。为了匹配查询，原型必须至少包含一个查询的可选组件。</T></li><li>WithNone<T>()：排除这些组件。为了匹配查询，原型不得包含任何查询的排除组件。<br>例如，以下查询包括包含 ObjectRotation 和 ObjectRotationSpeed 组件的原型，但不包括包含 Static 组件的任何原型：<figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">EntityQuery query &#x3D; new EntityQueryBuilder(Allocator.Temp)    .WithAllRW&lt;ObjectRotation&gt;()    .WithAll&lt;ObjectRotationSpeed&gt;()    .WithNone&lt;Static&gt;()    .Build(this);</code></pre></div></figure></T></li></ul><div class="note note-info">            <p>要处理可选组件，请使用 ArchetypeChunk.Has<T> 方法来确定块是否包含可选组件。这是因为同一块中的所有实体都具有相同的组件，因此您只需检查每个块是否存在可选组件一次：而不是每个实体一次。</T></p>          </div><p>您可以使用 EntityQueryBuilder.WithOptions() 来查找专门的原型。例如：</p><ul><li>IncludePrefab：包括包含 Prefab 标签组件的原型。</li><li>IncludeDisabledEntities：包括包含 Disabled 标签组件的原型。</li><li>FilterWriteGroup：仅包含 WriteGroup 中明确包含在查询中的组件的实体。排除具有来自同一 WriteGroup 的任何其他组件的实体。<br>有关选项的完整列表，请参阅 EntityQueryOptions。</li></ul><h5 id="按写入组过滤"><a href="#按写入组过滤" class="headerlink" title="按写入组过滤"></a>按写入组过滤</h5><p>在以下示例中，LuigiComponent 和 MarioComponent 是基于 CharacterComponent 组件的同一 WriteGroup 中的组件。此查询使用需要 CharacterComponent 和 MarioComponent 的 FilterWriteGroup 选项：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">public struct CharacterComponent : IComponentData &#123; &#125;[WriteGroup(typeof(CharacterComponent))]public struct LuigiComponent : IComponentData &#123; &#125;[WriteGroup(typeof(CharacterComponent))]public struct MarioComponent : IComponentData &#123; &#125;[RequireMatchingQueriesForUpdate]public partial class ECSSystem : SystemBase&#123;    protected override void OnCreate()    &#123;        var query &#x3D; new EntityQueryBuilder(Allocator.Temp)            .WithAllRW&lt;CharacterComponent&gt;()            .WithAll&lt;MarioComponent&gt;()            .WithOptions(EntityQueryOptions.FilterWriteGroup)            .Build(this);    &#125;    protected override void OnUpdate()    &#123;        throw new NotImplementedException();    &#125;&#125;</code></pre></div></figure><p>此查询排除任何同时具有 LuigiComponent 和 MarioComponent 的实体，因为 LuigiComponent 未明确包含在查询中。</p><p>这比 None 字段更有效，因为您不需要更改其他系统使用的查询，只要它们也使用写组。</p><p>您可以使用写入组来扩展现有系统。例如，如果您在另一个系统中将 CharacterComponent 和 LuigiComponent 定义为不受您控制的库的一部分，则可以将 MarioComponent 与 LuigiComponent 放在同一写入组中，以更改 CharacterComponent 的更新方式。然后，对于您添加到 MarioComponent 的任何实体，系统都会更新 CharacterComponent，但原始系统不会更新它。对于没有 MarioComponent 的实体，原始系统像以前一样更新 CharacterComponent。有关详细信息，请参阅有关写入组的文档。</p><h3 id="定义过滤器"><a href="#定义过滤器" class="headerlink" title="定义过滤器"></a>定义过滤器</h3><p>要进一步对实体进行排序，您可以使用过滤器根据以下内容排除实体：</p><p>共享组件过滤器：根据共享组件的特定值过滤实体集。<br>更改过滤器：根据特定组件类型的值是否已更改来过滤实体集。<br>在您对查询对象调用 ResetFilter 之前，您设置的过滤器一直有效。</p><p>要忽略查询的活动块过滤器，请使用名称以 IgnoreFilter 结尾的 EntityQuery 方法。这些方法通常比过滤等效方法更有效。例如，请参阅 IsEmpty 与 IsEmptyIgnoreFilter。</p><h4 id="使用共享组件过滤器"><a href="#使用共享组件过滤器" class="headerlink" title="使用共享组件过滤器"></a>使用共享组件过滤器</h4><p>要使用共享组件筛选器，请在 EntityQuery 中包含共享组件以及任何其他需要的组件，然后调用 SetSharedComponentFilter 方法。然后传入包含要选择的值的相同 ISharedComponent 类型的结构。所有值都必须匹配。您最多可以向过滤器添加两个不同的共享组件。</p><p>您可以随时更改筛选器，但如果您更改筛选器，它不会更改您从组 ToComponentDataArray<T> 或 ToEntityArray 方法接收的任何现有实体或组件数组。您必须重新创建这些数组。</T></p><p>以下示例定义了一个名为 SharedGrouping 的共享组件和一个仅处理组字段设置为 1 的实体的系统。</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">struct SharedGrouping : ISharedComponentData&#123;    public int Group;&#125;[RequireMatchingQueriesForUpdate]partial class ImpulseSystem : SystemBase&#123;    EntityQuery query;    protected override void OnCreate()    &#123;        query &#x3D; new EntityQueryBuilder(Allocator.Temp)            .WithAllRW&lt;ObjectPosition&gt;()            .WithAll&lt;Displacement, SharedGrouping&gt;()            .Build(this);    &#125;    protected override void OnUpdate()    &#123;        &#x2F;&#x2F; Only iterate over entities that have the SharedGrouping data set to 1        query.SetSharedComponentFilter(new SharedGrouping &#123; Group &#x3D; 1 &#125;);        var positions &#x3D; query.ToComponentDataArray&lt;ObjectPosition&gt;(Allocator.Temp);        var displacements &#x3D; query.ToComponentDataArray&lt;Displacement&gt;(Allocator.Temp);        for (int i &#x3D; 0; i &lt; positions.Length; i++)            positions[i] &#x3D; new ObjectPosition            &#123;                Value &#x3D; positions[i].Value + displacements[i].Value            &#125;;    &#125;&#125;</code></pre></div></figure><h4 id="使用更改过滤器"><a href="#使用更改过滤器" class="headerlink" title="使用更改过滤器"></a>使用更改过滤器</h4><p>如果您只需要在组件值更改时更新实体，请使用 SetChangedVersionFilter 方法将该组件添加到 EntityQuery 过滤器。例如，以下 EntityQuery 仅包含来自另一个系统已写入 Translation 组件的块的实体：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">EntityQuery query;protected override void OnCreate()&#123;    query &#x3D; new EntityQueryBuilder(Allocator.Temp)        .WithAllRW&lt;LocalToWorld&gt;()        .WithAll&lt;ObjectPosition&gt;()        .Build(this);    query.SetChangedVersionFilter(typeof(ObjectPosition));&#125;</code></pre></div></figure><p>为了提高效率，更改过滤器适用于整个原型块，而不是单个实体。更改过滤器还只检查声明对组件进行写访问的系统是否已运行，而不检查它是否更改了任何数据。例如，如果可以写入该组件类型的另一个作业访问该块，则更改过滤器将包括该块中的所有实体。这就是为什么您应该始终声明对不需要修改的组件的只读访问权限。</p><h3 id="按启用组件过滤"><a href="#按启用组件过滤" class="headerlink" title="按启用组件过滤"></a>按启用组件过滤</h3><p>启用组件允许在运行时启用和禁用单个实体上的组件。禁用实体上的组件不会将该实体移动到新的原型中，但出于 EntityQuery 匹配的目的，实体将被视为没有组件。具体来说：</p><p>如果一个实体禁用了组件 T，它将不会匹配需要组件 T 的查询（使用 WithAll<T>()）。<br>如果实体禁用了组件 T，它将匹配排除组件 T 的查询（使用 WithNone<T>()）。<br>大多数 EntityQuery 操作（例如 ToEntityArray 和 CalculateEntityCount）会自动过滤掉其可启用组件会导致它们与查询不匹配的实体。要禁用此过滤，请使用这些操作的 IgnoreFilter 变体，或在查询创建时传递 EntityQueryOptions.IgnoreComponentEnabledState。</T></T></p><p>有关更多详细信息，请参阅启用的组件文档。</p><h3 id="合并查询"><a href="#合并查询" class="headerlink" title="合并查询"></a>合并查询</h3><p>要有效地将多个查询合并为一个，您可以创建一个包含多个查询描述的查询。生成的查询与匹配任何提供的查询描述的原型相匹配。本质上，组合查询匹配查询描述的并集。以下示例选择包含 ObjectRotation 组件或 ObjectRotationSpeed 组件（或两者）的任何原型：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">EntityQuery query &#x3D; new EntityQueryBuilder(Allocator.Temp)    .WithAllRW&lt;ObjectRotation&gt;()    &#x2F;&#x2F; Start a new query description    .AddAdditionalQuery()    .WithAllRW&lt;ObjectRotationSpeed&gt;()    .Build(this);</code></pre></div></figure><h3 id="执行查询"><a href="#执行查询" class="headerlink" title="执行查询"></a>执行查询</h3><p>通常，您在安排使用它的作业时执行实体查询。您还可以调用返回实体、组件或原型块数组的 EntityQuery 方法之一：</p><p>ToEntityArray：返回所选实体的数组。<br>ToComponentDataArray：返回所选实体的 T 类型组件的数组。<br>CreateArchetypeChunkArray：返回包含所选实体的所有块。因为查询对原型、共享组件值和更改过滤器进行操作，这些对于块中的所有实体都是相同的，所以存储在返回的块集中的实体集与 ToEntityArray 返回的实体集相同。<br>上述方法的异步版本也可用，它安排一个作业来收集请求的数据。其中一些变体必须返回 NativeList 而不是 NativeArray 才能支持可启用的组件。请参阅 ToEntityListAsync、ToComponentDataListAsync 和 CreateArchetypeChunkArrayAsync。</p><h3 id="编辑器中的查询"><a href="#编辑器中的查询" class="headerlink" title="编辑器中的查询"></a>编辑器中的查询</h3><p>在编辑器中，以下图标代表查询： 。当您使用特定的实体窗口和检查器时，您会看到这一点。您还可以使用“查询”窗口查看与所选查询匹配的组件和实体。</p><h2 id="使用-EntityCommandBuffer-安排数据更改"><a href="#使用-EntityCommandBuffer-安排数据更改" class="headerlink" title="使用 EntityCommandBuffer 安排数据更改"></a>使用 EntityCommandBuffer 安排数据更改</h2><p>要对实体数据更改进行排队而不是立即执行更改，您可以使用 EntityCommandBuffer 结构，它创建一个线程安全的命令缓冲区。如果您想在作业完成时推迟任何结构更改，这将很有用。</p><h3 id="EntityCommandBuffer-方法"><a href="#EntityCommandBuffer-方法" class="headerlink" title="EntityCommandBuffer 方法"></a>EntityCommandBuffer 方法</h3><p>您可以使用 EntityCommandBuffer 中的方法来记录命令，这些方法反映了 EntityManager 的一些方法，例如：</p><ul><li>CreateEntity(EntityArchetype)：创建具有指定原型的新实体。</li><li>DestroyEntity(Entity)：销毁实体。</li><li>SetComponent<T>(Entity, T)：为实体上类型 T 的组件设置值。</T></li><li>AddComponent<T>(Entity)：将类型 T 的组件添加到实体。</T></li><li>RemoveComponent<T>(EntityQuery)：从匹配查询的所有实体中移除类型 T 的组件。<br>Unity 仅在调用主线程上的 Playback 方法时才将更改记录在 EntityCommandBuffer 中。如果您尝试在播放后记录任何对命令缓冲区的进一步更改，Unity 会抛出异常。</T></li></ul><p>EntityCommandBuffer 有一个作业安全句柄，类似于本机容器。如果您尝试对使用命令缓冲区的未完成计划作业执行以下任何操作，安全检查会抛出异常：</p><ul><li>通过其 AddComponent、Playback、Dispose 或其他方法访问 EntityCommandBuffer。</li><li>安排另一个访问相同 EntityCommandBuffer 的作业，除非新作业依赖于已安排的作业。</li></ul><h3 id="在单线程作业中使用-EntityCommandBuffer"><a href="#在单线程作业中使用-EntityCommandBuffer" class="headerlink" title="在单线程作业中使用 EntityCommandBuffer"></a>在单线程作业中使用 EntityCommandBuffer</h3><p>Unity 无法在作业中执行结构更改，因此您可以使用实体的命令缓冲区来推迟结构更改，直到 Unity 完成作业。例如：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">&#x2F;&#x2F; ... in a system update&#x2F;&#x2F; You don&#39;t specify a size because the buffer will grow as needed.EntityCommandBuffer ecb &#x3D; new EntityCommandBuffer(Allocator.TempJob);&#x2F;&#x2F; The ECB is captured by the ForEach job.&#x2F;&#x2F; Until completed, the job owns the ECB&#39;s job safety handle.Entities    .ForEach((Entity e, in FooComp foo) &#x3D;&gt;    &#123;        if (foo.Value &gt; 0)        &#123;            &#x2F;&#x2F; Record a command that will later add            &#x2F;&#x2F; BarComp to the entity.            ecb.AddComponent&lt;BarComp&gt;(e);        &#125;    &#125;).Schedule();this.Dependency.Complete();&#x2F;&#x2F; Now that the job is completed, you can enact the changes.&#x2F;&#x2F; Note that Playback can only be called on the main thread.ecb.Playback(this.EntityManager);&#x2F;&#x2F; You are responsible for disposing of any ECB you create.ecb.Dispose();</code></pre></div></figure><h3 id="在并行作业中使用-EntityCommandBuffer"><a href="#在并行作业中使用-EntityCommandBuffer" class="headerlink" title="在并行作业中使用 EntityCommandBuffer"></a>在并行作业中使用 EntityCommandBuffer</h3><p>如果要在并行作业中使用实体命令缓冲区，请使用 EntityCommandBuffer.ParallelWriter，它以线程安全的方式并发记录到命令缓冲区：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">EntityCommandBuffer ecb &#x3D; new EntityCommandBuffer(Allocator.TempJob);&#x2F;&#x2F; Methods of this writer record commands to &#x2F;&#x2F; the EntityCommandBuffer in a thread-safe way.EntityCommandBuffer.ParallelWriter parallelEcb &#x3D; ecb.AsParallelWriter();</code></pre></div></figure><blockquote><p>note<br>只有记录需要线程安全才能并发。回放在主线程上始终是单线程的。</p></blockquote><h4 id="确定性回放"><a href="#确定性回放" class="headerlink" title="确定性回放"></a>确定性回放</h4><p>因为命令的记录是跨线程拆分的，所以记录命令的顺序取决于作业调度，因此是不确定的。</p><p>确定性并不总是必不可少的，但产生确定性结果的代码更容易调试。还有一些网络场景需要在不同机器上获得一致的结果。但是，确定性会对性能产生影响，因此您可能需要在某些项目中接受不确定性。</p><p>您无法避免不确定的录制顺序，但可以通过以下方式确定命令的播放顺序：</p><ol><li>每个命令记录一个“排序键”int 作为第一个参数传递给每个命令方法。您必须调用 lambda 参数 entityInQueryIndex，否则 Entities.ForEach 将无法识别 int。</li><li>在播放时，在执行命令之前按命令的排序键对命令进行排序。<br>只要记录的排序键独立于调度，排序就可以确定播放顺序。</li></ol><p>在并行作业中，每个实体所需的排序键是一个数字，它与作业查询中的该实体具有固定且唯一的关联。</p><p>并行作业中提供的 entityInQueryIndex 值满足这些条件。在与作业查询匹配的原型块列表中，实体具有以下索引：</p><ul><li>第一个块的第一个实体有 entityInQueryIndex 0</li><li>第一个块的第二个实体有 entityInQueryIndex 1</li><li>第二个块的第一个实体有一个 entityInQueryIndex，它是第一个块的计数</li><li>第三个块的第一个实体有一个 entityInQueryIndex，它是前两个块的计数之和</li></ul><p>entityInQueryIndex 始终遵循此模式。</p><p>以下示例代码显示了并行作业中使用的实体命令缓冲区：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">&#x2F;&#x2F; ... in a system updateEntityCommandBuffer ecb &#x3D; new EntityCommandBuffer(Allocator.TempJob);&#x2F;&#x2F; We need to write to the ECB concurrently across threads.EntityCommandBuffer.ParallelWriter ecbParallel &#x3D; ecb.AsParallelWriter();&#x2F;&#x2F; The entityInQueryIndex is unique for each entity and will be&#x2F;&#x2F; consistent for each particular entity regardless of scheduling.Entities    .ForEach((Entity e, int entityInQueryIndex, in FooComp foo) &#x3D;&gt; &#123;        if (foo.Value &gt; 0)        &#123;            &#x2F;&#x2F; The first arg is the &#39;sort key&#39; recorded with the command.            ecbParallel.AddComponent&lt;BarComp&gt;(entityInQueryIndex, e);        &#125;    &#125;).Schedule();&#x2F;&#x2F; Playback is single-threaded as normal.this.Dependency.Complete();&#x2F;&#x2F; To ensure deterministic playback order,&#x2F;&#x2F; the commands are first sorted by their sort keys.ecb.Playback(this.EntityManager);ecb.Dispose();</code></pre></div></figure><h3 id="重用-EntityCommandBuffer-实例"><a href="#重用-EntityCommandBuffer-实例" class="headerlink" title="重用 EntityCommandBuffer 实例"></a>重用 EntityCommandBuffer 实例</h3><p>最好的做法是为每个作业提供自己的命令缓冲区。这是因为与在单个命令缓冲区中记录相同的命令相比，将一组命令记录到多个命令缓冲区的开销很小。</p><p>但是，您可以在非并行作业中重用相同的 EntityCommandBuffer，只要这些作业在调度中不重叠即可。如果您在并行作业中重复使用 EntityCommandBuffer 实例，这可能会导致播放中命令的意外排序顺序，除非每个作业的排序键位于不同的范围内。</p><h3 id="多重回放"><a href="#多重回放" class="headerlink" title="多重回放"></a>多重回放</h3><p>如果多次调用 Playback 方法，它会抛出异常。为避免这种情况，请使用 PlaybackPolicy.MultiPlayback 选项创建一个 EntityCommandBuffer 实例：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">&#x2F;&#x2F; ... in a system updateEntityCommandBuffer ecb &#x3D;        new EntityCommandBuffer(Allocator.TempJob, PlaybackPolicy.MultiPlayback);&#x2F;&#x2F; ... record commandsecb.Playback(this.EntityManager);&#x2F;&#x2F; Additional playbacks are OK because this ECB is MultiPlayback.ecb.Playback(this.EntityManager);ecb.Dispose();</code></pre></div></figure><p>如果您想重复生成一组实体，则多重播放很有用。为此，使用 EntityCommandBuffer 创建并配置一组新实体，然后重复播放以重新生成另一组匹配的实体。</p><h3 id="在主线程上使用-EntityCommandBuffer"><a href="#在主线程上使用-EntityCommandBuffer" class="headerlink" title="在主线程上使用 EntityCommandBuffer"></a>在主线程上使用 EntityCommandBuffer</h3><p>您可以在主线程上记录命令缓冲区更改。这在以下情况下很有用：</p><ul><li>延迟您的更改。</li><li>多次回放一组更改。</li><li>在一个统一的地方回放很多变化。这比将更改散布在框架的不同部分更有效。</li></ul><p>每个结构更改操作都会触发一个同步点，这意味着该操作必须等待部分或所有计划作业完成。如果将结构更改组合到命令缓冲区中，则帧的同步点会更少。</p><h3 id="使用-EntityCommandBufferSystem-自动播放和处理命令缓冲区"><a href="#使用-EntityCommandBufferSystem-自动播放和处理命令缓冲区" class="headerlink" title="使用 EntityCommandBufferSystem 自动播放和处理命令缓冲区"></a>使用 EntityCommandBufferSystem 自动播放和处理命令缓冲区</h3><p>您可以使用 EntityCommandBufferSystem 回放和处理命令缓冲区，而不是自己手动执行。去做这个：</p><ol><li>获取要进行播放的 EntityCommandBuffer 系统的实例。</li><li>通过系统创建一个 EntityCommandBuffer 实例。</li><li>安排一个将命令写入 EntityCommandBuffer 的作业。</li><li>注册系统完成的预定作业。</li></ol><p>例如：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">&#x2F;&#x2F; ... in a system&#x2F;&#x2F; Assume an EntityCommandBufferSystem exists named FooECBSystem.EntityCommandBufferSystem sys &#x3D;        this.World.GetExistingSystemManaged&lt;FooECBSystem&gt;();&#x2F;&#x2F; Create a command buffer that will be played back&#x2F;&#x2F; and disposed by MyECBSystem.EntityCommandBuffer ecb &#x3D; sys.CreateCommandBuffer();&#x2F;&#x2F; A ForEach with no argument to Schedule implicitly&#x2F;&#x2F; assigns its returned JobHandle to this.DependencyEntities    .ForEach((Entity e, in FooComp foo) &#x3D;&gt; &#123;        &#x2F;&#x2F; ... record to the ECB    &#125;).Schedule();&#x2F;&#x2F; Register the job so that it gets completed by the ECB system.sys.AddJobHandleForProducer(this.Dependency);</code></pre></div></figure><div class="note note-waring">            <p>不要手动回放和处置 EntityCommandBufferSystem 创建的 EntityCommandBuffer。 EntityCommandBufferSystem 为您完成这两件事。</p>          </div><p>在每次更新中，一个 EntityCommandBufferSystem：</p><ol><li>完成所有已注册的作业，以确保他们已完成录制）。</li><li>以创建它们的相同顺序播放通过系统创建的所有实体命令缓冲区。</li><li>处理 EntityCommandBuffer。</li></ol><h4 id="默认-EntityCommandBufferSystem-系统"><a href="#默认-EntityCommandBufferSystem-系统" class="headerlink" title="默认 EntityCommandBufferSystem 系统"></a>默认 EntityCommandBufferSystem 系统</h4><p>默认世界具有以下默认 EntityCommandBufferSystem 系统：</p><ul><li>BeginInitializationEntityCommandBufferSystem</li><li>EndInitializationEntityCommandBufferSystem</li><li>BeginSimulationEntityCommandBufferSystem</li><li>EndSimulationEntityCommandBufferSystem</li><li>BeginPresentationEntityCommandBufferSystem</li></ul><p>因为在 Unity 将渲染数据交给渲染器之后，帧中不会发生结构变化，所以没有 EndPresentationEntityCommandBufferSystem 系统。您可以改用 BeginInitializationEntityCommandBufferSystem：一帧的结尾是下一帧的开始。</p><p>这些更新在标准系统组的开头和结尾。有关详细信息，请参阅有关系统更新顺序的文档。</p><p>对于大多数用例，默认系统应该足够了，但如果需要，您可以创建自己的 EntityCommandBufferSystem：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">&#x2F;&#x2F; You should specify where exactly in the frame&#x2F;&#x2F; that the ECB system should update.[UpdateInGroup(typeof(SimulationSystemGroup))][UpdateAfter(typeof(FooSystem))]public class MyECBSystem : EntityCommandBufferSystem &#123;    &#x2F;&#x2F; This class is intentionally empty. There is generally no    &#x2F;&#x2F; reason to put any code in an EntityCommandBufferSystem.&#125;</code></pre></div></figure><h3 id="延迟实体"><a href="#延迟实体" class="headerlink" title="延迟实体"></a>延迟实体</h3><p>EntityCommandBuffer 方法 CreateEntity 和 Instantiate 记录创建实体的命令。这些方法只记录命令，不创建实体。因此，它们返回带有负索引的实体值，代表尚不存在的占位符实体。这些占位符实体值仅在同一 ECB 的记录命令中有意义。</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">&#x2F;&#x2F; ... in a systemEntityCommandBuffer ecb &#x3D; new EntityCommandBuffer(Allocator.TempJob);Entity placeholderEntity &#x3D; ecb.CreateEntity();&#x2F;&#x2F; Valid to use placeholderEntity in later commands of same ECB.ecb.AddComponent&lt;FooComp&gt;(placeholderEntity);&#x2F;&#x2F; The real entity is created, and&#x2F;&#x2F; FooComp is added to the real entity.ecb.Playback(this.EntityManager);&#x2F;&#x2F; Exception! The placeholderEntity has no meaning outside&#x2F;&#x2F; the ECB which created it, even after playback.this.EntityManager.AddComponent&lt;BarComp&gt;(placeholderEntity);ecb.Dispose();</code></pre></div></figure><p>AddComponent、SetComponent 或 SetBuffer 命令中记录的值可能具有 Entity 字段。在回放中，Unity 将这些组件或缓冲区中的任何占位符实体值重新映射到相应的实际实体。</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">&#x2F;&#x2F; ... in a systemEntityCommandBuffer ecb &#x3D; new EntityCommandBuffer(Allocator.TempJob);&#x2F;&#x2F; For all entities with a FooComp component...Entities    .WithAll&lt;FooComp&gt;()    .ForEach((Entity e) &#x3D;&gt;    &#123;        &#x2F;&#x2F; In playback, an actual entity will be created        &#x2F;&#x2F; that corresponds to this placeholder entity.        Entity placeholderEntity &#x3D; ecb.CreateEntity();        &#x2F;&#x2F; (Assume BarComp has an Entity field called TargetEnt.)        BarComp bar &#x3D; new BarComp &#123; TargetEnt &#x3D; placeholderEntity &#125;;        &#x2F;&#x2F; In playback, TargetEnt will be assigned the        &#x2F;&#x2F; actual Entity that corresponds to placeholderEntity.        ecb.AddComponent(e, bar);    &#125;).Run();&#x2F;&#x2F; After playback, each entity with FooComp now has a&#x2F;&#x2F; BarComp component whose TargetEnt references a new entity.ecb.Playback(this.EntityManager);ecb.Dispose();</code></pre></div></figure><h3 id="在-Entities-ForEach-方法中使用命令缓冲区"><a href="#在-Entities-ForEach-方法中使用命令缓冲区" class="headerlink" title="在 Entities.ForEach 方法中使用命令缓冲区"></a>在 Entities.ForEach 方法中使用命令缓冲区</h3><p>要在 Entities.ForEach 方法中使用命令缓冲区，请将 EntityCommandBuffer 参数传递给 lambda 表达式本身。仅支持一小部分 EntityCommandBuffer 方法，它们具有 [SupportedInEntitiesForEach] 属性：</p><ul><li>Entity Instantiate(实体实体)</li><li>void DestroyEntity(实体实体)</li><li>void AddComponent<T>(Entity e, T component) where T : unmanaged, IComponentData</T></li><li>void SetComponent<T>(Entity e, T component) where T : unmanaged, IComponentData</T></li><li>void RemoveComponent<T>(实体 e)<br>例如，以下代码执行此操作：</T></li></ul><ol><li>它检查每个实体以查看其 HealthLevel 是否为 0。</li><li>如果为真，它会记录一条销毁实体的命令。</li><li>它还指定 EndSimulationEntityCommandBufferSystem 应该回放命令。<figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">public struct HealthLevel : IComponentData&#123;    public int Value;&#125;Entities    .WithDeferredPlaybackSystem&lt;EndSimulationEntityCommandBufferSystem&gt;    .ForEach(        (Entity entity, EntityCommandBuffer buffer, HealthLevel healthLevel) &#x3D;&gt;         &#123;            if (healthLevel &#x3D;&#x3D; 0)            &#123;                buffer.DestroyEntity(entity);            &#125;        &#125;    ).ScheduleParallel();</code></pre></div></figure>当您在 ForEach() 函数中使用这些方法中的任何一种时，编译器会在运行时生成创建、填充、回放和处理 EntityCommandBuffer 实例或 EntityCommandBuffer.ParallelWriter 实例所需的代码（如果调用了 ScheduleParallel()） .</li></ol><p>在 ForEach() 之外调用这些方法会导致异常。</p><h4 id="在-Entities-forEach-中回放-EntityCommandBuffer"><a href="#在-Entities-forEach-中回放-EntityCommandBuffer" class="headerlink" title="在 Entities.forEach 中回放 EntityCommandBuffer"></a>在 Entities.forEach 中回放 EntityCommandBuffer</h4><p>要将 EntityCommandBuffer 参数传递给 ForEach() 函数，您还必须调用以下方法之一来指定何时回放命令：</p><ul><li>延迟回放：调用 WithDeferredPlaybackSystem<T>()，其中 T 标识回放命令的实体命令缓冲区系统。它必须是从 EntityCommandBufferSystem 派生的类型。</T></li><li>立即回放：在 ForEach() 函数完成所有迭代后立即调用 WithImmediatePlayback() 执行实体命令。您只能将 WithImmediatePlayback() 与 Run() 一起使用。</li></ul><p>编译器自动生成代码来创建和处理任何 EntityCommandBuffer 实例。</p><h2 id="查找任意数据"><a href="#查找任意数据" class="headerlink" title="查找任意数据"></a>查找任意数据</h2><p>访问和修改数据的最有效方法是使用具有实体查询和作业的系统。这以最高效的方式利用 CPU 资源，内存缓存未命中率最低。理想情况下，您应该使用最有效、最快的路径来执行大量数据转换。但是，有时您可能需要在程序的任意位置访问任意实体的任意组件。</p><p>您可以在实体的 IComponentData 及其动态缓冲区中查找数据。您查找数据的方式取决于您的代码是使用 Entities.ForEach、IJobEntityBatch 作业，还是在系统中执行的主线程上的其他一些方法。</p><h3 id="在系统中查找实体数据"><a href="#在系统中查找实体数据" class="headerlink" title="在系统中查找实体数据"></a>在系统中查找实体数据</h3><p>要从系统的 Entities.ForEach 或 Job.WithCode 方法中查找存储在任意实体组件中的数据，请使用 GetComponent<T>(Entity)</T></p><p>例如，以下代码使用 GetComponent<T>(Entity) 获取 Target 组件，该组件具有标识要定位的实体的实体字段。然后它将跟踪实体旋转到它们的目标：</T></p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">[RequireMatchingQueriesForUpdate]public partial class TrackingSystem : SystemBase&#123;    protected override void OnUpdate()    &#123;        float deltaTime &#x3D; SystemAPI.Time.DeltaTime;        Entities            .ForEach((ref Rotation orientation,            in LocalToWorld transform,            in Target target) &#x3D;&gt;            &#123;                &#x2F;&#x2F; Check to make sure the target Entity still exists and has                &#x2F;&#x2F; the needed component                if (!HasComponent&lt;LocalToWorld&gt;(target.entity))                    return;                &#x2F;&#x2F; Look up the entity data                LocalToWorld targetTransform &#x3D; GetComponent&lt;LocalToWorld&gt;(target.entity);                float3 targetPosition &#x3D; targetTransform.Position;                &#x2F;&#x2F; Calculate the rotation                float3 displacement &#x3D; targetPosition - transform.Position;                float3 upReference &#x3D; new float3(0, 1, 0);                quaternion lookRotation &#x3D; quaternion.LookRotationSafe(displacement, upReference);                orientation.Value &#x3D; math.slerp(orientation.Value, lookRotation, deltaTime);            &#125;)            .ScheduleParallel();    &#125;&#125;</code></pre></div></figure><p>如果要访问存储在动态缓冲区中的数据，还需要在 SystemBase 的 OnUpdate 方法中声明一个 BufferLookup 类型的局部变量。然后，您可以在 lambda 表达式中捕获局部变量。例如：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">public struct BufferData : IBufferElementData&#123;    public float Value;&#125;[RequireMatchingQueriesForUpdate]public partial class BufferLookupSystem : SystemBase&#123;    protected override void OnUpdate()    &#123;        BufferLookup&lt;BufferData&gt; buffersOfAllEntities            &#x3D; this.GetBufferLookup&lt;BufferData&gt;(true);        Entities            .ForEach((ref Rotation orientation,            in LocalToWorld transform,            in Target target) &#x3D;&gt;            &#123;                &#x2F;&#x2F; Check to make sure the target Entity with this buffer type still exists                if (!buffersOfAllEntities.HasBuffer(target.entity))                    return;                &#x2F;&#x2F; Get a reference to the buffer                DynamicBuffer&lt;BufferData&gt; bufferOfOneEntity &#x3D; buffersOfAllEntities[target.entity];                &#x2F;&#x2F; Use the data in the buffer                float avg &#x3D; 0;                for (var i &#x3D; 0; i &lt; bufferOfOneEntity.Length; i++)                &#123;                    avg +&#x3D; bufferOfOneEntity[i].Value;                &#125;                if (bufferOfOneEntity.Length &gt; 0)                    avg &#x2F;&#x3D; bufferOfOneEntity.Length;            &#125;)            .ScheduleParallel();    &#125;&#125;</code></pre></div></figure><h3 id="在作业中查找实体数据"><a href="#在作业中查找实体数据" class="headerlink" title="在作业中查找实体数据"></a>在作业中查找实体数据</h3><p>要在 IJobEntityBatch 等作业结构中随机访问组件数据，请使用以下类型之一：</p><ul><li>组件查找</li><li>缓冲区查找<br>这些类型获得一个类似于数组的组件接口，由 Entity 对象索引。</li></ul><p>要使用它们，请声明一个类型为 ComponentLookup 或 BufferLookup 的字段，设置该字段的值，然后安排作业。</p><p>例如，您可以使用 ComponentLookup 字段来查找实体的世界位置：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">[ReadOnly]public ComponentLookup&lt;LocalToWorld&gt; EntityPositions;</code></pre></div></figure><div class="note note-info">            <p>此声明使用 ReadOnly 属性。您应该始终将 ComponentLookup 对象声明为只读，除非您想写入您访问的组件。</p>          </div><p>以下示例说明了如何设置数据字段和安排作业：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">protected override void OnUpdate()&#123;    var job &#x3D; new ChaserSystemJob();    &#x2F;&#x2F; Set non-ECS data fields    job.deltaTime &#x3D; SystemAPI.Time.DeltaTime;    &#x2F;&#x2F; Schedule the job using Dependency property    Dependency &#x3D; job.ScheduleParallel(query, this.Dependency);&#125;</code></pre></div></figure><p>要查找组件的值，请在作业的 Execute 方法中使用实体对象：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">                float3 targetPosition &#x3D; entityPosition.Position;#if !ENABLE_TRANSFORM_V1                float3 chaserPosition &#x3D; transform.Value.Position;#else                float3 chaserPosition &#x3D; position.Value;#endif                float3 displacement &#x3D; targetPosition - chaserPosition;                float3 newPosition &#x3D; chaserPosition + displacement * deltaTime;#if !ENABLE_TRANSFORM_V1                transform.Value.Position &#x3D; newPosition;#else                position &#x3D; new Translation &#123; Value &#x3D; newPosition &#125;;#endif</code></pre></div></figure><p>以下完整示例显示了一个系统，该系统将具有目标字段的实体移动到目标的当前位置：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">    [RequireMatchingQueriesForUpdate]    public partial class MoveTowardsEntitySystem : SystemBase    &#123;        private EntityQuery query;        [BurstCompile]        private partial struct MoveTowardsJob : IJobEntity        &#123;            &#x2F;&#x2F; Read-only data stored (potentially) in other chunks            [ReadOnly]            public ComponentLookup&lt;LocalToWorld&gt; EntityPositions;            &#x2F;&#x2F; Non-entity data            public float deltaTime;#if !ENABLE_TRANSFORM_V1            public void Execute(ref LocalToWorldTransform transform, in Target target, in LocalToWorld entityPosition)#else            public void Execute(Translation position, in Target target, in LocalToWorld entityPosition)#endif            &#123;                &#x2F;&#x2F; Get the target Entity object                Entity targetEntity &#x3D; target.entity;                &#x2F;&#x2F; Check that the target still exists                if (!EntityPositions.HasComponent(targetEntity))                    return;                &#x2F;&#x2F; Update translation to move the chasing enitity toward the target                float3 targetPosition &#x3D; entityPosition.Position;#if !ENABLE_TRANSFORM_V1                float3 chaserPosition &#x3D; transform.Value.Position;                float3 displacement &#x3D; targetPosition - chaserPosition;                transform.Value.Position &#x3D; chaserPosition + displacement * deltaTime;#else                float3 chaserPosition &#x3D; position.Value;                float3 displacement &#x3D; targetPosition - chaserPosition;                position &#x3D; new Translation                &#123;                    Value &#x3D; chaserPosition + displacement * deltaTime                &#125;;#endif            &#125;        &#125;        protected override void OnCreate()        &#123;            &#x2F;&#x2F; Select all entities that have Translation and Target Component            query &#x3D; this.GetEntityQuery                (#if !ENABLE_TRANSFORM_V1                    typeof(LocalToWorldTransform),#else                    typeof(Translation),#endif                    ComponentType.ReadOnly&lt;Target&gt;()                );        &#125;        protected override void OnUpdate()        &#123;            &#x2F;&#x2F; Create the job            var job &#x3D; new MoveTowardsJob();            &#x2F;&#x2F; Set the component data lookup field            job.EntityPositions &#x3D; GetComponentLookup&lt;LocalToWorld&gt;(true);            &#x2F;&#x2F; Set non-ECS data fields            job.deltaTime &#x3D; SystemAPI.Time.DeltaTime;            &#x2F;&#x2F; Schedule the job using Dependency property            Dependency &#x3D; job.ScheduleParallel(query, Dependency);        &#125;    &#125;</code></pre></div></figure><h3 id="数据访问错误"><a href="#数据访问错误" class="headerlink" title="数据访问错误"></a>数据访问错误</h3><p>如果您查找的数据与您要在作业中读取和写入的数据重叠，则随机访问可能会导致竞争条件。</p><p>如果您确定要直接读取或写入的实体数据与要随机读取或写入的特定实体数据之间没有重叠，则可以使用 NativeDisableParallelForRestriction 属性标记访问器对象。</p><h2 id="写入组"><a href="#写入组" class="headerlink" title="写入组"></a>写入组</h2><p>写入组为一个系统提供了一种覆盖另一个系统的机制，即使您无法更改另一个系统。</p><p>一种常见的 ECS 模式是系统读取一组输入组件并将其写入另一个组件作为其输出。但是，您可能希望覆盖系统的输出，并使用基于不同输入集的不同系统来更新输出组件。</p><p>目标组件类型的写入组由 ECS 将 WriteGroup 属性应用于的所有其他组件类型组成，并以该目标组件类型作为参数。作为系统创建者，您可以使用写入组，以便您的系统用户可以排除您的系统将以其他方式选择和处理的实体。这种过滤机制允许系统用户根据自己的逻辑为排除的实体更新组件，同时让您的系统在其余部分照常运行。</p><h3 id="使用写组"><a href="#使用写组" class="headerlink" title="使用写组"></a>使用写组</h3><p>要使用写入组，您必须对系统中的查询使用写入组过滤器选项。这从查询中排除所有实体，这些实体具有来自查询中可写的任何组件的写入组的组件。</p><p>要覆盖使用写入组的系统，请将您自己的组件类型标记为该系统输出组件的写入组的一部分。原始系统忽略任何具有您的组件的实体，您可以使用您自己的系统更新这些实体的数据。</p><h3 id="编写组示例"><a href="#编写组示例" class="headerlink" title="编写组示例"></a>编写组示例</h3><p>在此示例中，您使用外部包根据角色的健康状况为游戏中的所有角色着色。为此，包中有两个组件：HealthComponent 和 ColorComponent；</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">public struct HealthComponent : IComponentData&#123;   public int Value;&#125;public struct ColorComponent : IComponentData&#123;   public float4 Value;&#125;</code></pre></div></figure><p>包中还有两个系统：</p><ol><li>ComputeColorFromHealthSystem，它从 HealthComponent 读取并写入 ColorComponent</li><li>RenderWithColorComponent，从 ColorComponent 读取<br>为了表示玩家何时使用能量提升并且他们的角色变得无敌，您将 InvincibleTagComponent 附加到角色的实体。在这种情况下，角色的颜色应该更改为单独的不同颜色，而上面的示例不适用。</li></ol><p>您可以创建自己的系统来覆盖 ColorComponent 值，但理想情况下 ComputeColorFromHealthSystem 不会计算实体的颜色。它应该忽略任何具有 InvincibleTagComponent 的实体。当屏幕上有成千上万的玩家时，这就变得更加重要。</p><p>这个系统来自另一个不知道 InvincibleTagComponent 的包，所以这是写组有用的时候。当您知道它计算的值无论如何都会被覆盖时，它允许系统忽略查询中的实体。您需要做两件事来支持这一点：</p><ol><li><p>将 InvincibleTagComponent 标记为 ColorComponent 写入组的一部分：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">[WriteGroup(typeof(ColorComponent))]struct InvincibleTagComponent : IComponentData &#123;&#125;</code></pre></div></figure><p>ColorComponent 的写入组由所有组件类型组成，这些组件类型具有以 typeof(ColorComponent) 作为参数的 WriteGroup 属性。</p></li><li><p>ComputeColorFromHealthSystem 必须明确支持写组。为此，系统需要为其所有查询指定 EntityQueryOptions.FilterWriteGroup 选项，如下所示：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">...protected override void OnUpdate() &#123;Entities    .WithName(&quot;ComputeColor&quot;)    .WithEntityQueryOptions(EntityQueryOptions.FilterWriteGroup) &#x2F;&#x2F; support write groups    .ForEach((ref ColorComponent color, in HealthComponent health) &#x3D;&gt; &#123;        &#x2F;&#x2F; compute color here    &#125;).ScheduleParallel();&#125;...</code></pre></div></figure><p>执行时，会发生以下情况：</p></li><li><p>系统检测到你写入 ColorComponent 因为它是一个引用参数</p></li><li><p>它查找 ColorComponent 的写入组并在其中找到 InvincibleTagComponent</p></li><li><p>它排除了所有具有 InvincibleTagComponent 的实体</p></li></ol><p>好处是，这允许系统根据系统未知的类型排除实体，并且可能存在于不同的包中。</p><div class="note note-info">            <p>有关更多示例，请参阅 Unity.Transforms 代码，它为其更新的每个组件使用写入组，包括 LocalToWorld。</p>          </div><h3 id="创建写入组"><a href="#创建写入组" class="headerlink" title="创建写入组"></a>创建写入组</h3><p>要创建写入组，请将 WriteGroup 属性添加到写入组中每个组件类型的声明中。 WriteGroup 属性采用一个参数，即组中组件用于更新的组件类型。单个组件可以是多个写入组的成员。</p><p>例如，如果您的系统在实体上存在组件 A 或 B 时写入组件 W，那么您可以为 W 定义一个写入组，如下所示：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">public struct W : IComponentData&#123;   public int Value;&#125;[WriteGroup(typeof(W))]public struct A : IComponentData&#123;   public int Value;&#125;[WriteGroup(typeof(W))]public struct B : IComponentData&#123;   public int Value;&#125;</code></pre></div></figure><p>您没有将写入组的目标（上例中的组件 W）添加到它自己的写入组。</p><h3 id="启用写组过滤"><a href="#启用写组过滤" class="headerlink" title="启用写组过滤"></a>启用写组过滤</h3><p>要启用写入组过滤，请在您的作业上设置 FilterWriteGroups 标志：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">public class AddingSystem : SystemBase&#123;   protected override void OnUpdate() &#123;      Entities          &#x2F;&#x2F; support write groups by setting EntityQueryOptions         .WithEntityQueryOptions(EntityQueryOptions.FilterWriteGroup)          .ForEach((ref W w, in B b) &#x3D;&gt; &#123;            &#x2F;&#x2F; perform computation here         &#125;).ScheduleParallel();&#125;&#125;</code></pre></div></figure><p>对于查询描述对象，在创建查询时设置标志：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">public class AddingSystem : SystemBase&#123;   private EntityQuery m_Query;   protected override void OnCreate()   &#123;       var queryDescription &#x3D; new EntityQueryDesc       &#123;           All &#x3D; new ComponentType[] &#123;              ComponentType.ReadWrite&lt;W&gt;(),              ComponentType.ReadOnly&lt;B&gt;()           &#125;,           Options &#x3D; EntityQueryOptions.FilterWriteGroup       &#125;;       m_Query &#x3D; GetEntityQuery(queryDescription);   &#125;   &#x2F;&#x2F; Define IJobEntityBatch struct and schedule...&#125;</code></pre></div></figure><p>当您在查询中启用写入组过滤时，该查询会将可写组件的写入组中的所有组件添加到查询的 None 列表中，除非您明确将它们添加到 All 或 Any 列表中。因此，如果查询明确需要来自特定写入组的实体上的每个组件，则查询只会选择该实体。如果实体具有来自该写入组的一个或多个附加组件，则查询将拒绝它。</p><p>在上面的示例代码中，查询：</p><ul><li>排除具有组件 A 的任何实体，因为 W 是可写的并且 A 是 W 的写入组的一部分。</li><li>不排除具有组件 B 的任何实体。即使 B 是 W 的写入组的一部分，它也在 All 列表中明确指定。</li></ul><h3 id="覆盖另一个使用写入组的系统"><a href="#覆盖另一个使用写入组的系统" class="headerlink" title="覆盖另一个使用写入组的系统"></a>覆盖另一个使用写入组的系统</h3><p>如果系统在其查询中使用写组过滤，您可以使用自己的系统覆盖该系统并写入那些组件。要覆盖系统，请将您自己的组件添加到其他系统写入的组件的写入组中。</p><p>因为写组过滤排除了查询未明确要求的写组中的任何组件，所以其他系统会忽略任何具有您的组件的实体。</p><p>例如，如果您想通过指定旋转的角度和轴来设置实体的方向，您可以创建一个组件和一个系统来将角度和轴值转换为四元数并将其写入 Unity.Transforms.Rotation零件。</p><p>为了防止 Unity.Transforms 系统更新 Rotation，无论除您之外的其他组件是否存在，您都可以将您的组件放在 Rotation 的写入组中：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">using System;using Unity.Collections;using Unity.Entities;using Unity.Transforms;using Unity.Mathematics;[Serializable][WriteGroup(typeof(Rotation))]public struct RotationAngleAxis : IComponentData&#123;   public float Angle;   public float3 Axis;&#125;</code></pre></div></figure><p>然后，您可以无竞争地使用 RotationAngleAxis 组件更新任何实体：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">using Unity.Burst;using Unity.Entities;using Unity.Jobs;using Unity.Collections;using Unity.Mathematics;using Unity.Transforms;public class RotationAngleAxisSystem : SystemBase&#123;   protected override void OnUpdate()   &#123;      Entities.ForEach((ref Rotation destination, in RotationAngleAxis source) &#x3D;&gt;      &#123;         destination.Value              &#x3D; quaternion.AxisAngle(math.normalize(source.Axis), source.Angle);      &#125;).ScheduleParallel();   &#125;&#125;</code></pre></div></figure><h3 id="扩展另一个使用写组的系统"><a href="#扩展另一个使用写组的系统" class="headerlink" title="扩展另一个使用写组的系统"></a>扩展另一个使用写组的系统</h3><p>如果你想扩展另一个系统而不是覆盖它，或者如果你想让未来的系统覆盖或扩展你的系统，那么你可以在你自己的系统上启用写组过滤。但是，当您这样做时，默认情况下两个系统都不会处理任何组件组合。您必须明确查询和处理每个组合。</p><p>在前面的示例中，它定义了一个写入组，其中包含以组件 W 为目标的组件 A 和 B。如果将名为 C 的新组件添加到写入组，那么知道 C 的新系统可以查询包含的实体C，这些实体是否也有组件 A 或 B 并不重要。</p><p>但是，如果新系统还启用了写组过滤，那就不再是这样了。如果您只需要组件 C，则写入组过滤会排除任何具有 A 或 B 的实体。相反，您必须显式查询每个有意义的组件组合。</p><div class="note note-info">            <p>您可以在适当的时候使用查询的 Any 子句。</p>          </div><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">var query &#x3D; new EntityQueryDesc&#123;    All &#x3D; new ComponentType[] &#123;       ComponentType.ReadOnly&lt;C&gt;(),        ComponentType.ReadWrite&lt;W&gt;()    &#125;,    Any &#x3D; new ComponentType[] &#123;       ComponentType.ReadOnly&lt;A&gt;(),        ComponentType.ReadOnly&lt;B&gt;()    &#125;,    Options &#x3D; EntityQueryOptions.FilterWriteGroup&#125;;</code></pre></div></figure><p>如果有任何实体包含未明确提及的写入组中的组件组合，则写入写入组目标的系统及其过滤器不会处理它们。但是，如果存在任何这些类型的实体，则很可能是程序中的逻辑错误，它们不应该存在。</p><h2 id="版本号"><a href="#版本号" class="headerlink" title="版本号"></a>版本号</h2><p>您可以使用 ECS 架构各部分的版本号（也称为世代）来检测潜在的变化并实施有效的优化策略，例如在数据自应用程序的最后一帧以来未发生变化时跳过处理。对实体执行快速版本检查以提高应用程序的性能非常有用。</p><p>此页面概述了 ECS 使用的所有不同版本号，以及导致它们更改的条件。</p><h3 id="版本号结构"><a href="#版本号结构" class="headerlink" title="版本号结构"></a>版本号结构</h3><p>所有版本号都是 32 位有符号整数。它们总是增加，除非它们环绕：有符号整数溢出是 C# 中定义的行为。这意味着要比较版本号，您应该使用（不）相等运算符，而不是关系运算符。</p><p>例如，检查 VersionB 是否比 VersionA 更新的正确方法是使用以下内容：</p><figure><div class="code-wrapper"><pre class="line-numbers language-c#" data-language="c#"><code class="language-c#">bool VersionBIsMoreRecent &#x3D; (VersionB - VersionA) &gt; 0;</code></pre></div></figure><p>无法保证版本号增加多少。</p><h3 id="实体版本号"><a href="#实体版本号" class="headerlink" title="实体版本号"></a>实体版本号</h3><p>EntityId 包含索引和版本号。因为 ECS 回收索引，所以每次实体销毁实体时它都会增加 EntityManager 中的版本号。如果在 EntityManager 中查找 EntityId 时版本号不匹配，则意味着引用的实体不再存在。</p><p>例如，在您通过 EntityId 获取一个单位正在跟踪的敌人的位置之前，您可以调用 ComponentDataFromEntity.Exists。这使用版本号来检查实体是否仍然存在。</p><h3 id="世界版本号"><a href="#世界版本号" class="headerlink" title="世界版本号"></a>世界版本号</h3><p>ECS每创建或销毁一个管理器（如系统），就会增加一个世界的版本号。</p><h3 id="作业组件系统版本号"><a href="#作业组件系统版本号" class="headerlink" title="作业组件系统版本号"></a>作业组件系统版本号</h3><p>EntityDataManager.GlobalVersion 在每次作业组件系统更新之前都会增加。</p><p>您应该将此版本号与 System.LastSystemVersion 结合使用。这会在每次作业组件系统更新后获取 EntityDataManager.GlobalVersion 的值。</p><p>您应该将此版本号与 Chunk.ChangeVersion[] 结合使用。</p><h4 id="块-ChangeVersion"><a href="#块-ChangeVersion" class="headerlink" title="块.ChangeVersion"></a>块.ChangeVersion</h4><p>对于原型中的每个组件类型，此数组包含 EntityDataManager.GlobalVersion 的值，在组件数组最后一次被访问为在此块中可写时。这并不能保证任何事情都发生了变化，只是它可能已经发生了变化。</p><p>您不能以可写方式访问共享组件，即使也为这些组件存储了版本号：它没有任何用处。</p><p>当您在 Entities.ForEach 构造中使用 WithChangeFilter() 方法时，ECS 将该特定组件的 Chunk.ChangeVersion 与 System.LastSystemVersion 进行比较，并且它仅处理其组件数组在系统上次开始运行后被访问为可写的块。</p><p>例如，如果保证一组单位的生命值数量自上一帧以来没有变化，则可以跳过检查这些单位是否应更新其损坏模型。</p><h3 id="非共享组件版本号"><a href="#非共享组件版本号" class="headerlink" title="非共享组件版本号"></a>非共享组件版本号</h3><p>对于每个非共享组件类型，每当涉及该类型的迭代器变得无效时，ECS 都会增加 EntityManager.m_ComponentTypeOrderVersion[] 版本号。换句话说，任何可能修改该类型数组（不是实例）的东西。</p><p>例如，如果您有特定组件标识的静态对象和每个块的边界框，则只需在该组件的类型顺序版本更改时更新这些边界框。</p><h3 id="共享组件版本号"><a href="#共享组件版本号" class="headerlink" title="共享组件版本号"></a>共享组件版本号</h3><p>当存储在引用共享组件的块中的实体发生任何结构更改时，SharedComponentDataManager.m_S​​haredComponentVersion[] 版本号会增加。</p><p>例如，如果您为每个共享组件保留一个实体计数，您可以依靠该版本号仅在相应版本号更改时重做每个计数。</p><h1 id="组件的使用"><a href="#组件的使用" class="headerlink" title="组件的使用"></a>组件的使用</h1><p>组件代表实体组件系统 (ECS) 架构中的数据。实体将组件关联在一起，系统提供可以读取和写入组件数据的逻辑。本节介绍 ECS 组件并说明如何使用它们。</p><table><thead><tr><th>标题</th><th>描述</th></tr></thead><tbody><tr><td>组件类型</td><td>了解不同的组件类型以及如何使用它们。</td></tr><tr><td>向实体添加组件</td><td>使用 C# 脚本从实体添加组件。</td></tr><tr><td>从实体中移除组件</td><td>使用 C# 脚本从实体中移除组件。</td></tr><tr><td>读取和写入组件值</td><td>使用 C# 脚本读取或写入组件值。</td></tr></tbody></table><h2 id="组件类型"><a href="#组件类型" class="headerlink" title="组件类型"></a>组件类型</h2><div class="note note-info">            <p>TODO</p>          </div><h2 id="向实体添加组件"><a href="#向实体添加组件" class="headerlink" title="向实体添加组件"></a>向实体添加组件</h2><div class="note note-info">            <p>TODO</p>          </div><h2 id="从实体中移除组件"><a href="#从实体中移除组件" class="headerlink" title="从实体中移除组件"></a>从实体中移除组件</h2><div class="note note-info">            <p>TODO</p>          </div><h2 id="读取和写入组件值"><a href="#读取和写入组件值" class="headerlink" title="读取和写入组件值"></a>读取和写入组件值</h2><div class="note note-info">            <p>TODO</p>          </div><h1 id="实体包中的脚本"><a href="#实体包中的脚本" class="headerlink" title="实体包中的脚本"></a>实体包中的脚本</h1><p>本节包含有关在实体中编写脚本时的最佳实践的信息，以及您可以在脚本中使用的一些功能。</p><table><thead><tr><th>标题</th><th>描述</th></tr></thead><tbody><tr><td>使用方面组织代码</td><td>使用方面将实体组件的子集组合到单个 C# 结构中。</td></tr><tr><td>Blob 资产</td><td>有关 blob 资产的信息，这些资产是针对流式处理优化的二进制数据片段。</td></tr><tr><td>在运行时加载场景</td><td>关于实体如何加载场景的信息。</td></tr><tr><td>转换系统</td><td>有关转换如何在实体中工作的信息。</td></tr></tbody></table><h2 id="使用-Baking-转换数据"><a href="#使用-Baking-转换数据" class="headerlink" title="使用 Baking 转换数据"></a>使用 Baking 转换数据</h2><div class="note note-info">            <p>TODO</p>          </div><h2 id="使用方面组织代码"><a href="#使用方面组织代码" class="headerlink" title="使用方面组织代码"></a>使用方面组织代码</h2><div class="note note-info">            <p>TODO</p>          </div><h2 id="转换系统"><a href="#转换系统" class="headerlink" title="转换系统"></a>转换系统</h2><div class="note note-info">            <p>TODO</p>          </div><h2 id="Blob-资产"><a href="#Blob-资产" class="headerlink" title="Blob 资产"></a>Blob 资产</h2><div class="note note-info">            <p>TODO</p>          </div><h2 id="在运行时加载场景"><a href="#在运行时加载场景" class="headerlink" title="在运行时加载场景"></a>在运行时加载场景</h2><div class="note note-info">            <p>TODO</p>          </div><h1 id="在编辑器中工作"><a href="#在编辑器中工作" class="headerlink" title="在编辑器中工作"></a>在编辑器中工作</h1><p>您可以在特定于实体的编辑器窗口中查看有关项目中 ECS 元素的信息。手册这一部分中的文档概述了编辑器中可用的窗口、检查器和工作流。用户手册的这一部分包含以下信息：</p><table><thead><tr><th>标题</th><th>描述</th></tr></thead><tbody><tr><td>实体首选项参考</td><td>首选项窗口中实体特定属性的参考。</td></tr><tr><td>使用创作和运行时数据</td><td>有关编辑器中不同数据模式的信息。</td></tr><tr><td>实体窗口</td><td>编辑器中各种窗口的参考，包括实体层次结构窗口。</td></tr><tr><td>实体Inspectors</td><td>有关特定于实体的检查员的信息。</td></tr></tbody></table><h2 id="实体首选项参考"><a href="#实体首选项参考" class="headerlink" title="实体首选项参考"></a>实体首选项参考</h2><div class="note note-info">            <p>TODO</p>          </div><h2 id="使用创作和运行时数据"><a href="#使用创作和运行时数据" class="headerlink" title="使用创作和运行时数据"></a>使用创作和运行时数据</h2><div class="note note-info">            <p>TODO</p>          </div><h2 id="实体窗口"><a href="#实体窗口" class="headerlink" title="实体窗口"></a>实体窗口</h2><div class="note note-info">            <p>TODO</p>          </div><h2 id="实体Inspectors"><a href="#实体Inspectors" class="headerlink" title="实体Inspectors"></a>实体Inspectors</h2><div class="note note-info">            <p>TODO</p>          </div><h1 id="性能和调试"><a href="#性能和调试" class="headerlink" title="性能和调试"></a>性能和调试</h1><p>本节包含有关可用于衡量项目性能的工具以及如何调试它的信息。</p><table><thead><tr><th>标题</th><th>描述</th></tr></thead><tbody><tr><td>Entities Profiler modules</td><td>有关 Unity Profiler 中内置的特定实体模块的信息。</td></tr><tr><td>Entities Journaling</td><td>使用 Entities Journaling 来调试您的项目。</td></tr></tbody></table><h2 id="Entities-Profiler-modules"><a href="#Entities-Profiler-modules" class="headerlink" title="Entities Profiler modules"></a>Entities Profiler modules</h2><div class="note note-info">            <p>TODO</p>          </div><h2 id="Entities-Journaling"><a href="#Entities-Journaling" class="headerlink" title="Entities Journaling"></a>Entities Journaling</h2><div class="note note-info">            <p>TODO</p>          </div>]]></content>
    
    
    
    <tags>
      
      <tag>Entities</tag>
      
      <tag>ECS</tag>
      
      <tag>DOTS</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>vscode远程容器开发</title>
    <link href="/2022/11/10/vscode-remote-containers/"/>
    <url>/2022/11/10/vscode-remote-containers/</url>
    
    <content type="html"><![CDATA[<p>之前在折腾HomeAssistant集成开发时接触到了VSCode的容器内开发，即使用容器构建开发环境，简化不同机器的开发环境的部署，确保团队环境一直，同时也方便了后修的测试。果然Docker是越用越爽。</p><span id="more"></span><p>再加上vscode的远程ssh连接模式，可以在不同的机器上统一连接到家中的服务器上，再使用这台机器去构建Docker容器，确保可在任意电脑上仅需安装vscode，就可以拥有统一的开发环境，这里就以hexo为例，记录下踩的那些坑。</p><h1 id="插件安装"><a href="#插件安装" class="headerlink" title="插件安装"></a>插件安装</h1><p>一共两个<br><code>Remote Development</code><br><code>DEV containers</code></p><h1 id="配置建立"><a href="#配置建立" class="headerlink" title="配置建立"></a>配置建立</h1><p>项目根目录下创建<code>.devcontiner</code>文件夹，然后是<code>devcontiner.json</code>配置文件，这里详细的配置参照<a href="https://containers.dev/implementors/json_reference/">containers.dev的文档</a>，下面贴出这里使用的配置</p><figure><div class="code-wrapper"><pre class="line-numbers language-json" data-language="json"><code class="language-json">&#123;    &quot;name&quot;: &quot;Hexo blog&quot;,    &quot;dockerFile&quot;: &quot;Dockerfile&quot;,    &quot;appPort&quot;: 4000,    &quot;containerUser&quot;: &quot;node&quot;,    &quot;extensions&quot;: [    ],    &quot;shutdownAction&quot;: &quot;stopContainer&quot;,    &quot;postCreateCommand&quot;: &quot;npm install;hexo s&quot;,    &quot;runArgs&quot;: [        &quot;--network&#x3D;nginxproxynet&quot;,        &quot;--net-alias&#x3D;hexoDev&quot;    ]&#125;</code></pre></div></figure><figure><div class="code-wrapper"><pre class="line-numbers language-docker" data-language="docker"><code class="language-docker">FROM node:ltsRUN npm install -g hexo-cli</code></pre></div></figure><p>这里在运行参数里配置了网络和别名，方便之后做反代，但后来发现不需要，vscode自带了一个功能，可以映射容器内的任意端口到本机（也就是vscode运行的机器，不是Docker服务器），猜测是用ssh隧道实现的。<br>这里有几个坑要注意，</p><ul><li>注意容器内用户和外部项目文件的用户一致，很多容器默认是root用户，我这里外面项目的用管理员账户，这会导致权限混乱，所以这里配置了<code>&quot;containerUser&quot;: &quot;node&quot;</code>node是这个镜像的默认管理员用户id为1000</li><li>创建容器后，内部的git没有复制外部的私钥，导致私有仓库无法上传，所以这里git下载和提交还是在容器外进行的</li><li><code>&quot;appPort&quot;: 4000</code>，这里指的docker自身的端口映射，但默认vscode是只映射到127.0.0.1上，远程访问还是不方便，而且这里修改<code>local port host</code>为<code>allInterfaces</code>在远程容器内也是无效的，具体看这个<a href="https://github.com/microsoft/vscode-remote-release/issues/6445">issues</a></li></ul>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>docker部署stable-diffusion-webui</title>
    <link href="/2022/11/09/stable-diffusion-webui-docker/"/>
    <url>/2022/11/09/stable-diffusion-webui-docker/</url>
    
    <content type="html"><![CDATA[<h1 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h1><p>stable-diffusion无疑是现在最热门的AI绘画模型了，stable-diffusion-webui提供了一个简单的通过web图形化调用的方法，但可惜没有提供对应的docker镜像，这里就尝试构建一个，方便之后的使用和分享。</p><span id="more"></span><h1 id="搭建"><a href="#搭建" class="headerlink" title="搭建"></a>搭建</h1><p>环境是一台ubuntu22.04服务器，配一张3070显卡。<br>docker安装以及显卡驱动的安装就省略吧。<br>从安装NVIDIA Container Toolkit开始。<br>按照nvidia的<a href="https://docs.nvidia.com/datacenter/cloud-native/container-toolkit/install-guide.html">指南</a></p><h2 id="设置软件包存储库和-GPG-密钥："><a href="#设置软件包存储库和-GPG-密钥：" class="headerlink" title="设置软件包存储库和 GPG 密钥："></a>设置软件包存储库和 GPG 密钥：</h2><figure><div class="code-wrapper"><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">distribution&#x3D;$(. &#x2F;etc&#x2F;os-release;echo $ID$VERSION_ID) \      &amp;&amp; curl -fsSL https:&#x2F;&#x2F;nvidia.github.io&#x2F;libnvidia-container&#x2F;gpgkey | sudo gpg --dearmor -o &#x2F;usr&#x2F;share&#x2F;keyrings&#x2F;nvidia-container-toolkit-keyring.gpg \      &amp;&amp; curl -s -L https:&#x2F;&#x2F;nvidia.github.io&#x2F;libnvidia-container&#x2F;$distribution&#x2F;libnvidia-container.list | \            sed &#39;s#deb https:&#x2F;&#x2F;#deb [signed-by&#x3D;&#x2F;usr&#x2F;share&#x2F;keyrings&#x2F;nvidia-container-toolkit-keyring.gpg] https:&#x2F;&#x2F;#g&#39; | \            sudo tee &#x2F;etc&#x2F;apt&#x2F;sources.list.d&#x2F;nvidia-container-toolkit.list</code></pre></div></figure><h2 id="apt安装"><a href="#apt安装" class="headerlink" title="apt安装"></a>apt安装</h2><figure><div class="code-wrapper"><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">sudo apt-get updatesudo apt-get install -y nvidia-docker2sudo systemctl restart docker</code></pre></div></figure><h2 id="测试"><a href="#测试" class="headerlink" title="测试"></a>测试</h2><figure><div class="code-wrapper"><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">sudo docker run --rm --gpus all nvidia&#x2F;cuda:11.0.3-base-ubuntu20.04 nvidia-smi</code></pre></div></figure><p>能看到nvidia-smi就算成功了<br><img src="/images/nvsmi.png" alt="nvidia-smi"></p><h1 id="搭建docker镜像"><a href="#搭建docker镜像" class="headerlink" title="搭建docker镜像"></a>搭建docker镜像</h1><p>基础镜像就选择 nvidia&#x2F;cuda:11.3.1-devel-ubuntu20.04<br>然后坑就来了</p><h2 id="cuda版本"><a href="#cuda版本" class="headerlink" title="cuda版本"></a>cuda版本</h2><p>因为webUI默认使用的torch是<code>pip install torch==1.12.1+cu113 --extra-index-url https://download.pytorch.org/whl/cu113</code>,所以这里要选择对应的11.3版本的cuda。</p><h2 id="用户要求非root"><a href="#用户要求非root" class="headerlink" title="用户要求非root"></a>用户要求非root</h2><p>WebUI脚本里去限制了root用户启动，这里在dockerFile里创建非root用户来启动</p><figure><div class="code-wrapper"><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">groupadd -r admin &amp;&amp; useradd -r -g admin -m admin \    &amp;&amp; mkdir &#x2F;workdir &amp;&amp; chown admin:admin &#x2F;workdir</code></pre></div></figure><p>别忘了赋予相应的权限。</p><h2 id="网络代理"><a href="#网络代理" class="headerlink" title="网络代理"></a>网络代理</h2><p>很多库和git仓库都要去下载，要设置好对应的代理，如果想打包时就使用代理，可以在~&#x2F;.docker&#x2F;config.json中配置</p><figure><div class="code-wrapper"><pre class="line-numbers language-json" data-language="json"><code class="language-json">&#123; &quot;proxies&quot;: &#123;   &quot;default&quot;:   &#123;     &quot;httpProxy&quot;: &quot;http:&#x2F;&#x2F;192.168.31.5:7890&quot;,     &quot;httpsProxy&quot;: &quot;http:&#x2F;&#x2F;192.168.31.5:7890&quot;,     &quot;noProxy&quot;: &quot;127.0.0.0&#x2F;8,localhost&quot;   &#125; &#125;&#125;</code></pre></div></figure><h2 id="python版本"><a href="#python版本" class="headerlink" title="python版本"></a>python版本</h2><p>虽然要求是3.10，但实测3.8也是可以的，但再低3.6好像就不行了。</p><h2 id="GPU配置"><a href="#GPU配置" class="headerlink" title="GPU配置"></a>GPU配置</h2><p>docker-compose.yml中配置</p><figure><div class="code-wrapper"><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml">deploy:  resources:    reservations:      devices:        - capabilities: [gpu]</code></pre></div></figure><h2 id="dockerfile中使用Python-venv"><a href="#dockerfile中使用Python-venv" class="headerlink" title="dockerfile中使用Python-venv"></a>dockerfile中使用Python-venv</h2><p>方便起见，shell选用bash</p><figure><div class="code-wrapper"><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">SHELL [&quot;&#x2F;bin&#x2F;bash&quot;, &quot;-c&quot;]</code></pre></div></figure><p>这里Dockerfile中的每一行 RUN 都是一个不同的过程。如果分开写，比如</p><figure><div class="code-wrapper"><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">RUN source venv&#x2F;bin&#x2F;activateRUN pip install opencv-python-headless</code></pre></div></figure><p>是没有效果的。<br>一定要连在一起</p><figure><div class="code-wrapper"><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">RUN source venv&#x2F;bin&#x2F;activate &amp;&amp; pip install opencv-python-headless</code></pre></div></figure><h1 id="Dockerfile"><a href="#Dockerfile" class="headerlink" title="Dockerfile"></a>Dockerfile</h1><figure><div class="code-wrapper"><pre class="line-numbers language-docker" data-language="docker"><code class="language-docker">FROM nvidia&#x2F;cuda:11.3.1-devel-ubuntu20.04SHELL [&quot;&#x2F;bin&#x2F;bash&quot;, &quot;-c&quot;]RUN apt-get update \    &amp;&amp; apt-get install git wget python3 python3-venv python3-pip -y \    &amp;&amp; groupadd -r admin &amp;&amp; useradd -r -g admin -m admin \    &amp;&amp; mkdir &#x2F;workdir &amp;&amp; chown admin:admin &#x2F;workdirUSER adminWORKDIR &#x2F;workdirRUN git clone https:&#x2F;&#x2F;github.com&#x2F;AUTOMATIC1111&#x2F;stable-diffusion-webui.gitCOPY model.ckpt &#x2F;workdir&#x2F;stable-diffusion-webui&#x2F;models&#x2F;Stable-diffusion&#x2F;WORKDIR &#x2F;workdir&#x2F;stable-diffusion-webuiENV PYTHONUNBUFFERED&#x3D;1RUN .&#x2F;webui.sh --skip-torch-cuda-test --exitRUN source venv&#x2F;bin&#x2F;activate &amp;&amp; pip install opencv-python-headlessCMD .&#x2F;webui.sh --listen</code></pre></div></figure><ul><li>这里的model.ckpt可以去<a href="https://huggingface.co/models?other=stable-diffusion">huggingface</a>下载，选择合适的stable-diffusion模型即可。</li><li><code>RUN ./webui.sh --skip-torch-cuda-test --exit</code>这里是把第一的初始化（安装依赖、下载仓库）也放到镜像中了</li><li>默认只监听127.0.0.1，添加–listen以监听0.0.0.0，默认端口是7860，这里也通过nginx反代出去了。</li></ul><p><img src="/images/sd.png" alt="nginx反代"></p><h1 id="docker-compose"><a href="#docker-compose" class="headerlink" title="docker-compose"></a>docker-compose</h1><figure><div class="code-wrapper"><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml">version: &quot;2.3&quot;services:  app:    image: stable-diffusion-webui-docker:0.1    container_name: stable-diffusion-webui    runtime: nvidia    environment:      - HTTP_PROXY&#x3D;http:&#x2F;&#x2F;clash:7890      - HTTPS_PROXY&#x3D;http:&#x2F;&#x2F;clash:7890      - no_proxy&#x3D;localhost,127.0.0.1,::1    deploy:      resources:        reservations:          devices:            - capabilities: [gpu]networks:  default:    external: true    name: nginxproxynet</code></pre></div></figure><p>这里也可以选择配置volume，方便之后的替换模型</p><figure><div class="code-wrapper"><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml">volumes:  - .&#x2F;Stable-diffusion:&#x2F;workdir&#x2F;stable-diffusion-webui&#x2F;models&#x2F;Stable-diffusion</code></pre></div></figure><h1 id="测试-1"><a href="#测试-1" class="headerlink" title="测试"></a>测试</h1><p>最后随便在网上找点提示词试一试，模型权重就用<a href="https://huggingface.co/runwayml/stable-diffusion-v1-5">runwayml&#x2F;stable-diffusion-v1-5</a>，</p><p><img src="/images/00000-3294290500-portrait%20phot___.png" alt="&quot;portrait photo of a old man crying, Tattles, sitting on bed, guages in ears, looking away, serious eyes, 50mm portrait photography, hard rim lighting photography–beta –ar 2:3 –beta –upbeta&quot;"></p><p><img src="/images/00002-535329683-temple%20in%20ruin___.png" alt="&quot;temple in ruines, forest, stairs, columns, cinematic, detailed, atmospheric, epic, concept art, Matte painting, background, mist, photo-realistic, concept art, volumetric light, cinematic epic + rule of thirds octane render, 8k, corona render, movie concept art, octane render, cinematic, trending on artstation, movie concept art, cinematic composition , ultra-detailed, realistic , hyper-realistic , volumetric lighting, 8k –ar 2:3 –test –uplight&quot;"></p><p>再整个二刺螈的模型权重试试<a href="https://huggingface.co/hakurei/waifu-diffusion-v1-3">waifu-diffusion-v1-3</a></p><p><img src="/images/00003-2351788143-(best%20quality___.png" alt="&quot;(best quality),(highly detailed),(masterpiece),(ultra-detailed),(absurdres),(((artbook))),((8k_wallpaper)),((an extremely delicate and beautiful)),((detailed light)),((ultra-detailed)),(anime face),((extremely_detailed_eyes_and_face)),((gorgeous)),((beautiful detailed eyes)),illustration,medium breast,(solo),((bishoujo)),St. Louis_(azur lane)，cosplay，(blue hair)，(((Side single ponytail hair))),(claret eyes)&quot;"></p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>unity Accelerator docker compose</title>
    <link href="/2022/11/09/unityAcc/"/>
    <url>/2022/11/09/unityAcc/</url>
    
    <content type="html"><![CDATA[<h1 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h1><p>不多介绍了，简单来说就是为了避免团队中大家重复导入资源占用时间，搞个局域网内的缓存。<br>这样只要有一个人导入了资源，其他人再导入时就可以直接从服务器拿，节省时间。</p><span id="more"></span><p>具体看<a href="https://docs.unity3d.com/2022.1/Documentation/Manual/UnityAccelerator.html">官方介绍</a><br>贴一个docker-compose.yml</p><h1 id="docker-compose-yml"><a href="#docker-compose-yml" class="headerlink" title="docker-compose.yml"></a>docker-compose.yml</h1><figure><div class="code-wrapper"><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml">version: &#39;3&#39;services:  unityAcc:    image: unitytechnologies&#x2F;accelerator    container_name: unityAcc    volumes:      -  .&#x2F;agent:&#x2F;agent    restart: always    environment:      - USER&#x3D;user      - PASSWORD&#x3D;password    ports:      - 10080:10080networks:  default:    external: true    name: nginxproxynet</code></pre></div></figure><p>这里没有暴露管理页面的80或443端口，而是统一放到nginx反向代理网络中，方便之后做反代。<br>本来打算10080端口也做反代的，但unity客户端不支持更改其他端口，只能这样暴露出来了。</p><p><img src="/images/uacc.png" alt="反向代理设置"></p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>屏幕空间反射 VR (URP)</title>
    <link href="/2022/11/08/SSR-URP-In-VR/"/>
    <url>/2022/11/08/SSR-URP-In-VR/</url>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>之前的那个VR中采用复用阴影贴图的方式，用独立的相机渲染反射贴图还是有很多不足，因为是复用的阴影贴图，如果镜子中的物体没有在正常相机内有阴影的话，镜子中也不会有阴影。<br>所以还是想尝试一下SSR即平面空间反射在VR单通道模式下的实现。</p><p><img src="/images/ssr01.png" alt="屏幕空间反射"></p><h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>目前URP还没有原生支持SSR，也不想再重复造一次轮子了，这里先选用ShinySSRR作为基础的SSR，在它的基础上适配VR。</p><p>直接在单通道下使用测试一下，理所当然的当然失败了。<br>先简单看一下它原生的方式，大体分为几个步骤。</p><p><img src="/images/ssrr.png" alt="ShinySSRR"></p><ul><li>首先是通过要反射的物体的顶点和法线去计算每个像素的反射方向，然后通过深度图不停的去步进采样，得到最终要采样的像素的UV。同时这里记录了每个像素的粗糙度。</li><li>通过上面得到的UV，去采样一次屏幕颜色</li><li>经典的Kawase模糊去模糊反射贴图</li><li>最后依据设定的模糊程度和粗糙度再去采样对应的模糊程度的贴图，与现有的颜色相混合。</li></ul><h1 id="填坑"><a href="#填坑" class="headerlink" title="填坑"></a>填坑</h1><p>这里的问题就出在第一步的步进上面，它是在屏幕空间去步进的（不知道该怎么表述这一点，它的步进、碰撞判定，全是在屏幕空间）。至于为什么能直接在屏幕空间去步进，还没有完全理解。但因为单通道的特殊性，单通道的相机的PV矩阵在这种情况下是不正确的。于是还是改为了在视角空间下去步进。<br>对比发现，相同的迭代步数，确实不如直接在屏幕空间步进的效果好。</p><p>当然这里它做了很多优化，比如从近平面去截取，依据起点和终点的像素距离去降低迭代次数等等。</p><p><img src="/images/ssrs.png" alt="迭代部分代码"></p><h1 id="Occlusion"><a href="#Occlusion" class="headerlink" title="Occlusion"></a>Occlusion</h1><p>因为VR中的实际显示面积并不是一个矩形，而是一个类似圆</p><p><img src="/images/oc.png" alt="Occlusion"></p><p>这恰好弥补了ssr边缘位置的缺陷，所以ssr在VR中实际的观感还是不错的。<br>但因为这毕竟不是原生支持的ssr，在与urp自带的lit着色器搭配使用时，并不能和urp自身的反射融合的很好（HDRP非VR模式下的SSR就可以根据当前像素是否能从SSR中获取反射而决定要不要使用反射探针）。还需要一些刻意的调整以防止和反射探针混淆。</p><h1 id="法线来源"><a href="#法线来源" class="headerlink" title="法线来源"></a>法线来源</h1><p>这里除了使用插件原生的从模型顶点和法线贴图生成法线，也可以直接使用URP自带的NormalTexture，打开SSAO即可。所以SSR和延迟管线才是绝配，但又因为延迟管线无法使用SMAA，所以这里只能舍弃延迟管线了。<br>使用NormalTexture还有一大好处是可以支持任何在shaderGraph中生成的shader。比如这里的用flipbook和视差贴图做的石子地面就可以和SSR无缝衔接起来。</p>]]></content>
    
    
    
    <tags>
      
      <tag>URP</tag>
      
      <tag>VR</tag>
      
      <tag>Reflection</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>体积云复刻</title>
    <link href="/2022/07/14/Cloud/"/>
    <url>/2022/07/14/Cloud/</url>
    
    <content type="html"><![CDATA[<p>体积云复刻。<br>在unity中复刻了这个ShaderToy中的效果。添加了视角的移动以及平面的反射。<br><a href="https://www.shadertoy.com/view/XslGRr">Clouds by  iq</a></p><span id="more"></span><p class="note note-primary">wasd qe移动 鼠标右键旋转</p><div id="threeShader" style="position: relative; width: 100%; height: 0px; padding-bottom: 75%;"><iframe src="/static/Cloud/index.html" allowfullscreen="true" name="ThreeJSFrame" title="ThreeJSFrame" style="position: absolute; width: 100%; height: 100%; left: 0px; top: 0px; border: none;"></iframe></div><figure><div class="code-wrapper"><pre class="line-numbers language-hlsl" data-language="hlsl"><code class="language-hlsl">Shader &quot;KM&#x2F;Cloud&quot;&#123;    Properties    &#123;        iChannel0(&quot;iChannel0&quot;, 2D) &#x3D; &quot;black&quot; &#123;&#125;        iChannel1(&quot;iChannel1&quot;, 2D) &#x3D; &quot;black&quot; &#123;&#125;        _StepTime(&quot;_StepTime&quot;, Range(0,10)) &#x3D; 1        [HDR] LightColor(&quot;LightColor&quot;, Color) &#x3D; (1.0, 0.95, 0.8)        [HDR] DarkColor(&quot;DarkColor&quot;, Color) &#x3D; (0.25, 0.3, 0.35)        [HDR] Color01(&quot;Color01&quot;, Color) &#x3D;  (1.0, 0.6, 0.3)        [HDR] Color02(&quot;Color02&quot;, Color) &#x3D;  (0.91, 0.98, 1.05)        height(&quot;height&quot;, Range(0,2)) &#x3D;  1.5        hd(&quot;hd&quot;, Range(0,2)) &#x3D;  1.75        index(&quot;index&quot;, Range(-2,2)) &#x3D;  -1        sunDiff(&quot;sunDiff&quot;, Range(0,10)) &#x3D;  0.3        skyIndex(&quot;skyIndex&quot;, Range(-0.003,0)) &#x3D;  -0.003    &#125;    SubShader    &#123;        Tags        &#123;            &quot;RenderType&quot;&#x3D;&quot;Transparent&quot; &quot;RenderPeipeline&quot; &#x3D; &quot;UniversalPepeline&quot; &quot;Queue&quot;&#x3D;&quot;Transparent&quot;        &#125;        LOD 100        Pass        &#123;            name &quot;ShaderToy&quot;            blend one zero            ZWrite off            ZTest always            Cull off            HLSLPROGRAM            #pragma  vertex vert            #pragma  fragment frag            #define iTime   (0.5*_Time.y%100)            #include &quot;Packages&#x2F;com.unity.render-pipelines.universal&#x2F;ShaderLibrary&#x2F;Core.hlsl&quot;            #include &quot;Packages&#x2F;com.unity.render-pipelines.universal&#x2F;ShaderLibrary&#x2F;Lighting.hlsl&quot;            TEXTURE2D_X(iChannel0);            SAMPLER(sampler_iChannel0);            TEXTURE2D_X(iChannel1);            SAMPLER(sampler_iChannel1);            CBUFFER_START(UnityPerMaterial)            float4 iChannel1_TexelSize;            float4 LightColor;            float4 DarkColor;            float4 Color01;            float4 Color02;            float height;            float hd;            float index;            float sunDiff;            float skyIndex;                        float _StepTime;            CBUFFER_END            &#x2F;&#x2F;            &#x2F;&#x2F; Copyright Inigo Quilez, 2013 - https:&#x2F;&#x2F;iquilezles.org&#x2F;            &#x2F;&#x2F; I am the sole copyright owner of this Work.            &#x2F;&#x2F; You cannot host, display, distribute or share this Work in any form,            &#x2F;&#x2F; including physical and digital. You cannot use this Work in any            &#x2F;&#x2F; commercial or non-commercial product, website or project. You cannot            &#x2F;&#x2F; sell this Work and you cannot mint an NFTs of it.            &#x2F;&#x2F; I share this Work for educational purposes, and you can link to it,            &#x2F;&#x2F; through an URL, proper attribution and unmodified screenshot, as part            &#x2F;&#x2F; of your educational material. If these conditions are too restrictive            &#x2F;&#x2F; please contact me and we&#39;ll definitely work it out.            &#x2F;&#x2F; Volumetric clouds. Not physically correct in any way -             &#x2F;&#x2F; it does the wrong extintion computations and also            &#x2F;&#x2F; works in sRGB instead of linear RGB color space. No            &#x2F;&#x2F; shadows are computed, no scattering is computed. It is            &#x2F;&#x2F; a volumetric raymarcher than samples an fBM and tweaks            &#x2F;&#x2F; the colors to make it look good.            &#x2F;&#x2F;            &#x2F;&#x2F; Lighting is done with only one extra sample per raymarch            &#x2F;&#x2F; step instead of using 3 to compute a density gradient,            &#x2F;&#x2F; by using this directional derivative technique:            &#x2F;&#x2F;            &#x2F;&#x2F; https:&#x2F;&#x2F;iquilezles.org&#x2F;articles&#x2F;derivative            &#x2F;&#x2F; 0: one 3d texture lookup            &#x2F;&#x2F; 1: two 2d texture lookups with hardware interpolation            &#x2F;&#x2F; 2: two 2d texture lookups with software interpolation            #define NOISE_METHOD 1            &#x2F;&#x2F; 0: no LOD            &#x2F;&#x2F; 1: yes LOD            #define USE_LOD 1            &#x2F;&#x2F; 0: sunset look            &#x2F;&#x2F; 1: bright look            #define LOOK 1            float noise(in float3 x)            &#123;                float3 p &#x3D; floor(x);                float3 f &#x3D; frac(x);                f &#x3D; f * f * (3.0 - 2.0 * f);                float2 uv &#x3D; (p.xy + float2(37.0, 239.0) * p.z) + f.xy;                float2 rg &#x3D; SAMPLE_TEXTURE2D_X_LOD(iChannel0, sampler_iChannel0, (uv + 0.5) &#x2F; 256.0, 0.0).yx;                return lerp(rg.x, rg.y, f.z) * 2.0 - 1.0;            &#125;            float map5(in float3 p)            &#123;                float3 q &#x3D; p - float3(0.0, 0.1, 1.0) * iTime;                float f;                f &#x3D; 0.50000 * noise(q);                q &#x3D; q * 2.02;                f +&#x3D; 0.25000 * noise(q);                q &#x3D; q * 2.03;                f +&#x3D; 0.12500 * noise(q);                q &#x3D; q * 2.01;                f +&#x3D; 0.06250 * noise(q);                q &#x3D; q * 2.02;                f +&#x3D; 0.03125 * noise(q);                return clamp(height +index* p.y  + hd * f, 0.0, 1.0);            &#125;            float map4(in float3 p)            &#123;                float3 q &#x3D; p - float3(0.0, 0.1, 1.0) * iTime;                float f;                f &#x3D; 0.50000 * noise(q);                q &#x3D; q * 2.02;                f +&#x3D; 0.25000 * noise(q);                q &#x3D; q * 2.03;                f +&#x3D; 0.12500 * noise(q);                q &#x3D; q * 2.01;                f +&#x3D; 0.06250 * noise(q);                return clamp(height +index* p.y  + hd * f, 0.0, 1.0);            &#125;            float map3(in float3 p)            &#123;                float3 q &#x3D; p - float3(0.0, 0.1, 1.0) * iTime;                float f;                f &#x3D; 0.50000 * noise(q);                q &#x3D; q * 2.02;                f +&#x3D; 0.25000 * noise(q);                q &#x3D; q * 2.03;                f +&#x3D; 0.12500 * noise(q);                return clamp(height +index* p.y   + hd * f, 0.0, 1.0);            &#125;            float map2(in float3 p)            &#123;                float3 q &#x3D; p - float3(0.0, 0.1, 1.0) * iTime;                float f;                f &#x3D; 0.50000 * noise(q);                q &#x3D; q * 2.02;                f +&#x3D; 0.25000 * noise(q);;                return clamp(height +index* p.y   + hd * f, 0.0, 1.0);            &#125;            static float3 sundir ;            #define MARCH(STEPS,MAPLOD) for(int i&#x3D;0; i&lt;STEPS; i++) &#123; t +&#x3D; max(0.06,0.05*t); float3 pos &#x3D; ro + t*rd;  if( sum.a&gt;0.99 ) continue; float den &#x3D; MAPLOD( pos ); if( den&gt;0.01 ) &#123; float dif &#x3D; clamp((den - MAPLOD(pos+sunDiff*sundir))&#x2F;0.6, 0.0, 1.0 ); float3 lin &#x3D; Color01 * dif + Color02; float4  col &#x3D; float4( lerp( LightColor.rgb, DarkColor, den ), den ); col.xyz *&#x3D; lin; col.xyz &#x3D; lerp( col.xyz, bgcol, 1.0-exp(skyIndex*t*t) ); col.w *&#x3D; 0.4; col.rgb *&#x3D; col.a; sum +&#x3D; col*(1.0-sum.a); &#125;  &#125;            float4 raymarch(in float3 ro, in float3 rd, in float3 bgcol, in int2 px)            &#123;                float4 sum &#x3D; (0.0);                                float2 uv &#x3D; (floor((px)) + 0.5) * iChannel1_TexelSize.xy;                float t &#x3D; 0.05 * SAMPLE_TEXTURE2D_X_LOD(iChannel1, sampler_iChannel1,uv, 0).x;                MARCH(40 * _StepTime, map5);                MARCH(40* _StepTime, map4);                MARCH(30* _StepTime, map3);                MARCH(30* _StepTime, map2);                return clamp(sum, 0.0, 1.0);            &#125;            float MAPLOD(float3 p)            &#123;                return map2(p);            &#125;            float4 test(int STEPS, in float3 ro, in float3 rd, in float3 bgcol)            &#123;                float4 sum &#x3D; (0.0);                float t &#x3D; 0.05*0.5;                                for (int i &#x3D; 0; i &lt; STEPS; i++)                &#123;                    t +&#x3D; max(0.06, 0.05 * t);                    float3 pos &#x3D; ro + t * rd;                    if (pos.y &lt; -3.0 || pos.y &gt; 2.0 || sum.a &gt; 0.99) continue;                    float den &#x3D; MAPLOD(pos);                    if (den &gt; 0.01)                    &#123;                        &#x2F;&#x2F; 沿光照方向的浓度差                        float dif &#x3D; clamp((den - MAPLOD(pos + sunDiff * sundir)) &#x2F; 0.6, 0.0, 1.0);                        &#x2F;&#x2F; 光照                        float3 lin &#x3D; Color01 * dif + Color02;                        &#x2F;&#x2F; 亮部和暗部颜色用浓度来Lerp                        float4 col &#x3D; float4(lerp(LightColor.xyz, DarkColor.xyz, den), den);                        col.xyz *&#x3D; lin;                        &#x2F;&#x2F; 和背景色随距离混合                        col.xyz &#x3D; lerp(col.xyz, bgcol, 1.0 - exp(-0.003 * t * t));                                                col.w *&#x3D; 0.4;                        col.rgb *&#x3D; col.a;                        sum +&#x3D; col * (1.0 - sum.a);                    &#125;                &#125;                                return sum;            &#125;            float4 render(in float3 ro, in float3 rd, in int2 px)            &#123;                &#x2F;&#x2F; background sky                         float sun &#x3D; clamp(dot(sundir, rd), 0.0, 1.0);                float3 col &#x3D; float3(0.6, 0.71, 0.75) - rd.y * 0.2 * float3(1.0, 0.5, 1.0) + 0.15 * 0.5;                col +&#x3D; 0.2 * float3(1.0, .6, 0.1) * pow(sun, 8.0);                &#x2F;&#x2F; clouds                        float4 res &#x3D; raymarch(ro, rd, col, px);                col &#x3D; col * (1.0 - res.w) + res.xyz;                &#x2F;&#x2F; sun glare                        col +&#x3D; float3(0.2, 0.08, 0.04) * pow(sun, 3.0);                &#x2F;&#x2F; col &#x3D; res.xyz;                return float4(col, 1.0);            &#125;            float3 mainImage(in float2 screen_pos)            &#123;                float2 uv &#x3D; screen_pos &#x2F; _ScreenParams;                &#x2F;&#x2F; camera                float3 ro &#x3D; _WorldSpaceCameraPos;                float3 rd &#x3D; ComputeWorldSpacePosition(uv, 0, UNITY_MATRIX_I_VP);                rd &#x3D; rd - ro;                rd &#x3D; normalize(rd);                return render(ro, rd, int2(screen_pos - 0.5));            &#125;                        struct Attributes            &#123;                float4 positionOS : POSITION;                float2 uv : TEXCOORD0;                UNITY_VERTEX_INPUT_INSTANCE_ID            &#125;;            struct Varyings            &#123;                float4 positionHCS : SV_POSITION;                float2 uv : TEXCOORD0;                float3 viewPos : TEXCOORD1;                UNITY_VERTEX_OUTPUT_STEREO            &#125;;            Varyings vert(Attributes input)            &#123;                Varyings output;                UNITY_SETUP_INSTANCE_ID(input);                UNITY_INITIALIZE_VERTEX_OUTPUT_STEREO(output);                output.positionHCS &#x3D; float4(input.positionOS.xy, 0.5, 0.5);                output.viewPos &#x3D; mul(UNITY_MATRIX_I_P, output.positionHCS).xyz;                output.uv &#x3D; input.uv;                return output;            &#125;            half4 frag(Varyings input):SV_Target            &#123;                UNITY_SETUP_STEREO_EYE_INDEX_POST_VERTEX(input);                sundir &#x3D; GetMainLight().direction;                const float2 screen_pos &#x3D; input.positionHCS.xy;                float3 color &#x3D; mainImage(screen_pos);                &#x2F;&#x2F; post                return float4(color, 1.0);            &#125;            ENDHLSL        &#125;    &#125;&#125;</code></pre></div></figure>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Cpoy From Shader Toy</title>
    <link href="/2022/07/05/Cpoy-From-Shader-Toy/"/>
    <url>/2022/07/05/Cpoy-From-Shader-Toy/</url>
    
    <content type="html"><![CDATA[<p>在unity中复刻了这个ShaderToy中的效果。添加了视角的移动以及平面的反射。<br><a href="https://www.shadertoy.com/view/Ms2SD1">Seascape” by Alexander Alekseev aka TDM - 2014</a><br><img src="/images/water.jpg" alt="搭配平面反射后的水面效果"></p><p class="note note-primary">wasd qe移动 鼠标右键旋转</p><div id="threeShader" style="position: relative; width: 100%; height: 0px; padding-bottom: 75%;"><iframe src="/static/ref-br/index.html" allowfullscreen="true" name="ThreeJSFrame" title="ThreeJSFrame" style="position: absolute; width: 100%; height: 100%; left: 0px; top: 0px; border: none;"></iframe></div>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>WebGL Brotli 压缩及vercel部署</title>
    <link href="/2022/07/04/WebGLBrotli/"/>
    <url>/2022/07/04/WebGLBrotli/</url>
    
    <content type="html"><![CDATA[<h1 id="Unity-WebGL-Brotli压缩测试"><a href="#Unity-WebGL-Brotli压缩测试" class="headerlink" title="Unity WebGL Brotli压缩测试"></a>Unity WebGL Brotli压缩测试</h1><p>各个静态网页代理配置的方式不一，以vercel举例。<br>需要在根目录下添加<code>vercel.json</code>文件，给已经被Unity用Br压缩过的文件重载或添加httpHeader。</p><p>Gzip也是一样，br改成gzip即可。</p><span id="more"></span><p>因为GithubPage不支持修改httpHeader，所以只有通过<a href="https://vercel.kuanmi.top/2022/07/04/WebGLBrotli/">vercel.kuanmi.top</a>访问此文章才能正常加载下面的unityWebGL。</p><p>最难受的是这里用的是一种<a href="https://github.com/isaacs/node-glob#glob-primer">glob</a>的模式去匹配路径。<br>这和一般后端的正则有点出入，反复试错了半下午才弄好。</p><div id="threeShader" style="position: relative; width: 100%; height: 0px; padding-bottom: 75%;"><iframe src="/static/BR/index.html" allowfullscreen="true" name="ThreeJSFrame" title="ThreeJSFrame" style="position: absolute; width: 100%; height: 100%; left: 0px; top: 0px; border: none;"></iframe></div><h1 id="vercel-json"><a href="#vercel-json" class="headerlink" title="vercel.json"></a>vercel.json</h1><figure><div class="code-wrapper"><pre class="line-numbers language-json" data-language="json"><code class="language-json">&#123;    &quot;headers&quot;: [        &#123;            &quot;source&quot;: &quot;&#x2F;static&#x2F;BR&#x2F;Build&#x2F;(.*)\\.data\\.br&quot;,            &quot;headers&quot;: [                &#123;                    &quot;key&quot;: &quot;Content-Encoding&quot;,                    &quot;value&quot;: &quot;br&quot;                &#125;,                &#123;                    &quot;key&quot;: &quot;Content-Type&quot;,                    &quot;value&quot;: &quot;application&#x2F;octet-stream&quot;                &#125;            ]        &#125;,        &#123;            &quot;source&quot;: &quot;&#x2F;static&#x2F;BR&#x2F;Build&#x2F;(.*)\\.js\\.br&quot;,            &quot;headers&quot;: [                &#123;                    &quot;key&quot;: &quot;Content-Encoding&quot;,                    &quot;value&quot;: &quot;br&quot;                &#125;,                &#123;                    &quot;key&quot;: &quot;Content-Type&quot;,                    &quot;value&quot;: &quot;application&#x2F;javascript&quot;                &#125;            ]        &#125;,        &#123;            &quot;source&quot;: &quot;&#x2F;static&#x2F;BR&#x2F;Build&#x2F;(.*)\\.wasm\\.br&quot;,            &quot;headers&quot;: [                &#123;                    &quot;key&quot;: &quot;Content-Encoding&quot;,                    &quot;value&quot;: &quot;br&quot;                &#125;,                &#123;                    &quot;key&quot;: &quot;Content-Type&quot;,                    &quot;value&quot;: &quot;application&#x2F;wasm&quot;                &#125;            ]        &#125;    ]&#125;</code></pre></div></figure>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>LoadShaderToy</title>
    <link href="/2022/07/04/LoadShaderToy/"/>
    <url>/2022/07/04/LoadShaderToy/</url>
    
    <content type="html"><![CDATA[<p>从ShaderToy复制一个简单的无贴图shader（或者使用最下面的），粘贴到下面，然后加载。</p><span id="more"></span><div id="threeShader" style="position: relative; width: 100%; height: 0px; padding-bottom: 120%;"><iframe src="/static/LoadShaderToy/index.html" name="ThreeJSFrame" title="ThreeJSFrame" style="position: absolute; width: 100%; height: 100%; left: 0px; top: 0px; border: none;"></iframe></div><figure><div class="code-wrapper"><pre class="line-numbers language-glsl" data-language="glsl"><code class="language-glsl">&#x2F;&#x2F; http:&#x2F;&#x2F;mathworld.wolfram.com&#x2F;HeartSurface.htmlfloat f(vec3 p) &#123;    vec3 pp &#x3D; p * p;    vec3 ppp &#x3D; pp * p;    float a &#x3D; pp.x + 2.25 * pp.y + pp.z - 1.0;    return a * a * a - (pp.x + 0.1125 * pp.y) * ppp.z;&#125;&#x2F;&#x2F; Bisection solver for yfloat h(float x, float z) &#123;    float a &#x3D; 0.0, b &#x3D; 0.75, y &#x3D; 0.5;    for (int i &#x3D; 0; i &lt; 10; i++) &#123;        if (f(vec3(x, y, z)) &lt;&#x3D; 0.0)            a &#x3D; y;        else            b &#x3D; y;        y &#x3D; (a + b) * 0.5;    &#125;    return y;&#125;&#x2F;&#x2F; Analytical gradient&#x2F;&#x2F; (-2 x z^3+6 x (-1.+x^2+2.25 y^2+z^2)^2) &#x2F;&#x2F; (-0.225 y z^3+13.5 y (-1.+x^2+2.25 y^2+z^2)^2)&#x2F;&#x2F; (z (-3 x^2 z-0.3375 y^2 z+6 (-1.+x^2+2.25 y^2+z^2)^2))vec3 normal(vec2 p) &#123;    vec3 v &#x3D; vec3(p.x, h(p.x, p.y), p.y);    vec3 vv &#x3D; v * v;    vec3 vvv &#x3D; vv * v;    float a &#x3D; -1.0 + dot(vv, vec3(1, 2.25, 1));   a *&#x3D; a;      return normalize(vec3(        -2.0 * v.x * vvv.z +  6.0 * v.x * a,      -0.225 * v.y * vvv.z + 13.5 * v.y * a,      v.z * (-3.0 * vv.x * v.z - 0.3375 * vv.y * v.z + 6.0 * a)));&#125;void mainImage( out vec4 fragColor, in vec2 fragCoord ) &#123;vec3 p &#x3D; vec3((2.0 * fragCoord.xy - iResolution.xy) &#x2F; min(iResolution.y, iResolution.x), 0);        float s &#x3D; sin(iTime * 5.0);    s *&#x3D; s;    s *&#x3D; s;    s *&#x3D; 0.1;    vec3 tp &#x3D; p * vec3(1.0 + s, 1.0 - s, 0.0) * 2.0;        vec3 c;    if (f(tp.xzy) &lt;&#x3D; 0.0) &#123;        vec3 n &#x3D; normal(tp.xy);        float diffuse &#x3D; dot(n, normalize(vec3(-1, 1, 1))) * 0.5 + 0.5;        float specular &#x3D; pow(max(dot(n, normalize(vec3(-1, 2, 1))), 0.0), 64.0);        float rim &#x3D; 1.0 - dot(n, vec3(0.0, 1.0, 0.0));        c &#x3D; diffuse * vec3(1.0, 0, 0) + specular * vec3(0.8) + rim * vec3(0.5);    &#125;else        c &#x3D; vec3(1.0, 0.8, 0.7 - 0.07 * p.y) * (1.0 - 0.15 * length(p));    fragColor &#x3D; vec4(c, 1);&#125;</code></pre></div></figure>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>UnityWebGL 测试</title>
    <link href="/2022/07/02/UnityWebGLTest/"/>
    <url>/2022/07/02/UnityWebGLTest/</url>
    
    <content type="html"><![CDATA[<p>没有使用压缩的版本，与使用了<a href="/2022/07/04/WebGLBrotli/">Brotli压缩</a>对比。</p><div id="threeShader" style="position: relative; width: 100%; height: 0px; padding-bottom: 75%;"><iframe src="/static/URPWebGL/index.html" allowfullscreen="true" name="ThreeJSFrame" title="ThreeJSFrame" style="position: absolute; width: 100%; height: 100%; left: 0px; top: 0px; border: none;"></iframe></div>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Hello ThreeJS</title>
    <link href="/2022/07/02/HelloThreeJS/"/>
    <url>/2022/07/02/HelloThreeJS/</url>
    
    <content type="html"><![CDATA[<p>恶补了一堆前端知识，用iframe内嵌一个ThreeJS的Demo。</p><div id="threeShader" style="position: relative; width: 100%; height: 0px; padding-bottom: 75%;"><iframe src="/static/HelloThreeJS/index.html" allowfullscreen="true" name="ThreeJSFrame" title="ThreeJSFrame" style="position: absolute; width: 100%; height: 100%; left: 0px; top: 0px; border: none;"></iframe></div><p></p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Hello ShaderToy</title>
    <link href="/2022/07/02/HelloShaderToy/"/>
    <url>/2022/07/02/HelloShaderToy/</url>
    
    <content type="html"><![CDATA[<p>叹为观止。</p><span id="more"></span><p><a href="https://www.shadertoy.com/view/Ms2SD1">Seascape” by Alexander Alekseev aka TDM - 2014</a></p><!--more--><div id="threeShader" style="position: relative; width: 100%; height: 0px; padding-bottom: 56.25%;"><iframe src="/static/HelloShaderToy/index.html" allowfullscreen="true" name="ThreeJSFrame" title="ThreeJSFrame" style="position: absolute; width: 100%; height: 100%; left: 0px; top: 0px; border: none;"></iframe></div>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>平面反射效果 VR (URP)</title>
    <link href="/2022/07/01/Plane-reflection-in-VR-URP/"/>
    <url>/2022/07/01/Plane-reflection-in-VR-URP/</url>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>前几日去HDRP下试验了各种效果在VR下的可用性，结果是一塌糊涂。像屏幕空间反射、或者Planar probe这种效果官方压根就<a href="https://issuetracker.unity3d.com/issues/xr-hdrp-planar-reflection-probes-reflection-is-misaligned-for-both-eyes-when-vr-is-enabled">没打算支持VR</a>，只能自力更生了。</p><span id="more"></span><p>本着柿子捡软的捏的原则，先从复刻平面探针入手吧。<br>如果不考虑VR这个实现其实不难，但Unity的单通道渲染实在是太不成熟了，坑一个接一个，这次记录一下踩坑教训。<br><a href="https://github.com/MaZhengKe/Plane-reflection-urp-vr">仓库Demo地址</a></p><p><img src="/images/water.jpg" alt="水面效果"><br><img src="/images/mirror05.jpg" alt="镜面效果"></p><h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>去HDRP的管线下看了半天，平面反射探针的实现和HDRP耦合的太高了，很难分离开直接移植到URP下，但大体思路就是用一个独立的相机去渲染到一张贴图上。然后基于这张贴图去玩各种花活。<br>出于性能考虑，就不单独去生成一个相机再去渲染了，还是直接在RenderFeature的层面，重写相机的位置和视锥体以及剔除，直接绘制，能节省很多开销。</p><h1 id="填坑"><a href="#填坑" class="headerlink" title="填坑"></a>填坑</h1><h2 id="非VR实现"><a href="#非VR实现" class="headerlink" title="非VR实现"></a>非VR实现</h2><p>思路很简单，先看非VR下的实现。<br>在ScriptableRenderPass中</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">CoreUtils.SetRenderTarget(cmd, m_MirrorTexture, ClearFlag.All, Color.clear);var mirrorPos &#x3D; MirrorPlanar.mirrorPos(MirrorPlanar.Plane,camTransform.position);var viewM &#x3D; MirrorPlanar.GetViewMat(camTransform.position, rotation);var projectionMatrix &#x3D; camera.projectionMatrix;projectionMatrix &#x3D; GL.GetGPUProjectionMatrix(projectionMatrix, cameraData.IsCameraProjectionMatrixFlipped());cmd.SetGlobalVector(worldSpaceCameraPos,new Vector4(mirrorPos.x,mirrorPos.y,mirrorPos.z,0));RenderingUtils.SetViewAndProjectionMatrices(cmd, viewM, projectionMatrix, false);</code></pre></div></figure><p>这里通过原Camera的坐标和旋转，以及要镜像的平面，生成了虚拟相机的坐标以及View矩阵和投影矩阵。然后写入shader中，覆盖了原相机的相机矩阵和投影矩阵。<br>这里不要忘记连带worldSpaceCameraPos一起写进去。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">context.ExecuteCommandBuffer(cmd);cmd.Clear();context.DrawRenderers(cullingResults, ref drawingSettings, ref m_FilteringSettings);context.DrawSkybox(cameraData.camera);cmd.SetGlobalTexture(s_MirrorTextureID, m_MirrorTexture.nameID);var viewMatrix &#x3D; camera.worldToCameraMatrix;RenderingUtils.SetViewAndProjectionMatrices(cmd, viewMatrix, cameraData.GetGPUProjectionMatrix(),false);</code></pre></div></figure><p>然后就是绘制，这里可以在之前通过设置layerMask等来控制要绘制的物体，防止地板遮挡等问题的出现。<br>绘制结束别忘记把之前重载的属性恢复回来。<br>这样就相当于从镜子的另一面用相同的相机绘制到了一张RT上。</p><p>然后是Shader部分，这里就很简单了</p><figure><div class="code-wrapper"><pre class="line-numbers language-hlsl" data-language="hlsl"><code class="language-hlsl">TEXTURE2D_X(_MirrorTex);SAMPLER(sampler_MirrorTex);half4 frag(Varyings input):SV_Target&#123;    UNITY_SETUP_STEREO_EYE_INDEX_POST_VERTEX(input);    float2 screenUV &#x3D; input.positionHCS &#x2F; _ScreenParams;    screenUV.x &#x3D; 1 - screenUV.x;    return SAMPLE_TEXTURE2D_X(_MirrorTex, sampler_MirrorTex, screenUV) * 0.8;    return 1;&#125;</code></pre></div></figure><p>拿到屏幕坐标，反转一下X轴，去采样之前渲染的那张RT就好了。<br><img src="/images/mirror01.jpg" alt="普通镜面"></p><h2 id="VR单通道模式下实现"><a href="#VR单通道模式下实现" class="headerlink" title="VR单通道模式下实现"></a>VR单通道模式下实现</h2><p>然后天坑就来了<br>第一个问题就是如何获取左右眼的世界坐标。<br>本来使用的是unity XR中的函数来获取，但这个支持的并不好，比如unity自带的mock hmd就获取不到。简单列一下代码吧。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public static Vector3 GetEyePosition(Side eye)&#123;    if (XRSettings.enabled)    &#123;        Vector3 pos;        InputDevice device &#x3D; InputDevices.GetDeviceAtXRNode(XRNode.Head);        if (device.isValid)        &#123;            Debug.Log($&quot;&#123;device&#125; isValid&quot;);            if (device.TryGetFeatureValue(eye &#x3D;&#x3D; Side.Left ? CommonUsages.leftEyePosition : CommonUsages.rightEyePosition,out pos))                return pos;        &#125;        Debug.LogError(&quot;can not find eyePos&quot;);        return default(Vector3);    &#125;    else    &#123;        return Camera.main.transform.position;    &#125;&#125;</code></pre></div></figure><p>最后还是通过Camera的View矩阵来获取比较准确。可能是我孤陋寡闻了，真不知道为什么这种常用的属性Unity竟然不提供一个简便的方式来获取。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">var leftEyePos &#x3D; camera.GetStereoViewMatrix(Camera.StereoscopicEye.Left).inverse.GetColumn(3);</code></pre></div></figure><p>本以为就那么简单，可最终的结果确怎么也对不上。折腾了半天打印了VR下的投影矩阵一看，才发现VR下左右眼的视锥体竟然不是个对称的视锥体。<br>之前一直使用<code>public static Matrix4x4 Perspective(float fov, float aspect, float zNear, float zFar)</code>这个函数来生成投影矩阵，导致我忘记了视锥体还有不对称这种情况。<br>于是改用<code>camera.GetStereoProjectionMatrix(Camera.StereoscopicEye.Left);</code>来获取，但结果还是有误差。<br><img src="/images/mirror02.png" alt="没有对齐"><br>这里猜一下就猜到还是因为视锥体左右不对称引起的，直觉告诉我，只要反转投影矩阵的第一行（也就是对最终X轴有影响的）的第三个参数（这个数值会和观察空间的深度相乘，最终会影响像素的左右偏移程度）就可以了，</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">leftPMat[8] *&#x3D; -1;rightPMat[8] *&#x3D; -1;</code></pre></div></figure><p>OK，搞定。<br><img src="/images/mirror03.png" alt="正确情况"></p><p><img src="/images/mirror04.jpg" alt="地面反射"></p><h2 id="渲染镜子背面问题"><a href="#渲染镜子背面问题" class="headerlink" title="渲染镜子背面问题"></a>渲染镜子背面问题</h2><p>当镜子背后有物体时，也会被一同渲染到镜子上。<br>能想到的办法要不就通过layerMask来过滤，要不就完全重构上面的代码。<br>以镜子作为镜像后的相机的近平面，利用投影矩阵刚好剔除了这部分物体。</p><p>最终通过这篇文章<a href="https://blog.csdn.net/puppet_master/article/details/80808486">Unity Shader-反射效果（CubeMap，Reflection Probe，Planar Reflection，Screen Space Reflection）</a>才知道已经有前人总结出了<a href="http://www.terathon.com/lengyel/Lengyel-Oblique.pdf">斜视锥体裁剪</a>。<br>所以最棘手的问题也解决了。只要在求出视角矩阵和投影矩阵后，再依据平面变换一次投影矩阵即可。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">var planeTr &#x3D; MirrorPlanar.Plane;var normal &#x3D; planeTr.forward;var d &#x3D; -Vector3.Dot(normal, planeTr.position);var plane &#x3D; new Vector4(normal.x, normal.y, normal.z, d);projectionMatrix &#x3D; CalculateObliqueMatrix(plane,viewM,projectionMatrix);......&#x2F;&#x2F;如果是VR下要分别对左右眼都变换一次leftPMat &#x3D; CalculateObliqueMatrix(plane,leftViewM,leftPMat);rightPMat &#x3D; CalculateObliqueMatrix(plane,rightViewM,rightPMat);</code></pre></div></figure><p><code>CalculateObliqueMatrix</code>函数请去原文查阅。<br><img src="/images/ObliqueMatrix.jpg" alt="剪裁掉镜子的背面"></p><h2 id="镜像坐标、旋转和矩阵"><a href="#镜像坐标、旋转和矩阵" class="headerlink" title="镜像坐标、旋转和矩阵"></a>镜像坐标、旋转和矩阵</h2><p>虽然直觉告诉我肯定有个简便和优雅的方法来求一个相机相对于空间任意平面的对称相机的方法，但实在是懒的想了，用了最笨的办法来求。<br>如果有哪位大神知道，还烦请留言告诉我。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public static Quaternion mirrorRot(Transform plane, Quaternion cam)&#123;    var forward &#x3D; plane.forward;    var reflect &#x3D; Vector3.Reflect(cam * Vector3.forward, forward);    var reflectup &#x3D; Vector3.Reflect(cam * Vector3.up, forward);    return Quaternion.LookRotation(reflect, reflectup);&#125;public static Vector3 mirrorPos(Transform plane, Vector3 oldPos)&#123;    var forward &#x3D; plane.forward;    var planPos &#x3D; plane.position;    var dis &#x3D; planPos - oldPos;    var cros &#x3D; Vector3.Cross(dis, forward).normalized;    var ccos &#x3D; Vector3.Cross(cros, forward);    var refDis &#x3D; Vector3.Reflect(dis, ccos);    return refDis + planPos;&#125;public static Matrix4x4 GetViewMat(Vector3 oldPos, Quaternion oldRot)&#123;    var newPos &#x3D; mirrorPos(Plane, oldPos);    var newRot &#x3D; mirrorRot(Plane, oldRot);    return Matrix4x4.TRS(newPos, newRot, new Vector3(1, 1, -1)).inverse;&#125;</code></pre></div></figure><h3 id="2022-x2F-07-x2F-05-更新"><a href="#2022-x2F-07-x2F-05-更新" class="headerlink" title="2022&#x2F;07&#x2F;05 更新"></a>2022&#x2F;07&#x2F;05 更新</h3><p>一个更简便的取对称点位置的函数，同样来自<a href="https://blog.csdn.net/puppet_master/article/details/80808486">Unity Shader-反射效果（CubeMap，Reflection Probe，Planar Reflection，Screen Space Reflection）</a><br>但这篇文章里构建的反射矩阵最终的结果并不正确，这会导致模型用反面渲染。</p><figure><div class="code-wrapper"><pre class="line-numbers language-CS" data-language="CS"><code class="language-CS">public static Vector3 mirrorPos(Transform plane, Vector3 oldPos)&#123;    var normal &#x3D; plane.forward;    var d &#x3D; -Vector3.Dot(normal, plane.position);    return oldPos - 2 * (Vector3.Dot(oldPos, normal) + d) * normal;&#125;</code></pre></div></figure><h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><h2 id="视锥体裁剪"><a href="#视锥体裁剪" class="headerlink" title="视锥体裁剪"></a>视锥体裁剪</h2><p>这里视锥体裁剪使用的还是之前的裁剪矩阵，要想正确构建一个适用于左右眼的裁剪矩阵太复杂了，干脆弄个超大的视锥体，等同于不要裁剪。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">CullingResults cullingResults;var cullMat &#x3D; Matrix4x4.Frustum(-1, 1, -1, 1, 0.0000001f, 10000000f);camera.cullingMatrix &#x3D; cullMat * viewM;camera.TryGetCullingParameters(out var cullingParameters);cullingResults &#x3D; context.Cull(ref cullingParameters);</code></pre></div></figure><h2 id="阴影剔除和附加光源裁剪的问题"><a href="#阴影剔除和附加光源裁剪的问题" class="headerlink" title="阴影剔除和附加光源裁剪的问题"></a>阴影剔除和附加光源裁剪的问题</h2><p>这里暂时没有太好的解决方案，不清楚URP绘制阴影的过程能否被干预，如果镜子中的物体没有在正常相机内有阴影的话，镜子中也不会有阴影。<br>这里去看HDRP的平面反射探针时也发现了，HDRP是真的虚拟了一个相机，重新绘制了阴影，包括后处理等一应俱全，这会增加不少性能消耗。<br>而这里这种方法，在URP官方示例场景下，开关效果GPU时间增加了1.7ms。</p><h2 id="天空盒问题"><a href="#天空盒问题" class="headerlink" title="天空盒问题"></a>天空盒问题</h2><p>这是<a href="https://forum.unity.com/threads/hlsl-or-shadergraph-skybox-shader-with-single-pass-instancing.1234660/">官方已知的BUG</a>，SPR管线下绘制天空盒到RT上需要手动将ZClip关闭。</p>]]></content>
    
    
    
    <tags>
      
      <tag>URP</tag>
      
      <tag>VR</tag>
      
      <tag>Reflection</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>LensFlare(SRP) in VR</title>
    <link href="/2022/06/27/LensFlare-SRP-in-VR/"/>
    <url>/2022/06/27/LensFlare-SRP-in-VR/</url>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>Unity的URP和HDRP针对LensFlare也就是炫光，使用的是同一套逻辑(LensFlare(SRP))。但就是不支持VR，无论是单通道还是多通道。</p><span id="more"></span><p>Unity这样做也有一定的道理，本来这个效果就是模拟镜头的一种缺陷，人眼一般不会看到这种炫光，所以VR不支持也是理所当然的。<br>但本着没事找事的心态，权当练习，我翻看了这部分的源码，做了一点修改，使之能兼容VR。</p><p>效果如下：<br><img src="/images/Len_01.jpg" alt="镜头炫光"><br><a href="https://github.com/MaZhengKe/LensFlareVR">仓库地址</a><br>在我给官方提了这个BUG后，回复是就是不支持。<br>论坛上也有有说<a href="https://forum.unity.com/threads/lensflare-srp-does-not-work-properly-in-vr-and-urp.1249408/">这个问题</a>已经存在很久了。<br><img src="/images/Len_Err.jpg" alt="平行光偏移"><img src="/images/Len_Err02.jpg" alt="点光源偏移"></p><hr><h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>先从看它的shader入手吧，很简单，就一个文件 LensFlareDataDriven.shader，分了4个pass，每个pass的区别仅仅在于Blend的方式，分别对应了Additive、Screen、Premultiply、Lerp四种BlendMode。<br><img src="/images/Len_BlendMode.png" alt="Lens Flare Data中的四种BlendMode"></p><p>关键的逻辑在LensFlareCommon.hlsl中，输入有两张贴图，颜色一张，遮挡一张，然后是一个颜色和5个向量，分别定义了颜色，旋转、缩放、屏幕位置、偏移、宽高比、遮挡半径、遮挡采样次数、是否离屏渲染以及边缘偏移、羽化范围、多边形属性等等。</p><figure><div class="code-wrapper"><pre class="line-numbers language-hlsl" data-language="hlsl"><code class="language-hlsl">TEXTURE2D(_FlareTex);SAMPLER(sampler_FlareTex);#if defined(HDRP_FLARE) &amp;&amp; defined(FLARE_OCCLUSION)TEXTURE2D_X(_FlareOcclusionTex);SAMPLER(sampler_FlareOcclusionTex);#endiffloat4 _FlareColorValue;float4 _FlareData0; &#x2F;&#x2F; x: localCos0, y: localSin0, zw: PositionOffsetXYfloat4 _FlareData1; &#x2F;&#x2F; x: OcclusionRadius, y: OcclusionSampleCount, z: ScreenPosZ, w: ScreenRatiofloat4 _FlareData2; &#x2F;&#x2F; xy: ScreenPos, zw: FlareSizefloat4 _FlareData3; &#x2F;&#x2F; x: Allow Offscreen, y: Edge Offset, z: Falloff, w: invSideCountfloat4 _FlareData4; &#x2F;&#x2F; x: SDF Roundness, y: Poly Radius, z: PolyParam0, w: PolyParam1</code></pre></div></figure><p>而且都通过定义宏的方式来增强了可读性，学到了。</p><figure><div class="code-wrapper"><pre class="line-numbers language-hlsl" data-language="hlsl"><code class="language-hlsl">#define _LocalCos0              _FlareData0.x#define _LocalSin0              _FlareData0.y#define _PositionTranslate      _FlareData0.zw#define _OcclusionRadius        _FlareData1.x#define _OcclusionSampleCount   _FlareData1.y#define _ScreenPosZ             _FlareData1.z#ifndef _FlareScreenRatio#define _FlareScreenRatio       _FlareData1.w#endif</code></pre></div></figure><hr><p>再往下就发现了从shadertoy拿来的工具函数<code>float4 ComputeCircle(float2 uv)</code> <code>float4 ComputePolygon(float2 uv_)</code>，用来程序化直接从UV绘制椭圆和多边形的。</p><figure><div class="code-wrapper"><pre class="line-numbers language-hlsl" data-language="hlsl"><code class="language-hlsl">float4 GetFlareShape(float2 uv)&#123;#ifdef FLARE_CIRCLE    return ComputeCircle(uv);#elif defined(FLARE_POLYGON)    return ComputePolygon(uv);#else    return SAMPLE_TEXTURE2D(_FlareTex, sampler_FlareTex, uv);#endif&#125;</code></pre></div></figure><p>这里看到绘制分成三种，圆，多边形，贴图。对应了这里的Type<br><img src="/images/Len_Type.png" alt="Lens Flare Data中的三种类型"></p><hr><p>最后来看看vert函数</p><figure><div class="code-wrapper"><pre class="line-numbers language-hlsl" data-language="hlsl"><code class="language-hlsl">float4 posPreScale &#x3D; float4(2.0f, 2.0f, 1.0f, 1.0f) * GetQuadVertexPosition(input.vertexID) - float4(1.0f, 1.0f, 0.0f, 0.0);float2 uv &#x3D; GetQuadTexCoord(input.vertexID);uv.x &#x3D; 1.0f - uv.x;output.texcoord.xy &#x3D; uv;posPreScale.xy *&#x3D; _FlareSize;float2 local &#x3D; Rotate(posPreScale.xy, _LocalCos0, _LocalSin0);local.x *&#x3D; screenRatio;output.positionCS.xy &#x3D; local + _ScreenPos + _PositionTranslate;output.positionCS.z &#x3D; 1.0f;output.positionCS.w &#x3D; 1.0f;</code></pre></div></figure><p>用传入的屏幕坐标来缩放、旋转、乘上宽高比，加上UV偏移和另一个传入的偏移量直接写入positionCS。这里的positionCS是裁剪空间坐标，因为w设置为1，那xy范围就是(-1,1)，(0,0)表示屏幕中心。</p><hr><p>看到这就已经猜到它为什么不支持VR了，因为屏幕坐标是事先用C#脚本通过Camera的属性计算好的，而且估计计算时没有考虑VR下有两个摄像机，导致最终的屏幕位置是错误的。</p><h1 id="修改"><a href="#修改" class="headerlink" title="修改"></a>修改</h1><p>那修改的思路就确定了，增加一个向量，记录炫光分别在左右眼下的屏幕位置，依据USING_STEREO_MATRICES和unity_StereoEyeIndex来判断当前到底该使用哪一个屏幕坐标。</p><h2 id="脚本修改"><a href="#脚本修改" class="headerlink" title="脚本修改"></a>脚本修改</h2><p>回到代码中，从LensFlareComponent一路往回推，发现主要的逻辑代码都在LensFlareCommonSRP中，用单例来管理，而调用则是在URP的UniversalRenderer中，在后处理时，由PostProcessPass调用，通过FrameDebugger也能看到。</p><p>URP的代码没法修改，只好全部另起炉灶，自定义RenderFeature、pass、Component等，把原来的逻辑先复制一遍，开始修改。<br><img src="/images/Len_File.jpg" alt="目录结构"><br>从PostProcessPass的DoLensFlareDatadriven中可以看到</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">void DoLensFlareDatadriven(Camera camera, CommandBuffer cmd, RenderTargetIdentifier source, bool usePanini, float paniniDistance, float paniniCropToFit)&#123;    var gpuView &#x3D; camera.worldToCameraMatrix;    var gpuNonJitteredProj &#x3D; GL.GetGPUProjectionMatrix(camera.projectionMatrix, true);    &#x2F;&#x2F; Zero out the translation component.    gpuView.SetColumn(3, new Vector4(0, 0, 0, 1));    var gpuVP &#x3D; gpuNonJitteredProj * camera.worldToCameraMatrix;        LensFlareCommonSRP.DoLensFlareDataDrivenCommon(m_Materials.lensFlareDataDriven, LensFlareCommonSRP.Instance, camera, (float)m_Descriptor.width, (float)m_Descriptor.height,        usePanini, paniniDistance, paniniCropToFit,        true,        camera.transform.position,        gpuVP,        cmd, source,        (Light light, Camera cam, Vector3 wo) &#x3D;&gt; &#123; return GetLensFlareLightAttenuation(light, cam, wo); &#125;,        ShaderConstants._FlareOcclusionTex, ShaderConstants._FlareOcclusionIndex,        ShaderConstants._FlareTex, ShaderConstants._FlareColorValue,        ShaderConstants._FlareData0, ShaderConstants._FlareData1, ShaderConstants._FlareData2, ShaderConstants._FlareData3, ShaderConstants._FlareData4,        false);&#125;</code></pre></div></figure><p>这里是用的camera.worldToCameraMatrix来直接拿的矩阵，但在VR环境下，应该要用camera.GetStereoViewMatrix(eye);的方式分别来取左右眼的矩阵。所以这里添加上左右眼的VP矩阵，传入DoLensFlareDataDrivenCommon中。</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">Matrix4x4 getMatrixFromEye(Camera camera, Camera.StereoscopicEye eye)&#123;    var gpuView &#x3D; camera.GetStereoViewMatrix(eye);    var gpuNonJitteredProj &#x3D; GL.GetGPUProjectionMatrix(camera.GetStereoNonJitteredProjectionMatrix(eye), true);    &#x2F;&#x2F; Zero out the translation component.    gpuView.SetColumn(3, new Vector4(0, 0, 0, 1));    var gpuVP &#x3D; gpuNonJitteredProj * gpuView;    return gpuVP;&#125;void DoLensFlareDatadriven(Camera camera, CommandBuffer cmd, RenderTargetIdentifier source, bool usePanini,    float paniniDistance, float paniniCropToFit)&#123;    ......    var leftGpuVP &#x3D; getMatrixFromEye(camera, Camera.StereoscopicEye.Left);    var rightGpuVP &#x3D; getMatrixFromEye(camera, Camera.StereoscopicEye.Right);        LensFlareCommonMK.DoLensFlareDataDrivenCommon(m_Material, LensFlareCommonMK.Instance, camera,    (float)m_Descriptor.width, (float)m_Descriptor.height,    usePanini, paniniDistance, paniniCropToFit,    true,    camera.transform.position,    gpuVP,    leftGpuVP,    rightGpuVP,    cmd, source,    ......&#125;</code></pre></div></figure><p>来到DoLensFlareDataDrivenCommon函数，这里必须吐槽下这个函数，近16个参数就不说了，函数体也有500多行。。。<br>在这里增加一个函数来计算炫光在左右眼中的屏幕位置        </p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">static public Vector2 GetScreenPos(Camera.MonoOrStereoscopicEye eye, LensFlareComponentMK comp, Camera cam, bool isCameraRelative, Matrix4x4 viewProjMatrix)&#123;    Light light &#x3D; comp.GetComponent&lt;Light&gt;();    Vector3 positionWS;    Vector3 viewportPos;    bool isDirLight &#x3D; false;    if (light !&#x3D; null &amp;&amp; light.type &#x3D;&#x3D; LightType.Directional)    &#123;        positionWS &#x3D; -light.transform.forward * cam.farClipPlane;        isDirLight &#x3D; true;    &#125;    else    &#123;        positionWS &#x3D; comp.transform.position;    &#125;    viewportPos &#x3D; WorldToViewport(eye, cam, !isDirLight, isCameraRelative, viewProjMatrix, positionWS);    Vector2 screenPos &#x3D; new Vector2(2.0f * viewportPos.x - 1.0f, 1.0f - 2.0f * viewportPos.y);    return screenPos;&#125;</code></pre></div></figure><p>在使用相机位置的时候也分别要用左右眼球的位置</p><figure><div class="code-wrapper"><pre class="line-numbers language-csharp" data-language="csharp"><code class="language-csharp">public static Vector3 GetEyePosition(Side eye)&#123;    if (XRSettings.enabled)    &#123;        Vector3 posLeft;        InputDevice device &#x3D;            InputDevices.GetDeviceAtXRNode(eye &#x3D;&#x3D; Side.Left ? XRNode.LeftEye : XRNode.RightEye);        if (device.isValid)        &#123;            if (device.TryGetFeatureValue(                    eye &#x3D;&#x3D; Side.Left ? CommonUsages.leftEyePosition : CommonUsages.rightEyePosition,                    out posLeft))                return posLeft;        &#125;        Debug.LogError(&quot;can not find eyePos&quot;);        return default(Vector3);    &#125;    else    &#123;        return Camera.main.transform.position;    &#125;&#125;</code></pre></div></figure><p>计算出来后，添加进去</p><figure><div class="code-wrapper"><pre class="line-numbers language-hlsl" data-language="hlsl"><code class="language-hlsl">var leftPos &#x3D; GetScreenPos(Camera.MonoOrStereoscopicEye.Left,comp,cam,isCameraRelative,LeftViewProjMatrix);var rightPos &#x3D; GetScreenPos(Camera.MonoOrStereoscopicEye.Right,comp,cam,isCameraRelative,RightViewProjMatrix);cmd.SetGlobalVector(ShaderConstants._FlareData5,new Vector4(leftPos.x,leftPos.y,rightPos.x,rightPos.y));</code></pre></div></figure><hr><h2 id="Shader修改"><a href="#Shader修改" class="headerlink" title="Shader修改"></a>Shader修改</h2><p>然后是shader的修改<br>增加一行输入，并定义好宏</p><figure><div class="code-wrapper"><pre class="line-numbers language-hlsl" data-language="hlsl"><code class="language-hlsl">float4 _FlareData5;   #define _LeftScreenPos          _FlareData5.xy#define _RightScreenPos         _FlareData5.zw</code></pre></div></figure><p>vert中根据再unity提供的宏来判断一下</p><figure><div class="code-wrapper"><pre class="line-numbers language-hlsl" data-language="hlsl"><code class="language-hlsl">#if defined(USING_STEREO_MATRICES)float2 _ScreenPos &#x3D; unity_StereoEyeIndex &#x3D;&#x3D; 1? _RightScreenPos : _LeftScreenPos;# elsefloat2 _ScreenPos &#x3D; _CameraScreenPos;# endif</code></pre></div></figure><p>就完工了。</p><p><img src="/images/Len_01.jpg" alt="平行光"><img src="/images/Len_02.jpg" alt="点光源"></p><p>这里其实还是有个小问题，这种效果其实不符合物理。<br>因为如果在左右眼分别放置一个相机，拍出来的炫光的偏移位置（<code>#define _PositionTranslate      _FlareData0.zw</code>）其实应该是各不相同的。<br>但这会造成体验VR的人视觉混乱，所以为了体验效果，这里使用的偏移还是之前相机（也就是两眼中间位置）的偏移，只是修复了炫光原点的屏幕坐标位置。</p>]]></content>
    
    
    
    <tags>
      
      <tag>URP</tag>
      
      <tag>LensFlare</tag>
      
      <tag>VR</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>OutlineInURP VR</title>
    <link href="/2022/06/23/OutlineInURP-VR/"/>
    <url>/2022/06/23/OutlineInURP-VR/</url>
    
    <content type="html"><![CDATA[<h1 id="URP管线下VR适用的外轮廓描边"><a href="#URP管线下VR适用的外轮廓描边" class="headerlink" title="URP管线下VR适用的外轮廓描边"></a>URP管线下VR适用的外轮廓描边</h1><p><a href="https://github.com/MaZhengKe/OutlineRendererFeatureVR">github地址</a> </p><hr><p>因为很多模型不给平滑法线，没法用法线外扩，只能用类似后处理的方式来描边了.</p><p>好处是通用性强，任何模型都能用，弊端就是性能消耗大一点。 </p><span id="more"></span><p>VR下大概增加了0.4ms</p><p>主要参考自 <a href="https://zhuanlan.zhihu.com/p/170241589">https://zhuanlan.zhihu.com/p/170241589</a> 在此基础上改为URP的RenderFeature，并支持了VR中SinglePass</p><p><img src="/images/All.jpg" alt="全部显示"></p><p>大体思路是先绘制遮罩，再遮罩外扩。</p><ul><li>支持多重RenderFeature的叠加</li><li>mass抗锯齿</li><li>遮挡显示与否</li><li>支持VR中SinglePass渲染模式</li><li>采用了RenderLayerMask，避免了占用LayerMask<!-- more --></li></ul><p><img src="/images/Hide.jpg" alt="遮挡不显示"></p><p><img src="/images/mult.jpg" alt="多重RenderFeature叠加"></p>]]></content>
    
    
    
    <tags>
      
      <tag>URP</tag>
      
      <tag>VR</tag>
      
      <tag>RenderFeature</tag>
      
      <tag>Outline</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
